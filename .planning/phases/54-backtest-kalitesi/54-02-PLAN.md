---
phase: 54-backtest-kalitesi
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/app/backtest/page.tsx
  - frontend/src/app/backtest/backtest.module.css
autonomous: true
requirements:
  - BACK-02
  - REJ-03

must_haves:
  truths:
    - "Backtest sayfasında rejim kırılım tablosu görünür: Boğa/Ayı/Yatay/Volatil/Bilinmiyor satırları, her biri sinyal sayısı, başarı oranı ve ortalama getiri sütunları ile"
    - "Filtre bar'da rejim dropdown'ı bulunur; seçim API'ye ?regime=X parametresi olarak iletilir"
    - "Slipaj maliyet özeti KPI kartı: brüt getiri ve net getiri (maliyet sonrası) yan yana gösterilir"
    - "Rejim filtresi seçildiğinde tablo ve KPI kartlar filtrelenmiş veriye göre güncellenir"
    - "api.ts'de SignalOutcomeItem.regime alanı ve SignalCalibrationResponse.by_regime dizisi mevcut"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "regime alanları içeren güncel type tanımları ve regime parametreli API metodları"
      contains: "by_regime"
    - path: "frontend/src/app/backtest/page.tsx"
      provides: "rejim kırılım tablosu, rejim filtresi, slipaj KPI kartı"
      contains: "regimeFilter"
    - path: "frontend/src/app/backtest/backtest.module.css"
      provides: "rejim tablosu ve slipaj KPI kartı CSS sınıfları"
      contains: "regimeTable"
  key_links:
    - from: "frontend/src/app/backtest/page.tsx:loadData"
      to: "api.getSignalCalibration"
      via: "regime parametresi ile çağrı"
      pattern: "regimeFilter"
    - from: "frontend/src/app/backtest/page.tsx"
      to: "SignalCalibrationResponse.by_regime"
      via: "calibration.by_regime map"
      pattern: "by_regime"
---

<objective>
Frontend backtest sayfasına rejim kırılım tablosu, rejim filtresi ve slipaj maliyet KPI kartı ekle. Plan 54-01'in genişlettiği backend API'yi tüket.

Purpose: BACK-02 (rejim bazlı performans görünürlüğü) ve REJ-03 (rejim filtresi UI) gereksinimlerini karşılar.
Output: api.ts yeni type'larla, page.tsx rejim kırılım bölümüyle, backtest.module.css yeni sınıflarla güncellenir.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/54-backtest-kalitesi/54-CONTEXT.md
@frontend/src/lib/api.ts
@frontend/src/app/backtest/page.tsx
@frontend/src/app/backtest/backtest.module.css

<interfaces>
Plan 54-01'den yeni API yanıt yapısı:

calibration_report() yeni anahtarlar:
```python
{
    # mevcut anahtarlar...
    "by_regime": [
        {
            "key": "Boğa",       # veya "Ayı" / "Yatay" / "Volatil" / "Bilinmiyor"
            "count": 45,
            "success": 28,
            "partial": 10,
            "failure": 7,
            "success_rate": 62.2,
            "non_failure_rate": 84.4,
            "avg_return_pct": 3.2,
            "avg_excess_return_pct": 1.1,
        },
        ...
    ],
    "by_slippage_cost": {
        "assumed_round_trip_cost_pct": 0.6,
        "gross_avg_return_pct": 2.8,
        "net_avg_return_pct": 2.2,
        "note": "Orta kademe likidite varsayımı (20bps)...",
    },
}
```

list_outcomes() items yeni alan:
```python
{"id": ..., "symbol": ..., "regime": "Boğa", ...}  # regime: string | None
```

Mevcut API metodları (frontend/src/lib/api.ts satır 1187-1191):
```typescript
getSignalOutcomes: (limit: number, horizon: '1w' | '1m') =>
  apiFetch<SignalOutcomesResponse>(`/signals/outcomes?limit=${limit}&horizon=${horizon}`),
getSignalCalibration: (horizon: '1w' | '1m', minCount: number) =>
  apiFetch<SignalCalibrationResponse>(`/signals/calibration?horizon=${horizon}&min_count=${minCount}`),
```

Mevcut type'lar (frontend/src/lib/api.ts):
- SignalOutcomeItem — regime alanı eksik, eklenecek
- SignalCalibrationResponse — by_regime ve by_slippage_cost eksik, eklenecek
- SignalCalibrationBucket — mevcut, by_regime ile paylaşılır

Mevcut backtest/page.tsx state'leri:
- outcomes, summary, calibration, calibration1m, loading, error
- period, actionFilter, outcomeFilter
- Eklenecek: regimeFilter (string, default "")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts type güncellemeleri ve regime parametreli API metodları</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    frontend/src/lib/api.ts
  </read_first>
  <action>
    1) `SignalOutcomeItem` interface'ine yeni alan ekle (satır 543'ten önce, `evaluated_at` sonrasına):
       ```typescript
       regime: string | null;
       ```

    2) `SignalCalibrationResponse` interface'ine yeni alanlar ekle (`suggestions_policy` sonrasına):
       ```typescript
       by_regime: SignalCalibrationBucket[];
       by_slippage_cost: {
         assumed_round_trip_cost_pct: number;
         gross_avg_return_pct: number | null;
         net_avg_return_pct: number | null;
         note: string;
       } | null;
       ```

    3) `getSignalOutcomes` metodunu güncelle — regime parametresi ekle:
       ```typescript
       getSignalOutcomes: (limit: number = 20, horizon: '1w' | '1m' = '1w', regime?: string) =>
         apiFetch<SignalOutcomesResponse>(
           `/signals/outcomes?limit=${limit}&horizon=${horizon}${regime ? `&regime=${encodeURIComponent(regime)}` : ''}`
         ),
       ```

    4) `getSignalCalibration` metodunu güncelle — regime parametresi ekle:
       ```typescript
       getSignalCalibration: (horizon: '1w' | '1m' = '1w', minCount: number = 1, regime?: string) =>
         apiFetch<SignalCalibrationResponse>(
           `/signals/calibration?horizon=${horizon}&min_count=${minCount}${regime ? `&regime=${encodeURIComponent(regime)}` : ''}`
         ),
       ```

    Her iki metodun mevcut imzasını yer yerinde güncelle; imza değişikliği geriye dönük uyumludur (opsiyonel parametre).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -30; grep -q "by_regime" src/lib/api.ts &amp;&amp; grep -q "regime: string | null" src/lib/api.ts &amp;&amp; echo "type checks pass"</automated>
  </verify>
  <done>
    SignalOutcomeItem.regime alanı mevcut. SignalCalibrationResponse.by_regime ve by_slippage_cost mevcut. API metodları regime opsiyonel parametresi kabul ediyor. TypeScript derleme hatası yok.
  </done>
</task>

<task type="auto">
  <name>Task 2: Backtest sayfasına rejim kırılım tablosu, rejim filtresi ve slipaj KPI kartı</name>
  <files>
    frontend/src/app/backtest/page.tsx,
    frontend/src/app/backtest/backtest.module.css
  </files>
  <read_first>
    frontend/src/app/backtest/page.tsx,
    frontend/src/app/backtest/backtest.module.css
  </read_first>
  <action>
    **page.tsx değişiklikleri:**

    1) State sabitlerine rejim filtresi seçenekleri ekle (PERIOD_OPTIONS bloğundan sonra):
       ```typescript
       const REGIME_OPTIONS = [
         { label: 'Tüm Rejimler', value: '' },
         { label: 'Boğa', value: 'Boğa' },
         { label: 'Ayı', value: 'Ayı' },
         { label: 'Yatay', value: 'Yatay' },
         { label: 'Volatil', value: 'Volatil' },
       ] as const;
       ```

    2) Component state'lerine ekle (mevcut `outcomeFilter` state'inden sonra):
       ```typescript
       const [regimeFilter, setRegimeFilter] = useState('');
       ```

    3) `loadData()` fonksiyonunu güncelle — regimeFilter'ı API çağrısına ilet:
       ```typescript
       const [outcomesRes, calibRes, calibRes1m] = await Promise.all([
         api.getSignalOutcomes(limit, '1w', regimeFilter || undefined),
         api.getSignalCalibration('1w', 1, regimeFilter || undefined),
         api.getSignalCalibration('1m', 1, regimeFilter || undefined),
       ]);
       ```
       `useEffect` bağımlılık dizisine `regimeFilter` ekle: `[period, regimeFilter]`

    4) KPI kartlar bölümüne slipaj maliyet kartı ekle (kpiGrid içinde, mevcut 4 karttan sonra):
       ```tsx
       <div className={styles.kpiCard}>
         <p className={styles.kpiLabel}>Net Getiri (Maliyet Sonrası)</p>
         <p
           className={styles.kpiValue}
           style={{ color: returnColor(calibration?.by_slippage_cost?.net_avg_return_pct ?? null) }}
         >
           {loading
             ? '…'
             : calibration?.by_slippage_cost?.net_avg_return_pct != null
             ? fmtPct(calibration.by_slippage_cost.net_avg_return_pct)
             : '—'}
         </p>
         <p className={styles.kpiSub}>
           Brüt: {loading ? '…' : calibration?.by_slippage_cost?.gross_avg_return_pct != null
             ? fmtPct(calibration.by_slippage_cost.gross_avg_return_pct)
             : '—'}
           {' '}| Maliyet: %{calibration?.by_slippage_cost?.assumed_round_trip_cost_pct ?? 0.6}
         </p>
       </div>
       ```

    5) Filtre bar'a rejim dropdown'ı ekle (mevcut outcomeFilter select'ten sonra):
       ```tsx
       <select
         className={styles.filterSelect}
         value={regimeFilter}
         onChange={(e) => setRegimeFilter(e.target.value)}
       >
         {REGIME_OPTIONS.map((o) => (
           <option key={o.value} value={o.value}>{o.label}</option>
         ))}
       </select>
       ```

    6) Filtre bar'dan SONRA, tablo bölümünden ÖNCE, rejim kırılım tablosu ekle. Yalnızca `calibration?.by_regime` doluysa ve yüklenme tamamlandıysa göster:
       ```tsx
       {!loading && calibration?.by_regime && calibration.by_regime.length > 0 && (
         <div className={styles.regimeSection}>
           <h2 className={styles.sectionTitle}>Rejim Bazlı Performans</h2>
           <div className={styles.tableWrapper}>
             <table className={styles.regimeTable}>
               <thead>
                 <tr>
                   <th>Rejim</th>
                   <th>Sinyal</th>
                   <th>Başarı %</th>
                   <th>Ort. Getiri</th>
                   <th>Ort. Relatif</th>
                 </tr>
               </thead>
               <tbody>
                 {calibration.by_regime.map((row) => (
                   <tr key={row.key}>
                     <td>
                       <span className={styles[`regime${row.key}` as keyof typeof styles] ?? ''}>
                         {row.key}
                       </span>
                     </td>
                     <td>{row.count}</td>
                     <td style={{ color: row.success_rate >= 55 ? 'var(--accent-green)' : row.success_rate >= 40 ? 'var(--accent)' : 'var(--accent-red)' }}>
                       %{row.success_rate.toFixed(1)}
                     </td>
                     <td style={{ color: returnColor(row.avg_return_pct) }}>
                       {fmtPct(row.avg_return_pct)}
                     </td>
                     <td style={{ color: returnColor(row.avg_excess_return_pct) }}>
                       {fmtPct(row.avg_excess_return_pct)}
                     </td>
                   </tr>
                 ))}
               </tbody>
             </table>
           </div>
         </div>
       )}
       ```

    **backtest.module.css değişiklikleri:**

    7) Aşağıdaki CSS sınıflarını dosya sonuna ekle:
       ```css
       /* ─── Rejim Kırılım Bölümü ─── */
       .regimeSection {
         margin-bottom: 1.5rem;
       }

       .sectionTitle {
         font-size: 1rem;
         font-weight: 600;
         color: var(--text);
         margin: 0 0 0.75rem;
       }

       .regimeTable {
         width: 100%;
         border-collapse: collapse;
         font-size: 0.875rem;
       }

       .regimeTable th,
       .regimeTable td {
         padding: 0.5rem 0.75rem;
         text-align: left;
         border-bottom: 1px solid var(--border);
       }

       .regimeTable th {
         color: var(--text-muted);
         font-weight: 500;
         font-size: 0.8rem;
         text-transform: uppercase;
         letter-spacing: 0.03em;
       }

       .regimeTable tbody tr:hover {
         background: var(--card-hover, rgba(255,255,255,0.03));
       }

       /* Rejim renk etiketleri */
       .regimeBoğa { color: var(--accent-green); font-weight: 600; }
       .regimeAyı { color: var(--accent-red); font-weight: 600; }
       .regimeYatay { color: var(--accent); font-weight: 600; }
       .regimeVolatil { color: #f59e0b; font-weight: 600; }
       .regimeBilinmiyor { color: var(--text-muted); font-weight: 400; }
       ```
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | head -20 &amp;&amp; grep -q "regimeFilter" src/app/backtest/page.tsx &amp;&amp; grep -q "by_regime" src/app/backtest/page.tsx &amp;&amp; grep -q "regimeTable" src/app/backtest/backtest.module.css &amp;&amp; echo "frontend checks pass"</automated>
  </verify>
  <done>
    page.tsx: regimeFilter state'i, slipaj KPI kartı, filtre dropdown'ı, rejim kırılım tablosu mevcut. backtest.module.css: regimeSection, regimeTable ve rejim renk sınıfları mevcut. TypeScript hatası yok.
  </done>
</task>

</tasks>

<verification>
- `grep -q "by_regime" frontend/src/lib/api.ts` eşleşir
- `grep -q "regime: string | null" frontend/src/lib/api.ts` eşleşir
- `grep -q "regimeFilter" frontend/src/app/backtest/page.tsx` eşleşir
- `grep -q "regimeTable" frontend/src/app/backtest/backtest.module.css` eşleşir
- `cd frontend && npx tsc --noEmit` hata vermez
- Tarayıcıda /backtest sayfası: 5 KPI kartı (Net Getiri dahil), rejim dropdown, rejim kırılım tablosu görünür
</verification>

<success_criteria>
- Kullanıcı "Boğa" seçtiğinde sayfa hem sinyal tablosu hem KPI kartları Boğa rejimine ait sinyallere göre güncellenir
- Rejim kırılım tablosu her rejim için sinyal sayısı, başarı oranı ve ortalama getiri gösterir
- Net Getiri KPI kartı brüt getiri ile round-trip maliyet farkını gösterir
- TypeScript sıkı modunda derleme hatası olmaz
</success_criteria>

<output>
Tamamlanınca `.planning/phases/54-backtest-kalitesi/54-02-SUMMARY.md` oluştur. İçerik: eklenen type'lar, yeni state'ler, render edilen bileşenler, CSS sınıfları.
</output>
