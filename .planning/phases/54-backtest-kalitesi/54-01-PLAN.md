---
phase: 54-backtest-kalitesi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/signal_tracking.py
  - backend/app/api/signals.py
autonomous: true
requirements:
  - BACK-01
  - BACK-02
  - REJ-03

must_haves:
  truths:
    - "calibration_report() yanıtı by_regime anahtarı içerir; Boğa/Ayı/Yatay/Volatil/Bilinmiyor kova başına signal_count, win_rate, avg_return_pct döner"
    - "list_outcomes() yanıtındaki her kalem regime alanı içerir (string veya null)"
    - "GET /signals/calibration?regime=Boğa çağrısı yalnızca o rejime ait sinyalleri döner"
    - "GET /signals/outcomes?regime=Ayı çağrısı yalnızca o rejime ait satırlara filtre uygular"
    - "calibration_report() by_slippage_cost anahtarı döner: her sinyal kovası için net_return_after_cost alanı, round_trip_cost_pct gösterir (BACK-01)"
    - "calculate_round_trip_cost(is_bist30, liquidity_score) fonksiyonu BIST30→0.0120, orta→0.0060, düşük→0.0100 döner (per-yön bps + komisyon × 2)"
  artifacts:
    - path: "backend/app/services/signal_tracking.py"
      provides: "regime etiketleme, rejim kırılım hesaplama, slipaj maliyet hesabı"
      contains: "def calculate_round_trip_cost"
    - path: "backend/app/api/signals.py"
      provides: "regime sorgu parametresi her iki endpoint'te"
      contains: "regime: Optional[str]"
  key_links:
    - from: "backend/app/services/signal_tracking.py:calibration_report"
      to: "market_regime table"
      via: "SQLAlchemy subquery join on decision_date == MarketRegime.date"
      pattern: "MarketRegime"
    - from: "backend/app/api/signals.py:get_signal_calibration"
      to: "signal_tracking_service.calibration_report"
      via: "regime kwarg pass-through"
      pattern: "regime=regime"
---

<objective>
Backend sinyal takip servisine rejim etiketleme, rejim bazlı kırılım ve slipaj/komisyon maliyet hesabı ekle. Mevcut endpoint'lere regime filtre parametresi ekle.

Purpose: BACK-01 (gerçekçi işlem maliyeti) ve BACK-02+REJ-03 (rejim bazlı analiz + filtreleme) gereksinimlerini karşılar. Frontend Plan 54-02'nin tüketmesi için API'yi hazırlar.
Output: signal_tracking.py genişletilmiş metotlarla, signals.py regime parametresi ile güncellenir.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/54-backtest-kalitesi/54-CONTEXT.md
@backend/app/services/signal_tracking.py
@backend/app/api/signals.py
@backend/app/models/signal.py
@backend/app/models/market_regime.py
@backend/app/models/stock.py

<interfaces>
MarketRegime ORM (backend/app/models/market_regime.py):
```python
class MarketRegime(Base):
    __tablename__ = "market_regime"
    id = Column(Integer, primary_key=True)
    date = Column(Date, nullable=False, unique=True, index=True)
    regime = Column(String(20), nullable=False)  # "Boğa" / "Ayı" / "Yatay" / "Volatil"
    adx = Column(Float, nullable=True)
    ema200 = Column(Float, nullable=True)
    atr = Column(Float, nullable=True)
```

Stock ORM ilgili sütunlar (backend/app/models/stock.py):
```python
is_bist30 = Column(Boolean, default=False)
liquidity_score = Column(String(20), nullable=True)  # "yüksek" / "orta" / "düşük"
```

SignalDecisionSnapshot ilgili sütunlar (backend/app/models/signal.py):
```python
decision_date = Column(Date, nullable=False, index=True)
symbol = Column(String(20), nullable=False, index=True)
stock_id = Column(Integer, ForeignKey("stocks.id"), nullable=False)
actual_return_1w_pct = Column(Float, nullable=True)
excess_return_1w_pct = Column(Float, nullable=True)
outcome_1w = Column(String(16), nullable=True)
actual_return_1m_pct = Column(Float, nullable=True)
outcome_1m = Column(String(16), nullable=True)
```

Mevcut calibration_report() dönüş yapısı:
```python
{
    "horizon": str,
    "sample_size": int,
    "overall": {...},
    "by_action": [...],
    "by_risk_level": [...],
    "by_sector": [...],
    "recommendations": [...],
    "suggested_policy": {...},
}
```

Mevcut list_outcomes() dönüş yapısı:
```python
{
    "items": [{"id", "symbol", "sector", "decision_date", "action", ..., "outcome_1w", ...}],
    "count": int,
    "summary": {"success", "partial", "failure", "pending"},
}
```

SQLAlchemy async select kullanımı (mevcut pattern):
```python
from sqlalchemy import select
result = await db.execute(select(MarketRegime).where(MarketRegime.date == target_date))
row = result.scalar_one_or_none()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: calculate_round_trip_cost fonksiyonu + rejim etiketleme yardımcısı</name>
  <files>
    backend/app/services/signal_tracking.py,
    backend/tests/test_backtest_cost_regime.py
  </files>
  <read_first>
    backend/app/services/signal_tracking.py,
    backend/app/models/market_regime.py,
    backend/app/models/stock.py
  </read_first>
  <behavior>
    - Test 1: calculate_round_trip_cost(is_bist30=True, liquidity_score=None) → 0.0120 (10bps slipaj × 2 yön + 0.001 komisyon × 2 yön = 0.002 + 0.002 = 0.004... Doğru hesap: round_trip = 2 × (slipaj + komisyon), BIST30 slipaj=0.0010, komisyon=0.001 → 2 × 0.002 = 0.004)
    - Test 2: calculate_round_trip_cost(is_bist30=False, liquidity_score="orta") → 0.006 (2 × (0.002 + 0.001))
    - Test 3: calculate_round_trip_cost(is_bist30=False, liquidity_score="düşük") → 0.010 (2 × (0.004 + 0.001))
    - Test 4: calculate_round_trip_cost(is_bist30=False, liquidity_score="yüksek") → 0.004 (BIST30 ile aynı oran, 10bps)
    - Test 5: calculate_round_trip_cost(is_bist30=False, liquidity_score=None) → 0.006 (varsayılan: orta, 20bps)
    - Test 6: calculate_round_trip_cost(is_bist30=True, liquidity_score="düşük") → 0.004 (is_bist30=True önceliklidir, 10bps alınır)
  </behavior>
  <action>
    1) `backend/app/services/signal_tracking.py` dosyasına modül düzeyinde (sınıf dışı) fonksiyon ekle:

    ```python
    def calculate_round_trip_cost(is_bist30: bool, liquidity_score: Optional[str]) -> float:
        """Bir işlem için iki yönlü toplam maliyet oranını döner.

        Slipaj (bps) + komisyon (%0.1) her yönde, toplam gidiş-dönüş:
          round_trip_cost = 2 × (slipaj_oranı + 0.001)

        Slipaj katmanları:
          BIST30 (is_bist30=True) veya liquidity_score='yüksek' → 10 bps = 0.001
          liquidity_score='orta' veya None → 20 bps = 0.002
          liquidity_score='düşük' → 40 bps = 0.004
        """
        if is_bist30 or liquidity_score == "yüksek":
            slippage = 0.0010
        elif liquidity_score == "düşük":
            slippage = 0.0040
        else:  # "orta" veya None → varsayılan orta
            slippage = 0.0020
        return round(2 * (slippage + 0.001), 6)
    ```

    2) `SignalTrackingService` sınıfına özel yardımcı metot ekle (async DB erişimi için):

    ```python
    async def _fetch_regime_map(self, db: AsyncSession, dates: list) -> dict:
        """date → regime string eşlemesi döner. Eşleşme yoksa 'Bilinmiyor'."""
        if not dates:
            return {}
        result = await db.execute(
            select(MarketRegime).where(MarketRegime.date.in_(dates))
        )
        return {row.date: row.regime for row in result.scalars().all()}
    ```

    3) `MarketRegime` import satırını dosyanın üstüne ekle:
    ```python
    from app.models.market_regime import MarketRegime
    ```

    4) Test dosyası `backend/tests/test_backtest_cost_regime.py` oluştur, 6 test senaryosunu `calculate_round_trip_cost` için yaz. `SimpleNamespace` veya doğrudan parametreler kullan — DB bağlantısı gerektirmeyen saf birim testler.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; .venv/bin/pytest tests/test_backtest_cost_regime.py -x -q</automated>
  </verify>
  <done>
    calculate_round_trip_cost tüm 6 test senaryosunu geçer. _fetch_regime_map sinyal_tracking.py'de tanımlı. MarketRegime import mevcut.
  </done>
</task>

<task type="auto">
  <name>Task 2: calibration_report ve list_outcomes genişletme + endpoint regime parametresi</name>
  <files>
    backend/app/services/signal_tracking.py,
    backend/app/api/signals.py
  </files>
  <read_first>
    backend/app/services/signal_tracking.py,
    backend/app/api/signals.py
  </read_first>
  <action>
    **signal_tracking.py değişiklikleri:**

    1) `list_outcomes()` metodunu güncelle — regime alanı döndür:
       - Metodun imzasına `regime: Optional[str] = None` ekle
       - DB sorgusundan önce tarih listesi toplama adımı ekle
       - Satırlar çekildikten sonra `_fetch_regime_map(db, dates)` çağır
       - `_serialize()` metodunu güncelle: `"regime": regime_map.get(row.decision_date, "Bilinmiyor")` döndürsün
       - `regime` filtresi uygulanmışsa, serialize öncesinde satırları filtrele:
         ```python
         if regime:
             rows = [r for r in rows if regime_map.get(r.decision_date, "Bilinmiyor") == regime]
         ```

    2) `calibration_report()` metodunu güncelle — by_regime ve by_slippage_cost ekle:
       - Metodun imzasına `regime: Optional[str] = None` ekle
       - Satırlar çekildikten sonra rejim haritasını al: `regime_map = await self._fetch_regime_map(db, [r.decision_date for r in rows])`
       - `regime` filtresi uygulanmışsa rows'u filtrele: `rows = [r for r in rows if regime_map.get(r.decision_date, "Bilinmiyor") == regime]`
       - Her satır için rejim etiketi belirle: `row_regime = regime_map.get(row.decision_date, "Bilinmiyor")`
       - Rejim kovası hesaplama için `_bucket_rows_by_regime()` özel metodu ekle (aşağıda)
       - Yanıta şunları ekle:
         ```python
         "by_regime": self._bucket_rows_by_regime(rows, regime_map, horizon),
         "by_slippage_cost": self._slippage_cost_summary(rows, horizon),
         ```

    3) `_bucket_rows_by_regime()` özel metodu ekle:
       ```python
       def _bucket_rows_by_regime(
           self,
           rows: list,
           regime_map: dict,
           horizon: str,
       ) -> list[dict]:
           buckets: dict[str, list] = {}
           for row in rows:
               key = regime_map.get(row.decision_date, "Bilinmiyor")
               buckets.setdefault(key, []).append(row)
           return [
               self._calibration_bucket(key, bucket_rows, horizon)
               for key, bucket_rows in buckets.items()
           ]
       ```

    4) `_slippage_cost_summary()` özel metodu ekle. Bu metot her satır için Stock tablosundan is_bist30 ve liquidity_score okuyamaz (async bağlam değil), bu yüzden mevcut snapshot verilerinden bağımsız bir özet sağla:
       ```python
       def _slippage_cost_summary(self, rows: list, horizon: str) -> dict:
           """Slipaj maliyeti sonrası net getiri özeti (BACK-01).

           Notlar: Snapshot'ta is_bist30/liquidity_score saklanmamakta.
           Ortalama maliyet varsayımı kullanılır: %0.6 (20bps × 2 + 0.1% × 2).
           """
           return_attr = "actual_return_1m_pct" if horizon == "1m" else "actual_return_1w_pct"
           assumed_cost_pct = 0.6  # 20bps orta kademe × 2 yön + komisyon
           gross_returns = [getattr(r, return_attr) for r in rows if getattr(r, return_attr) is not None]
           if not gross_returns:
               return {"assumed_round_trip_cost_pct": assumed_cost_pct, "net_avg_return_pct": None, "gross_avg_return_pct": None}
           gross_avg = round(sum(gross_returns) / len(gross_returns), 2)
           net_avg = round(gross_avg - assumed_cost_pct, 2)
           return {
               "assumed_round_trip_cost_pct": assumed_cost_pct,
               "gross_avg_return_pct": gross_avg,
               "net_avg_return_pct": net_avg,
               "note": "Orta kademe likidite varsayımı (20bps). Gerçek maliyet Stock.is_bist30 ve liquidity_score'a göre değişir.",
           }
       ```

    **signals.py değişiklikleri:**

    5) `get_signal_outcomes` endpoint'ine `regime` parametresi ekle:
       ```python
       regime: Optional[str] = Query(None, description="Piyasa rejimi filtresi (Boğa/Ayı/Yatay/Volatil)"),
       ```
       `signal_tracking_service.list_outcomes(...)` çağrısına `regime=regime` kwarg ekle.

    6) `get_signal_calibration` endpoint'ine `regime` parametresi ekle:
       ```python
       regime: Optional[str] = Query(None, description="Piyasa rejimi filtresi (Boğa/Ayı/Yatay/Volatil)"),
       ```
       `signal_tracking_service.calibration_report(...)` çağrısına `regime=regime` kwarg ekle.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; .venv/bin/python -c "
from app.services.signal_tracking import signal_tracking_service, calculate_round_trip_cost
import inspect
sig = inspect.signature(signal_tracking_service.calibration_report)
assert 'regime' in sig.parameters, 'calibration_report regime parametresi eksik'
sig2 = inspect.signature(signal_tracking_service.list_outcomes)
assert 'regime' in sig2.parameters, 'list_outcomes regime parametresi eksik'
assert hasattr(signal_tracking_service, '_bucket_rows_by_regime'), '_bucket_rows_by_regime eksik'
assert hasattr(signal_tracking_service, '_slippage_cost_summary'), '_slippage_cost_summary eksik'
print('Tum kontroller gecti')
" &amp;&amp; .venv/bin/python -c "from app.api.signals import router; print('signals import ok')"</automated>
  </verify>
  <done>
    calibration_report by_regime ve by_slippage_cost döner. list_outcomes items her kalemde regime içerir. Her iki endpoint regime sorgu parametresini kabul eder. Modül importları hatasız.
  </done>
</task>

</tasks>

<verification>
- `grep -q "def calculate_round_trip_cost" backend/app/services/signal_tracking.py` eşleşir
- `grep -q "MarketRegime" backend/app/services/signal_tracking.py` eşleşir
- `grep -c "regime" backend/app/api/signals.py` >= 4 döner (her iki endpoint'te parametre + kwarg)
- `pytest backend/tests/test_backtest_cost_regime.py` 6 testin tamamını geçer
- `cd backend && .venv/bin/python -c "from app.services.signal_tracking import signal_tracking_service"` hatasız çalışır
</verification>

<success_criteria>
- calculate_round_trip_cost BIST30 için 0.004, orta için 0.006, düşük için 0.010 döner
- calibration_report yanıtı by_regime (kovalar listesi) ve by_slippage_cost (özet dict) içerir
- list_outcomes her kalemde regime string alanı içerir
- Her iki endpoint regime=X ile filtrelenebilir
- Plan 54-02 (frontend) hiçbir backend değişikliği gerektirmeden bu verileri tüketebilir
</success_criteria>

<output>
Tamamlanınca `.planning/phases/54-backtest-kalitesi/54-01-SUMMARY.md` oluştur. İçerik: değiştirilen metotlar, yeni fonksiyonlar, test sonuçları, API yanıt yapısındaki yeni anahtarlar.
</output>
