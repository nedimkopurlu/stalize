---
phase: 55-ui-hisse-detay-checklist
plan: 02
type: execute
wave: 2
depends_on:
  - 55-01
files_modified:
  - frontend/src/app/stocks/[symbol]/page.tsx
  - frontend/src/app/stocks/[symbol]/page.module.css
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "Hero section'da 'Pozisyon Aç' butonu görünür"
    - "Butona tıklandığında backdrop'lu modal açılır"
    - "Modal 7 checklist maddesini otomatik dolu değerlerle gösterir"
    - "Madde 1 (Rejim): mevcut regime.regime değerini renk göstergesiyle yansıtır"
    - "Madde 2 (Likidite): s.liquidity_score değerini ve düşükse uyarı metnini gösterir"
    - "Madde 3 (Skor): s.overall_score ve recommendation değerini gösterir"
    - "Madde 4 (Korelasyon): sabit bilgi metni gösterir, kullanıcı kendisi onaylar"
    - "Madde 5 (Tavan/Taban): daily_change_pct >= 9.8 veya <= -9.8 ise uyarı, değilse temiz mesaj gösterir"
    - "Madde 6 (Pozisyon Büyüklüğü): positionSize verisinden %1 ve %2 risk lot önerisini gösterir"
    - "Madde 7 (Çıkış Planı): ATR bazlı stop/hedef pre-fill metin alanı içerir, kullanıcı düzenleyebilir"
    - "Her maddenin yanında kullanıcının işaretleyeceği bir checkbox var"
    - "'Tümünü Onayladım' butonu modalı kapatır, backend çağrısı yapmaz"
    - "Backdrop'a veya X butonuna tıklamak modalı kapatır"
    - "Modal açıkken body scroll kilitlenir"
  artifacts:
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "PreTradeChecklistModal bileşeni + checklistOpen state + positionBtn"
      contains: "checklistOpen"
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "7 checklist maddesi otomatik dolu değerlerle"
      contains: "Piyasa Rejimi"
    - path: "frontend/src/app/stocks/[symbol]/page.module.css"
      provides: "modal, modalBackdrop, modalCard, checklistItem CSS class'ları"
      contains: ".modalBackdrop"
  key_links:
    - from: "positionBtn onClick"
      to: "setChecklistOpen(true)"
      via: "React state"
      pattern: "setChecklistOpen\\(true\\)"
    - from: "PreTradeChecklistModal"
      to: "regime, s.liquidity_score, s.overall_score, s.daily_change_pct, positionSize, technical"
      via: "props veya closure — page state'inden türetilir"
      pattern: "checklistOpen"
    - from: "Çıkış Planı textarea"
      to: "technical.stop_loss, technical.target_price (ATR bazlı pre-fill)"
      via: "formatPrice helper"
      pattern: "exitPlan"
---

<objective>
Hisse detay sayfasına "Pozisyon Aç" butonu ve 7 maddelik ön-işlem checklist modalı ekle.

Purpose: Kullanıcı pozisyon açmadan önce rejim, likidite, skor, korelasyon, tavan/taban, pozisyon büyüklüğü ve çıkış planını tek ekranda gözden geçirsin — karar kullanıcıda, tüm veri mevcut.
Output: Modal bileşen (inline page.tsx'de), "Pozisyon Aç" butonu hero'da, 7 auto-populated checklist maddesi, CSS-only modal.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/55-ui-hisse-detay-checklist/55-CONTEXT.md
@.planning/phases/55-ui-hisse-detay-checklist/55-01-SUMMARY.md
@frontend/src/app/stocks/[symbol]/page.tsx
@frontend/src/app/stocks/[symbol]/page.module.css

<interfaces>
<!-- 55-01'den sonra page.tsx'in state'leri ve mevcut değişkenler -->

Mevcut state'ler (page.tsx içinde, değiştirilmez):
```typescript
const [regime, setRegime] = useState<MarketRegimeResponse | null>(null);
const [positionSize, setPositionSize] = useState<PositionSizeResponse | null>(null);
const [technical, setTechnical] = useState<TechnicalResult | null>(null);
const [decision, setDecision] = useState<InvestmentDecision | null>(null);
```

Mevcut türetilmiş değişkenler (return bloğu öncesinde zaten hesaplanmış):
```typescript
const s = detail.stock;              // StockSummary & { currency, last_data_update }
const recommendation = bd?.breakdown?.recommendation ?? s.recommendation ?? null;
const planStop = decision?.stop_loss ?? stopLoss;    // number | null
const planTarget = decision?.target_price ?? targetPrice;  // number | null
const chartPositive = (s.daily_change_pct ?? 0) >= 0;
```

StockSummary alanları (UI-02 için kullanılacaklar):
```typescript
s.liquidity_score   // "yüksek" | "orta" | "düşük" | null
s.overall_score     // number | null
s.daily_change_pct  // number | null
```

PositionSizeResponse:
```typescript
interface PositionSizeResponse {
  symbol: string;
  current_price: number;
  atr_14: number | null;
  stop_distance: number | null;
  portfolio_value: number;
  risk_amount_1pct: number;
  risk_amount_2pct: number;
  max_shares_1pct: number | null;
  max_shares_2pct: number | null;
}
```

MarketRegimeResponse:
```typescript
interface MarketRegimeResponse {
  regime: 'Boğa' | 'Ayı' | 'Yatay' | 'Volatil' | string;
  date: string;
  adx: number | null;
  ema200: number | null;
  atr: number | null;
}
```

Mevcut helper'lar (page.tsx içinde, yeniden tanımlama yapma):
- `formatPrice(value: number | null): string` — StockHelpers'dan import
- `formatScoreNumber(value, digits)` — page.tsx içinde tanımlı
- `indicatorValue(value, suffix, digits)` — page.tsx içinde tanımlı

CSS token'lar (page.module.css'de :root tanımlı):
```css
--bg-primary: #0d0e10
--bg-surface: #16181c
--bg-elevated: #1c1f24
--border-primary: rgba(255,255,255,0.06)
--accent: #f59e0b
--accent-green: #10b981
--accent-red: #ef4444
--text-primary: #f5f5f7
--text-muted: #8b8d93
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PreTradeChecklistModal bileşeni ve CSS</name>
  <files>
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/stocks/[symbol]/page.module.css
  </files>
  <read_first>
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/stocks/[symbol]/page.module.css
  </read_first>
  <action>
    1) `page.module.css`'e modal CSS'ini ekle (dosyanın sonuna):

    ```css
    /* ── Pre-trade Checklist Modal (Phase 55 UI-02) ─────────── */
    .modalBackdrop {
      position: fixed;
      inset: 0;
      z-index: 100;
      background: rgba(0, 0, 0, 0.72);
      backdrop-filter: blur(4px);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
      overflow-y: auto;
    }

    .modalCard {
      background: var(--bg-surface);
      border: 1px solid var(--border-primary);
      border-radius: 12px;
      padding: 28px;
      width: 100%;
      max-width: 540px;
      position: relative;
      max-height: 90vh;
      overflow-y: auto;
    }

    .modalHeader {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      margin-bottom: 6px;
    }

    .modalTitle {
      font-family: var(--font-fraunces);
      font-size: 1.4rem;
      font-weight: 800;
      color: var(--text-primary);
      margin: 0;
    }

    .modalSubtitle {
      font-size: 0.8rem;
      color: var(--text-muted);
      margin: 0 0 24px;
      line-height: 1.5;
    }

    .modalCloseBtn {
      background: none;
      border: none;
      color: var(--text-muted);
      font-size: 1.2rem;
      cursor: pointer;
      padding: 0 0 0 12px;
      line-height: 1;
      flex-shrink: 0;
      transition: color 0.15s;
    }

    .modalCloseBtn:hover {
      color: var(--text-primary);
    }

    .checklistItems {
      display: flex;
      flex-direction: column;
      gap: 0;
    }

    .checklistItem {
      display: grid;
      grid-template-columns: 28px 1fr 28px;
      gap: 10px;
      align-items: start;
      padding: 14px 0;
      border-bottom: 1px solid var(--border-primary);
    }

    .checklistItem:last-child {
      border-bottom: none;
    }

    .checklistIcon {
      font-size: 1rem;
      line-height: 1.4;
      color: var(--text-muted);
    }

    .checklistContent {
      display: flex;
      flex-direction: column;
      gap: 3px;
    }

    .checklistLabel {
      font-size: 0.68rem;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--text-dim);
    }

    .checklistValue {
      font-size: 0.9rem;
      color: var(--text-primary);
      line-height: 1.45;
    }

    .checklistNote {
      font-size: 0.76rem;
      color: var(--text-muted);
      line-height: 1.4;
    }

    .checklistWarning {
      font-size: 0.8rem;
      color: var(--accent-red);
      font-weight: 600;
    }

    .checklistCheckbox {
      width: 18px;
      height: 18px;
      accent-color: var(--accent);
      cursor: pointer;
      margin-top: 2px;
    }

    .exitPlanTextarea {
      width: 100%;
      margin-top: 8px;
      padding: 8px 10px;
      background: var(--bg-elevated);
      border: 1px solid var(--border-primary);
      border-radius: 6px;
      color: var(--text-primary);
      font-size: 0.82rem;
      line-height: 1.5;
      resize: vertical;
      min-height: 64px;
      font-family: inherit;
    }

    .exitPlanTextarea:focus {
      outline: none;
      border-color: var(--accent);
    }

    .modalDivider {
      height: 1px;
      background: var(--border-primary);
      margin: 20px 0;
    }

    .modalConfirmBtn {
      width: 100%;
      padding: 12px 20px;
      background: rgba(16, 185, 129, 0.15);
      color: var(--accent-green);
      border: 1px solid rgba(16, 185, 129, 0.35);
      border-radius: 8px;
      font-size: 0.9rem;
      font-weight: 700;
      cursor: pointer;
      transition: background 0.15s;
      letter-spacing: 0.02em;
    }

    .modalConfirmBtn:hover {
      background: rgba(16, 185, 129, 0.25);
    }

    /* ── Pozisyon Aç button (hero) ── */
    .positionBtn {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 9px 18px;
      background: rgba(16, 185, 129, 0.12);
      color: var(--accent-green);
      border: 1px solid rgba(16, 185, 129, 0.3);
      border-radius: 8px;
      font-size: 0.82rem;
      font-weight: 700;
      cursor: pointer;
      transition: background 0.15s;
      letter-spacing: 0.02em;
      margin-top: 4px;
    }

    .positionBtn:hover {
      background: rgba(16, 185, 129, 0.22);
    }

    @media (max-width: 480px) {
      .modalCard {
        padding: 20px;
        border-radius: 8px;
      }
      .modalBackdrop {
        padding: 12px;
      }
    }
    ```

    2) `page.tsx`'e `PreTradeChecklistModal` bileşenini ekle. `RegimeSection` bileşeninin hemen altına, module-level fonksiyon olarak:

    ```typescript
    // ── Pre-trade Checklist Modal ─────────────────────────────

    interface ChecklistModalProps {
      open: boolean;
      onClose: () => void;
      regime: MarketRegimeResponse | null;
      liquidityScore: string | null | undefined;
      overallScore: number | null | undefined;
      recommendation: string | null;
      dailyChangePct: number | null | undefined;
      positionSize: PositionSizeResponse | null;
      planStop: number | null;
      planTarget: number | null;
      symbol: string;
    }

    function PreTradeChecklistModal({
      open,
      onClose,
      regime,
      liquidityScore,
      overallScore,
      recommendation,
      dailyChangePct,
      positionSize,
      planStop,
      planTarget,
      symbol,
    }: ChecklistModalProps) {
      const [exitPlan, setExitPlan] = React.useState(() => {
        const stopStr = planStop != null ? `₺${formatPrice(planStop)}` : 'ATR×2 aşağısı';
        const targetStr = planTarget != null ? `₺${formatPrice(planTarget)}` : 'ATR×3 yukarısı';
        return `Stop: ${stopStr}, Hedef: ${targetStr}`;
      });

      // Body scroll kilitle
      React.useEffect(() => {
        if (open) {
          document.body.style.overflow = 'hidden';
        } else {
          document.body.style.overflow = '';
        }
        return () => { document.body.style.overflow = ''; };
      }, [open]);

      if (!open) return null;

      const regimeColor: Record<string, string> = {
        'Boğa': 'var(--accent-green)',
        'Ayı': 'var(--accent-red)',
        'Yatay': 'var(--text-muted)',
        'Volatil': '#f59e0b',
      };
      const regimeCol = regime ? (regimeColor[regime.regime] ?? 'var(--text-muted)') : 'var(--text-muted)';

      const isLimit = Math.abs(dailyChangePct ?? 0) >= 9.8;
      const limitDir = (dailyChangePct ?? 0) >= 9.8 ? 'TAVAN' : 'TABAN';

      const liqLabel = liquidityScore === 'yüksek' ? 'Yüksek'
        : liquidityScore === 'orta' ? 'Orta'
        : liquidityScore === 'düşük' ? 'Düşük'
        : 'Bilinmiyor';

      return (
        <div
          className={styles.modalBackdrop}
          onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}
          role="dialog"
          aria-modal="true"
          aria-labelledby="checklist-title"
        >
          <div className={styles.modalCard}>
            <div className={styles.modalHeader}>
              <h2 className={styles.modalTitle} id="checklist-title">
                Ön-işlem Kontrol Listesi
              </h2>
              <button className={styles.modalCloseBtn} onClick={onClose} aria-label="Kapat">×</button>
            </div>
            <p className={styles.modalSubtitle}>
              {symbol} için pozisyon açmadan önce her maddeyi gözden geçirin ve onaylayın.
            </p>

            <div className={styles.checklistItems}>

              {/* 1. Piyasa Rejimi */}
              <div className={styles.checklistItem}>
                <span className={styles.checklistIcon}>📊</span>
                <div className={styles.checklistContent}>
                  <span className={styles.checklistLabel}>Piyasa Rejimi</span>
                  <span className={styles.checklistValue} style={{ color: regimeCol }}>
                    {regime ? regime.regime : 'Bilinmiyor'}
                  </span>
                  <span className={styles.checklistNote}>
                    {regime?.regime === 'Ayı' || regime?.regime === 'Volatil'
                      ? 'Dikkat: Zor piyasa koşulları — pozisyon büyüklüğünü azaltın.'
                      : 'Rejim pozisyon kararını destekliyor.'}
                  </span>
                </div>
                <input type="checkbox" className={styles.checklistCheckbox} />
              </div>

              {/* 2. Likidite */}
              <div className={styles.checklistItem}>
                <span className={styles.checklistIcon}>💧</span>
                <div className={styles.checklistContent}>
                  <span className={styles.checklistLabel}>Likidite</span>
                  <span className={styles.checklistValue}>{liqLabel}</span>
                  {liquidityScore === 'düşük' && (
                    <span className={styles.checklistWarning}>
                      Düşük likidite — emirler beklenen fiyattan işlem görmeyebilir.
                    </span>
                  )}
                </div>
                <input type="checkbox" className={styles.checklistCheckbox} />
              </div>

              {/* 3. Genel Skor */}
              <div className={styles.checklistItem}>
                <span className={styles.checklistIcon}>✦</span>
                <div className={styles.checklistContent}>
                  <span className={styles.checklistLabel}>Genel Skor</span>
                  <span className={styles.checklistValue}>
                    {overallScore != null ? overallScore.toFixed(1) : '—'}
                    {recommendation ? ` — ${recommendation}` : ''}
                  </span>
                  <span className={styles.checklistNote}>
                    {(overallScore ?? 0) >= 60
                      ? 'Skor yeterli — devam edilebilir.'
                      : 'Skor düşük — yüksek riskli giriş.'}
                  </span>
                </div>
                <input type="checkbox" className={styles.checklistCheckbox} />
              </div>

              {/* 4. Korelasyon */}
              <div className={styles.checklistItem}>
                <span className={styles.checklistIcon}>🔗</span>
                <div className={styles.checklistContent}>
                  <span className={styles.checklistLabel}>Korelasyon</span>
                  <span className={styles.checklistValue}>Manuel kontrol gerekli</span>
                  <span className={styles.checklistNote}>
                    Portföydeki diğer pozisyonlarla korelasyonu kontrol edin. Portföy sayfasındaki korelasyon matrisini kullanın.
                  </span>
                </div>
                <input type="checkbox" className={styles.checklistCheckbox} />
              </div>

              {/* 5. Tavan / Taban */}
              <div className={styles.checklistItem}>
                <span className={styles.checklistIcon}>⚡</span>
                <div className={styles.checklistContent}>
                  <span className={styles.checklistLabel}>Tavan / Taban Durumu</span>
                  {isLimit ? (
                    <>
                      <span className={styles.checklistValue} style={{ color: limitDir === 'TAVAN' ? 'var(--accent-green)' : 'var(--accent-red)' }}>
                        {limitDir} devre kesici aktif
                      </span>
                      <span className={styles.checklistWarning}>
                        Hisse bugün {limitDir}'da. İşlem fiyatları garanti edilemez.
                      </span>
                    </>
                  ) : (
                    <span className={styles.checklistValue}>Tavan/taban yok — normal işlem</span>
                  )}
                </div>
                <input type="checkbox" className={styles.checklistCheckbox} />
              </div>

              {/* 6. Pozisyon Büyüklüğü */}
              <div className={styles.checklistItem}>
                <span className={styles.checklistIcon}>📐</span>
                <div className={styles.checklistContent}>
                  <span className={styles.checklistLabel}>Pozisyon Büyüklüğü</span>
                  <span className={styles.checklistValue}>
                    {positionSize
                      ? `%1 risk → ${positionSize.max_shares_1pct ?? '—'} lot · %2 risk → ${positionSize.max_shares_2pct ?? '—'} lot`
                      : 'Portföyünüzün %1-2\'sini risk edin.'}
                  </span>
                  <span className={styles.checklistNote}>
                    Stop mesafesi ATR×2. Önerilen maksimum lot sayısı.
                  </span>
                </div>
                <input type="checkbox" className={styles.checklistCheckbox} />
              </div>

              {/* 7. Çıkış Planı */}
              <div className={styles.checklistItem}>
                <span className={styles.checklistIcon}>🚪</span>
                <div className={styles.checklistContent}>
                  <span className={styles.checklistLabel}>Çıkış Planı</span>
                  <span className={styles.checklistNote}>Planınızı aşağıda düzenleyebilirsiniz:</span>
                  <textarea
                    className={styles.exitPlanTextarea}
                    value={exitPlan}
                    onChange={(e) => setExitPlan(e.target.value)}
                    rows={3}
                  />
                </div>
                <input type="checkbox" className={styles.checklistCheckbox} />
              </div>

            </div>

            <div className={styles.modalDivider} />

            <button className={styles.modalConfirmBtn} onClick={onClose}>
              Tümünü Onayladım — Devam Et
            </button>
          </div>
        </div>
      );
    }
    ```

    NOT: `PositionSizeResponse` tipi `api.ts`'den zaten import ediliyor. `React` import `React, { use, useCallback, useEffect, useState }` zaten var. Modal içinde `React.useState` ve `React.useEffect` kullanılır (React namespace'i zaten import edilmiş). `formatPrice` zaten import edilmiş. `MarketRegimeResponse` zaten import edilmiş.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend &amp;&amp; grep -q 'PreTradeChecklistModal' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; grep -q 'modalBackdrop' src/app/stocks/\[symbol\]/page.module.css &amp;&amp; grep -q 'checklistItem' src/app/stocks/\[symbol\]/page.module.css &amp;&amp; echo "modal component ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'PreTradeChecklistModal' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
    - `grep -q '.modalBackdrop' frontend/src/app/stocks/[symbol]/page.module.css` eşleşir
    - `grep -q '.checklistItem' frontend/src/app/stocks/[symbol]/page.module.css` eşleşir
    - `grep -q 'exitPlanTextarea' frontend/src/app/stocks/[symbol]/page.module.css` eşleşir
    - `grep -q 'Tümünü Onayladım' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
    - `grep -c 'checklistItem' frontend/src/app/stocks/[symbol]/page.tsx` en az 7 döner (7 madde)
    - `cd frontend && npm run build 2>&1 | grep -E 'error TS'` boş çıktı verir
  </acceptance_criteria>
  <done>
    PreTradeChecklistModal bileşeni tanımlanmış, 7 madde mevcut, modal CSS tam, TypeScript derleme hatası yok.
  </done>
</task>

<task type="auto">
  <name>Task 2: "Pozisyon Aç" butonu ve modal entegrasyonu</name>
  <files>
    frontend/src/app/stocks/[symbol]/page.tsx
  </files>
  <read_first>
    frontend/src/app/stocks/[symbol]/page.tsx
  </read_first>
  <action>
    1) `StockDetailPage` bileşenine `checklistOpen` state'i ekle (mevcut state tanımlarının sonuna):

    ```typescript
    const [checklistOpen, setChecklistOpen] = useState(false);
    ```

    2) Return bloğunda hero section içinde, `<RegimeBadge regime={regime} />` satırının hemen ardına "Pozisyon Aç" butonu ekle:

    ```tsx
    <RegimeBadge regime={regime} />
    <button
      className={styles.positionBtn}
      onClick={() => setChecklistOpen(true)}
    >
      + Pozisyon Aç
    </button>
    ```

    NOT: `positionBtn` CSS class'ı 55-02 Task 1'de page.module.css'e eklendi (içeride `.positionBtn` tanımlı). Buton `heroLeft` div'inin içinde, flex column layout'ta altta görünür.

    3) Return bloğunda `<AppShell>` içinde, editorialPage div'inin kapanış tag'ından ÖNCE `PreTradeChecklistModal`'ı render et:

    ```tsx
    <PreTradeChecklistModal
      open={checklistOpen}
      onClose={() => setChecklistOpen(false)}
      regime={regime}
      liquidityScore={s.liquidity_score}
      overallScore={s.overall_score}
      recommendation={recommendation}
      dailyChangePct={s.daily_change_pct}
      positionSize={positionSize}
      planStop={planStop}
      planTarget={planTarget}
      symbol={symbol}
    />
    ```

    `planStop` ve `planTarget` return bloğu öncesinde zaten hesaplanmış değişkenler:
    ```typescript
    const planStop = decision?.stop_loss ?? stopLoss;
    const planTarget = decision?.target_price ?? targetPrice;
    ```

    4) Return bloğunda loading ve not-found erken return'lerini kontrol et: `checklistOpen` state'i yalnızca ana return path'inde gerekli, erken return'ler değişmez.

    5) `recommendation` değişkeni zaten return bloğu öncesinde mevcut:
    ```typescript
    const recommendation = bd?.recommendation ?? s.recommendation ?? null;
    ```
    Modal'a doğrudan `recommendation` geçilir.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend &amp;&amp; grep -q 'checklistOpen' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; grep -q 'positionBtn' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; grep -q 'setChecklistOpen' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; echo "modal integration ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'checklistOpen' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
    - `grep -q 'positionBtn' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
    - `grep -c 'setChecklistOpen' frontend/src/app/stocks/[symbol]/page.tsx` en az 2 döner (open ve close)
    - `grep -q 'PreTradeChecklistModal' frontend/src/app/stocks/[symbol]/page.tsx` bileşen render satırı mevcut
    - `cd frontend && npm run build 2>&1 | grep -E 'error TS'` boş çıktı verir
    - Tarayıcıda `/stocks/THYAO` açıldığında hero'da "Pozisyon Aç" butonu görünür
    - Butona tıklanınca modal açılır, backdrop'a tıklanınca kapanır
  </acceptance_criteria>
  <done>
    "Pozisyon Aç" butonu hero'da görünür, modal tıklanınca açılır ve kapanır, 7 madde render edilir, backend çağrısı yapılmaz.
  </done>
</task>

</tasks>

<verification>
- `grep -q 'checklistOpen' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
- `grep -q 'PreTradeChecklistModal' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
- `grep -q '.modalBackdrop' frontend/src/app/stocks/[symbol]/page.module.css` eşleşir
- `grep -c 'checklistItem' frontend/src/app/stocks/[symbol]/page.tsx` >= 7 döner
- `cd frontend && npm run build` başarıyla tamamlanır (TypeScript hatası yok)
- `/stocks/[herhangi-symbol]` açıldığında: Hero'da "Pozisyon Aç" görünür, tıklanınca modal açılır, 7 madde otomatik dolu, "Tümünü Onayladım" modalı kapatır
</verification>

<success_criteria>
- Kullanıcı "Pozisyon Aç" butonuna tıklar → modal açılır
- Modal'da 7 madde: rejim rengi ve adı, likidite seviyesi (uyarı varsa kırmızı), genel skor ve tavsiye, korelasyon notu, tavan/taban durumu, %1/%2 risk lot önerisi, düzenlenebilir çıkış planı alanı
- Her maddenin yanında kullanıcı checkboxu var
- "Tümünü Onayladım" → modal kapanır, sayfada hiçbir şey değişmez, API çağrısı olmaz
- UI-02 gereksinimi karşılanmış sayılır
</success_criteria>

<output>
After completion, create `.planning/phases/55-ui-hisse-detay-checklist/55-02-SUMMARY.md` capturing: modal component props interface, 7 checklist items and their data sources, button placement, CSS classes added, build status.
</output>
