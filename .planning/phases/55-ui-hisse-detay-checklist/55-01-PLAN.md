---
phase: 55-ui-hisse-detay-checklist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/stocks/[symbol]/page.tsx
  - frontend/src/app/stocks/[symbol]/page.module.css
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Sayfa yüklendiğinde topNav'ın hemen altında 7 anchor linkten oluşan yapışkan bir section nav görünür"
    - "Her section link (Hero, Skor Özeti, Teknik, Temel, Haberler, Piyasa Rejimi, İlgili Hisseler) ilgili bölüme scroll eder"
    - "Mevcut tüm içerik (skor dökümü, grafik, teknik/temel, haberler, peers) sayfada korunur, hiçbir şey kaldırılmaz"
    - "Sayfanın DOM'unda id='hero', id='skor-ozeti', id='teknik', id='temel', id='haberler', id='piyasa-rejimi', id='ilgili-hisseler' elementleri bulunur"
    - "regime verisi mevcutsa 'Piyasa Rejimi' section'ı ADX, EMA200 ve ATR değerlerini gösterir"
    - "regime null ise Piyasa Rejimi section'ı hiç render edilmez"
    - "Section nav mobilde yatay scroll destekler (overflow-x: auto)"
  artifacts:
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "SectionNav bileşeni, id attribute'lu section wrapper'lar, RegimiSection bileşeni"
      contains: "id=\"hero\""
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "RegimeSection bileşeni ADX/EMA200/ATR gösterir"
      contains: "id=\"piyasa-rejimi\""
    - path: "frontend/src/app/stocks/[symbol]/page.module.css"
      provides: "sectionNav, sectionNavLink, sectionNavLinkActive CSS class'ları"
      contains: ".sectionNav"
  key_links:
    - from: "SectionNav anchor href='#hero'"
      to: "DOM element id='hero'"
      via: "HTML hash navigation"
      pattern: "id=\"hero\""
    - from: "regime state (MarketRegimeResponse)"
      to: "RegimeSection bileşeni"
      via: "props: regime.adx, regime.ema200, regime.atr, regime.regime"
      pattern: "regime\\.adx"
---

<objective>
Hisse detay sayfasını hiyerarşik section'lara böl ve yapışkan bir section navigasyon bar'ı ekle.

Purpose: Uzun scroll sayfada kullanıcının hızlıca istediği bölüme (Teknik, Temel, Haberler vb.) gidebilmesini sağlamak — içerik değişmeden, sadece yapı düzenleniyor.
Output: 7 anchor linkli sticky sectionNav, DOM'da id attribute'lu section wrapper'lar, yeni "Piyasa Rejimi" dedicated section card'ı.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/55-ui-hisse-detay-checklist/55-CONTEXT.md
@frontend/src/app/stocks/[symbol]/page.tsx
@frontend/src/app/stocks/[symbol]/page.module.css

<interfaces>
<!-- Mevcut page.tsx'den kritik state'ler ve yapı — executor bunları doğrudan kullanır -->

State'ler (zaten mevcut, değiştirilmez):
```typescript
const [regime, setRegime] = useState<MarketRegimeResponse | null>(null);
const [technical, setTechnical] = useState<TechnicalResult | null>(null);
```

MarketRegimeResponse (api.ts'den):
```typescript
export interface MarketRegimeResponse {
  regime: 'Boğa' | 'Ayı' | 'Yatay' | 'Volatil' | string;
  date: string;
  adx: number | null;
  ema200: number | null;
  atr: number | null;
}
```

Mevcut section sırası (DOM'da, değiştirilmeyecek içerik):
1. topNav div
2. `<section className={styles.hero}>` — hero
3. `<section className={styles.breakdownSection}>` — skor dökümü
4. `<section className={styles.positionSizeSection}>` — pozisyon büyüklüğü
5. `<section className={styles.thesisSection}>` — yatırım kararı
6. `<section className={styles.chartSection}>` — grafik
7. `<section className={styles.dossierSection}>` — teknik + temel kartlar
8. `<section className={styles.newsDossier}>` — haberler
9. `<section className={styles.analysisSection}>` — model gerekçeleri
10. `<section className={styles.bottomSection}>` — peers

Mevcut topNav CSS:
```css
.topNav {
  position: sticky;
  top: 0;
  z-index: 10;
  /* height: approx 53px (14px padding top + 14px bottom + content) */
}
```

RegimeBadge bileşeni zaten mevcut:
```typescript
function RegimeBadge({ regime }: { regime: MarketRegimeResponse | null }) {
  const colorMap: Record<string, string> = {
    'Boğa': 'var(--accent-green)',
    'Ayı': 'var(--accent-red)',
    'Yatay': 'var(--text-muted)',
    'Volatil': '#f59e0b',
  };
  // ...
}
```

indicatorValue helper (zaten mevcut):
```typescript
function indicatorValue(value: number | null | undefined, suffix = '', digits = 2) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SectionNav bileşeni ve CSS</name>
  <files>
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/stocks/[symbol]/page.module.css
  </files>
  <read_first>
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/stocks/[symbol]/page.module.css
  </read_first>
  <action>
    1) `page.module.css`'e şu CSS class'larını ekle (dosyanın sonuna append et):

    ```css
    /* ── Section Nav (Phase 55 UI-01) ───────────────────────── */
    .sectionNav {
      position: sticky;
      top: 53px; /* topNav height */
      z-index: 9;
      background: var(--bg-primary);
      border-bottom: 1px solid var(--border-primary);
      display: flex;
      gap: 0;
      padding: 0 40px;
      overflow-x: auto;
      scrollbar-width: none;
      -ms-overflow-style: none;
    }

    .sectionNav::-webkit-scrollbar {
      display: none;
    }

    .sectionNavLink {
      display: inline-flex;
      align-items: center;
      padding: 10px 16px;
      font-size: 0.75rem;
      font-weight: 600;
      color: var(--text-muted);
      text-decoration: none;
      border-bottom: 2px solid transparent;
      white-space: nowrap;
      transition: color 0.15s, border-color 0.15s;
      cursor: pointer;
      background: none;
      border-top: none;
      border-left: none;
      border-right: none;
    }

    .sectionNavLink:hover {
      color: var(--text-primary);
    }

    .sectionNavLinkActive {
      color: var(--accent);
      border-bottom-color: var(--accent);
    }

    @media (max-width: 768px) {
      .sectionNav {
        padding: 0 20px;
      }
    }
    ```

    2) `page.tsx`'e `SectionNav` bileşenini ekle — dosyadaki diğer helper bileşenlerin altına (RegimeBadge'den sonra, Skeleton'dan önce), module-level fonksiyon olarak:

    ```typescript
    // ── Section Nav ───────────────────────────────────────────

    const SECTION_NAV_ITEMS = [
      { id: 'hero', label: 'Hero' },
      { id: 'skor-ozeti', label: 'Skor Özeti' },
      { id: 'teknik', label: 'Teknik' },
      { id: 'temel', label: 'Temel' },
      { id: 'haberler', label: 'Haberler' },
      { id: 'piyasa-rejimi', label: 'Piyasa Rejimi' },
      { id: 'ilgili-hisseler', label: 'İlgili Hisseler' },
    ] as const;

    function SectionNav({ activeSection }: { activeSection: string }) {
      return (
        <nav className={styles.sectionNav} aria-label="Sayfa bölümleri">
          {SECTION_NAV_ITEMS.map((item) => (
            <a
              key={item.id}
              href={`#${item.id}`}
              className={`${styles.sectionNavLink} ${activeSection === item.id ? styles.sectionNavLinkActive : ''}`}
            >
              {item.label}
            </a>
          ))}
        </nav>
      );
    }
    ```

    3) `StockDetailPage` bileşenine `activeSection` state'i ekle (mevcut state tanımlarının yanına):

    ```typescript
    const [activeSection, setActiveSection] = useState('hero');
    ```

    4) `StockDetailPage` bileşenine `useEffect` ile `IntersectionObserver` ekle (mevcut `useEffect`'lerin ardına):

    ```typescript
    useEffect(() => {
      const sectionIds = SECTION_NAV_ITEMS.map((item) => item.id);
      const observers: IntersectionObserver[] = [];

      sectionIds.forEach((id) => {
        const el = document.getElementById(id);
        if (!el) return;
        const observer = new IntersectionObserver(
          ([entry]) => {
            if (entry.isIntersecting) setActiveSection(id);
          },
          { threshold: 0.2, rootMargin: '-60px 0px -60% 0px' }
        );
        observer.observe(el);
        observers.push(observer);
      });

      return () => observers.forEach((o) => o.disconnect());
    }, [loading]);
    ```

    5) `return` bloğunda, `topNav` div'inin hemen ardına `<SectionNav activeSection={activeSection} />` ekle:

    ```tsx
    {/* topNav div */}
    <div className={styles.topNav}>...</div>

    {/* ── Section Nav ── */}
    <SectionNav activeSection={activeSection} />

    {/* ── Hero section ── */}
    <section id="hero" className={styles.hero}>
      ...
    </section>
    ```

    NOT: Mevcut `<section className={styles.hero}>` → `<section id="hero" className={styles.hero}>` olarak güncelle (sadece `id` attribute ekle, class değişmez).
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend &amp;&amp; grep -q 'id="hero"' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; grep -q 'sectionNav' src/app/stocks/\[symbol\]/page.module.css &amp;&amp; grep -q 'SectionNav' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; echo "section nav ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'id="hero"' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
    - `grep -q 'SectionNav' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
    - `grep -q '.sectionNav' frontend/src/app/stocks/[symbol]/page.module.css` eşleşir
    - `grep -q 'sectionNavLinkActive' frontend/src/app/stocks/[symbol]/page.module.css` eşleşir
    - `cd frontend && npm run build 2>&1 | grep -v warning | grep -v info` TypeScript hatası vermez
  </acceptance_criteria>
  <done>
    SectionNav bileşeni render edilir, 7 anchor link mevcut, CSS sticky nav tanımlı.
  </done>
</task>

<task type="auto">
  <name>Task 2: Section id'leri ve yeni Piyasa Rejimi section'ı</name>
  <files>
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/stocks/[symbol]/page.module.css
  </files>
  <read_first>
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/stocks/[symbol]/page.module.css
  </read_first>
  <action>
    1) `page.tsx` return bloğunda mevcut section'lara `id` attribute'larını ekle (içerik DEĞİŞTİRİLMEZ, sadece id eklenir):

    - `<section className={styles.hero}>` → `<section id="hero" className={styles.hero}>`
    - breakdownSection, positionSizeSection, thesisSection → bunları tek bir `<div id="skor-ozeti">` wrapper'ı içine al:
      ```tsx
      <div id="skor-ozeti">
        {/* ── Score breakdown section ── */}
        {bd && bd.components.length > 0 && (
          <section className={styles.breakdownSection}>...</section>
        )}
        {/* ── PORT-03: Pozisyon Büyüklüğü ── */}
        {positionSize && (
          <section className={styles.positionSizeSection}>...</section>
        )}
        {/* ── Investment thesis ── */}
        <section className={styles.thesisSection}>...</section>
      </div>
      ```
    - chartSection → `<section id="teknik" className={styles.chartSection}>`
    - dossierSection'ı böl: teknik card'ı `id="teknik"` div içine dahil et, temel card'ı `id="temel"` wrapper'a al:
      ```tsx
      <div id="teknik">
        <section className={styles.chartSection}>...</section>
        <section className={styles.dossierSection}>
          {/* Teknik kard — mevcut ilk article */}
          <article className={styles.dossierCard}>...Teknik Analiz...</article>
        </section>
      </div>
      <div id="temel">
        <section style={{ padding: '0 40px 32px' }}>
          {/* Temel kard — mevcut ikinci article */}
          <article className={styles.dossierCard}>...Temel Analiz...</article>
        </section>
      </div>
      ```
      NOT: dossierSection zaten `display: grid; grid-template-columns: 1fr 1fr` kullanıyor. İki kart'ı ayrı section'lara bölerken her kartı tek başına render etmek için `dossierSection` grid wrapper'ını çözmen gerekebilir. Alternatif: dossierSection'a `id="teknik"` ekle ve temel kart için yeni bir `<div id="temel">` ekle dossier'in hemen ardından temel article'ı taşıyarak. Hangisi daha temiz görünürse onu seç — önemli olan her iki kartın render edilmesi.
    - `<section className={styles.newsDossier}>` → `<section id="haberler" className={styles.newsDossier}>`
    - analysisSection + bottomSection → `<div id="ilgili-hisseler">` wrapper içine al

    2) `page.tsx`'e `RegimeSection` bileşenini ekle (SectionNav'ın altına, module-level):

    ```typescript
    // ── Regime Section ────────────────────────────────────────

    function RegimeSection({ regime }: { regime: MarketRegimeResponse | null }) {
      if (!regime) return null;
      const colorMap: Record<string, string> = {
        'Boğa': 'var(--accent-green)',
        'Ayı': 'var(--accent-red)',
        'Yatay': 'var(--text-muted)',
        'Volatil': '#f59e0b',
      };
      const color = colorMap[regime.regime] ?? 'var(--text-muted)';
      return (
        <section id="piyasa-rejimi" className={styles.regimeSection}>
          <div className={styles.regimeSectionCard}>
            <div className={styles.sectionEyebrow}>Piyasa Rejimi</div>
            <div className={styles.regimeSectionTitle} style={{ color }}>
              {regime.regime}
            </div>
            <div className={styles.regimeSectionGrid}>
              <div className={styles.regimeSectionItem}>
                <span className={styles.regimeSectionLabel}>ADX</span>
                <span className={styles.regimeSectionValue}>
                  {indicatorValue(regime.adx, '', 1)}
                </span>
                <small>Trend gücü — 25 üstü güçlü trend</small>
              </div>
              <div className={styles.regimeSectionItem}>
                <span className={styles.regimeSectionLabel}>EMA 200</span>
                <span className={styles.regimeSectionValue}>
                  {indicatorValue(regime.ema200, '', 0)}
                </span>
                <small>Uzun vadeli trend filtresi</small>
              </div>
              <div className={styles.regimeSectionItem}>
                <span className={styles.regimeSectionLabel}>ATR</span>
                <span className={styles.regimeSectionValue}>
                  {indicatorValue(regime.atr, '', 2)}
                </span>
                <small>Piyasa volatilitesi</small>
              </div>
            </div>
          </div>
        </section>
      );
    }
    ```

    3) Return bloğunda `<div id="ilgili-hisseler">` wrapper'ından hemen önce `<RegimeSection regime={regime} />` ekle:

    ```tsx
    <RegimeSection regime={regime} />
    <div id="ilgili-hisseler">
      {scoreReasonRows.length > 0 && (
        <section className={styles.analysisSection}>...</section>
      )}
      {peers.length > 0 && (
        <section className={styles.bottomSection}>...</section>
      )}
    </div>
    ```

    4) `page.module.css`'e Regime Section CSS'ini ekle (dosyanın sonuna):

    ```css
    /* ── Regime Section (Phase 55 UI-01) ────────────────────── */
    .regimeSection {
      padding: 0 40px 32px;
    }

    .regimeSectionCard {
      background: var(--bg-surface);
      border: 1px solid var(--border-primary);
      border-radius: 8px;
      padding: 22px;
    }

    .regimeSectionTitle {
      font-family: var(--font-fraunces);
      font-size: 2rem;
      font-weight: 800;
      margin: 8px 0 20px;
    }

    .regimeSectionGrid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1px;
      background: var(--border-primary);
      border-radius: 6px;
      overflow: hidden;
      border: 1px solid var(--border-primary);
    }

    .regimeSectionItem {
      background: var(--bg-elevated);
      padding: 14px 16px;
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .regimeSectionLabel {
      font-size: 0.65rem;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--text-dim);
    }

    .regimeSectionValue {
      font-family: var(--font-fraunces);
      font-size: 1.4rem;
      font-weight: 700;
      color: var(--text-primary);
    }

    .regimeSectionItem small {
      font-size: 0.72rem;
      color: var(--text-muted);
      line-height: 1.4;
    }

    @media (max-width: 768px) {
      .regimeSection {
        padding: 0 20px 32px;
      }
      .regimeSectionGrid {
        grid-template-columns: 1fr;
      }
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend &amp;&amp; grep -q 'id="piyasa-rejimi"' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; grep -q 'id="haberler"' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; grep -q 'id="skor-ozeti"' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; grep -q 'RegimeSection' src/app/stocks/\[symbol\]/page.tsx &amp;&amp; grep -q '.regimeSectionCard' src/app/stocks/\[symbol\]/page.module.css &amp;&amp; echo "ids and regime ok"</automated>
  </verify>
  <acceptance_criteria>
    - DOM'da `id="hero"`, `id="skor-ozeti"`, `id="teknik"`, `id="temel"`, `id="haberler"`, `id="piyasa-rejimi"`, `id="ilgili-hisseler"` elementleri mevcut
    - `grep 'id=' frontend/src/app/stocks/[symbol]/page.tsx` 7 farklı section id'sini listeler
    - `RegimeSection` bileşeni `regime?.adx`, `regime?.ema200`, `regime?.atr` değerlerini gösterir
    - `grep -q '.regimeSectionCard' frontend/src/app/stocks/[symbol]/page.module.css` eşleşir
    - Mevcut tüm içerik (breakdown, positionSize, thesis, chart, dossier, news, analysis, peers) DOM'da korunur
    - `cd frontend && npm run build 2>&1 | grep -E '^.*(error|Error).*$' | grep -v 'node_modules'` boş çıktı verir
  </acceptance_criteria>
  <done>
    7 section id'si DOM'da, Piyasa Rejimi kartı ADX/EMA200/ATR ile render edilir, tüm mevcut içerik yerinde.
  </done>
</task>

</tasks>

<verification>
- `grep -c 'id="' frontend/src/app/stocks/[symbol]/page.tsx` en az 7 döner
- `grep -q 'SectionNav' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
- `grep -q '.sectionNav' frontend/src/app/stocks/[symbol]/page.module.css` eşleşir
- `grep -q 'RegimeSection' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
- `grep -q 'regime.adx' frontend/src/app/stocks/[symbol]/page.tsx` eşleşir
- `cd frontend && npm run build` TypeScript veya ESLint hatası vermez
- Sayfayı tarayıcıda açıldığında topNav'ın altında 7 linkli nav bar görünür
</verification>

<success_criteria>
- Kullanıcı "Skor Özeti" linkine tıklar, sayfa `id="skor-ozeti"` elementine scroll eder
- `id="piyasa-rejimi"` section'ı, verisi varsa ADX + EMA200 + ATR gösteren bir kart render eder
- Mevcut tüm içerik (breakdown, chart, teknik, temel, haberler, peers) eksiksiz görünür
- UI-01 gereksinimi karşılanmış sayılır
</success_criteria>

<output>
After completion, create `.planning/phases/55-ui-hisse-detay-checklist/55-01-SUMMARY.md` capturing: added section ids, SectionNav component signature, RegimeSection component, CSS classes added, build status.
</output>
