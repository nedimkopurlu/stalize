---
phase: 45-veri-tazeligi-sistem-sagligi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/stocks.py
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - VERI-01
  - VERI-03
  - VERI-04

must_haves:
  truths:
    - "Hisse listesi API yanıtında her hisse için updated_at alanı bulunur"
    - "Frontend StockSummary interface updated_at: string | null alanını içerir"
    - "StockAnalysisResponse.generated_at alanı frontend'de erişilebilir (zaten mevcut)"
  artifacts:
    - path: "backend/app/api/stocks.py"
      provides: "GET /stocks list endpoint updated_at field"
      contains: "\"updated_at\": s.updated_at.isoformat()"
    - path: "frontend/src/lib/api.ts"
      provides: "StockSummary interface updated_at field"
      contains: "updated_at: string | null"
  key_links:
    - from: "backend/app/api/stocks.py"
      to: "frontend/src/lib/api.ts"
      via: "JSON serialization of Stock.updated_at"
      pattern: "updated_at.*isoformat"
---

<objective>
Backend `/stocks` list endpoint'e `updated_at` alanı eklenir ve frontend `StockSummary` interface'i güncellenir.

Purpose: Stale data banner ve altbilgi hesaplaması için frontend'in `updated_at` değerine ihtiyacı var. Backend Stock modelinde `updated_at` kolonu mevcut (DateTime) ancak list endpoint response'una dahil edilmemiş. `StockAnalysisResponse.generated_at` zaten mevcut — frontend interface'i doğrulanmış, ek backend değişikliği gerekmez.

Output:
- `backend/app/api/stocks.py` → stocks list response dict'ine `updated_at` eklendi
- `frontend/src/lib/api.ts` → `StockSummary` interface'e `updated_at: string | null` eklendi
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-veri-tazeligi-sistem-sagligi/45-CONTEXT.md

<interfaces>
<!-- Mevcut kod kontratları — executor keşif yapmadan kullanabilir -->

From backend/app/models/stock.py:42:
```python
updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
```

From backend/app/api/stocks.py (list endpoint response, line 146-168):
```python
return {
    "stocks": [
        {
            "symbol": s.symbol, "name": s.name, "sector": s.sector,
            "industry": s.industry, "current_price": s.current_price,
            "daily_change_pct": s.daily_change_pct, "volume": s.volume,
            "volume_ratio": compute_volume_ratio(s.volume, avg_volumes.get(s.id)),
            "market_cap": s.market_cap, "is_bist30": s.is_bist30,
            "is_bist100": s.is_bist100,
            "is_bist250": s.is_bist250,
            "market_tier": s.market_tier,
            "technical_score": s.technical_score,
            "fundamental_score": s.fundamental_score,
            "sentiment_score": s.sentiment_score,
            "overall_score": s.overall_score,
            "recommendation": s.recommendation,
        }
        for s in stocks
    ],
    "total": total,
    "limit": limit,
    "offset": offset,
}
```

From frontend/src/lib/api.ts (StockSummary, line 46-67):
```typescript
export interface StockSummary {
  symbol: string;
  name: string;
  sector: string | null;
  industry?: string | null;
  current_price: number | null;
  daily_change_pct: number | null;
  volume: number | null;
  volume_ratio: number | null;
  market_cap: number | null;
  is_bist30: boolean;
  is_bist100?: boolean;
  is_bist250?: boolean;
  market_tier?: string | null;
  technical_score: number | null;
  fundamental_score: number | null;
  sentiment_score: number | null;
  overall_score: number | null;
  recommendation: string | null;
  stop_loss?: number | null;
  target_price?: number | null;
}
```

From frontend/src/lib/api.ts (StockAnalysisResponse, line 907-912):
```typescript
export interface StockAnalysisResponse {
  symbol: string;
  analysis: string;
  cached: boolean;
  generated_at: string;  // Zaten mevcut — backend de datetime.now().isoformat() döndürüyor
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend list endpoint'e updated_at ekle</name>
  <files>backend/app/api/stocks.py</files>
  <read_first>
    - backend/app/api/stocks.py satır 146-168 (list endpoint return bloğu)
    - backend/app/models/stock.py satır 40-45 (updated_at kolonu)
  </read_first>
  <action>
`backend/app/api/stocks.py` dosyasında `GET /stocks` endpoint'inin return bloğundaki stocks list comprehension'a `updated_at` alanını ekle.

Mevcut satır 162'den sonra (recommendation'dan sonra), her `s` için:
```python
"updated_at": s.updated_at.isoformat() if s.updated_at else None,
```

`Stock.updated_at` bir `DateTime(timezone=True)` kolonu — `isoformat()` çağrısı ISO 8601 UTC string üretir (örn. `"2026-05-14T08:23:00+00:00"`).

Hiçbir başka değişiklik yapma — yalnızca bu tek satırı list comprehension dict'ine ekle.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && grep -n "updated_at.*isoformat" backend/app/api/stocks.py | head -5</automated>
  </verify>
  <done>
    `grep` çıktısı `"updated_at": s.updated_at.isoformat() if s.updated_at else None,` içeren satırı gösterir (satır numarası ~162-165 arasında).
  </done>
  <acceptance_criteria>
    - grep 'updated_at.*isoformat' backend/app/api/stocks.py satır bulur
    - Başka hiçbir satır değişmemiş (diff yalnızca tek satır ekleme)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: StockSummary interface'e updated_at ekle</name>
  <files>frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/lib/api.ts satır 46-67 (StockSummary interface)
  </read_first>
  <action>
`frontend/src/lib/api.ts` dosyasında `StockSummary` interface'ine `updated_at` alanını ekle.

`stop_loss?: number | null;` satırından önce (mevcut son iki opsiyonel alan öncesinde) şu satırı ekle:
```typescript
updated_at?: string | null;
```

Opsiyonel (`?`) yapılma sebebi: Mevcut frontend component'leri bu alanı beklemeden çalışıyor — geriye dönük uyumluluk korunur.

`StockAnalysisResponse.generated_at` alanını DOKUNMA — zaten mevcut ve doğru tiplenmiş.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && grep -n "updated_at" frontend/src/lib/api.ts</automated>
  </verify>
  <done>
    `grep` çıktısı `StockSummary` interface içinde `updated_at?: string | null;` satırını gösterir.
  </done>
  <acceptance_criteria>
    - grep 'updated_at' frontend/src/lib/api.ts StockSummary içinde satır bulur
    - TypeScript derleme hatası yok: cd frontend && npx tsc --noEmit 2>&1 | head -20 çıktısı hata içermez
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Task 1 doğrulama
grep -n "updated_at.*isoformat" /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/app/api/stocks.py

# Task 2 doğrulama
grep -n "updated_at" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/lib/api.ts

# TypeScript tip kontrolü
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- Backend `/stocks` endpoint yanıtı her hisse için `updated_at` ISO string (veya null) içerir
- `StockSummary` interface `updated_at?: string | null` alanına sahip
- TypeScript derleme hatası yok
- Wave 2 plan (45-02) bu alanları kullanmaya hazır
</success_criteria>

<output>
Tamamlandığında `.planning/phases/45-veri-tazeligi-sistem-sagligi/45-01-SUMMARY.md` oluştur.
</output>
