---
phase: 45-veri-tazeligi-sistem-sagligi
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - frontend/src/app/stocks/page.tsx
  - frontend/src/app/stocks/page.module.css
  - frontend/src/app/stocks/[symbol]/page.tsx
  - frontend/src/app/stocks/[symbol]/page.module.css
autonomous: true
requirements:
  - VERI-01
  - VERI-02
  - VERI-03
  - VERI-04

must_haves:
  truths:
    - "Hisse listesi altbilgisinde en son updated_at değerinden üretilmiş saat gösterilir (örn. 'Son güncelleme: 14:23')"
    - "Veriler 8 saatten eskiyse hisse listesinin üstünde sarı uyarı banner'ı görünür"
    - "Hisse detay sayfasında Temel Analiz bölüm başlığı yanında fund.period badge'i görünür (örn. '2024-Q3')"
    - "Hisse detay sayfasında AI analiz paneli altında küçük gri metin tarih notu görünür"
  artifacts:
    - path: "frontend/src/app/stocks/page.tsx"
      provides: "Stale banner ve altbilgi bileşenleri"
      contains: "staleBanner\|tableFooter\|Son güncelleme"
    - path: "frontend/src/app/stocks/page.module.css"
      provides: "Stale banner ve altbilgi CSS sınıfları"
      contains: "staleBanner\|tableFooter"
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "Fundamental period badge ve AI tarih notu"
      contains: "periodBadge\|analysisDate\|Bu analiz"
    - path: "frontend/src/app/stocks/[symbol]/page.module.css"
      provides: "Period badge ve analiz tarih CSS sınıfları"
      contains: "periodBadge\|analysisDate"
  key_links:
    - from: "frontend/src/app/stocks/page.tsx"
      to: "StockSummary.updated_at"
      via: "Math.max ile tüm hisselerden en son tarih seçimi"
      pattern: "updated_at.*Date\|latestUpdate"
    - from: "frontend/src/app/stocks/[symbol]/page.tsx"
      to: "StockAnalysisResponse.generated_at"
      via: "analyzeStock response.generated_at frontend state'e kaydedilir"
      pattern: "generated_at\|analysisDate"
---

<objective>
Dört frontend UI özelliği eklenir: hisse listesi altbilgisi (VERI-01), stale data banner (VERI-03), fundamental dönem etiketi (VERI-02), AI analiz tarih notu (VERI-04).

Purpose: Kullanıcı verinin ne kadar güncel olduğunu görsel olarak anlamalı; 8 saatten eski veri için uyarı almalı; AI analizinin hangi tarihe dayandığını bilmeli.

Output:
- `stocks/page.tsx` + `page.module.css` → altbilgi + stale banner
- `stocks/[symbol]/page.tsx` + `page.module.css` → period badge + analiz tarih notu
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-veri-tazeligi-sistem-sagligi/45-CONTEXT.md
@.planning/phases/45-veri-tazeligi-sistem-sagligi/45-01-SUMMARY.md

<interfaces>
<!-- 45-01 planından gelen ve mevcut kod kontratları -->

From frontend/src/lib/api.ts (StockSummary — 45-01 sonrası):
```typescript
export interface StockSummary {
  // ... mevcut alanlar ...
  updated_at?: string | null;   // 45-01'de eklendi
}
```

From frontend/src/lib/api.ts (StockAnalysisResponse):
```typescript
export interface StockAnalysisResponse {
  symbol: string;
  analysis: string;
  cached: boolean;
  generated_at: string;  // ISO 8601 UTC string — zaten mevcut
}
```

From frontend/src/lib/api.ts (StockFundamentals):
```typescript
export interface StockFundamentals {
  symbol: string;
  period: string | null;   // "2024-Q3" formatında veya null
  pe_ratio: number | null;
  // ...
}
```

Mevcut state yapısı (stocks/[symbol]/page.tsx):
```typescript
const [analysis, setAnalysis] = useState<string | null>(null);  // satır 308
// handleAnalyze içinde:
setAnalysis(result.analysis);  // satır 329 — result.generated_at kullanılmıyor
```

Mevcut AI analiz render (stocks/[symbol]/page.tsx, satır 761-765):
```tsx
{analysis !== null && (
  <div className={styles.analyzePanel}>
    <p className={styles.analyzeText}>{analysis}</p>
  </div>
)}
```

Mevcut fundamental section header (stocks/[symbol]/page.tsx, satır 990-997):
```tsx
<article className={styles.dossierCard}>
  <div className={styles.cardHeaderLine}>
    <div>
      <div className={styles.sectionEyebrow}>Temel Analiz</div>
      <h2>Değerleme, kârlılık ve bilanço</h2>
    </div>
    <span className={styles.scoreChip}>…/100 veya —</span>
  </div>
```

Mevcut liste altı (stocks/page.tsx, satır 330-343):
```tsx
          {!loading && filtered.length === 0 && (
            <div className={styles.emptyState}>…</div>
          )}
        </div>
      </div>
    </AppShell>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hisse listesi — stale banner ve altbilgi</name>
  <files>
    frontend/src/app/stocks/page.tsx
    frontend/src/app/stocks/page.module.css
  </files>
  <read_first>
    - frontend/src/app/stocks/page.tsx tam dosya (özellikle satır 93-115 state tanımları, satır 182-345 JSX return)
    - frontend/src/app/stocks/page.module.css tam dosya
  </read_first>
  <action>
**page.tsx değişiklikleri:**

1. State ekle — `stocks` state tanımından sonra (satır ~97):
```typescript
const [latestUpdate, setLatestUpdate] = useState<Date | null>(null);
```

2. `setStocks(res.stocks)` çağrısından hemen sonra (satır ~117), en son `updated_at` tarihini hesapla:
```typescript
// En son updated_at tarihini bul (VERI-01, VERI-03)
const dates = res.stocks
  .map((s) => s.updated_at ? new Date(s.updated_at) : null)
  .filter((d): d is Date => d !== null && !isNaN(d.getTime()));
if (dates.length > 0) {
  setLatestUpdate(new Date(Math.max(...dates.map((d) => d.getTime()))));
}
```

3. Stale hesaplama helper — `SortIcon` bileşeninden önce (satır ~66):
```typescript
function isStale(date: Date | null): boolean {
  if (!date) return false;
  return Date.now() - date.getTime() > 8 * 3600 * 1000;
}

function formatUpdateTime(date: Date | null): string {
  if (!date) return '';
  return date.toLocaleTimeString('tr-TR', { hour: '2-digit', minute: '2-digit' });
}
```

4. JSX içinde — `{loadError && ...}` bloğundan ÖNCE (sarı banner):
```tsx
{/* ── Stale data uyarısı (VERI-03) ── */}
{!loading && latestUpdate !== null && isStale(latestUpdate) && (
  <div className={styles.staleBanner}>
    ⚠ Veriler 8+ saat önce güncellendi — piyasa kapalı olabilir
  </div>
)}
```

5. JSX içinde — `</div>` (tableWrap kapanışından) SONRA, `</div>` (page kapanışından) ÖNCE (altbilgi):
```tsx
{/* ── Altbilgi — son güncelleme (VERI-01) ── */}
{!loading && latestUpdate !== null && (
  <div className={styles.tableFooter}>
    Son güncelleme: {formatUpdateTime(latestUpdate)}
  </div>
)}
```

**page.module.css değişiklikleri:**

Dosyanın sonuna ekle:

```css
/* ── Stale banner (VERI-03) ── */
.staleBanner {
  background: rgba(234, 179, 8, 0.12);
  border: 1px solid rgba(234, 179, 8, 0.35);
  border-radius: 8px;
  color: #eab308;
  font-size: 0.8125rem;
  padding: 10px 16px;
  margin-bottom: 16px;
}

/* ── Tablo altbilgisi (VERI-01) ── */
.tableFooter {
  margin-top: 12px;
  font-size: 0.75rem;
  color: var(--text-dim);
  text-align: right;
}
```
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && grep -n "staleBanner\|tableFooter\|latestUpdate\|isStale\|Son güncelleme" frontend/src/app/stocks/page.tsx | head -15</automated>
  </verify>
  <done>
    `grep` çıktısı `staleBanner`, `tableFooter`, `latestUpdate`, `isStale`, `Son güncelleme` içeren satırları gösterir. CSS dosyasında da `.staleBanner` ve `.tableFooter` sınıfları bulunur.
  </done>
  <acceptance_criteria>
    - grep 'staleBanner' frontend/src/app/stocks/page.tsx satır bulur
    - grep 'tableFooter' frontend/src/app/stocks/page.tsx satır bulur
    - grep 'staleBanner' frontend/src/app/stocks/page.module.css satır bulur
    - grep 'tableFooter' frontend/src/app/stocks/page.module.css satır bulur
    - grep 'latestUpdate' frontend/src/app/stocks/page.tsx en az 3 satır bulur (state, set, JSX)
    - TypeScript derleme hatası yok: cd frontend && npx tsc --noEmit 2>&1 | grep -i error | head -10
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Hisse detay — fundamental period badge ve AI analiz tarih notu</name>
  <files>
    frontend/src/app/stocks/[symbol]/page.tsx
    frontend/src/app/stocks/[symbol]/page.module.css
  </files>
  <read_first>
    - frontend/src/app/stocks/[symbol]/page.tsx satır 295-335 (state tanımları ve handleAnalyze)
    - frontend/src/app/stocks/[symbol]/page.tsx satır 750-770 (AI analiz paneli)
    - frontend/src/app/stocks/[symbol]/page.tsx satır 988-1012 (fundamental dossier)
    - frontend/src/app/stocks/[symbol]/page.module.css (mevcut sınıflar)
  </read_first>
  <action>
**page.tsx değişiklikleri:**

**A) AI analiz tarih state (VERI-04):**

1. Mevcut `const [analysis, setAnalysis] = useState<string | null>(null);` satırından SONRA:
```typescript
const [analysisDate, setAnalysisDate] = useState<string | null>(null);
```

2. `handleAnalyze` (veya benzeri async fonksiyon) içinde `setAnalysis(result.analysis)` satırından SONRA:
```typescript
// Analiz tarihini kaydet (VERI-04) — generated_at ISO string'den yerel tarih formatına çevir
setAnalysisDate(
  result.generated_at
    ? new Date(result.generated_at).toLocaleDateString('tr-TR', {
        day: '2-digit', month: '2-digit', year: 'numeric',
      })
    : new Date().toLocaleDateString('tr-TR', { day: '2-digit', month: '2-digit', year: 'numeric' })
);
```

3. JSX içinde mevcut analiz paneli (`analyzePanel` div) içinde, `<p className={styles.analyzeText}>` paragrafından SONRA:
```tsx
{analysisDate && (
  <p className={styles.analysisDate}>
    Bu analiz {analysisDate} verisine dayanmaktadır.
  </p>
)}
```

**B) Fundamental period badge (VERI-02):**

`stocks/[symbol]/page.tsx` dosyasında fundamental dossier bölümündeki `cardHeaderLine` div'ini bul (satır ~990-997). `<div className={styles.sectionEyebrow}>Temel Analiz</div>` satırını şu şekilde değiştir:

```tsx
<div className={styles.sectionEyebrow}>
  Temel Analiz
  {fundamentals?.period && fundamentals.period !== 'vendor-data-missing' && (
    <span className={styles.periodBadge}>{fundamentals.period}</span>
  )}
</div>
```

**page.module.css değişiklikleri:**

Dosyanın sonuna ekle:

```css
/* ── AI analiz tarih notu (VERI-04) ── */
.analysisDate {
  margin: 10px 0 0;
  font-size: 0.72rem;
  color: var(--text-dim);
  font-style: italic;
}

/* ── Fundamental dönem badge (VERI-02) ── */
.periodBadge {
  display: inline-block;
  margin-left: 8px;
  padding: 1px 6px;
  background: rgba(245, 158, 11, 0.12);
  border: 1px solid rgba(245, 158, 11, 0.25);
  border-radius: 4px;
  font-size: 0.65rem;
  font-weight: 600;
  color: var(--accent);
  vertical-align: middle;
  letter-spacing: 0.04em;
}
```
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && grep -n "analysisDate\|periodBadge\|Bu analiz\|analysisDate\|generated_at" frontend/src/app/stocks/\[symbol\]/page.tsx | head -15</automated>
  </verify>
  <done>
    `grep` çıktısı `analysisDate` state'i, `periodBadge`, `Bu analiz` metnini ve `generated_at` referansını gösterir. CSS dosyasında `.analysisDate` ve `.periodBadge` sınıfları bulunur.
  </done>
  <acceptance_criteria>
    - grep 'analysisDate' frontend/src/app/stocks/\[symbol\]/page.tsx en az 3 satır bulur (state, set, JSX)
    - grep 'periodBadge' frontend/src/app/stocks/\[symbol\]/page.tsx satır bulur
    - grep 'Bu analiz' frontend/src/app/stocks/\[symbol\]/page.tsx satır bulur
    - grep 'periodBadge' frontend/src/app/stocks/\[symbol\]/page.module.css satır bulur
    - grep 'analysisDate' frontend/src/app/stocks/\[symbol\]/page.module.css satır bulur
    - TypeScript derleme hatası yok: cd frontend && npx tsc --noEmit 2>&1 | grep -i error | head -10
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
# Task 1 — hisse listesi kontrolleri
grep -n "staleBanner\|tableFooter\|latestUpdate\|isStale" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.tsx
grep -n "staleBanner\|tableFooter" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.module.css

# Task 2 — hisse detay kontrolleri
grep -n "analysisDate\|periodBadge\|Bu analiz" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.tsx
grep -n "analysisDate\|periodBadge" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.module.css

# TypeScript derleme kontrolü
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- Hisse listesinde `updated_at` en son değerinden saati gösteren altbilgi bulunur
- 8 saatten eski veri varsa sarı `staleBanner` görünür (8 saatten yeni veride görünmez)
- Hisse detay Temel Analiz başlığı yanında `(2024-Q3)` formatında period badge görünür (period null veya vendor-data-missing ise gösterilmez)
- AI analiz sonucunun altında `Bu analiz DD.MM.YYYY verisine dayanmaktadır.` küçük gri metin görünür
- TypeScript derleme hatası yok
</success_criteria>

<output>
Tamamlandığında `.planning/phases/45-veri-tazeligi-sistem-sagligi/45-02-SUMMARY.md` oluştur.
</output>
