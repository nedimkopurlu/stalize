---
phase: 34-frontend-tasar-m-d-zeltmeleri
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - frontend/src/app/model-portfolio/page.tsx
  - frontend/src/app/model-portfolio/page.module.css
  - frontend/src/app/page.tsx
  - frontend/src/app/page.module.css
  - frontend/src/lib/api.ts
  - frontend/src/components/SparklineWidget.tsx
autonomous: true
requirements: [DESIGN-03, DESIGN-04, DESIGN-05, DESIGN-06]
must_haves:
  truths:
    - "Model portfolio page renders 6 strategy cards: Temettü Avcısı, Büyüme Lokomotifleri, Defansif Kalkan, Momentum, Değer Yatırımı, Karma"
    - "Hover background on .stockRow uses var(--bg-elevated) instead of hardcoded rgba(255,255,255,0.03)"
    - "frontend/src/components/SparklineWidget.tsx no longer exists in the repo"
    - "api.ts no longer exports getSourceCatalog, scanSource, getSourceHealthHistory methods"
    - "api.ts no longer declares orphan interfaces SourceCatalogEntry/SourceCatalog/SourceHealthHistory/SourceHealthRun/ScanSourceResponse and Sparkline-only interfaces"
    - "Dashboard portfolio card with 0 positions shows 'Henüz portföy eklenmedi' empty state with link to /portfolio"
    - "Dashboard portfolio card with positions still renders the position list (no regression)"
    - "TypeScript build (npx tsc --noEmit) passes with zero errors"
  artifacts:
    - path: "frontend/src/app/model-portfolio/page.tsx"
      provides: "STRATEGIES constant array (6 entries) + non-clickable strategy card rendering"
      contains: "Temettü Avcısı"
    - path: "frontend/src/app/model-portfolio/page.module.css"
      provides: "Strategy grid + card CSS"
      contains: ".strategyCard"
    - path: "frontend/src/app/page.module.css"
      provides: "Theme-aware stockRow hover + portfolioEmpty styles"
      contains: "var(--bg-elevated)"
    - path: "frontend/src/app/page.tsx"
      provides: "Empty-state block when positions.length === 0"
      contains: "Henüz portföy eklenmedi"
    - path: "frontend/src/lib/api.ts"
      provides: "Trimmed api object without dead source-catalog/sparkline methods"
  key_links:
    - from: "Dashboard portfolio card empty branch"
      to: "/portfolio route via Next.js Link"
      via: "<Link href=\"/portfolio\">"
      pattern: "href=\"/portfolio\""
    - from: "Model portfolio page"
      to: "STRATEGIES constant"
      via: "STRATEGIES.map() rendering 6 cards"
      pattern: "STRATEGIES\\.map"
---

<objective>
Implement DESIGN-03 (6 strategy cards on model-portfolio), DESIGN-04 (theme-aware hover fix), DESIGN-05 (delete SparklineWidget + dead api.ts methods), and DESIGN-06 (richer portfolio empty state on dashboard). Pure frontend; no backend changes.

Purpose: Final prototype-parity polish for v5.0 Phase 34 — all 6 design tutarsızlıkları cleared.

Output: Strategy cards section + CSS, hardcoded rgba removed, dead code removed, dashboard portfolio empty-state block + CSS.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/34-frontend-tasar-m-d-zeltmeleri/34-RESEARCH.md
@frontend/CLAUDE.md
@frontend/src/app/model-portfolio/page.tsx
@frontend/src/app/model-portfolio/page.module.css
@frontend/src/app/page.tsx
@frontend/src/app/page.module.css
@frontend/src/lib/api.ts
@frontend/src/components/SparklineWidget.tsx
@frontend/src/app/globals.css

<interfaces>
<!-- Dead code targets (to be removed from frontend/src/lib/api.ts per RESEARCH.md): -->
<!-- Methods: getSourceCatalog, scanSource, getSourceHealthHistory -->
<!-- Orphan interfaces (research lists these names): -->
<!--   SourceCatalogEntry / SourceCatalogItem -->
<!--   SourceCatalog / SourceCatalogResponse -->
<!--   SourceHealthHistory / SourceHealthHistoryResponse -->
<!--   SourceHealthRun / SourceHealthHistoryItem -->
<!--   ScanSourceResponse -->
<!--   SourceHealthRollupItem, SourceHealthDashboardResponse, SourceHealthAlertItem -->
<!--   SparklinePoint, SparklineResponse (only used by SparklineWidget being deleted) -->

<!-- KEEP intact: HealthResponse, HealthSourceStatus, all Market*/Portfolio*/ModelPortfolio*/Intelligence*/StockSummary types -->

<!-- Dashboard portfolio empty branch (page.tsx line 233-235 currently): -->
```tsx
) : (
  <Link href="/portfolio" className={styles.emptyLink}>Pozisyon ekle</Link>
)}
```

<!-- globals.css confirms: --bg-elevated is #1c1f24 (dark) and #f4f4f5 (light) — both safe for hover -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 6 strategy cards to model-portfolio page</name>
  <files>frontend/src/app/model-portfolio/page.tsx, frontend/src/app/model-portfolio/page.module.css</files>
  <read_first>
    - frontend/src/app/model-portfolio/page.tsx (full file — find AiPortfolioSection insertion point)
    - frontend/src/app/model-portfolio/page.module.css (full file — confirm no existing .strategyCard rules)
    - frontend/src/app/globals.css (CSS variables: --accent, --accent-green, --accent-red, --accent-blue, --accent-purple, --accent-indigo, --text-muted, --bg-elevated, --border)
    - .planning/phases/34-frontend-tasar-m-d-zeltmeleri/34-RESEARCH.md (Pattern 5 strategy card data)
  </read_first>
  <behavior>
    - Test 1: STRATEGIES constant array has exactly 6 entries
    - Test 2: All 6 names appear in rendered DOM: 'Temettü Avcısı', 'Büyüme Lokomotifleri', 'Defansif Kalkan', 'Momentum', 'Değer Yatırımı', 'Karma'
    - Test 3: Cards render as <div> elements (not <a>/<Link> — no destination route exists)
    - Test 4: CSS module exposes .strategyGrid and .strategyCard classes
  </behavior>
  <action>
    **A) frontend/src/app/model-portfolio/page.tsx** — Add a `STRATEGIES` constant near the top of the file (after imports, before the page component) and render a section below the existing `AiPortfolioSection`.

    Add this constant at module scope:

    ```tsx
    const STRATEGIES = [
      { id: 'temettu',  icon: '💰', name: 'Temettü Avcısı',         desc: 'Yüksek ve istikrarlı temettü ödeyen hisseler; pasif gelir odaklı.',          badge: 'Defansif',   accent: 'var(--accent-green)' },
      { id: 'buyume',   icon: '🚀', name: 'Büyüme Lokomotifleri',   desc: 'Güçlü gelir büyümesi ve pazar payı kazanan sektör liderleri.',                badge: 'Agresif',    accent: 'var(--accent-blue)' },
      { id: 'defansif', icon: '🛡️', name: 'Defansif Kalkan',        desc: 'Dayanıklı tüketim ve kamu hizmetleri; piyasa dalgalanmalarına karşı koruma.', badge: 'Düşük Risk', accent: 'var(--accent-indigo)' },
      { id: 'momentum', icon: '⚡', name: 'Momentum',                desc: 'Son 3-6 ayda güçlü fiyat ivmesi gösteren, trend sürebilecek hisseler.',       badge: 'Aktif',      accent: 'var(--accent)' },
      { id: 'deger',    icon: '📊', name: 'Değer Yatırımı',          desc: 'Düşük F/K ve PD/DD ile gerçek değerinin altında işlem gören hisseler.',       badge: 'Uzun Vade',  accent: 'var(--accent-purple)' },
      { id: 'karma',    icon: '🎯', name: 'Karma',                   desc: 'Temettü, büyüme ve değer unsurlarını dengeleyen çeşitli portföy.',            badge: 'Dengeli',    accent: 'var(--text-muted)' },
    ] as const;
    ```

    Inside the page's main JSX, AFTER the `<AiPortfolioSection ... />` block (or whatever the existing primary section is) and BEFORE the closing `AppShell`/page wrapper, insert:

    ```tsx
    <section className={styles.strategiesSection}>
      <div className={styles.strategiesHeader}>
        <h2 className={styles.strategiesTitle}>Strateji Şablonları</h2>
        <p className={styles.strategiesSub}>Yatırım amacına göre hazırlanmış altı şablon. Yakında tıklanabilir hale gelecek.</p>
      </div>
      <div className={styles.strategyGrid}>
        {STRATEGIES.map((s) => (
          <div key={s.id} className={styles.strategyCard} style={{ borderTopColor: s.accent }}>
            <div className={styles.strategyIcon} aria-hidden="true">{s.icon}</div>
            <div className={styles.strategyName}>{s.name}</div>
            <div className={styles.strategyDesc}>{s.desc}</div>
            <div className={styles.strategyBadge} style={{ color: s.accent }}>{s.badge}</div>
          </div>
        ))}
      </div>
    </section>
    ```

    Do NOT wrap cards in `<Link>` — destination page does not exist (per RESEARCH.md Open Question 2).
    Do NOT change any existing rendering — append the section at the bottom of the page body.

    **B) frontend/src/app/model-portfolio/page.module.css** — Append these rules at the end of the file:

    ```css
    .strategiesSection {
      margin-top: 32px;
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .strategiesHeader {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .strategiesTitle {
      font-size: 18px;
      font-weight: 600;
      color: var(--text-primary);
      margin: 0;
    }

    .strategiesSub {
      font-size: 13px;
      color: var(--text-muted);
      margin: 0;
    }

    .strategyGrid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
    }

    @media (max-width: 900px) { .strategyGrid { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 600px) { .strategyGrid { grid-template-columns: 1fr; } }

    .strategyCard {
      background: var(--bg-elevated);
      border: 1px solid var(--border);
      border-top: 3px solid var(--accent);
      border-radius: 10px;
      padding: 16px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      transition: transform 0.15s ease, border-color 0.15s ease;
    }

    .strategyCard:hover { transform: translateY(-2px); }

    .strategyIcon { font-size: 22px; line-height: 1; }
    .strategyName { font-size: 15px; font-weight: 600; color: var(--text-primary); }
    .strategyDesc { font-size: 13px; line-height: 1.5; color: var(--text-muted); }
    .strategyBadge {
      align-self: flex-start;
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 0.04em;
      text-transform: uppercase;
      padding: 3px 8px;
      border: 1px solid currentColor;
      border-radius: 999px;
      margin-top: 4px;
    }
    ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/tsc-34-02-task1.log; ! grep -E "model-portfolio/page\\.tsx.*error" /tmp/tsc-34-02-task1.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Temettü Avcısı\\|Büyüme Lokomotifleri\\|Defansif Kalkan\\|Momentum\\|Değer Yatırımı\\|Karma" frontend/src/app/model-portfolio/page.tsx` returns ≥ 6
    - `grep -n "STRATEGIES.map" frontend/src/app/model-portfolio/page.tsx` returns exactly 1 match
    - `grep -n "\\.strategyCard {" frontend/src/app/model-portfolio/page.module.css` returns exactly 1 match
    - `grep -n "\\.strategyGrid {" frontend/src/app/model-portfolio/page.module.css` returns exactly 1 match
    - `grep -n "<Link.*strategy\\|href=.*strategy" frontend/src/app/model-portfolio/page.tsx` returns 0 matches (cards are <div>, not links)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Model portfolio page shows a "Strateji Şablonları" section with 6 cards in a responsive grid; each card displays icon, name, description, and badge; TypeScript build passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Theme-aware hover fix + dashboard portfolio empty state</name>
  <files>frontend/src/app/page.tsx, frontend/src/app/page.module.css</files>
  <read_first>
    - frontend/src/app/page.tsx (lines 215-237 portfolio hero card; line 233-235 current empty branch)
    - frontend/src/app/page.module.css (line 373 .stockRow:hover; near .emptyLink rule)
    - frontend/src/app/globals.css (confirm --bg-elevated is defined for both themes)
  </read_first>
  <behavior>
    - Test 1: page.module.css line for .stockRow:hover uses var(--bg-elevated)
    - Test 2: No occurrence of "rgba(255, 255, 255, 0.03)" remains in page.module.css
    - Test 3: When positions.length === 0, dashboard renders text "Henüz portföy eklenmedi"
    - Test 4: When positions.length === 0, dashboard renders Link href="/portfolio"
    - Test 5: When positions.length > 0, dashboard renders position list (no empty state)
  </behavior>
  <action>
    **A) DESIGN-04 hover fix** — `frontend/src/app/page.module.css` line 373.

    Replace:
    ```css
    .stockRow:hover { background: rgba(255, 255, 255, 0.03); }
    ```
    With:
    ```css
    .stockRow:hover { background: var(--bg-elevated); }
    ```

    Also scan the whole file for any other `rgba(255, 255, 255, 0.03)` occurrences. If found in a hover/active rule, replace with `var(--bg-elevated)`. Do NOT touch borders, shadows, or non-hover backgrounds (those are intentional dark-mode styling).

    **B) DESIGN-06 portfolio empty state** — `frontend/src/app/page.tsx` lines 233-235.

    Replace the existing branch:
    ```tsx
    ) : (
      <Link href="/portfolio" className={styles.emptyLink}>Pozisyon ekle</Link>
    )}
    ```
    With:
    ```tsx
    ) : (
      <div className={styles.portfolioEmpty}>
        <p className={styles.portfolioEmptyText}>Henüz portföy eklenmedi.</p>
        <Link href="/portfolio" className={styles.emptyLink}>Pozisyon ekle →</Link>
      </div>
    )}
    ```

    Do NOT touch the populated branch (`positions.length > 0` block at lines 224-232) — it must keep rendering the existing position list. Conditional render must remain intact.

    **C) Append CSS for the empty state** — at the end of `frontend/src/app/page.module.css`:

    ```css
    .portfolioEmpty {
      display: flex;
      flex-direction: column;
      gap: 10px;
      margin-top: 8px;
    }

    .portfolioEmptyText {
      font-size: 13px;
      color: var(--text-muted);
      line-height: 1.5;
      margin: 0;
    }
    ```

    Do NOT remove or modify the existing `.emptyLink` rule (the new block uses it).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/tsc-34-02-task2.log; ! grep -E "page\\.tsx.*error" /tmp/tsc-34-02-task2.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rgba(255, 255, 255, 0.03)" frontend/src/app/page.module.css` returns 0 matches
    - `grep -n ".stockRow:hover { background: var(--bg-elevated)" frontend/src/app/page.module.css` returns exactly 1 match
    - `grep -n "Henüz portföy eklenmedi" frontend/src/app/page.tsx` returns exactly 1 match
    - `grep -n "portfolioEmpty" frontend/src/app/page.tsx` returns at least 1 match (className usage)
    - `grep -n "\\.portfolioEmpty {" frontend/src/app/page.module.css` returns exactly 1 match
    - `grep -n "positions.length > 0 ?" frontend/src/app/page.tsx` returns ≥ 1 match (conditional kept)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Light-mode hover on stock rows shows a visible elevated background; dashboard portfolio card with 0 positions shows "Henüz portföy eklenmedi" plus a "Pozisyon ekle →" link to /portfolio; populated portfolio still renders the position list.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete SparklineWidget + remove dead api.ts methods and orphan interfaces</name>
  <files>frontend/src/components/SparklineWidget.tsx, frontend/src/lib/api.ts</files>
  <read_first>
    - frontend/src/components/SparklineWidget.tsx (full file — confirm only self-imports)
    - frontend/src/lib/api.ts (full file — locate getSourceCatalog, scanSource, getSourceHealthHistory and their interfaces)
    - .planning/phases/34-frontend-tasar-m-d-zeltmeleri/34-RESEARCH.md (Pitfall 3 deletion order)
  </read_first>
  <behavior>
    - Test 1: frontend/src/components/SparklineWidget.tsx no longer exists
    - Test 2: api.ts has zero references to getSourceCatalog/scanSource/getSourceHealthHistory
    - Test 3: api.ts has zero declarations of SparklinePoint/SparklineResponse and the source-catalog/source-health interface family
    - Test 4: No file in frontend/src imports SparklineWidget
    - Test 5: TypeScript build is clean
  </behavior>
  <action>
    **Step 1 — Verify no imports of the dead code remain.** Run:

    ```bash
    grep -rn "SparklineWidget\\|getSourceCatalog\\|scanSource\\|getSourceHealthHistory" frontend/src/ --include="*.ts" --include="*.tsx"
    ```

    Expected matches: ONLY in `frontend/src/components/SparklineWidget.tsx` (self) and `frontend/src/lib/api.ts` (declarations). If any OTHER file matches, STOP and report — do not proceed with deletion until those callers are removed.

    **Step 2 — Delete the file:**

    ```bash
    rm frontend/src/components/SparklineWidget.tsx
    ```

    **Step 3 — Edit `frontend/src/lib/api.ts`** to remove three methods and their associated interfaces.

    Remove these `api` object methods (currently around lines 870–884 per RESEARCH.md, but search by name to be safe):
    - `getSourceCatalog`
    - `scanSource`
    - `getSourceHealthHistory`

    Remove these orphan interfaces (search by name; delete in this order to avoid transient TS errors per Pitfall 3):
    1. `SourceHealthAlertItem`
    2. `SourceHealthHistoryItem` (a.k.a. `SourceHealthRun`)
    3. `SourceCatalogItem` (a.k.a. `SourceCatalogEntry`)
    4. `SourceCatalogResponse` (a.k.a. `SourceCatalog`)
    5. `SourceHealthHistoryResponse` (a.k.a. `SourceHealthHistory`)
    6. `SourceHealthRollupItem`
    7. `SourceHealthDashboardResponse`
    8. `ScanSourceResponse` (if present)
    9. `SparklinePoint`
    10. `SparklineResponse`

    Use the actual interface names that exist in your local api.ts — RESEARCH.md and the planning context list slightly different aliases. Treat both naming sets as the same target: any interface whose ONLY consumers were the three deleted methods OR SparklineWidget.tsx.

    **DO NOT remove:**
    - `HealthResponse`, `HealthSourceStatus`
    - Any `Market*`, `Portfolio*`, `ModelPortfolio*`, `Intelligence*`, `StockSummary`, `Bist100History*` interfaces
    - The `apiFetch` generic, the `api` object itself, or any imports

    **Step 4 — Verify TypeScript build:**

    ```bash
    cd frontend && npx tsc --noEmit
    ```

    If TS reports `TS2304 Cannot find name 'X'` for a Source*/Sparkline* interface, that means an unexpected consumer still references it. Either:
    - That consumer was missed in Step 1 (re-grep and remove it), OR
    - That interface should NOT have been deleted (restore it).

    Iterate until the build is clean.
  </action>
  <verify>
    <automated>cd frontend && [ ! -f src/components/SparklineWidget.tsx ] && ! grep -rn "SparklineWidget\\|getSourceCatalog\\|scanSource\\|getSourceHealthHistory" src/ --include="*.ts" --include="*.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f frontend/src/components/SparklineWidget.tsx` (file removed)
    - `grep -rn "SparklineWidget" frontend/src/` returns 0 matches
    - `grep -n "getSourceCatalog\\b" frontend/src/lib/api.ts` returns 0 matches
    - `grep -n "scanSource\\b" frontend/src/lib/api.ts` returns 0 matches
    - `grep -n "getSourceHealthHistory\\b" frontend/src/lib/api.ts` returns 0 matches
    - `grep -n "interface SparklinePoint\\|interface SparklineResponse" frontend/src/lib/api.ts` returns 0 matches
    - `grep -n "interface SourceCatalog\\|interface SourceHealthHistory\\|interface SourceHealthRun\\|interface SourceHealthRollupItem\\|interface SourceHealthDashboardResponse\\|interface SourceHealthAlertItem\\|interface ScanSourceResponse" frontend/src/lib/api.ts` returns 0 matches
    - `grep -n "interface HealthResponse" frontend/src/lib/api.ts` returns ≥ 1 match (kept intact)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    SparklineWidget.tsx deleted; api.ts trimmed of 3 unused methods and the entire family of source-catalog/sparkline-only interfaces; TypeScript build remains green; no other component breaks.
  </done>
</task>

</tasks>

<verification>
Final phase-wide check from repo root:

```bash
cd frontend && npx tsc --noEmit && npx next build --no-lint
```

Manual visual sanity (not blocking):
- /model-portfolio shows 6 strategy cards under "Strateji Şablonları"
- Cards render in 3-column grid on desktop, 2 on tablet, 1 on mobile
- Dashboard with empty portfolio shows "Henüz portföy eklenmedi" message + arrow link
- Dashboard with populated portfolio still shows the position list (regression check)
- Light-mode hover on stock rows shows visible grey elevation (not invisible)
- No TypeScript or build errors
</verification>

<success_criteria>
- DESIGN-03: 6 strategy cards rendered (grep confirms all 6 names)
- DESIGN-04: Hardcoded rgba removed from page.module.css; var(--bg-elevated) used instead
- DESIGN-05: SparklineWidget.tsx deleted; 3 dead api.ts methods removed; orphan interfaces removed; TS build clean
- DESIGN-06: Dashboard empty-state block with "Henüz portföy eklenmedi" + /portfolio Link; populated branch unaffected
- TypeScript (`npx tsc --noEmit`) exits 0
- Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/34-frontend-tasar-m-d-zeltmeleri/34-02-SUMMARY.md` documenting:
- Files modified and deleted (SparklineWidget removed)
- STRATEGIES constant content
- Exact list of api.ts symbols removed (final names as they existed in repo)
- Verification command outputs
- Confirmation that DESIGN-03..06 are all in place
</output>
