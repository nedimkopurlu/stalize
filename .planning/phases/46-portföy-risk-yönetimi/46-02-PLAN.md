---
phase: 46-portföy-risk-yönetimi
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - frontend/src/app/portfolio/page.tsx
  - frontend/src/app/portfolio/page.module.css
autonomous: true
requirements: [RISK-02, RISK-03]
must_haves:
  truths:
    - "Bir sektörün ağırlığı %35'i geçtiğinde portföy sayfasında turuncu görsel uyarı satırı görünür"
    - "Bir hissenin ağırlığı %20'yi geçtiğinde portföy sayfasında turuncu görsel uyarı satırı görünür"
    - "Uyarı metni 'Bankacılık sektöründe yoğunlaşma: %42 ⚠ (eşik: %35)' formatındadır"
    - "Uyarı bölümü pozisyon listesinin / sectorDist bölümünün üstünde ayrı bir section'da listelenir, K/Z tablosuna karışmaz"
    - "Yoğunlaşma yoksa uyarı bölümü gizlenir (boş alan bırakmaz)"
  artifacts:
    - path: "frontend/src/app/portfolio/page.tsx"
      provides: "riskAlerts section render — sector + single-position threshold checks"
      contains: "riskAlerts"
    - path: "frontend/src/app/portfolio/page.module.css"
      provides: "riskAlerts, riskAlertItem, riskAlertIcon CSS classes with warning color"
      contains: "riskAlert"
  key_links:
    - from: "frontend/src/app/portfolio/page.tsx render"
      to: "riskGuard.sector_exposure / riskGuard.positions"
      via: "useMemo derived alerts array filtered by threshold"
      pattern: "exposure_pct > (35|20)"
---

<objective>
Portfolio sayfasına yoğunlaşma uyarıları eklenir: bir sektör >%35 veya tek bir hisse >%20 olduğunda turuncu uyarı satırı gösterilir. Uyarılar pozisyon tablosunun üstünde ayrı bir bölümde listelenir.

Purpose: Kullanıcı tek bir sektöre/hisseye fazla bağımlı olduğunu görsel olarak anlar (RISK-02, RISK-03).
Output: portfolio/page.tsx içinde `riskAlerts` section + page.module.css'de turuncu uyarı CSS sınıfları.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/46-portföy-risk-yönetimi/46-CONTEXT.md
@.planning/phases/46-portföy-risk-yönetimi/46-01-SUMMARY.md

@frontend/src/app/portfolio/page.tsx
@frontend/src/app/portfolio/page.module.css

<interfaces>
<!-- Plan 46-01 already added these to portfolio/page.tsx — DO NOT redeclare -->

State (from Plan 46-01):
```typescript
const [riskGuard, setRiskGuard] = useState<PortfolioRiskResponse | null>(null);
const [loadingRiskGuard, setLoadingRiskGuard] = useState(true);
```

Available data shape (already imported):
```typescript
riskGuard?.sector_exposure: Array<{ sector: string; exposure_pct: number; ... }>
riskGuard?.positions: Array<{ symbol: string; exposure_pct: number; ... }>
// exposure_pct is 0-100 scale
```

Existing CSS tokens in globals.css used elsewhere in project:
- `var(--accent-yellow)` — used for stale-data banner (Phase 45)
- `var(--accent-red)`, `var(--accent-green)`, `var(--text-muted)`, `var(--bg-card)`, `var(--border)`

Existing thresholds per CONTEXT.md:
- Sector: 35% (D-02)
- Single position: 20% (D-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Threshold derivation + riskAlerts section render</name>
  <files>frontend/src/app/portfolio/page.tsx</files>
  <read_first>
    - frontend/src/app/portfolio/page.tsx (lines 420-460 for useMemo patterns, lines 460-568 for where to insert riskAlerts)
    - frontend/src/app/portfolio/page.module.css (search for `--accent-yellow` to confirm token availability)
    - .planning/phases/46-portföy-risk-yönetimi/46-01-SUMMARY.md (confirm riskGuard state name)
  </read_first>
  <action>
    1. ADD threshold constants near top of file (after `EMPTY_FORM` constant around line 44):

    ```typescript
    // ─── Risk thresholds (Phase 46 RISK-02, RISK-03) ───
    const SECTOR_CONCENTRATION_THRESHOLD = 35;   // RISK-02: %35
    const POSITION_CONCENTRATION_THRESHOLD = 20; // RISK-03: %20
    ```

    2. INSIDE the `PortfolioPage` component (after the `positionsWithWeight` useMemo around line 422-428), ADD a derived alerts useMemo:

    ```typescript
    // ─── Concentration alerts (RISK-02, RISK-03) ───
    type ConcentrationAlert = {
      key: string;
      kind: 'sector' | 'position';
      label: string;
      pct: number;
      threshold: number;
      message: string;
    };

    const concentrationAlerts = useMemo<ConcentrationAlert[]>(() => {
      if (!riskGuard) return [];
      const alerts: ConcentrationAlert[] = [];

      // Sector concentration (RISK-02)
      for (const sec of riskGuard.sector_exposure) {
        if (sec.exposure_pct > SECTOR_CONCENTRATION_THRESHOLD) {
          const name = sec.sector || 'Bilinmiyor';
          alerts.push({
            key: `sector-${sec.sector}`,
            kind: 'sector',
            label: name,
            pct: sec.exposure_pct,
            threshold: SECTOR_CONCENTRATION_THRESHOLD,
            message: `${name} sektöründe yoğunlaşma: %${sec.exposure_pct.toFixed(0)} (eşik: %${SECTOR_CONCENTRATION_THRESHOLD})`,
          });
        }
      }

      // Single position concentration (RISK-03)
      for (const pos of riskGuard.positions) {
        if (pos.exposure_pct > POSITION_CONCENTRATION_THRESHOLD) {
          alerts.push({
            key: `position-${pos.symbol}`,
            kind: 'position',
            label: pos.symbol,
            pct: pos.exposure_pct,
            threshold: POSITION_CONCENTRATION_THRESHOLD,
            message: `${pos.symbol} tek hisse ağırlığı: %${pos.exposure_pct.toFixed(0)} (eşik: %${POSITION_CONCENTRATION_THRESHOLD})`,
          });
        }
      }

      // Sort highest exposure first
      return alerts.sort((a, b) => b.pct - a.pct);
    }, [riskGuard]);
    ```

    3. RENDER the `riskAlerts` section. Insert this BETWEEN the `</div>` closing of `heroGrid` and the `<section className={styles.sectorDist}>` (added in Plan 46-01). Locate the comment `{/* ─── Sektör Dağılımı (RISK-01) ─── */}` and insert immediately BEFORE it:

    ```tsx
    {/* ─── Yoğunlaşma Uyarıları (RISK-02, RISK-03) ─── */}
    {concentrationAlerts.length > 0 && (
      <section className={styles.riskAlerts} aria-label="Yoğunlaşma uyarıları">
        <p className={styles.riskAlertsTitle}>Yoğunlaşma Uyarıları</p>
        <ul className={styles.riskAlertsList}>
          {concentrationAlerts.map((alert) => (
            <li key={alert.key} className={styles.riskAlertItem}>
              <span className={styles.riskAlertIcon} aria-hidden="true">⚠</span>
              <span className={styles.riskAlertMessage}>{alert.message}</span>
            </li>
          ))}
        </ul>
      </section>
    )}
    ```

    4. Per D-02 (sektör eşiği %35), D-03 (hisse eşiği %20), D-05 (uyarılar K/Z tablosuna müdahale etmez — heroGrid altında, sectorDist üstünde ayrı section). Boş array → section gizlenir (`length > 0` guard).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "(portfolio/page|error TS)" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SECTOR_CONCENTRATION_THRESHOLD = 35" frontend/src/app/portfolio/page.tsx` returns 1 match
    - `grep -n "POSITION_CONCENTRATION_THRESHOLD = 20" frontend/src/app/portfolio/page.tsx` returns 1 match
    - `grep -n "concentrationAlerts" frontend/src/app/portfolio/page.tsx` returns at least 3 matches (declaration + useMemo dep + render map)
    - `grep -n "riskAlerts" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "yoğunlaşma\|Yoğunlaşma" frontend/src/app/portfolio/page.tsx` returns at least 2 matches
    - `grep -n "tek hisse ağırlığı" frontend/src/app/portfolio/page.tsx` returns 1 match
    - `cd frontend && npx tsc --noEmit` produces no errors related to portfolio
  </acceptance_criteria>
  <done>concentrationAlerts useMemo derives alerts from riskGuard; uyarı section heroGrid altında, sectorDist üstünde render edilir; uyarı yokken section render edilmez.</done>
</task>

<task type="auto">
  <name>Task 2: riskAlerts CSS — turuncu uyarı stili</name>
  <files>frontend/src/app/portfolio/page.module.css</files>
  <read_first>
    - frontend/src/app/portfolio/page.module.css (full file — search for any existing `--accent-yellow` use; check `.errorBanner` for similar banner pattern)
    - frontend/src/app/globals.css (search for `--accent-yellow` to confirm token exists; if missing use hex `#f59e0b` per CONTEXT.md D-disc)
  </read_first>
  <action>
    APPEND to end of `frontend/src/app/portfolio/page.module.css`:

    ```css
    /* ─── Yoğunlaşma Uyarıları (Phase 46 RISK-02, RISK-03) ─── */
    .riskAlerts {
      background: rgba(245, 158, 11, 0.08);
      border: 1px solid rgba(245, 158, 11, 0.35);
      border-radius: 12px;
      padding: 16px 20px;
      margin-bottom: 24px;
    }

    .riskAlertsTitle {
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: #f59e0b;
      margin: 0 0 10px;
    }

    .riskAlertsList {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      gap: 6px;
    }

    .riskAlertItem {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 13px;
      color: var(--text);
      line-height: 1.4;
    }

    .riskAlertIcon {
      flex-shrink: 0;
      width: 22px;
      height: 22px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: rgba(245, 158, 11, 0.18);
      color: #f59e0b;
      border-radius: 50%;
      font-size: 14px;
    }

    .riskAlertMessage {
      flex: 1;
    }

    @media (max-width: 600px) {
      .riskAlerts {
        padding: 12px 14px;
      }
      .riskAlertItem {
        font-size: 12px;
      }
    }
    ```

    Color value `#f59e0b` chosen per CONTEXT.md Claude's Discretion. Mevcut tasarım tokenlarına uygun (Phase 45 stale-data ile aynı palette ailesi).
  </action>
  <verify>
    <automated>grep -c "riskAlerts\|riskAlertItem\|riskAlertIcon\|riskAlertMessage" frontend/src/app/portfolio/page.module.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.riskAlerts " frontend/src/app/portfolio/page.module.css` or `\.riskAlerts {` returns at least 1 match
    - `grep -n "\.riskAlertsTitle" frontend/src/app/portfolio/page.module.css` returns at least 1 match
    - `grep -n "\.riskAlertItem" frontend/src/app/portfolio/page.module.css` returns at least 1 match
    - `grep -n "\.riskAlertIcon" frontend/src/app/portfolio/page.module.css` returns at least 1 match
    - `grep -n "#f59e0b" frontend/src/app/portfolio/page.module.css` returns at least 2 matches (border + text colors)
    - `grep -n "@media (max-width: 600px)" frontend/src/app/portfolio/page.module.css` returns at least 1 match (responsive)
  </acceptance_criteria>
  <done>CSS sınıfları eklenmiş; turuncu (#f59e0b) renk paleti uygulanmış; mobil için padding/font-size ayarı yapılmış.</done>
</task>

</tasks>

<verification>
- TypeScript compile başarılı: `cd frontend && npx tsc --noEmit` 0 hata
- Aktif pozisyon yokken veya hiçbir threshold aşılmadığında riskAlerts section render edilmez
- Test senaryosu: Kullanıcı bir hisseden büyük miktarda alırsa (örn. 1 hissenin portföy değeri toplamın %25'ini geçerse), "X tek hisse ağırlığı: %25 (eşik: %20)" uyarısı görünür
- Test senaryosu: Tüm pozisyonlar aynı sektörden ise, "Bankacılık sektöründe yoğunlaşma: %100 (eşik: %35)" uyarısı görünür
- Mevcut sectorDist (Plan 46-01) ve riskRows etkilenmemiştir
</verification>

<success_criteria>
- RISK-02 satisfied: Sektör ağırlığı >%35 → turuncu uyarı satırı görünür
- RISK-03 satisfied: Tek hisse ağırlığı >%20 → turuncu uyarı satırı görünür
- Uyarılar pozisyon listesinin üstünde ayrı bölümde (K/Z tablosuna müdahale etmez)
- Uyarı yokken bölüm gizlenir
</success_criteria>

<output>
After completion, create `.planning/phases/46-portföy-risk-yönetimi/46-02-SUMMARY.md` with:
- What was built (concentrationAlerts useMemo + riskAlerts section + CSS)
- Files touched
- Phase 46 closing notes — RISK-01..04 hepsi tamamlandı
</output>
