---
phase: 46-portföy-risk-yönetimi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/portfolio/page.tsx
  - frontend/src/app/portfolio/page.module.css
autonomous: true
requirements: [RISK-01, RISK-04]
must_haves:
  truths:
    - "Portfolio sayfası açıldığında api.getPortfolioRiskGuard(totalValue) çağrılır ve cevap state'e yazılır"
    - "Sektör dağılımı bölümü (sectorDist) pozisyon tablosunun üstünde görünür ve her sektör için adı + yüzde + yatay bar gösterir"
    - "Sektörler ağırlık büyükten küçüğe sıralanır; ilk 3 sektör vurgulanır"
    - "Risk özet kartında 'Açık pozisyon: X hisse' satırı bulunur"
    - "Risk özet kartında 'En büyük 3 sektör: Bankacılık %42, Enerji %18, Sanayi %15' formatında satır bulunur"
  artifacts:
    - path: "frontend/src/app/portfolio/page.tsx"
      provides: "PortfolioRiskResponse fetch + sectorDist section render + riskRows top-3 sectors row"
      contains: "getPortfolioRiskGuard"
    - path: "frontend/src/app/portfolio/page.module.css"
      provides: "sectorDist, sectorRow, sectorBar, sectorBarFill, sectorTop3 CSS classes"
      contains: "sectorBar"
  key_links:
    - from: "frontend/src/app/portfolio/page.tsx loadData()"
      to: "api.getPortfolioRiskGuard"
      via: "Promise.all parallel fetch after totalValue compute"
      pattern: "getPortfolioRiskGuard\\("
    - from: "frontend/src/app/portfolio/page.tsx render"
      to: "riskGuard.sector_exposure"
      via: "map over sorted sectors"
      pattern: "sector_exposure"
---

<objective>
Portfolio sayfasına sektör dağılımı görselleştirmesi (yatay bar chart) eklenir ve risk özet kartına toplam pozisyon sayısı + en büyük 3 sektör bilgisi entegre edilir.

Purpose: Kullanıcı portföyünün hangi sektörlere dağıldığını bir bakışta görür (RISK-01, RISK-04).
Output: portfolio/page.tsx içinde `riskGuard` state + `sectorDist` section + güncellenmiş `riskRows`; page.module.css'de yeni CSS sınıfları.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/46-portföy-risk-yönetimi/46-CONTEXT.md

@frontend/src/app/portfolio/page.tsx
@frontend/src/app/portfolio/page.module.css
@frontend/src/lib/api.ts

<interfaces>
<!-- Existing types — do NOT redefine. Import from @/lib/api -->

From frontend/src/lib/api.ts (line 217):
```typescript
export interface PortfolioRiskResponse {
  portfolio_value: number;
  invested_pct: number;
  cash_pct: number;
  recommended_cash_pct: number;
  cash_action: { status: string; detail: string; remediation: string };
  risk_score: number;
  risk_level: 'low' | 'medium' | 'high' | string;
  estimated_capital_at_risk: number;
  market_regime: Record<string, unknown>;
  sector_exposure: Array<{
    sector: string;
    exposure: number;
    exposure_pct: number;   // 0-100 scale
    limit_pct: number;
    status: string;
  }>;
  positions: Array<{
    symbol: string;
    sector: string;
    entry_price: number;
    current_price: number;
    quantity: number;
    exposure: number;
    exposure_pct: number;   // 0-100 scale
    stop_loss: number | null;
    target_price: number | null;
    stop_gap_pct: number | null;
    target_gap_pct: number | null;
    at_risk: boolean;
    score: number | null;
  }>;
  rules: Record<string, number>;
}
```

From frontend/src/lib/api.ts (line 1143):
```typescript
api.getPortfolioRiskGuard(portfolioValue: number = 100000) => Promise<PortfolioRiskResponse>
// Endpoint: GET /risk/portfolio?portfolio_value=${portfolioValue}
```

Existing portfolio/page.tsx state shape (line 220-243):
- positions: PortfolioPosition[]
- history: PortfolioHistoryResponse | null
- loadingPositions, loadingHistory, loadingWatchlist booleans
- Existing currentValue compute (line 405-407): activePositions.reduce((s, p) => s + (p.current_price ?? p.entry_price) * p.quantity, 0)
- Existing risk variable (line 416): const risk = history?.risk_summary;
- Existing riskRows section (line 535-565) inside right card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: riskGuard state + fetch with totalValue</name>
  <files>frontend/src/app/portfolio/page.tsx</files>
  <read_first>
    - frontend/src/app/portfolio/page.tsx (lines 220-300 for state setup, 400-420 for value compute)
    - frontend/src/lib/api.ts (lines 217-254 for PortfolioRiskResponse, line 1143 for getPortfolioRiskGuard)
  </read_first>
  <action>
    1. Import `PortfolioRiskResponse` from `@/lib/api`:
       Update existing import line 8 to: `import api, { PortfolioHistoryResponse, PortfolioPosition, PortfolioRiskResponse, StockSummary } from '@/lib/api';`
    2. Add new state next to existing loading flags (around line 227):
       ```typescript
       const [riskGuard, setRiskGuard] = useState<PortfolioRiskResponse | null>(null);
       const [loadingRiskGuard, setLoadingRiskGuard] = useState(true);
       ```
    3. Add new fetch function after `fetchHistory()` (before `fetchWatchlist`):
       ```typescript
       async function fetchRiskGuard(totalValue: number) {
         setLoadingRiskGuard(true);
         try {
           // Use 100000 fallback when portfolio is empty (mirrors api default)
           const safeValue = totalValue > 0 ? totalValue : 100000;
           const data = await api.getPortfolioRiskGuard(safeValue);
           setRiskGuard(data);
         } catch {
           // Non-blocking — silently skip (matches fetchHistory pattern)
           setRiskGuard(null);
         } finally {
           setLoadingRiskGuard(false);
         }
       }
       ```
    4. The `currentValue` is computed AFTER positions load (line 405-407 inside render body), so use a useEffect that depends on `positions`:
       Add useEffect after the existing initial useEffect (around line 249):
       ```typescript
       useEffect(() => {
         if (loadingPositions) return;
         const active = positions.filter((p) => p.is_active);
         const tv = active.reduce((s, p) => s + (p.current_price ?? p.entry_price) * p.quantity, 0);
         void fetchRiskGuard(tv);
       }, [positions, loadingPositions]);
       ```
    5. Reference D-01 (yatay bar chart with CSS), D-02 (Promise.all parallel — here we use derived totalValue so we keep separate effect). Per CONTEXT.md decisions.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "(portfolio/page|error TS)" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PortfolioRiskResponse" frontend/src/app/portfolio/page.tsx` returns at least 2 matches (import + state type)
    - `grep -n "getPortfolioRiskGuard" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "setRiskGuard" frontend/src/app/portfolio/page.tsx` returns at least 2 matches (declaration + setter call)
    - `grep -n "fetchRiskGuard" frontend/src/app/portfolio/page.tsx` returns at least 2 matches
    - `cd frontend && npx tsc --noEmit` produces no errors in `portfolio/page.tsx`
  </acceptance_criteria>
  <done>riskGuard state populated after positions load; no TS errors; data flows from API to component state.</done>
</task>

<task type="auto">
  <name>Task 2: Sektör dağılımı bölümü (sectorDist section) render</name>
  <files>frontend/src/app/portfolio/page.tsx, frontend/src/app/portfolio/page.module.css</files>
  <read_first>
    - frontend/src/app/portfolio/page.tsx (lines 447-567 for heroGrid + riskRows context, lines 567-600 for where to insert)
    - frontend/src/app/portfolio/page.module.css (search for `.heroGrid`, `.card`, `.cardEyebrow` to match style tokens)
  </read_first>
  <action>
    1. Insert new section in `portfolio/page.tsx` AFTER the closing `</div>` of `heroGrid` (around line 567, before the `{/* ─── Takip Listesi ─── */}` comment):

    ```tsx
    {/* ─── Sektör Dağılımı (RISK-01) ─── */}
    <section className={styles.sectorDist}>
      <div className={styles.sectorDistHeader}>
        <p className={styles.cardEyebrow}>SEKTÖR DAĞILIMI</p>
        <h2 className={styles.sectorDistTitle}>Portföyün sektörlere göre dağılımı</h2>
      </div>
      {loadingRiskGuard ? (
        <div className={styles.loadingWrap}>Sektör dağılımı yükleniyor…</div>
      ) : !riskGuard || riskGuard.sector_exposure.length === 0 ? (
        <div className={styles.transEmpty}>Sektör dağılımı için aktif pozisyon gerekli</div>
      ) : (
        <div className={styles.sectorList}>
          {[...riskGuard.sector_exposure]
            .sort((a, b) => b.exposure_pct - a.exposure_pct)
            .map((sec, idx) => (
              <div
                key={sec.sector}
                className={`${styles.sectorRow} ${idx < 3 ? styles.sectorRowTop : ''}`}
              >
                <span className={styles.sectorName}>{sec.sector || 'Bilinmiyor'}</span>
                <div className={styles.sectorBar}>
                  <div
                    className={styles.sectorBarFill}
                    style={{ width: `${Math.min(sec.exposure_pct, 100)}%` }}
                  />
                </div>
                <span className={styles.sectorPct}>{sec.exposure_pct.toFixed(1)}%</span>
              </div>
            ))}
        </div>
      )}
    </section>
    ```

    2. Add CSS classes to `portfolio/page.module.css` (append at end of file). Use existing tokens (`var(--bg-card)`, `var(--text-muted)`, `var(--accent-green)`):

    ```css
    /* ─── Sektör Dağılımı (Phase 46) ─── */
    .sectorDist {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 24px;
      margin-bottom: 24px;
    }

    .sectorDistHeader {
      margin-bottom: 16px;
    }

    .sectorDistTitle {
      font-size: 18px;
      font-weight: 600;
      color: var(--text);
      margin: 4px 0 0;
    }

    .sectorList {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .sectorRow {
      display: grid;
      grid-template-columns: 140px 1fr 56px;
      align-items: center;
      gap: 12px;
      font-size: 13px;
      color: var(--text);
    }

    .sectorRowTop .sectorName {
      font-weight: 600;
    }

    .sectorName {
      color: var(--text-muted);
    }

    .sectorBar {
      height: 8px;
      background: rgba(255, 255, 255, 0.06);
      border-radius: 4px;
      overflow: hidden;
    }

    .sectorBarFill {
      height: 100%;
      background: var(--accent-green);
      border-radius: 4px;
      transition: width 0.3s ease;
    }

    .sectorPct {
      text-align: right;
      font-variant-numeric: tabular-nums;
      color: var(--text);
      font-weight: 500;
    }

    @media (max-width: 600px) {
      .sectorRow {
        grid-template-columns: 100px 1fr 48px;
        gap: 8px;
        font-size: 12px;
      }
    }
    ```

    3. Per D-01 (yatay bar CSS, harici kütüphane yok), D-03 (sıralama büyükten küçüğe), D-04 (ilk 3 vurgu — `sectorRowTop` font-weight:600). Empty sector string → "Bilinmiyor" fallback.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "(portfolio/page|error TS)" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "sectorDist" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "sectorBar" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "sectorBarFill" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "sortedSectorExposure\|sector_exposure" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "\.sectorDist " frontend/src/app/portfolio/page.module.css` or `\.sectorDist {` returns at least 1 match
    - `grep -n "\.sectorBar " frontend/src/app/portfolio/page.module.css` or `\.sectorBar {` returns at least 1 match
    - `grep -n "\.sectorBarFill" frontend/src/app/portfolio/page.module.css` returns at least 1 match
    - `grep -n "\.sectorRowTop" frontend/src/app/portfolio/page.module.css` returns at least 1 match
    - `cd frontend && npx tsc --noEmit` produces no errors related to portfolio
  </acceptance_criteria>
  <done>Sektör dağılımı bölümü heroGrid altında ve takip listesinin üstünde görünür; her sektör için ad + yatay bar (genişlik exposure_pct%) + yüzde gösterir; ilk 3 sektör vurgulanır; aktif pozisyon yokken empty state mesajı gösterilir.</done>
</task>

<task type="auto">
  <name>Task 3: Risk özet kartına 'Açık pozisyon' ve 'En büyük 3 sektör' satırları (RISK-04)</name>
  <files>frontend/src/app/portfolio/page.tsx, frontend/src/app/portfolio/page.module.css</files>
  <read_first>
    - frontend/src/app/portfolio/page.tsx (lines 519-566 for existing riskRows section)
    - frontend/src/app/portfolio/page.module.css (search for `.riskRow`, `.riskRowLabel`, `.riskRowNote` to match style)
  </read_first>
  <action>
    1. In `portfolio/page.tsx`, MODIFY the existing "Aktif pozisyon" `riskRow` (line ~536-544): update label and note to match RISK-04 spec. Replace the existing block:

    ```tsx
    <div className={styles.riskRow}>
      <span className={styles.riskRowLabel}>Açık pozisyon</span>
      <div className={styles.riskRowRight}>
        <span className={styles.riskRowValue}>
          {activePositions.length} hisse
        </span>
        <span className={styles.riskRowNote}>Portföydeki açık pozisyon sayısı</span>
      </div>
    </div>
    ```

    (Replaces the previous "Aktif pozisyon" row which used `risk?.active_positions ?? positions.length`. Use `activePositions.length` directly — already computed at line ~393.)

    2. ADD new "En büyük 3 sektör" row INSIDE the `riskRows` div, AFTER the "Hedef yakın" row (right before the closing `</div>` of `riskRows` around line 565):

    ```tsx
    <div className={`${styles.riskRow} ${styles.riskRowSectors}`}>
      <span className={styles.riskRowLabel}>En büyük 3 sektör</span>
      <div className={styles.riskRowRight}>
        {loadingRiskGuard ? (
          <span className={styles.riskRowNote}>Hesaplanıyor…</span>
        ) : !riskGuard || riskGuard.sector_exposure.length === 0 ? (
          <span className={styles.riskRowNote}>Aktif pozisyon yok</span>
        ) : (
          <span className={styles.riskRowSectorList}>
            {[...riskGuard.sector_exposure]
              .sort((a, b) => b.exposure_pct - a.exposure_pct)
              .slice(0, 3)
              .map((s) => `${s.sector || 'Bilinmiyor'} %${s.exposure_pct.toFixed(0)}`)
              .join(', ')}
          </span>
        )}
      </div>
    </div>
    ```

    3. Add CSS in `portfolio/page.module.css` (append at end):

    ```css
    .riskRowSectors .riskRowRight {
      align-items: flex-end;
    }

    .riskRowSectorList {
      font-size: 12px;
      color: var(--text);
      text-align: right;
      line-height: 1.4;
      max-width: 220px;
    }

    @media (max-width: 600px) {
      .riskRowSectorList {
        max-width: 100%;
        font-size: 11px;
      }
    }
    ```

    4. Per D-04 (CONTEXT.md): activePositions zaten p.is_active filtresi ile mevcut (page.tsx line ~393). sector_exposure boş gelirse fallback gösterilir.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -E "(portfolio/page|error TS)" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Açık pozisyon" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "En büyük 3 sektör" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "riskRowSectors\|riskRowSectorList" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "slice(0, 3)" frontend/src/app/portfolio/page.tsx` returns at least 1 match
    - `grep -n "\.riskRowSectorList" frontend/src/app/portfolio/page.module.css` returns at least 1 match
    - The old "Aktif pozisyon" / "Portföydeki açık pozisyon" line replaced — `grep -c "Portföydeki açık pozisyon$" frontend/src/app/portfolio/page.tsx` returns 0
    - `cd frontend && npx tsc --noEmit` produces no errors related to portfolio
  </acceptance_criteria>
  <done>Risk özet kartında 'Açık pozisyon: X hisse' satırı (activePositions.length) ve 'En büyük 3 sektör: Bankacılık %42, Enerji %18, Sanayi %15' formatında ayrı satır görünür; yükleme/boş durumlar düzgün handle edilir.</done>
</task>

</tasks>

<verification>
- Portfolio sayfası `npm run dev` ile açıldığında console hatası vermez
- TS compile başarılı (`npx tsc --noEmit`)
- Aktif pozisyon yokken sectorDist bölümü "Sektör dağılımı için aktif pozisyon gerekli" mesajı gösterir
- En az 1 aktif pozisyon eklendiğinde sectorDist barları ve riskRows içindeki "En büyük 3 sektör" satırı dolu görünür
- Sektörler büyükten küçüğe sıralı; ilk 3 sektör adı bold (sectorRowTop)
</verification>

<success_criteria>
- RISK-01 satisfied: Sektör dağılımı görselleştirmesi yatay bar chart ile gösterilir
- RISK-04 satisfied: Risk özet kartına aktif pozisyon sayısı ve en büyük 3 sektör bilgisi eklenmiştir
- 0 TypeScript hatası
- Mevcut K/Z tablosu, hero grafiği, watchlist bölümleri etkilenmemiştir
</success_criteria>

<output>
After completion, create `.planning/phases/46-portföy-risk-yönetimi/46-01-SUMMARY.md` with:
- What was built (riskGuard state, sectorDist section, updated riskRows)
- Files touched
- Notes for Plan 02 (riskGuard already fetched and available; uyarılar Plan 02'de eklenecek)
</output>
