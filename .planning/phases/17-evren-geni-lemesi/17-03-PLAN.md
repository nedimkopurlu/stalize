# Plan 17-03: Stock Model Enrichment + Config Swap

**Wave:** 2 (depends on 17-01 bist_full_universe.py existing)
**Goal:** Stock modeline market_tier ve is_bist250 ekle; config.py'i full universe'e geçir

## Tasks

### 17-03-T1: stock.py modeline yeni alanlar ekle

**File:** `backend/app/models/stock.py`

After `is_bist100` column, add:
```python
is_bist250 = Column(Boolean, default=False)
market_tier = Column(String(20), nullable=True)  # yıldız / ana / gelişen
```

### 17-03-T2: Alembic migration 003 oluştur

**File:** `backend/alembic/versions/003_add_market_tier_bist250.py`

```python
"""Add market_tier and is_bist250 to stocks

Revision ID: 003
Revises: 002
Create Date: 2026-04-27
"""
from alembic import op
import sqlalchemy as sa

revision = "003"
down_revision = "002"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "stocks",
        sa.Column("is_bist250", sa.Boolean(), nullable=True, server_default="false"),
    )
    op.add_column(
        "stocks",
        sa.Column("market_tier", sa.String(length=20), nullable=True),
    )


def downgrade() -> None:
    op.drop_column("stocks", "market_tier")
    op.drop_column("stocks", "is_bist250")
```

### 17-03-T3: config.py'i full universe'e geçir

**File:** `backend/app/core/config.py`

Add import for full universe alongside existing bist100 import:
```python
from app.data.bist_full_universe import (
    BIST_FULL_UNIVERSE,
    get_bist_full_symbols,
    get_bist_full_company_map,
    get_bist_full_sector_map,
)
```

In `Settings` class, add new fields (keep BIST100_* for backward compat):
```python
BIST_FULL_UNIVERSE: List[Dict[str, object]] = BIST_FULL_UNIVERSE
BIST_FULL_SYMBOLS: List[str] = get_bist_full_symbols()
BIST_FULL_COMPANIES: Dict[str, str] = get_bist_full_company_map()
BIST_FULL_SECTORS: Dict[str, str] = get_bist_full_sector_map()
```

### 17-03-T4: DataCollector universe kaynağını full universe'e geçir

**File:** `backend/app/services/data_collector.py`

In `DataCollector.__init__()`, change:
```python
# BEFORE
self.universe = list(settings.BIST100_UNIVERSE)

# AFTER
self.universe = list(settings.BIST_FULL_UNIVERSE)
```

Also update `initialize_stocks()` to set `is_bist100`, `is_bist250`, `market_tier` from universe entry:
```python
canonical_is_bist100 = bool(canonical.get("is_bist100", False))
canonical_is_bist250 = bool(canonical.get("is_bist250", False))
canonical_market_tier = str(canonical.get("market_tier", "ana"))

# When creating Stock():
is_bist100=canonical_is_bist100,
is_bist250=canonical_is_bist250,
market_tier=canonical_market_tier,
```

### 17-03-T5: Stocks API endpoint is_bist250 + market_tier filtresi ekle

**File:** `backend/app/api/v1/stocks.py`

Add query params `bist100: Optional[bool] = None` and `bist250: Optional[bool] = None` to `/stocks` endpoint.
Add filter conditions when params provided.

## Acceptance Criteria

- `alembic upgrade head` hatasız çalışıyor
- `Stock` modeli `market_tier` ve `is_bist250` alanlarına sahip
- DataCollector full universe kullanıyor (100 değil 200+ hisse)
- BIST100_UNIVERSE, BIST100_* config değerleri hâlâ erişilebilir (backward compat)
- `/stocks?bist100=true` filtresi çalışıyor
- `/stocks?bist250=true` filtresi çalışıyor
