# Plan 17-04: Frontend Infinite Scroll + Filtreler

**Wave:** 3 (depends on 17-03 backend changes being available)
**Goal:** 500 hisseyi infinite scroll ile yönetilebilir göster; BIST100/250 toggle ekle

## Tasks

### 17-04-T1: StockSummary type'ına yeni alanlar ekle

**File:** `frontend/src/lib/api.ts`

Add to `StockSummary` interface:
```typescript
is_bist250?: boolean;
market_tier?: string | null;
```

Add to `getStocks` params:
```typescript
bist100?: boolean;
bist250?: boolean;
```

Update `getStocks` function body to pass `bist100` and `bist250` params to API.

### 17-04-T2: stocks/page.tsx — Infinite scroll implementasyonu

**File:** `frontend/src/app/stocks/page.tsx`

Replace current single-load pattern with infinite scroll:

**State changes:**
- Add `page` state (number, starts at 0)
- Add `hasMore` state (boolean)
- Add `loadingMore` state (boolean)
- Add `filterBist100` state (boolean, default false)
- Add `filterBist250` state (boolean, default false)
- Change `limit` from 100 to 50

**Load function refactor:**
```typescript
const loadStocks = useCallback(async (pageNum: number, append: boolean) => {
  if (append) setLoadingMore(true);
  else setLoading(true);
  try {
    const res = await api.getStocks({
      sort_by: sortBy,
      limit: 50,
      offset: pageNum * 50,
      search: search || undefined,
      bist30: filterBist30 || undefined,
      bist100: filterBist100 || undefined,
      bist250: filterBist250 || undefined,
      recommendation: filterRec || undefined,
      sector: filterSector || undefined,
    });
    if (append) {
      setStocks(prev => [...prev, ...res.stocks]);
    } else {
      setStocks(res.stocks);
    }
    setTotal(res.total);
    setHasMore(res.stocks.length === 50);
  } catch { /* */ }
  finally {
    if (append) setLoadingMore(false);
    else setLoading(false);
  }
}, [filterBist30, filterBist100, filterBist250, filterRec, filterSector, search, sortBy]);
```

**Filter change reset:**
```typescript
useEffect(() => {
  setPage(0);
  void loadStocks(0, false);
}, [filterBist30, filterBist100, filterBist250, filterRec, filterSector, sortBy]);
```

**Debounced search reset:**
```typescript
useEffect(() => {
  const timer = setTimeout(() => {
    setPage(0);
    void loadStocks(0, false);
  }, 300);
  return () => clearTimeout(timer);
}, [search]);
```

**IntersectionObserver sentinel:**
```typescript
const sentinelRef = useRef<HTMLDivElement>(null);

useEffect(() => {
  const observer = new IntersectionObserver(
    (entries) => {
      if (entries[0].isIntersecting && hasMore && !loadingMore && !loading) {
        const nextPage = page + 1;
        setPage(nextPage);
        void loadStocks(nextPage, true);
      }
    },
    { threshold: 0.1 }
  );
  if (sentinelRef.current) observer.observe(sentinelRef.current);
  return () => observer.disconnect();
}, [hasMore, loadingMore, loading, page, loadStocks]);
```

**Sentinel element in JSX (after table):**
```tsx
<div ref={sentinelRef} style={{ height: 1 }} />
{loadingMore && (
  <div style={{ textAlign: 'center', padding: '16px', color: 'var(--text-muted)' }}>
    Yükleniyor...
  </div>
)}
{!hasMore && stocks.length > 0 && (
  <div style={{ textAlign: 'center', padding: '16px', color: 'var(--text-muted)', fontSize: '0.8rem' }}>
    Tüm {total} hisse yüklendi
  </div>
)}
```

### 17-04-T3: BIST100 ve BIST250 filter toggle ekle

**File:** `frontend/src/app/stocks/page.tsx`

Add two toggle buttons to filter bar (after BIST30 button):
```tsx
<button
  className={`btn ${filterBist100 ? 'btn-primary' : 'btn-ghost'} btn-sm`}
  onClick={() => setFilterBist100(!filterBist100)}
>
  BIST100
</button>
<button
  className={`btn ${filterBist250 ? 'btn-primary' : 'btn-ghost'} btn-sm`}
  onClick={() => setFilterBist250(!filterBist250)}
>
  BIST250
</button>
```

### 17-04-T4: "Listeyi Yenile" butonunu güncelle

Update the refresh button handler to reset to page 0:
```tsx
onClick={() => { setPage(0); void loadStocks(0, false); }}
```

### 17-04-T5: result count göstergesini güncelle

Update the result count to show loaded vs total:
```tsx
<span className={styles.resultCount}>
  {stocks.length} / {total} hisse
</span>
```

## Acceptance Criteria

- İlk yükleme 50 hisse getiriyor
- Sayfayı kaydırdıkça yeni 50'şer hisse ekleniyor (append, sayfa sıfırlanmıyor)
- BIST30, BIST100, BIST250 toggle'ları çalışıyor
- Filtre değişince liste başa dönüp yeniden yükleniyor
- Arama 300ms debounce ile çalışıyor
- "Tüm N hisse yüklendi" mesajı en sona gelince gösteriyor
- TypeScript hata yok
