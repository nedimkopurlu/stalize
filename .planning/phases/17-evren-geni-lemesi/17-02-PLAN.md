# Plan 17-02: DataCollector Rate Limiting İyileştirmesi

**Wave:** 1 (no dependencies — parallel with 17-01)
**Goal:** 500 hisse için DataCollector'ı güvenli çalıştır: batch sleep 10/1s, hata izolasyonu

## Tasks

### 17-02-T1: collect_price_data() batch sleep güncelle

**File:** `backend/app/services/data_collector.py`

Change line 182-184:
```python
# BEFORE
if (i + 1) % 5 == 0:
    await db.commit()
    await asyncio.sleep(2)

# AFTER
if (i + 1) % 10 == 0:
    await db.commit()
    await asyncio.sleep(1)
```

Also wrap `_collect_stock_prices` call in try/except:
```python
for i, symbol in enumerate(self.symbols):
    try:
        await self._collect_stock_prices(db, symbol, period)
    except Exception as e:
        logger.warning(f"  ⚠️ {symbol} fiyat verisi atlandı: {e}")
    if (i + 1) % 10 == 0:
        await db.commit()
        await asyncio.sleep(1)
await db.commit()
```

### 17-02-T2: initialize_stocks() batch sleep + hata izolasyonu ekle

**File:** `backend/app/services/data_collector.py`

In `initialize_stocks()`, add batch counter and sleep after every 10 symbols processed.
Current loop has no sleep. After the `db.add(stock)` call, add:
```python
processed += 1
if processed % 10 == 0:
    await db.commit()
    await asyncio.sleep(1)
    logger.info(f"  ⏱️ {processed}/{len(self.symbols)} hisse işlendi")
```

Also update docstring to reflect 500-hisse support.

### 17-02-T3: DataCollector docstring güncelle

Update class docstring from "BIST100 veri toplama" to "Borsa İstanbul veri toplama" to reflect expanded scope.

## Acceptance Criteria

- `collect_price_data()` her 10 hissede bir 1s sleep yapıyor (5/2s yerine)
- `initialize_stocks()` her 10 hissede bir commit + sleep yapıyor
- Her iki fonksiyonda hata olan sembol log'a yazılıp devam ediliyor, batch durmuyor
- Syntax hatası yok (AST kontrolü geçiyor)
