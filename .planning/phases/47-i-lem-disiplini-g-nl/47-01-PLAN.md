---
phase: 47-i-lem-disiplini-g-nl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/portfolio_v2.py
  - backend/alembic/versions/007_portfolio_position_discipline_fields.py
  - backend/app/api/portfolio_v2.py
autonomous: true
requirements: [GUNLUK-01, GUNLUK-02]

must_haves:
  truths:
    - "PortfolioPosition ORM modeli exit_reason ve invalidation_condition alanlarını içeriyor"
    - "Alembic migration çalıştırıldığında portfolio_positions tablosuna iki yeni nullable sütun ekleniyor"
    - "PATCH /portfolio/positions/{id}/close çağrısı exit_reason değerini alıp kaydediyor"
    - "POST /portfolio/positions çağrısı invalidation_condition değerini alıp kaydediyor"
    - "GET /portfolio/positions yanıtı her pozisyon için exit_reason ve invalidation_condition döndürüyor"
  artifacts:
    - path: "backend/app/models/portfolio_v2.py"
      provides: "exit_reason ve invalidation_condition Column tanımları"
      contains: "exit_reason = Column(String(50), nullable=True)"
    - path: "backend/alembic/versions/007_portfolio_position_discipline_fields.py"
      provides: "Geri alınabilir Alembic migration"
      contains: "revision = \"007\""
    - path: "backend/app/api/portfolio_v2.py"
      provides: "Genişletilmiş PositionCreate ve PositionClose Pydantic modelleri + endpoint mantığı"
      contains: "exit_reason: Optional[str] = None"
  key_links:
    - from: "backend/alembic/versions/007_portfolio_position_discipline_fields.py"
      to: "backend/app/models/portfolio_v2.py"
      via: "Alembic autogenerate — tablodaki sütunların ORM modeliyle örtüşmesi"
      pattern: "portfolio_positions.*exit_reason"
    - from: "backend/app/api/portfolio_v2.py PositionClose"
      to: "backend/app/models/portfolio_v2.py PortfolioPosition.exit_reason"
      via: "pos.exit_reason = body.exit_reason"
      pattern: "pos\\.exit_reason = body\\.exit_reason"
---

<objective>
Backend ORM modeline exit_reason ve invalidation_condition alanları eklenir; Alembic migration ile mevcut tabloya nullable sütunlar eklenir; close_position ve add_position endpoint'leri bu alanları kabul edip kaydeder.

Purpose: Frontend plan (47-02) bu alan değerlerini API'ye gönderebilmek için backend'in onları desteklemesi gerekiyor. Migration mevcut kayıtları bozmaz.
Output: Güncellenmiş model, 007 migration dosyası, güncellenmiş API router.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- backend/app/models/portfolio_v2.py mevcut PortfolioPosition modeli (satır 9-29) -->
```python
class PortfolioPosition(Base):
    __tablename__ = "portfolio_positions"
    id = Column(Integer, primary_key=True, index=True)
    symbol = Column(String(20), nullable=False, index=True)
    entry_price = Column(Float, nullable=False)
    quantity = Column(Float, nullable=False)
    entry_date = Column(Date, nullable=False)
    stop_loss = Column(Float, nullable=True)
    target_price = Column(Float, nullable=True)
    rationale = Column(Text, nullable=True)
    is_active = Column(Boolean, default=True, nullable=False)
    exit_price = Column(Float, nullable=True)
    exit_date = Column(Date, nullable=True)
    realized_pnl = Column(Float, nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
```

<!-- backend/app/api/portfolio_v2.py mevcut Pydantic modelleri (satır 52-65) -->
```python
class PositionCreate(BaseModel):
    symbol: str
    entry_price: float
    quantity: float
    entry_date: date
    stop_loss: Optional[float] = None
    target_price: Optional[float] = None
    rationale: Optional[str] = None

class PositionClose(BaseModel):
    exit_price: float
    exit_date: date
```

<!-- backend/alembic/versions/006_signal_snapshot_sector.py migration pattern -->
```python
revision = "006"
down_revision = "005"
branch_labels = None
depends_on = None

def upgrade() -> None:
    bind = op.get_bind()
    inspector = sa.inspect(bind)
    columns = {column["name"] for column in inspector.get_columns("signal_decision_snapshots")}
    if "sector" not in columns:
        op.add_column("signal_decision_snapshots", sa.Column("sector", sa.String(100), nullable=True))

def downgrade() -> None:
    op.drop_column("signal_decision_snapshots", "sector")
```

<!-- close_position endpoint (satır 302-350) — pos.exit_reason ataması buraya ekleniyor -->
<!-- add_position endpoint (satır 250-297) — invalidation_condition kısmı buraya ekleniyor -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ORM modeline iki yeni sütun ekle</name>
  <files>backend/app/models/portfolio_v2.py</files>
  <action>
    PortfolioPosition sınıfına (satır 27'nin hemen altına, updated_at'tan önce) şu iki satırı ekle:

    ```python
    exit_reason = Column(String(50), nullable=True)           # çıkış nedeni: Stop Tetiklendi / Hedefe Ulaştı / Senaryo Bozuldu / Diğer: ...
    invalidation_condition = Column(Text, nullable=True)       # kararı bozan koşul (serbest metin)
    ```

    Mevcut import satırı zaten `String`, `Text` içeriyor — ek import gerekmez.
    Satır sırası: exit_date'ten sonra, realized_pnl'den önce olacak şekilde ekle.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && grep -n "exit_reason\|invalidation_condition" backend/app/models/portfolio_v2.py</automated>
  </verify>
  <done>Her iki Column tanımı da portfolio_v2.py modelinde görünüyor; syntax hatası yok</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration dosyası oluştur</name>
  <files>backend/alembic/versions/007_portfolio_position_discipline_fields.py</files>
  <action>
    Mevcut 006 migration'ının pattern'ini izleyerek yeni dosyayı manuel yaz (autogenerate kullanma — sadece schema sync değil spesifik SQL çalıştırıyoruz):

    ```python
    """add exit_reason and invalidation_condition to portfolio_positions

    Revision ID: 007
    Revises: 006
    Create Date: 2026-05-14
    """
    from alembic import op
    import sqlalchemy as sa

    revision = "007"
    down_revision = "006"
    branch_labels = None
    depends_on = None


    def upgrade() -> None:
        bind = op.get_bind()
        inspector = sa.inspect(bind)
        columns = {col["name"] for col in inspector.get_columns("portfolio_positions")}
        if "exit_reason" not in columns:
            op.add_column(
                "portfolio_positions",
                sa.Column("exit_reason", sa.String(50), nullable=True),
            )
        if "invalidation_condition" not in columns:
            op.add_column(
                "portfolio_positions",
                sa.Column("invalidation_condition", sa.Text(), nullable=True),
            )


    def downgrade() -> None:
        op.drop_column("portfolio_positions", "invalidation_condition")
        op.drop_column("portfolio_positions", "exit_reason")
    ```

    Dosyayı tam olarak yukarıdaki içerikle oluştur.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && python -c "import ast; ast.parse(open('backend/alembic/versions/007_portfolio_position_discipline_fields.py').read()); print('syntax ok')"</automated>
  </verify>
  <done>007 migration dosyası oluşturuldu; syntax hatası yok; revision="007", down_revision="006"</done>
</task>

<task type="auto">
  <name>Task 3: API endpoint'lerini güncelle</name>
  <files>backend/app/api/portfolio_v2.py</files>
  <action>
    **3a — PositionCreate Pydantic modeline alan ekle (satır 59 sonrası):**
    `rationale: Optional[str] = None` satırından sonra şunu ekle:
    ```python
    invalidation_condition: Optional[str] = None
    ```

    **3b — PositionClose Pydantic modeline alan ekle (satır 64 sonrası):**
    `exit_date: date` satırından sonra şunu ekle:
    ```python
    exit_reason: Optional[str] = None
    ```

    **3c — add_position endpoint'inde kaydetme mantığı:**
    `rationale=body.rationale` parametresinin bulunduğu PortfolioPosition oluşturma bloğunu bul ve şu satırı ekle:
    ```python
    invalidation_condition=body.invalidation_condition,
    ```

    **3d — close_position endpoint'inde kaydetme mantığı:**
    `pos.realized_pnl = realized_pnl` satırından sonra şunu ekle:
    ```python
    pos.exit_reason = body.exit_reason
    ```

    **3e — get_positions endpoint serialization dict'ine iki yeni alan ekle:**
    `get_positions` fonksiyonundaki `output.append({...})` bloğunu bul (yaklaşık satır 126-142).
    `"realized_pnl": pos.realized_pnl,` satırından sonra şunu ekle:
    ```python
    "exit_reason": pos.exit_reason,
    "invalidation_condition": pos.invalidation_condition,
    ```
    Bu olmadan frontend GET isteğine yanıt alsa da exit_reason ve invalidation_condition her zaman None/undefined olur — GUNLUK-03 ve GUNLUK-04 çalışmaz.

    Not: PositionClose'daki exit_reason Optional olarak işaretlenmiş olsa da frontend her zaman bir değer gönderecek. Backend nullable olarak kabul eder.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && python -c "
import sys; sys.path.insert(0, 'backend')
from app.api.portfolio_v2 import PositionCreate, PositionClose
pc = PositionCreate(symbol='TEST', entry_price=10.0, quantity=5, entry_date='2026-01-01', invalidation_condition='test koşul')
cl = PositionClose(exit_price=12.0, exit_date='2026-06-01', exit_reason='Stop Tetiklendi')
print('PositionCreate invalidation_condition:', pc.invalidation_condition)
print('PositionClose exit_reason:', cl.exit_reason)
"</automated>
  </verify>
  <done>PositionCreate invalidation_condition field'ını kabul ediyor; PositionClose exit_reason field'ını kabul ediyor; Python import hatası yok</done>
</task>

</tasks>

<verification>
Tüm üç task tamamlandıktan sonra:

1. ORM syntax kontrolü:
   ```bash
   cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && python -c "from app.models.portfolio_v2 import PortfolioPosition; print([c.name for c in PortfolioPosition.__table__.columns])" 2>&1 | grep -E "exit_reason|invalidation_condition"
   ```
   Her iki alan görünmeli.

2. Migration zinciri:
   ```bash
   grep "down_revision" backend/alembic/versions/007_portfolio_position_discipline_fields.py
   ```
   `down_revision = "006"` döndürmeli.

3. Migration çalıştırma (eğer DB erişimi varsa):
   ```bash
   cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -m alembic upgrade 007
   ```
</verification>

<success_criteria>
- PortfolioPosition modelinde exit_reason (String 50, nullable) ve invalidation_condition (Text, nullable) alanları mevcut
- 007 migration dosyası idempotent upgrade/downgrade içeriyor ve zincir 006→007 doğru
- PositionCreate.invalidation_condition ve PositionClose.exit_reason Pydantic alanları mevcut
- close_position endpoint'i exit_reason'ı pos'a yazıyor
- add_position endpoint'i invalidation_condition'ı yeni pozisyon kaydına yazıyor
- get_positions endpoint'i her pozisyon dict'ine exit_reason ve invalidation_condition dahil ediyor
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/47-i-lem-disiplini-g-nl/47-01-SUMMARY.md` dosyasını oluştur.
</output>
