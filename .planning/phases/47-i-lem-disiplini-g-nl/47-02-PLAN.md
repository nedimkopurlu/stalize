---
phase: 47-i-lem-disiplini-g-nl
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/app/portfolio/page.tsx
autonomous: true
requirements: [GUNLUK-01, GUNLUK-02]

must_haves:
  truths:
    - "Pozisyon ekleme formunda 'Kararı bozan koşul' textarea görünüyor"
    - "Pozisyon kapatma diyaloğunda çıkış nedeni select'i zorunlu — boş bırakılamaz"
    - "'Diğer' seçildiğinde ek textarea açılıyor"
    - "handleAddPosition invalidation_condition'ı API body'sine ekliyor"
    - "handleClosePosition exit_reason boşsa veya 'Diğer' ve açıklama boşsa işlemi engelliyor"
    - "Backend'e doğru format gönderiliyor: 'Diğer' ise 'Diğer: [açıklama]'"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "Genişletilmiş PortfolioPosition interface ve addPosition/closePosition tipler"
      contains: "exit_reason: string | null"
    - path: "frontend/src/app/portfolio/page.tsx"
      provides: "Güncellenmiş PositionForm tipi, EMPTY_FORM, closeForm state, handler fonksiyonlar, form JSX"
      contains: "invalidation_condition: string"
  key_links:
    - from: "frontend/src/app/portfolio/page.tsx handleClosePosition"
      to: "frontend/src/lib/api.ts api.closePosition"
      via: "exit_reason değeri backend format'ında iletiliyor"
      pattern: "closePosition.*exit_reason"
    - from: "frontend/src/app/portfolio/page.tsx handleAddPosition"
      to: "frontend/src/lib/api.ts api.addPosition"
      via: "invalidation_condition trim edilip opsiyonel olarak ekleniyor"
      pattern: "addPosition.*invalidation_condition"
---

<objective>
api.ts interface'leri yeni alanları kapsayacak şekilde genişletilir. portfolio/page.tsx içinde PositionForm tipi, EMPTY_FORM, closeForm state, validation mantığı ve iki form'un JSX render'ı güncellenir.

Purpose: Kullanıcı "Kararı bozan koşul" girebilmeli ve "Kapat" diyaloğunda çıkış nedeni seçmek zorunda kalmalı. Bu plan saf frontend mantığıdır — backend 47-01'de hazır.
Output: Güncellenmiş api.ts interface'leri; güncellenen form state/handler/JSX.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- frontend/src/lib/api.ts — mevcut PortfolioPosition interface (satır 596-613) -->
```typescript
export interface PortfolioPosition {
  id: number;
  symbol: string;
  entry_price: number;
  quantity: number;
  entry_date: string;
  stop_loss: number | null;
  target_price: number | null;
  rationale: string | null;
  current_price: number | null;
  pnl_pct: number | null;
  partial: boolean;
  is_active: boolean;
  exit_price: number | null;
  exit_date: string | null;
  realized_pnl: number | null;
}
```

<!-- frontend/src/lib/api.ts — mevcut addPosition ve closePosition (satır 1100-1118) -->
```typescript
addPosition: (data: {
  symbol: string;
  entry_price: number;
  quantity: number;
  entry_date: string;
  stop_loss?: number;
  target_price?: number;
  rationale?: string;
}) =>
  apiFetch<{ id: number; symbol: string; status: string }>('/portfolio/positions', {
    method: 'POST',
    body: data,
  }),

closePosition: (id: number, data: { exit_price: number; exit_date: string }) =>
  apiFetch<{ id: number; symbol: string; realized_pnl: number | null; status: string }>(
    `/portfolio/positions/${id}/close`,
    { method: 'PATCH', body: data },
  ),
```

<!-- portfolio/page.tsx — mevcut PositionForm tipi ve EMPTY_FORM (satır 22-44) -->
```typescript
type PositionForm = {
  symbol: string;
  entry_price: string;
  quantity: string;
  entry_date: string;
  stop_loss: string;
  target_price: string;
  rationale: string;
};

const EMPTY_FORM: PositionForm = {
  symbol: '',
  entry_price: '',
  quantity: '',
  entry_date: todayISO(),
  stop_loss: '',
  target_price: '',
  rationale: '',
};
```

<!-- portfolio/page.tsx — mevcut closeForm state (satır 245-248) -->
```typescript
const [closeForm, setCloseForm] = useState<{ exit_price: string; exit_date: string }>({
  exit_price: '',
  exit_date: todayISO(),
});
```

<!-- portfolio/page.tsx — mevcut handleClosePosition (satır 410-429) -->
```typescript
async function handleClosePosition() {
  if (closingId === null) return;
  const exitPrice = parseFloat(closeForm.exit_price);
  if (isNaN(exitPrice) || exitPrice <= 0) return;
  setCloseLoading(true);
  setCloseError(null);
  try {
    await api.closePosition(closingId, {
      exit_price: exitPrice,
      exit_date: closeForm.exit_date,
    });
    setClosingId(null);
    setCloseForm({ exit_price: '', exit_date: todayISO() });
    await Promise.all([fetchPositions(), fetchHistory()]);
  } catch (e) {
    setCloseError(e instanceof Error ? e.message : 'Pozisyon kapatılamadı.');
  } finally {
    setCloseLoading(false);
  }
}
```

<!-- portfolio/page.tsx — mevcut handleAddPosition (satır 386-394, api.addPosition çağrısı) -->
```typescript
await api.addPosition({
  symbol,
  entry_price: entryPrice,
  quantity,
  entry_date: form.entry_date,
  ...(stopLoss !== undefined ? { stop_loss: stopLoss } : {}),
  ...(targetPrice !== undefined ? { target_price: targetPrice } : {}),
  ...(form.rationale.trim() ? { rationale: form.rationale.trim() } : {}),
});
```

<!-- portfolio/page.tsx — closeForm JSX (satır 910-942, aktif pozisyon tablosundaki inline close form) -->
Satır 910: `{closingId === pos.id ? (`
Satır 911: `<div className={styles.closeForm}>`
İçeride: exit_price input, exit_date input, Kaydet/İptal butonları
Satır 942: `closeError && closingId === pos.id`

<!-- portfolio/page.tsx — modalda rationale textarea (satır 1143-1151) -->
```tsx
<label className={styles.field}>
  <span>Not</span>
  <textarea
    value={form.rationale}
    onChange={(event) => updateForm('rationale', event.target.value)}
    placeholder="Opsiyonel"
    rows={3}
  />
</label>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api.ts interface ve metot tiplerini genişlet</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
    **1a — PortfolioPosition interface'e iki alan ekle:**
    `realized_pnl: number | null;` satırından sonra şunları ekle:
    ```typescript
    exit_reason: string | null;
    invalidation_condition: string | null;
    ```

    **1b — addPosition data tipine alan ekle:**
    `rationale?: string;` satırından sonra şunu ekle:
    ```typescript
    invalidation_condition?: string;
    ```

    **1c — closePosition data tipini genişlet:**
    Mevcut: `data: { exit_price: number; exit_date: string }`
    Yeni:   `data: { exit_price: number; exit_date: string; exit_reason: string }`
    (exit_reason zorunlu — backend Optional kabul etse de frontend her zaman değer gönderir)
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript derleme hatası yok; PortfolioPosition interface exit_reason ve invalidation_condition içeriyor; closePosition data tipi exit_reason içeriyor</done>
</task>

<task type="auto">
  <name>Task 2: Form state, handler ve JSX güncelle</name>
  <files>frontend/src/app/portfolio/page.tsx</files>
  <action>
    **2a — PositionForm tipine alan ekle (satır 29 sonrası, rationale'dan sonra):**
    ```typescript
    invalidation_condition: string;
    ```

    **2b — EMPTY_FORM'a alan ekle (satır 43 sonrası, rationale: '' dan sonra):**
    ```typescript
    invalidation_condition: '',
    ```

    **2c — closeForm state tipini ve başlangıç değerini güncelle:**
    Mevcut:
    ```typescript
    const [closeForm, setCloseForm] = useState<{ exit_price: string; exit_date: string }>({
      exit_price: '',
      exit_date: todayISO(),
    });
    ```
    Yeni:
    ```typescript
    const [closeForm, setCloseForm] = useState<{
      exit_price: string;
      exit_date: string;
      exit_reason: string;
      exit_reason_other: string;
    }>({
      exit_price: '',
      exit_date: todayISO(),
      exit_reason: '',
      exit_reason_other: '',
    });
    ```

    **2d — handleClosePosition'ı güncelle:**
    Validation guard'ları ekle ve API çağrısını güncelle:
    ```typescript
    async function handleClosePosition() {
      if (closingId === null) return;
      const exitPrice = parseFloat(closeForm.exit_price);
      if (isNaN(exitPrice) || exitPrice <= 0) return;
      // GUNLUK-02: exit_reason zorunlu
      if (!closeForm.exit_reason) {
        setCloseError('Çıkış nedeni seçilmesi zorunludur.');
        return;
      }
      if (closeForm.exit_reason === 'Diğer' && !closeForm.exit_reason_other.trim()) {
        setCloseError('"Diğer" seçildiğinde kısa açıklama zorunludur.');
        return;
      }
      const exitReason = closeForm.exit_reason === 'Diğer'
        ? `Diğer: ${closeForm.exit_reason_other.trim()}`
        : closeForm.exit_reason;
      setCloseLoading(true);
      setCloseError(null);
      try {
        await api.closePosition(closingId, {
          exit_price: exitPrice,
          exit_date: closeForm.exit_date,
          exit_reason: exitReason,
        });
        setClosingId(null);
        setCloseForm({ exit_price: '', exit_date: todayISO(), exit_reason: '', exit_reason_other: '' });
        await Promise.all([fetchPositions(), fetchHistory()]);
      } catch (e) {
        setCloseError(e instanceof Error ? e.message : 'Pozisyon kapatılamadı.');
      } finally {
        setCloseLoading(false);
      }
    }
    ```

    **2e — handleAddPosition'daki api.addPosition çağrısına alan ekle:**
    `...(form.rationale.trim() ? { rationale: form.rationale.trim() } : {}),` satırından sonra:
    ```typescript
    ...(form.invalidation_condition.trim() ? { invalidation_condition: form.invalidation_condition.trim() } : {}),
    ```
    Ayrıca `setForm({ ...EMPTY_FORM, entry_date: todayISO() })` ifadesi EMPTY_FORM'da yeni alan olduğundan otomatik sıfırlanır — ek değişiklik gerekmez.

    **2f — Inline close form JSX'e çıkış nedeni alanları ekle:**
    Aktif pozisyon tablosundaki inline close form'u bul (`closingId === pos.id` dalı, yaklaşık satır 910-942).
    exit_date input'undan sonra, Kaydet/İptal butonlarından önce şunları ekle:
    ```tsx
    <select
      value={closeForm.exit_reason}
      onChange={(e) => setCloseForm((f) => ({ ...f, exit_reason: e.target.value, exit_reason_other: '' }))}
      className={styles.closeInput}
      aria-label="Çıkış nedeni"
    >
      <option value="">Çıkış nedeni seçin…</option>
      <option value="Stop Tetiklendi">Stop Tetiklendi</option>
      <option value="Hedefe Ulaştı">Hedefe Ulaştı</option>
      <option value="Senaryo Bozuldu">Senaryo Bozuldu</option>
      <option value="Diğer">Diğer</option>
    </select>
    {closeForm.exit_reason === 'Diğer' && (
      <textarea
        value={closeForm.exit_reason_other}
        onChange={(e) => setCloseForm((f) => ({ ...f, exit_reason_other: e.target.value }))}
        placeholder="Kısa açıklama giriniz"
        rows={2}
        className={styles.closeInput}
        aria-label="Diğer çıkış nedeni açıklaması"
      />
    )}
    ```

    **2g — Modal formuna "Kararı bozan koşul" textarea ekle:**
    Mevcut rationale textarea'sından (`<label className={styles.field}><span>Not</span>...`) sonra şunu ekle:
    ```tsx
    <label className={styles.field}>
      <span>Kararı bozan koşul</span>
      <textarea
        value={form.invalidation_condition}
        onChange={(event) => updateForm('invalidation_condition', event.target.value)}
        placeholder="Ör. MACD negatif geçerse veya 90 TL altına sararsa"
        rows={2}
      />
    </label>
    ```
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TypeScript derleme hatası yok; closeForm state'i exit_reason ve exit_reason_other içeriyor; modal formda "Kararı bozan koşul" alanı mevcut; close form'da select ve koşullu textarea mevcut; handleClosePosition exit_reason validation içeriyor</done>
</task>

</tasks>

<verification>
Her iki task tamamlandıktan sonra:

1. TypeScript tam derleme:
   ```bash
   cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1
   ```
   Sıfır hata bekleniyor.

2. Alan varlığı kontrolü:
   ```bash
   grep -n "exit_reason\|invalidation_condition" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/lib/api.ts
   grep -n "exit_reason\|invalidation_condition\|Kararı bozan" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/portfolio/page.tsx
   ```
</verification>

<success_criteria>
- PortfolioPosition interface exit_reason: string | null ve invalidation_condition: string | null içeriyor
- closePosition data tipi exit_reason: string içeriyor
- addPosition data tipi invalidation_condition?: string içeriyor
- PositionForm tipi ve EMPTY_FORM invalidation_condition içeriyor
- closeForm state exit_reason ve exit_reason_other içeriyor
- handleClosePosition: exit_reason boşsa hata mesajı; 'Diğer' ve açıklama boşsa hata mesajı; başarılı gönderimde format 'Diğer: [metin]' ya da doğrudan seçenek
- Modal formunda rationale'dan sonra 'Kararı bozan koşul' textarea var
- Aktif pozisyon satırındaki inline close form'da çıkış nedeni select + koşullu textarea var
- `npm run build` (veya tsc --noEmit) TypeScript hatası üretmiyor
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/47-i-lem-disiplini-g-nl/47-02-SUMMARY.md` dosyasını oluştur.
</output>
