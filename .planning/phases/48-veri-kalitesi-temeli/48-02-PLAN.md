---
phase: 48-veri-kalitesi-temeli
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/StockHelpers.tsx
  - frontend/src/app/stocks/page.tsx
  - frontend/src/app/stocks/[symbol]/page.tsx
  - frontend/src/app/model-portfolio/page.tsx
autonomous: true
requirements:
  - TECH-01

must_haves:
  truths:
    - "safeLabel and safeLabelTooltip exist as named exports in StockHelpers.tsx"
    - "No inline `function safeLabel(` definition remains in any frontend page"
    - "stocks/page.tsx, stocks/[symbol]/page.tsx, model-portfolio/page.tsx all import safeLabel and safeLabelTooltip from '@/components/StockHelpers'"
    - "Frontend type-checks without errors related to safeLabel"
  artifacts:
    - path: "frontend/src/components/StockHelpers.tsx"
      provides: "Single source of truth for safeLabel and safeLabelTooltip"
      contains: "export function safeLabel"
  key_links:
    - from: "frontend/src/app/stocks/page.tsx"
      to: "frontend/src/components/StockHelpers.tsx"
      via: "import safeLabel from StockHelpers"
      pattern: "import.*safeLabel.*StockHelpers"
    - from: "frontend/src/app/stocks/[symbol]/page.tsx"
      to: "frontend/src/components/StockHelpers.tsx"
      via: "import safeLabel from StockHelpers"
      pattern: "import.*safeLabel.*StockHelpers"
    - from: "frontend/src/app/model-portfolio/page.tsx"
      to: "frontend/src/components/StockHelpers.tsx"
      via: "import safeLabel from StockHelpers"
      pattern: "import.*safeLabel.*StockHelpers"
---

<objective>
Move `safeLabel()` and `safeLabelTooltip()` helpers to `StockHelpers.tsx` as named exports; remove inline copies from the 3 pages that have them (stocks list, stock detail, model portfolio). This is TECH-01.

Purpose: Eliminate duplication; single source of truth for recommendation → safe Türkçe label mapping.
Output: Refactored helpers + 3 pages importing from the central location.

Note: Phase CONTEXT lists "5 pages" but only 3 pages contain inline copies (verified via grep — watchlist/page.tsx and backtest/page.tsx do not call safeLabel). This plan refactors the 3 confirmed sites.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-veri-kalitesi-temeli/48-CONTEXT.md
@frontend/src/components/StockHelpers.tsx
@frontend/src/app/stocks/page.tsx
@frontend/src/app/stocks/[symbol]/page.tsx
@frontend/src/app/model-portfolio/page.tsx

<interfaces>
Inline implementation in `frontend/src/app/stocks/page.tsx` lines 34-44 is the canonical version to extract:

```typescript
function safeLabel(rec: string | null): string { ... }
function safeLabelTooltip(rec: string | null): string { ... }
```

All three pages have IDENTICAL implementations. Extract verbatim from `stocks/page.tsx` (master copy).

Existing exports in `StockHelpers.tsx`: `formatPrice`, `formatVolume`, `formatMarketCap`, `formatPercentage`. Pattern: `export function name(...) { ... }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add safeLabel and safeLabelTooltip as named exports in StockHelpers.tsx</name>
  <files>frontend/src/components/StockHelpers.tsx</files>
  <read_first>
    frontend/src/components/StockHelpers.tsx,
    frontend/src/app/stocks/page.tsx
  </read_first>
  <action>
    1) Read the EXACT body of `safeLabel` and `safeLabelTooltip` from `frontend/src/app/stocks/page.tsx` (lines 34-44 area). Copy verbatim.

    2) Append to `frontend/src/components/StockHelpers.tsx` as named exports (alongside existing `formatPrice` etc.):

       ```typescript
       export function safeLabel(rec: string | null): string {
         // verbatim body from stocks/page.tsx
       }

       export function safeLabelTooltip(rec: string | null): string {
         // verbatim body from stocks/page.tsx
       }
       ```

    3) Do NOT alter the recommendation → label mapping, change return types, or rename anything. This is a pure relocation.

    4) Do NOT remove or rename existing exports.
  </action>
  <verify>
    <automated>grep -E '^export function safeLabel\b' frontend/src/components/StockHelpers.tsx &amp;&amp; grep -E '^export function safeLabelTooltip\b' frontend/src/components/StockHelpers.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^export function safeLabel\(' frontend/src/components/StockHelpers.tsx` matches.
    - `grep -E '^export function safeLabelTooltip\(' frontend/src/components/StockHelpers.tsx` matches.
    - Existing exports (formatPrice, formatVolume, formatMarketCap, formatPercentage) untouched — `grep -c '^export function' frontend/src/components/StockHelpers.tsx` returns >= 6.
    - Function bodies match the originals (visual diff against stocks/page.tsx lines 34-44).
  </acceptance_criteria>
  <done>
    StockHelpers.tsx exports safeLabel and safeLabelTooltip; no other exports altered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace inline definitions with imports in 3 pages</name>
  <files>
    frontend/src/app/stocks/page.tsx,
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/model-portfolio/page.tsx
  </files>
  <read_first>
    frontend/src/app/stocks/page.tsx,
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/model-portfolio/page.tsx,
    frontend/src/components/StockHelpers.tsx
  </read_first>
  <action>
    For each of the 3 pages:

    1) **Delete the inline definitions** of `safeLabel` and `safeLabelTooltip` (both functions):
       - `frontend/src/app/stocks/page.tsx` lines ~34-44.
       - `frontend/src/app/stocks/[symbol]/page.tsx` lines ~166-176.
       - `frontend/src/app/model-portfolio/page.tsx` lines ~59-69.

    2) **Add (or extend) the import** at the top of each file:
       ```typescript
       import { safeLabel, safeLabelTooltip } from '@/components/StockHelpers';
       ```
       If the file already imports OTHER helpers from `@/components/StockHelpers`, extend that existing import statement (e.g., `import { formatPrice, safeLabel, safeLabelTooltip } from '@/components/StockHelpers';`) — do NOT add a duplicate import line.

    3) Do NOT modify any call sites (`safeLabel(stock.recommendation)`, `safeLabelTooltip(...)`) — signatures and behavior are identical.

    4) Do NOT touch `frontend/src/app/watchlist/page.tsx` or `frontend/src/app/backtest/page.tsx` — they do not use safeLabel.

    5) Run `cd frontend && npx tsc --noEmit` to confirm no TypeScript errors.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; ! grep -nE '^function safeLabel\b' src/app/stocks/page.tsx src/app/stocks/\[symbol\]/page.tsx src/app/model-portfolio/page.tsx &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '^function safeLabel\(' frontend/src/app/stocks/page.tsx` returns NO match.
    - `grep -E '^function safeLabel\(' 'frontend/src/app/stocks/[symbol]/page.tsx'` returns NO match.
    - `grep -E '^function safeLabel\(' frontend/src/app/model-portfolio/page.tsx` returns NO match.
    - `grep -E '^function safeLabelTooltip\(' frontend/src/app/stocks/page.tsx` returns NO match.
    - `grep -E '^function safeLabelTooltip\(' 'frontend/src/app/stocks/[symbol]/page.tsx'` returns NO match.
    - `grep -E '^function safeLabelTooltip\(' frontend/src/app/model-portfolio/page.tsx` returns NO match.
    - Each of the 3 files contains a line matching `import.*safeLabel.*from\s+['"]@/components/StockHelpers['"]`.
    - `npx tsc --noEmit` in frontend/ exits 0.
  </acceptance_criteria>
  <done>
    No inline safeLabel/safeLabelTooltip definitions remain in pages; all three pages import from StockHelpers; TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- StockHelpers.tsx contains 6+ named exports (existing 4 + safeLabel + safeLabelTooltip).
- Zero inline `function safeLabel(` definitions remain across the frontend.
- All 3 target pages have the import line.
- `npx tsc --noEmit` passes.
- ESLint passes (`cd frontend && npm run lint`).
</verification>

<success_criteria>
- TECH-01 closed: single source of truth for safeLabel.
- No behavior change — visual output of recommendation badges identical pre/post refactor.
- Future pages can `import { safeLabel } from '@/components/StockHelpers'` without copy-paste.
</success_criteria>

<output>
After completion, create `.planning/phases/48-veri-kalitesi-temeli/48-02-SUMMARY.md` capturing: extracted source location, files modified, TypeScript build result.
</output>
