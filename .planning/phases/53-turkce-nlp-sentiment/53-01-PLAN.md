---
phase: 53-turkce-nlp-sentiment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/kap_parser.py
  - backend/app/services/macro_news.py
  - backend/app/main.py
  - backend/requirements.txt
autonomous: true
requirements:
  - NLP-01
  - NLP-02

must_haves:
  truths:
    - "vaderSentiment paketi requirements.txt içinde bulunmuyor ve codebase'de hiçbir VADER import kalmıyor"
    - "KAP duyuruları APScheduler aracılığıyla GPT-4o-mini'ye batch gönderiliyor; sonuçlar DB'deki sentiment_label alanına yazılıyor"
    - "macro_news.py içindeki _score_headline() Türkçe keyword öncelikli çalışıyor; VADER import veya bağımlılığı yok"
    - "OpenAI API çağrısı başarısız olursa kural tabanlı fallback devreye giriyor; exception uygulama ayakta kalmasını bozmuyor"
  artifacts:
    - path: "backend/app/services/kap_parser.py"
      provides: "analyze_kap_sentiment_batch() ve _process_sentiment_batch() async metodları"
      exports: ["KAPParser.analyze_kap_sentiment_batch", "run_kap_scan"]
    - path: "backend/app/services/macro_news.py"
      provides: "classify_turkish_sentiment() fonksiyonu, Türkçe-öncelikli keyword listeleri"
    - path: "backend/app/main.py"
      provides: "background_kap_scan içinde batch sentiment çağrısı"
    - path: "backend/requirements.txt"
      provides: "vaderSentiment girişi bulunmayan temiz bağımlılık listesi"
  key_links:
    - from: "background_kap_scan (main.py)"
      to: "kap_parser.analyze_kap_sentiment_batch()"
      via: "stored_ids listesi ile çağrı"
      pattern: "analyze_kap_sentiment_batch"
    - from: "kap_parser._process_sentiment_batch()"
      to: "gemini_service._client.chat.completions.create()"
      via: "gemini_service.generate() wrapper"
      pattern: "gemini_service\\.generate"
    - from: "NewsItem.sentiment_label"
      to: "_process_sentiment_batch() sonucu"
      via: "DB update (pozitif→positive, negatif→negative, nötr→neutral)"
      pattern: "sentiment_label.*=.*en_label"
---

<objective>
VADER bağımlılığını kalıcı olarak kaldır; KAP duyuruları için OpenAI GPT-4o-mini batch sentiment
pipeline'ını kur; macro_news.py'yi Türkçe-öncelikli keyword seti ile yeniden yaz.

Purpose: BIST haberleri Türkçe olduğu için İngilizce VADER anlamsız sonuç üretiyordu. GPT-4o-mini
zaten entegre (Phase 35); aynı istemciyi KAP sentiment için kullanmak maliyetleri minimize eder.

Output:
- kap_parser.py: analyze_kap_sentiment_batch() ve _process_sentiment_batch() metodları
- macro_news.py: classify_turkish_sentiment() fonksiyonu, genişletilmiş Türkçe keyword listesi
- main.py: background_kap_scan içinde batch sentiment adımı
- requirements.txt: vaderSentiment satırı yok (zaten yoksa doğrulama kaydı)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-turkce-nlp-sentiment/53-CONTEXT.md

@backend/app/services/kap_parser.py
@backend/app/services/macro_news.py
@backend/app/services/gemini_service.py
@backend/app/main.py
@backend/app/models/news.py
@backend/requirements.txt

<interfaces>
<!-- OpenAIService — backend/app/services/gemini_service.py -->
```python
class OpenAIService:
    _client: AsyncOpenAI   # initialized when OPENAI_API_KEY is set
    _configured: bool

    async def generate(
        self,
        prompt: str,
        system_prompt: Optional[str] = None,
        model: Optional[str] = None,
    ) -> str: ...

gemini_service = OpenAIService()  # singleton
```

<!-- Settings — backend/app/core/config.py -->
```python
class Settings(BaseSettings):
    OPENAI_API_KEY: Optional[str] = None
    OPENAI_MODEL: str = "gpt-4o-mini"
```

<!-- NewsItem — backend/app/models/news.py -->
```python
class NewsItem(Base):
    __tablename__ = "news_items"
    id: int
    stock_id: int | None
    title: str
    summary: str | None
    sentiment_label: str | None   # "positive" | "negative" | "neutral"
    is_processed: bool
    # sentiment_label alanı mevcut — migration GEREKMİYOR
```

<!-- KAPParser.store_announcements — MEVCUT imza (Task 1'de değişecek) -->
```python
async def store_announcements(self, announcements: List[Dict]) -> int:
    """Şu an: sadece stored_count döner. Task 1 sonrası: (int, list[int]) döner."""
```

<!-- run_kap_scan — mevcut imza -->
```python
async def run_kap_scan() -> int: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: analyze_kap_sentiment_batch() — OpenAI GPT-4o-mini batch KAP sentiment</name>
  <files>backend/app/services/kap_parser.py</files>
  <behavior>
    - analyze_kap_sentiment_batch([]) hiçbir şey yapmadan döner
    - analyze_kap_sentiment_batch(ids) gemini_service._configured=False ise uyarı log yazar, DB'ye dokunmaz
    - _process_sentiment_batch: OpenAI JSON array ["pozitif","negatif","nötr"] parse eder; pozitif→"positive", negatif→"negative", nötr→"neutral" DB'ye yazar
    - OpenAI parse hatası: logger.warning yazar, fonksiyon sessizce döner (uygulama çökmez)
    - store_announcements artık (stored_count, stored_ids) tuple döner; stored_ids yeni kayıt ID'lerini içerir
  </behavior>
  <action>
KAPParser sınıfına iki async metod ekle. _analyze_announcement() metodundan hemen sonra:

**1. analyze_kap_sentiment_batch(self, news_ids: list[int]) -> None**

Mantık:
- news_ids boşsa return
- gemini_service import et; _configured False ise logger.warning ve return
- BATCH_SIZE = 20; range(0, len(news_ids), BATCH_SIZE) ile _process_sentiment_batch() çağır

**2. _process_sentiment_batch(self, batch_ids: list[int]) -> None**

Mantık:
- AsyncSessionLocal() ile DB aç
- select(NewsItem).where(NewsItem.id.in_(batch_ids)) ile item'ları getir
- items boşsa return
- Her item için: f"{item.title or ''} {item.summary or ''}".strip()[:300] metni al
- "1. {text}\n2. {text}" formatlı numbered_list oluştur
- Prompt (Türkçe):
  "Asagidaki {N} KAP duyurusunu tek tek degerlendir.\n"
  "Her birini yalnizca 'pozitif', 'negatif' veya 'notr' olarak siniflandir.\n"
  "Yanit olarak SADECE JSON array dondur, baska aciklama ekleme.\n"
  "Ornek: [\"pozitif\", \"notr\", \"negatif\"]\n\n{numbered_list}"
- gemini_service.generate(prompt=..., system_prompt="Sen bir Turk borsa uzmansin. Verilen KAP duyurularini yatirimci bakis acisindan siniflandir.", model="gpt-4o-mini") cagir
- raw response'ta "[" ve "]" bul; json.loads() ile parse et
- Parse hata: logger.warning yaz, return (DB'ye yazma)
- tr_to_en = {"pozitif": "positive", "negatif": "negative", "notr": "neutral", "nötr": "neutral"}
- Her item icin: en_label = tr_to_en.get(labels_tr[idx].lower().strip(), "neutral"); item.sentiment_label = en_label; item.is_processed = True
- db.commit(); hata durumunda db.rollback() + logger.error

**store_announcements() dégisikligi:**
- Fonksiyon imzasi degismez (disaridan parametreler ayni)
- Donus degeri int yerine tuple[int, list[int]] olur: return stored_count, stored_ids
- stored_ids listesini metod basinda tanimla: stored_ids: list[int] = []
- db.add(news) sonrasi await db.flush() ekle; stored_ids.append(news.id) yap
- Commit hatasi durumunda stored_ids temizleme GEREKMEZ (zaten rollback)

**run_kap_scan() dégisikligi:**
- stored = await kap_parser.store_announcements(announcements) satirini
  stored, stored_ids = await kap_parser.store_announcements(announcements) yap
- Var olan "if stored > 0: record_source_success(...)" blogu aynen kals
- stored_ids artik mevcut ama bu task'ta henuz kullanilmiyor — Task 3'te baglanacak
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "
from app.services.kap_parser import KAPParser, run_kap_scan
import inspect, asyncio

p = KAPParser()
assert hasattr(p, 'analyze_kap_sentiment_batch'), 'analyze_kap_sentiment_batch eksik'
assert hasattr(p, '_process_sentiment_batch'), '_process_sentiment_batch eksik'
assert inspect.iscoroutinefunction(p.analyze_kap_sentiment_batch), 'async degil'
assert inspect.iscoroutinefunction(p._process_sentiment_batch), 'async degil'

# Bos liste: istisna firlamamali
asyncio.run(p.analyze_kap_sentiment_batch([]))

# store_announcements imzasi
sig = inspect.signature(p.store_announcements)
print('store_announcements parametreleri:', list(sig.parameters.keys()))

print('OK: tum kontroller gecti')
"
    </automated>
  </verify>
  <done>
    - analyze_kap_sentiment_batch() ve _process_sentiment_batch() KAPParser sinifinda mevcut ve async
    - store_announcements() (stored_count, stored_ids) tuple donuyor
    - run_kap_scan() stored, stored_ids = ... ile unpacking yapiyor
    - Bos liste cagrisi exception firlatmiyor
  </done>
</task>

<task type="auto">
  <name>Task 2: Türkçe keyword classifier — macro_news.py ve requirements.txt temizligi</name>
  <files>backend/app/services/macro_news.py, backend/requirements.txt</files>
  <action>
**macro_news.py degisiklikleri:**

Dosyanin basinda (sinif taniminin uzerinde, modul seviyesinde) classify_turkish_sentiment()
fonksiyonunu ve genisletilmis keyword listelerini tanimla:

```python
# ── Türkçe NLP: kural tabanli duygu siniflandirici ──────────────────────────
TURKISH_POSITIVE_KEYWORDS = [
    "artis", "buyume", "kar", "rekor", "guclu", "olumlu", "yukselis",
    "kazanc", "basari", "ihracat artisi", "temettu", "yatirim", "anlesma",
    "sozlesme", "ihale", "buyuyor", "artti", "yukseldi", "geri alim",
    "not artirimi", "gorunum pozitif", "surplus", "beat", "strong", "growth",
    "upgrade", "rally", "gain", "recovery",
]
TURKISH_NEGATIVE_KEYWORDS = [
    "dusus", "zarar", "kayip", "risk", "zayif", "olumsuz", "kriz",
    "baski", "gerileme", "endise", "iptal", "ceza", "dava", "sorusturma",
    "geriledi", "dusty", "azaldi", "konkordato", "not indirimi", "gorunum negatif",
    "miss", "weak", "downgrade", "loss", "drop", "fall", "cut", "crash",
    "tumble", "decline", "probe", "lawsuit",
]

def classify_turkish_sentiment(text: str) -> float:
    """
    Türkce ve Ingilizce karma anahtar kelimelerle haber basligini skora cevir.
    Donus: -1.0 ile +1.0 arasi float (MacroNewsCollector._score_headline ile ayni aralik).
    """
    # Türkce karakterleri normalize et
    mapping = str.maketrans("çğıöşüÇĞİÖŞÜ", "cgiosuCGIOSU")
    text_norm = text.translate(mapping).lower()

    pos = sum(1 for kw in TURKISH_POSITIVE_KEYWORDS if kw in text_norm)
    neg = sum(1 for kw in TURKISH_NEGATIVE_KEYWORDS if kw in text_norm)
    raw = (pos * 0.22) - (neg * 0.22)
    return max(-1.0, min(1.0, raw))
```

MacroNewsCollector sinifi icerisinde:
- _score_headline() metodunu classify_turkish_sentiment() cagrisina yonlendir:
  ```python
  def _score_headline(self, headline: str) -> float:
      return classify_turkish_sentiment(headline)
  ```
- __init__ icindeki self.positive_keywords ve self.negative_keywords listelerini SIL —
  bunlar artik modul seviyesinde TURKISH_POSITIVE_KEYWORDS ve TURKISH_NEGATIVE_KEYWORDS.
- _determine_direction() ve _determine_magnitude() icindeki self.surge_keywords ve
  self.plunge_keywords referanslarini KORUYARAK self.surge_keywords listesini Turkce
  karsiliklari ile genislet:
  ```python
  self.surge_keywords = [
      "surge", "jump", "soar", "spike", "skyrocket", "hike", "rally", "gain",
      "ustunde", "yukselis", "artti", "rekor", "yukseldi",
  ]
  self.plunge_keywords = [
      "plunge", "crash", "tumble", "drop", "fall", "slide", "cut", "decline",
      "altinda", "dusus", "dusty", "geriledi", "azaldi",
  ]
  self.extreme_keywords = [
      "record", "massive", "extreme", "historic", "shock", "panic", "collapse",
      "rekor", "sok", "cokus", "tarihi", "asiri",
  ]
  ```

VADER ile ilgili HICBIR import yok — dogrulama icin grep: requirements.txt ve tum .py
dosyalarinda "vaderSentiment" veya "SentimentIntensityAnalyzer" arama.

**requirements.txt degisikligi:**
- Dosyada "vaderSentiment" satirini ara. Varsa sil. Yoksa degistirme — sadece dogrula.
- Dosyaya yorum satiri ekle: `# vaderSentiment kaldirildi — Phase 53 (Turkce NLP)`
  (Bu satiri "# LLM — Phase 35" yorumunun altina ekle.)
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "
# VADER import kontrolu
import subprocess, sys
result = subprocess.run(['grep', '-r', 'vaderSentiment\|SentimentIntensityAnalyzer', 'app/'], capture_output=True, text=True)
if result.stdout.strip():
    print('HATA: VADER referansi bulundu:', result.stdout)
    sys.exit(1)

# classify_turkish_sentiment import ve calisma
from app.services.macro_news import classify_turkish_sentiment, MacroNewsCollector
score = classify_turkish_sentiment('Rekor karlilik ve yuksek temettu aciklandi')
assert score > 0, f'Pozitif metin icin skor pozitif olmali: {score}'
score2 = classify_turkish_sentiment('Buyuk zarar ve kriz haberi cikti')
assert score2 < 0, f'Negatif metin icin skor negatif olmali: {score2}'

c = MacroNewsCollector()
assert hasattr(c, '_score_headline')
print('OK: VADER yok, classify_turkish_sentiment calisiyor, skorlar dogru')
" && grep -c "vaderSentiment" requirements.txt && echo "requirements.txt temiz (0 esleme bekleniyor)"
    </automated>
  </verify>
  <done>
    - classify_turkish_sentiment() modul seviyesinde tanimli ve dogru skorlar uretiyor
    - MacroNewsCollector._score_headline() bu fonksiyonu cagiiriyor
    - vaderSentiment codebase'de hicbir yerde bulunamaz (grep 0 sonuc)
    - requirements.txt'de vaderSentiment satiri yok
  </done>
</task>

<task type="auto">
  <name>Task 3: APScheduler entegrasyonu — background_kap_scan'de batch sentiment tetikleme</name>
  <files>backend/app/main.py</files>
  <action>
background_kap_scan() fonksiyonunu guncelle. Mevcut:

```python
async def background_kap_scan():
    from app.services.kap_parser import run_kap_scan
    from app.services.scoring import scoring_engine
    logging.info("JOB_START source=kap")
    try:
        stored = await run_kap_scan()
        if stored > 0:
            await scoring_engine.update_all_scores()
    except Exception as e:
        logging.error(f"KAP Tarama Hatasi: {e}")
```

Yeni hali:

```python
async def background_kap_scan():
    from app.services.kap_parser import run_kap_scan, kap_parser
    from app.services.scoring import scoring_engine
    logging.info("JOB_START source=kap")
    try:
        stored, stored_ids = await run_kap_scan()
        if stored > 0:
            await scoring_engine.update_all_scores()
        # Yeni KAP duyurulari icin OpenAI batch sentiment analizi
        if stored_ids:
            try:
                await kap_parser.analyze_kap_sentiment_batch(stored_ids)
                logging.info("KAP_SENTIMENT_BATCH tamamlandi item_count=%d", len(stored_ids))
            except Exception as sentiment_err:
                # Sentiment hatasi taramayı durdurmasin
                logging.warning("KAP_SENTIMENT_BATCH hatasi: %s", sentiment_err)
    except Exception as e:
        logging.error(f"KAP Tarama Hatasi: {e}")
```

Degisiklikler:
1. `stored = await run_kap_scan()` → `stored, stored_ids = await run_kap_scan()`
2. Import'a `kap_parser` eklendi
3. `if stored_ids:` blogu eklendi — stored_ids bos ise API cagrisi yapilmaz
4. Inner try/except: sentiment hatasi dis try/except'e tasmiyor
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "
import ast, sys

with open('app/main.py', 'r') as f:
    source = f.read()

tree = ast.parse(source)

# background_kap_scan fonksiyonunu bul
found = False
for node in ast.walk(tree):
    if isinstance(node, ast.AsyncFunctionDef) and node.name == 'background_kap_scan':
        func_src = ast.unparse(node)
        assert 'stored_ids' in func_src, 'stored_ids yok'
        assert 'analyze_kap_sentiment_batch' in func_src, 'sentiment batch cagrisi yok'
        assert 'kap_parser' in func_src, 'kap_parser import eksik'
        found = True
        break

assert found, 'background_kap_scan bulunamadi'

# Syntax kontrolu
compile(source, 'main.py', 'exec')
print('OK: background_kap_scan guncellendi, syntax gecerli')
"
    </automated>
  </verify>
  <done>
    - background_kap_scan() stored, stored_ids = await run_kap_scan() ile cagiriyor
    - stored_ids doluysa analyze_kap_sentiment_batch() tetikleniyor
    - Sentiment hatasi KAP taramasini durdurmaz (inner try/except)
    - Python syntax hatasi yok
  </done>
</task>

</tasks>

<verification>
Tum task'lar tamamlandiktan sonra:

```bash
# 1. VADER tamamen kaldirildimi?
grep -r "vaderSentiment\|SentimentIntensityAnalyzer" /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/
# Beklenen: hicbir sonuc

# 2. requirements.txt temiz mi?
grep "vaderSentiment" /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/requirements.txt
# Beklenen: no such line (exit code 1)

# 3. Modul import testi
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend
python -c "from app.services.kap_parser import kap_parser, run_kap_scan; from app.services.macro_news import classify_turkish_sentiment, MacroNewsCollector; print('Import OK')"

# 4. Sentiment skor sanity check
python -c "
from app.services.macro_news import classify_turkish_sentiment
assert classify_turkish_sentiment('rekor kar artisi') > 0
assert classify_turkish_sentiment('buyuk zarar kriz') < 0
assert classify_turkish_sentiment('aciklama yapildi') == 0.0
print('Sentiment skorlari mantikli')
"

# 5. Syntax kontrolu
python -m py_compile app/services/kap_parser.py app/services/macro_news.py app/main.py
echo "Syntax OK"
```
</verification>

<success_criteria>
- `grep -r "vaderSentiment" backend/` sifir sonuc donduruyor
- `classify_turkish_sentiment("rekor kar")` > 0, `classify_turkish_sentiment("buyuk zarar")` < 0
- `KAPParser.analyze_kap_sentiment_batch` mevcut, async, bos liste icin sessizce donuyor
- `store_announcements()` (int, list[int]) tuple donuyor
- `background_kap_scan()` stored_ids ile batch sentiment tetikliyor
- `python -m py_compile` uc dosya icin de hata vermiyor
</success_criteria>

<output>
Tamamlaninca olustur: `.planning/phases/53-turkce-nlp-sentiment/53-01-SUMMARY.md`

Ozet icermeli:
- Hangi metodlar eklendi (imzalar)
- store_announcements() donus degeri degisikligi
- background_kap_scan() yeni akisi
- vaderSentiment durumu (zaten yoktu / silindi)
- Calistigini dogrulayan test ciktilari
</output>
