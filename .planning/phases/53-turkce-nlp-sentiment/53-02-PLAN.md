---
phase: 53-turkce-nlp-sentiment
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - frontend/src/app/stocks/[symbol]/page.tsx
  - frontend/src/app/stocks/[symbol]/page.module.css
autonomous: true
requirements:
  - NLP-01
  - NLP-02

must_haves:
  truths:
    - "Hisse detay sayfasinda KAP haberleri icin sentiment badge renk kodlamasi calisir"
    - "data-sentiment CSS attribute'u hem 'positive'/'negative'/'neutral' (mevcut) hem de 'pozitif'/'negatif'/'notr' (OpenAI donus etiketi yolu) degerlerini renklendiriyor"
    - "sentimentLabel() fonksiyonu tum olasi label degerlerini Turkce goruntuluyor"
    - "Intelligence sayfasinda sentiment label'lari varsa Turkce goruntuleniyor"
  artifacts:
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "sentimentLabel() Turkce label coverage; pozitif/negatif/notr degerlerini de karsilar"
    - path: "frontend/src/app/stocks/[symbol]/page.module.css"
      provides: "data-sentiment CSS renk kurallari pozitif/negatif/notr degerlerini de kapsiyor"
  key_links:
    - from: "NewsItem.sentiment_label (DB)"
      to: "sentimentLabel() fonksiyonu"
      via: "StockNewsItem.sentiment_label prop"
      pattern: "sentiment_label.*positive|negative|neutral"
    - from: "data-sentiment attribute"
      to: "CSS .newsRowMeta span[data-sentiment='...']"
      via: "JSX span element"
      pattern: "data-sentiment.*positive|negative|neutral|pozitif|negatif"
---

<objective>
Frontend'in sentiment label goruntulemesini OpenAI batch sentimentinin cikti labellarindan (en:
positive/negative/neutral) haberdar et; CSS renk kurallarini Turkce Patikayi da kapsar sekilde
guncelle; intelligence sayfasinda sentiment kullaniminini kontrol et.

Purpose: Plan 53-01 KAP duyurulari icin OpenAI'dan Turkce etiket ("pozitif") alip Ingilizce'ye
ceviriyor ("positive") ve DB'ye oyle yaziyor. Ancak bir edge case var: DB'de hali hazirda Turkce
label kayitliysa veya ileride label mapping degisirse UI bozulur. Bu plan her iki formati da
renklendirerek savunmacilik saglar. Ayrica intelligence sayfasinda sentiment kullanimi olup
olmadigi kontrol edilir.

Output:
- sentimentLabel(): pozitif/negatif/notr degerlerini de kapsar
- CSS: data-sentiment="pozitif" / "negatif" icin renk kurallari eklenir
- Intelligence sayfasi sentiment label kontrolu (degisiklik gerekirse guncellenir)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/53-turkce-nlp-sentiment/53-CONTEXT.md
@.planning/phases/53-turkce-nlp-sentiment/53-01-SUMMARY.md

@frontend/src/app/stocks/[symbol]/page.tsx
@frontend/src/app/stocks/[symbol]/page.module.css
@frontend/src/app/intelligence/page.tsx
@frontend/src/lib/api.ts

<interfaces>
<!-- Mevcut sentimentLabel fonksiyonu — page.tsx satir 276-281 -->
```typescript
function sentimentLabel(item: StockNewsItem) {
  if (item.sentiment_label === 'positive') return 'Olumlu';
  if (item.sentiment_label === 'negative') return 'Olumsuz';
  if (item.sentiment_label === 'neutral') return 'Nötr';
  return item.sentiment_score != null ? formatSignedPct(item.sentiment_score * 100) : 'Etki yok';
}
```

<!-- Mevcut kullanim — page.tsx satir 106 -->
```tsx
<span data-sentiment={item.sentiment_label || 'neutral'}>{sentimentLabel(item)}</span>
```

<!-- Mevcut CSS — page.module.css satir 798-803 -->
```css
.newsRowMeta span[data-sentiment='positive'] { color: var(--color-pos); }
.newsRowMeta span[data-sentiment='negative'] { color: var(--color-neg); }
/* 'neutral' icin kural yok — varsayilan renk */
```

<!-- StockNewsItem interface — api.ts satir 594-598 -->
```typescript
export interface StockNewsItem {
  sentiment_label: string | null;
  sentiment_score: number | null;
  // ...diger alanlar
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sentimentLabel() genisletme ve CSS renk kurallari</name>
  <files>
    frontend/src/app/stocks/[symbol]/page.tsx,
    frontend/src/app/stocks/[symbol]/page.module.css
  </files>
  <action>
**page.tsx — sentimentLabel() fonksiyonunu guncelle:**

Mevcut fonksiyonu asagidaki ile degistir (iki satir ekle — Turkce label eslesmesi icin):

```typescript
function sentimentLabel(item: StockNewsItem) {
  if (item.sentiment_label === 'positive' || item.sentiment_label === 'pozitif') return 'Olumlu';
  if (item.sentiment_label === 'negative' || item.sentiment_label === 'negatif') return 'Olumsuz';
  if (item.sentiment_label === 'neutral' || item.sentiment_label === 'nötr' || item.sentiment_label === 'notr') return 'Nötr';
  return item.sentiment_score != null ? formatSignedPct(item.sentiment_score * 100) : 'Etki yok';
}
```

**page.module.css — data-sentiment CSS kurallarini genislet:**

Mevcut CSS kurallarinin hemen altina Turkce deger kurallari ekle:

```css
/* Türkçe label uyumu — Phase 53 NLP */
.newsRowMeta span[data-sentiment='pozitif'] {
  color: var(--color-pos);
}
.newsRowMeta span[data-sentiment='negatif'] {
  color: var(--color-neg);
}
.newsRowMeta span[data-sentiment='nötr'],
.newsRowMeta span[data-sentiment='notr'] {
  color: var(--color-muted, inherit);
}
```

NOT: --color-pos ve --color-neg degiskenleri zaten globals.css'te tanimli. --color-muted
tanimli degilse sadece 'inherit' kullan.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -20 && echo "TypeScript OK"</automated>
  </verify>
  <done>
    - sentimentLabel() "pozitif"/"negatif"/"notr"/"nötr" degerlerini dogru Turkce label'a eslestiriyor
    - CSS data-sentiment="pozitif" icin yesil, "negatif" icin kirmizi renk uyguluyor
    - TypeScript derleme hatasi yok
  </done>
</task>

<task type="auto">
  <name>Task 2: Intelligence sayfasi sentiment label kontrolu ve eger gerekirse guncelleme</name>
  <files>frontend/src/app/intelligence/page.tsx</files>
  <action>
intelligence/page.tsx dosyasini incele:

1. "sentiment_label" veya "sentiment" aramasini yap.
2. Eger sayfada sentiment_label'i dogrudan "positive"/"negative" string olarak karsilastiran
   bir kod varsa (orn. `item.sentiment_label === 'positive'`), ayni sentimentLabel() genisletme
   mantigini uygula: Turkce degerler icin fallback ekle.
3. Eger sayfada sentiment_label icin renk sinifi uygulayan bir `data-sentiment` attribute'u
   veya class degisimi varsa, Turkce label degerlerini de kapsayacak sekilde guncelle.
4. Eger intelligence/page.tsx'de sentiment_label kullanimi YOKSA hicbir degisiklik yapma —
   sadece kontrol ettim notunu SUMMARY'ye yaz.

Intelligence sayfasi KAP feed'ini goruntuluyorsa (`kap_category`, `sentiment_label` alanlarini
okuyorsa) ve label rendering iceriyorsa, ayni Turkce uyum genisletmesini uygula.

Eger degisiklik yapildiysa: `npx tsc --noEmit` ile TypeScript hata kontrolu yap.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -20 && echo "TypeScript OK"</automated>
  </verify>
  <done>
    - intelligence/page.tsx sentiment_label kullanimindan haberdar
    - Turkce label eslestirmesi uygulandiysa TypeScript hatasi yok
    - Kullanim yoksa SUMMARY'de "kontrol edildi, degisiklik gerekmedi" notu var
  </done>
</task>

</tasks>

<verification>
Tum task'lar tamamlandiktan sonra:

```bash
# 1. TypeScript derleme
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend
npx tsc --noEmit
# Beklenen: hata yok

# 2. sentimentLabel coverage kontrolu (manuel kod okuma)
grep -n "pozitif\|negatif\|notr\|nötr" frontend/src/app/stocks/\[symbol\]/page.tsx
# Beklenen: sentimentLabel() icinde en az 3 esleme

# 3. CSS coverage
grep -n "pozitif\|negatif" frontend/src/app/stocks/\[symbol\]/page.module.css
# Beklened: data-sentiment="pozitif" ve "negatif" kurallari

# 4. Intelligence sayfasi sentiment kontrolu
grep -n "sentiment_label\|sentiment" frontend/src/app/intelligence/page.tsx | head -10
# Beklenen: varsa Turkce coverage eklendi, yoksa bos
```
</verification>

<success_criteria>
- TypeScript `npx tsc --noEmit` hata vermiyor
- stocks/[symbol] sayfasinda sentiment badge'lari "pozitif"/"negatif"/"notr" DB degerlerini de dogru renkle gosteriyor
- Intelligence sayfasi sentiment coverage kontrolu yapildi; varsa Turkce uyum eklendi
- Hicbir mevcut test kirilmadi
</success_criteria>

<output>
Tamamlaninca olustur: `.planning/phases/53-turkce-nlp-sentiment/53-02-SUMMARY.md`

Icermeli:
- sentimentLabel() degisikligi (before/after)
- CSS kural eklemeleri
- Intelligence sayfasi kontrol sonucu (degisiklik yapildi mi, yapilmadiysa neden)
- TypeScript derleme dogrulama sonucu
</output>
