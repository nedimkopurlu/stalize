---
phase: 43-karar-dili-guvenligi-skor-aciklanabilirligi
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - frontend/src/app/stocks/[symbol]/page.tsx
  - frontend/src/app/stocks/[symbol]/page.module.css
  - frontend/src/app/stocks/page.tsx
  - frontend/src/app/stocks/page.module.css
autonomous: true
requirements:
  - KARAR-02
  - KARAR-03
  - SKOR-01
  - SKOR-02
  - SKOR-03

must_haves:
  truths:
    - "Hisse detay sayfasinda metrik kartlarin altinda 'Skor Dökümü' bölümü görünüyor"
    - "Skor Dökümü bölümünde her bileşen (Temel/Teknik/Haber/vb.) için progress bar + yüzde katkı gösteriliyor"
    - "Eksik bileşen varsa sarı uyarı ikonu ve 'Eksik veri — ağırlık yeniden dağıtıldı' metni çıkıyor"
    - "Hisse listesinde ve hisse detayinda veri bütünlüğü göstergesi ('3/3 bileşen mevcut' veya '2/3') görünüyor"
    - "Yüksek volatilite durumunda (20 günlük değişim >%15) sarı uyarı ikonu + hover tooltip çıkıyor"
  artifacts:
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "ScoreBreakdownSection bölümü + volatilite uyarısı + bileşen sayacı"
      contains: "ScoreBreakdownSection\|bileşen mevcut\|volatilite"
    - path: "frontend/src/app/stocks/[symbol]/page.module.css"
      provides: "Skor dökümü CSS siniflari"
      contains: "breakdownSection\|breakdownBar"
    - path: "frontend/src/app/stocks/page.tsx"
      provides: "Hisse listesi skor sütununda bileşen sayacı"
      contains: "bileşen mevcut"
    - path: "frontend/src/app/stocks/page.module.css"
      provides: "CSS siniflari bileşen sayacı için"
      contains: "componentBadge\|integrityHint"
  key_links:
    - from: "GET /stocks/{symbol}/score-breakdown"
      to: "scoreBreakdown state (zaten loadStock icinde fetch ediliyor)"
      via: "api.getStockScoreBreakdown(symbol) — satir 317"
      pattern: "scoreBreakdown"
    - from: "scoreBreakdown.breakdown.components"
      to: "ScoreBreakdownSection bölümü"
      via: "bd?.components map"
      pattern: "bd\?.components"
    - from: "scoreBreakdown.breakdown.summary.available_component_count"
      to: "'N/3 bileşen mevcut' metin göstergesi"
      via: "bd?.summary.available_component_count"
      pattern: "available_component_count"
    - from: "allPrices (20 günlük fiyat)"
      to: "volatilite uyarı ikonu"
      via: "20g degisim frontend hesaplamasi — son 20 kapanis fiyati"
      pattern: "volatilityAlert"
---

<objective>
Hisse detay sayfasına mevcut `/stocks/{symbol}/score-breakdown` endpoint'inden gelen bileşen dökümü progress bar formatında eklenir. Eksik bileşen uyarısı, bileşen sayacı (veri bütünlüğü göstergesi) ve yüksek volatilite uyarısı eklenir.

Purpose: Kullanıcı "bu skor nasıl hesaplandı?" sorusunu görerek anlayabilir (SKOR-01, SKOR-02, SKOR-03). Eksik veri ve yüksek volatilite şeffaf şekilde gösterilir (KARAR-02, KARAR-03).
Output: Hisse detayda "Skor Dökümü" bölümü + hisse listesinde ve detayda bileşen sayacı + volatilite uyarısı.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-karar-dili-guvenligi-skor-aciklanabilirligi/43-CONTEXT.md
@.planning/phases/43-karar-dili-guvenligi-skor-aciklanabilirligi/43-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/lib/api.ts (mevcut, degistirme):
```typescript
export interface ScoreBreakdownComponent {
  key: string;
  label: string;
  raw_score: number | null;
  base_weight: number;
  normalized_weight: number;
  contribution: number;
  reason: string;
}

export interface ScoreBreakdownResponse {
  symbol: string;
  name: string;
  breakdown: {
    overall_score: number;
    recommendation: string;
    components: ScoreBreakdownComponent[];
    missing_components: string[];  // NOT: endpoint string[] döndürüyor (key listesi)
    summary: {
      available_component_count: number;
      missing_component_count: number;
      normalization_applied: boolean;
      weight_coverage: number;
    };
  };
  timestamp: string;
}
```

Mevcut state (stocks/[symbol]/page.tsx ~line 270, 402):
```typescript
const [scoreBreakdown, setScoreBreakdown] = useState<ScoreBreakdownResponse | null>(null);
// ...
const bd = scoreBreakdown?.breakdown ?? null;
// bd?.components — ScoreBreakdownComponent[]
// bd?.summary.available_component_count — number
// bd?.summary.missing_component_count — number
// bd?.missing_components — string[] (key listesi, ör. ['fundamental_score'])
```

Fiyat verisi (mevcut):
```typescript
const allPrices = prices?.prices ?? detail.recent_prices ?? [];
// allPrices: PricePoint[] — { date, open, high, low, close, volume, ... }
// Son 20 günün kapanış: allPrices.slice(-20).map(p => p.close)
```

Mevcut ScoreBar componenti (satir ~113-138):
```typescript
function ScoreBar({ label, value }: { label: string; value: number | null }) { ... }
// Kullanır: styles.scoreBar, styles.scoreBarHeader, styles.scoreBarLabel,
//           styles.scoreBarValue, styles.scoreBarTrack, styles.scoreBarFill
```

CSS tokens (page.module.css :root):
```
--bg-primary: #0d0e10
--bg-surface: #16181c
--bg-elevated: #1c1f24
--border-primary: rgba(255, 255, 255, 0.06)
--text-primary: #f5f5f7
--text-muted: #8b8d93
--accent: #f59e0b          (amber — "nötr" tonu)
--accent-green: #10b981
--accent-red: #ef4444
```

Bileşen label mapping (backend'den geliyor, ama Türkçe label garantilenmiyor):
```
key: 'fundamental_score' → label: 'Temel'
key: 'technical_score'   → label: 'Teknik'
key: 'sentiment_score'   → label: 'Haber'
key: 'company_event_score' → label: 'Sirket Olayi' (backend ASCII)
key: 'macro_regime_score'  → label: 'Makro Rejim'
key: 'risk_overlay_score'  → label: 'Risk Katmani'
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Skor Dökümü bölümü — hisse detay sayfasina progress bar breakdown + eksik bileşen uyarisi ekle</name>
  <files>
    frontend/src/app/stocks/[symbol]/page.tsx
    frontend/src/app/stocks/[symbol]/page.module.css
  </files>
  <read_first>
    - frontend/src/app/stocks/[symbol]/page.tsx (tam dosya — özellikle satir 111-138 ScoreBar, 400-420 bd hesaplamalari, 652-680 scoreBars bölümü, 660-850 bölüm düzeni)
    - frontend/src/app/stocks/[symbol]/page.module.css (scoreBar, scoreCard CSS siniflari)
  </read_first>
  <action>
**page.tsx degisiklikleri:**

1. Volatilite uyarı hesaplamasini ekle. `const periodDelta = getPeriodDelta(allPrices);` satirinin ALTINA:

```typescript
// Volatilite uyarısı: son 20 günlük fiyat değişimi >%15 (KARAR-03)
const volatilityAlert = (() => {
  const last20 = allPrices.slice(-20);
  if (last20.length < 2) return false;
  const first = last20[0].close;
  const last = last20[last20.length - 1].close;
  if (!first || !last) return false;
  return Math.abs((last - first) / first) * 100 > 15;
})();
```

2. Bileşen sayaci hesapla. Ayni blokun ALTINA:

```typescript
// Veri bütünlüğü: kaç bileşen mevcut (KARAR-02)
const totalComponentCount = bd?.summary ? (bd.summary.available_component_count + bd.summary.missing_component_count) : null;
const availableComponentCount = bd?.summary?.available_component_count ?? null;
```

3. Hisse detay sayfasindaki `scoreBars` div'inin (satir ~652-658) ALTINA, `scoreCard` kapanisinin ONCESINE bileşen sayaci göstergesini ekle:

```tsx
{availableComponentCount !== null && totalComponentCount !== null && (
  <div className={styles.componentIntegrity}>
    <span
      title={availableComponentCount < totalComponentCount
        ? 'Bazı bileşenler eksik; ağırlıklar mevcut veriye göre yeniden dağıtıldı.'
        : 'Tüm bileşenler eksiksiz mevcut.'}
    >
      {availableComponentCount < totalComponentCount ? '⚠ ' : ''}
      {availableComponentCount}/{totalComponentCount} bileşen mevcut
    </span>
    {volatilityAlert && (
      <span
        className={styles.volatilityWarning}
        title="Yüksek volatilite — sinyaller daha az güvenilir"
      >
        ⚠ Yüksek volatilite
      </span>
    )}
  </div>
)}
```

4. `scoreCard` kapanisindan (</div>) SONRA, bir sonraki bölümün ONCESINE "Skor Dökümü" bölümünü ekle. bd mevcut oldugunda render edilir:

```tsx
{bd && bd.components.length > 0 && (
  <section className={styles.breakdownSection}>
    <h2 className={styles.breakdownTitle}>Skor Dökümü</h2>
    <p className={styles.breakdownSubtitle}>
      Her bileşenin genel skora katkısı — toplam 100 üzerinden
    </p>

    {bd.missing_component_count > 0 && (
      <div className={styles.breakdownMissingAlert}>
        <span>⚠</span>
        <span>Eksik veri — ağırlık yeniden dağıtıldı</span>
      </div>
    )}

    <div className={styles.breakdownBars}>
      {bd.components.map((comp) => {
        const pct = Math.max(0, Math.min(100, comp.raw_score ?? 0));
        const katkiPct = Math.round(comp.normalized_weight * 100);
        const barColor =
          pct >= 65 ? 'var(--accent-green)'
          : pct >= 40 ? 'var(--accent)'
          : 'var(--accent-red)';

        const labelMap: Record<string, string> = {
          fundamental_score: 'Temel',
          technical_score: 'Teknik',
          sentiment_score: 'Haber',
          company_event_score: 'Şirket Olayı',
          macro_regime_score: 'Makro Rejim',
          risk_overlay_score: 'Risk Katmanı',
        };
        const displayLabel = labelMap[comp.key] ?? comp.label;

        return (
          <div key={comp.key} className={styles.breakdownBar} title={comp.reason}>
            <div className={styles.breakdownBarHeader}>
              <span className={styles.breakdownBarLabel}>{displayLabel}</span>
              <span className={styles.breakdownBarMeta}>
                <strong style={{ color: barColor }}>{comp.raw_score != null ? comp.raw_score.toFixed(0) : '—'}</strong>
                {' '}
                <span className={styles.breakdownBarKatki}>— %{katkiPct} katkı</span>
              </span>
            </div>
            <div className={styles.breakdownBarTrack}>
              <div
                className={styles.breakdownBarFill}
                style={{ width: `${pct}%`, background: barColor }}
              />
            </div>
          </div>
        );
      })}
    </div>

    <p className={styles.breakdownNote}>
      Skor, mevcut bileşenlerin normalize edilmiş ağırlıklı ortalamasıdır. Eksik bileşen varsa ağırlıklar yeniden dağıtılır.
    </p>
  </section>
)}
```

NOT: `bd.missing_component_count` dogrudan property degil, `bd.summary.missing_component_count` kullanilmali. JSX icinde `bd.summary.missing_component_count` ile duzeltilmeli.

**page.module.css degisiklikleri:**

Dosyanin SONUNA su CSS bloglari eklenir:

```css
/* ── Component integrity badge ───────────────────────────── */
.componentIntegrity {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 8px 0 4px;
  font-size: 0.72rem;
  color: var(--text-muted);
  flex-wrap: wrap;
}

.volatilityWarning {
  color: var(--accent);
  font-size: 0.72rem;
  cursor: default;
}

/* ── Score breakdown section ─────────────────────────────── */
.breakdownSection {
  max-width: 800px;
  margin: 0 auto;
  padding: 48px 40px 0;
}

.breakdownTitle {
  font-size: 1.1rem;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0 0 4px;
}

.breakdownSubtitle {
  font-size: 0.8rem;
  color: var(--text-muted);
  margin: 0 0 20px;
}

.breakdownMissingAlert {
  display: flex;
  align-items: center;
  gap: 8px;
  background: rgba(245, 158, 11, 0.10);
  border: 1px solid rgba(245, 158, 11, 0.25);
  border-radius: 6px;
  padding: 8px 12px;
  font-size: 0.78rem;
  color: var(--accent);
  margin-bottom: 16px;
}

.breakdownBars {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.breakdownBar {
  cursor: default;
}

.breakdownBarHeader {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin-bottom: 5px;
}

.breakdownBarLabel {
  font-size: 0.8rem;
  color: var(--text-muted);
}

.breakdownBarMeta {
  font-size: 0.82rem;
  color: var(--text-primary);
}

.breakdownBarKatki {
  font-size: 0.74rem;
  color: var(--text-muted);
}

.breakdownBarTrack {
  height: 6px;
  background: var(--bg-elevated);
  border-radius: 3px;
  overflow: hidden;
}

.breakdownBarFill {
  height: 100%;
  border-radius: 3px;
  transition: width 0.4s ease;
}

.breakdownNote {
  font-size: 0.72rem;
  color: var(--text-dim);
  margin-top: 16px;
  line-height: 1.5;
}

@media (max-width: 768px) {
  .breakdownSection {
    padding: 32px 20px 0;
  }
}
```
  </action>
  <verify>
    <automated>grep -n "breakdownSection\|ScoreBreakdownSection\|bileşen mevcut\|volatilityAlert\|breakdownBars" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.tsx && grep -n "breakdownSection\|breakdownBar\|componentIntegrity" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.module.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "breakdownSection" frontend/src/app/stocks/\[symbol\]/page.tsx` >= 1 (bölüm JSX'te kullaniliyor)
    - `grep "breakdownSection" frontend/src/app/stocks/\[symbol\]/page.module.css` CSS sinifi tanimlanmis
    - `grep "volatilityAlert" frontend/src/app/stocks/\[symbol\]/page.tsx` volatilite hesaplamasini bulur
    - `grep "bileşen mevcut" frontend/src/app/stocks/\[symbol\]/page.tsx` sayac metnini bulur
    - `grep "Eksik veri" frontend/src/app/stocks/\[symbol\]/page.tsx` eksik bileşen uyarisini bulur
    - `grep "normalized_weight" frontend/src/app/stocks/\[symbol\]/page.tsx` katkı hesaplamasini bulur
    - `cd frontend && npm run build 2>&1 | grep -i "error"` — TypeScript/build hatasi yok
  </acceptance_criteria>
  <done>Hisse detay sayfasinda "Skor Dökümü" bölümü görünür, her bileşen için progress bar + katkı yüzdesi gösterilir. Eksik bileşen varsa sarı uyarı, yüksek volatilite varsa uyarı ikonu çıkar.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hisse listesinde veri bütünlüğü göstergesi (bileşen sayacı + volatilite uyarisi)</name>
  <files>
    frontend/src/app/stocks/page.tsx
    frontend/src/app/stocks/page.module.css
  </files>
  <read_first>
    - frontend/src/app/stocks/page.tsx (tam dosya — özellikle StockSummary interface kullanimi, satir 216-265 row render)
    - frontend/src/app/stocks/page.module.css (mevcut td siniflari)
    - frontend/src/lib/api.ts (StockSummary interface — fundamental_score, technical_score, sentiment_score alanlari)
  </read_first>
  <action>
**Önemli sinir:** Hisse listesi endpoint'i `/api/stocks` dönüşünde score-breakdown verisi yoktur — sadece `fundamental_score`, `technical_score`, `sentiment_score` alanları mevcuttur (StockSummary interface). Bu yüzden bileşen sayacı bu üç alan üzerinden hesaplanir.

**stocks/page.tsx degisiklikleri:**

1. Dosyanin helper bölümüne (SortIcon öncesi) su fonksiyon eklenir:

```typescript
// Veri bütünlüğü: 3 temel bileşeni say (KARAR-02)
function componentCount(stock: StockSummary): { available: number; total: number } {
  const total = 3;
  const available = [stock.fundamental_score, stock.technical_score, stock.sentiment_score]
    .filter((v) => v !== null && v !== undefined).length;
  return { available, total };
}

// 20 günlük volatilite proxy: StockSummary'de fiyat geçmişi yok
// Volatilite için daily_change_pct kullanilir: abs(daily_change_pct) > 4 eşiği
// (günlük %4+ hareket yüksek volatilite sinyali)
function isHighDailyVolatility(stock: StockSummary): boolean {
  return stock.daily_change_pct !== null && Math.abs(stock.daily_change_pct) > 4;
}
```

NOT: 20 günlük fiyat geçmisi hisse listesinde mevcut degil. CONTEXT.md "Deferred Ideas" bölümünde "Volatilite için ayrı endpoint — şimdilik frontend'de hesaplanacak" deniyor. Burada `daily_change_pct > 4%` (günlük) proxy olarak kullanilir. Bu, context kararına uygundur.

2. Row render (satir ~218-248) icinde fundamental_score hücresini (satir ~237-239) degistir. Mevcut:
```tsx
<td className={`${styles.td} ${styles.tdRight} ${styles.mono} ${styles.muted} ${styles.hideMobile}`}>
  {stock.fundamental_score !== null ? stock.fundamental_score.toFixed(0) : '—'}
</td>
```

Yeni (fundamental_score + bileşen sayacı + volatilite uyarısı):
```tsx
<td className={`${styles.td} ${styles.tdRight} ${styles.hideMobile}`}>
  <div className={styles.integrityCell}>
    <span className={`${styles.mono} ${styles.muted}`}>
      {stock.fundamental_score !== null ? stock.fundamental_score.toFixed(0) : '—'}
    </span>
    <span
      className={styles.componentBadge}
      title={componentCount(stock).available < componentCount(stock).total
        ? 'Bazı bileşenler eksik; genel skor kısmi veriye dayanıyor.'
        : 'Tüm bileşenler mevcut.'}
      data-incomplete={componentCount(stock).available < componentCount(stock).total || undefined}
    >
      {componentCount(stock).available}/{componentCount(stock).total}
    </span>
    {isHighDailyVolatility(stock) && (
      <span
        className={styles.volWarn}
        title="Yüksek volatilite — sinyaller daha az güvenilir"
      >
        ⚠
      </span>
    )}
  </div>
</td>
```

**stocks/page.module.css degisiklikleri:**

Dosyanin SONUNA eklenir:

```css
/* ── Integrity cell (hisse listesi) ──────────────────────── */
.integrityCell {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 6px;
}

.componentBadge {
  font-size: 0.67rem;
  color: var(--text-dim);
  background: var(--bg-elevated);
  border-radius: 3px;
  padding: 1px 5px;
  white-space: nowrap;
  cursor: default;
  border: 1px solid var(--border-primary);
}

.componentBadge[data-incomplete] {
  color: var(--accent);
  border-color: rgba(245, 158, 11, 0.3);
}

.volWarn {
  font-size: 0.72rem;
  color: var(--accent);
  cursor: default;
}
```
  </action>
  <verify>
    <automated>grep -n "componentCount\|componentBadge\|integrityCell\|isHighDailyVolatility" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.tsx && grep -n "componentBadge\|integrityCell\|volWarn" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.module.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "componentCount" frontend/src/app/stocks/page.tsx` helper fonksiyonu ve kullanimi bulur
    - `grep "componentBadge" frontend/src/app/stocks/page.tsx` row'da badge kullanimi bulur
    - `grep "componentBadge" frontend/src/app/stocks/page.module.css` CSS sinifi tanimlanmis
    - `grep "isHighDailyVolatility" frontend/src/app/stocks/page.tsx` volatilite proxy fonksiyonu bulur
    - `grep "volWarn" frontend/src/app/stocks/page.module.css` uyari CSS sinifi tanimlanmis
    - `cd frontend && npm run build 2>&1 | grep -i "error"` — TypeScript/build hatasi yok
  </acceptance_criteria>
  <done>Hisse listesinde F/K hücresinin yaninda "3/3" veya "2/3" bileşen sayacı görünür. Eksik bileşende amber renk + tooltip. Yüksek günlük hareket durumunda ⚠ ikonu görünür.</done>
</task>

</tasks>

<verification>
Skor dökümü bölümü ve veri bütünlüğü göstergesi için tam kontrol:

```bash
# Skor dökümü bölümü
grep -c "breakdownSection\|breakdownBar\|breakdownBars" \
  /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.tsx

# Bileşen sayacı — hisse detay
grep -n "bileşen mevcut\|available_component_count" \
  /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.tsx

# Bileşen sayacı — hisse listesi
grep -n "componentCount\|componentBadge" \
  /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.tsx

# Volatilite uyarısı her iki yerde
grep -n "volatilityAlert\|isHighDailyVolatility\|volWarn" \
  /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.tsx \
  /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.tsx

# TypeScript build
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npm run build 2>&1 | tail -15
```
</verification>

<success_criteria>
- Hisse detay sayfasinda "Skor Dökümü" bölümü mevcut, bd.components.length > 0 iken görünür
- Her bileşen için: isim, skor (0-100), normalize edilmiş katkı yüzdesi, renk kodlu progress bar
- Eksik bileşen varsa amber uyarı kutusu: "Eksik veri — ağırlık yeniden dağıtıldı"
- Hisse detayda ve listede bileşen sayacı ("3/3" veya "2/3") mevcut
- Yüksek volatilite durumunda ⚠ ikonu + tooltip hem listede hem detayda mevcut
- Frontend TypeScript build hatasi yok
- Backend degisikliği yok
</success_criteria>

<output>
Tamamlaninca `.planning/phases/43-karar-dili-guvenligi-skor-aciklanabilirligi/43-02-SUMMARY.md` olustur.
</output>
