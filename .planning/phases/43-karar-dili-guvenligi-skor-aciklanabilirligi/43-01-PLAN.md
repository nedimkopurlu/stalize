---
phase: 43-karar-dili-guvenligi-skor-aciklanabilirligi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/stocks/page.tsx
  - frontend/src/app/stocks/[symbol]/page.tsx
  - frontend/src/app/model-portfolio/page.tsx
  - frontend/src/app/page.tsx
autonomous: true
requirements:
  - KARAR-01
  - KARAR-04

must_haves:
  truths:
    - "Hisse listesinde (/stocks) recommendation kolonu 'GÜÇLÜ AL' yerine 'Yüksek Öncelikli İzleme' gibi güvenli etiketi gösteriyor"
    - "Hisse detay hero kartında ('AI Karar Kartı') primaryDecision güvenli etiketle gösteriliyor"
    - "Model portföy tablosunda HoldingRow içindeki recommendation güvenli etiketle gösteriliyor"
    - "Dashboard 'Bugünün Adayları' ideaCard'larında stock.recommendation güvenli etiketle gösteriliyor"
    - "Her güvenli etiketin yanında title tooltip metni mevcut"
  artifacts:
    - path: "frontend/src/app/stocks/page.tsx"
      provides: "safeLabel() çağrısı recommendation hücresinde"
      contains: "safeLabel"
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "safeLabel helper fonksiyonu + tooltip uygulaması"
      contains: "safeLabel"
    - path: "frontend/src/app/model-portfolio/page.tsx"
      provides: "HoldingRow içinde safeLabel kullanımı"
      contains: "safeLabel"
    - path: "frontend/src/app/page.tsx"
      provides: "ideaCard recommendation safeLabel ile gösteriliyor"
      contains: "safeLabel"
  key_links:
    - from: "recommendation string (backend)"
      to: "safeLabel() helper"
      via: "display-layer mapping — DB değiştirilmez"
      pattern: "safeLabel"
    - from: "safeLabel helper"
      to: "title attribute tooltip"
      via: "title={SAFE_LABEL_TOOLTIP[rec]}"
      pattern: "SAFE_LABEL_TOOLTIP"
---

<objective>
Direktif öneri etiketleri ("GÜÇLÜ AL", "AL", "TUT", "SAT", "GÜÇLÜ SAT") tüm frontend gösterim noktalarında güvenli, direktif olmayan dile çevrilir.

Purpose: Kullanıcıya yatırım kararını dikte etmek yerine izleme çerçevesi sunar (KARAR-01). Her etiketin yanına hover tooltip eklenerek etiket anlamı açıklanır (KARAR-04).
Output: `safeLabel()` helper + `SAFE_LABEL_TOOLTIP` sabit — 4 frontend sayfasında kullanılır. DB değişikliği yok.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-karar-dili-guvenligi-skor-aciklanabilirligi/43-CONTEXT.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/lib/api.ts:
```typescript
export interface StockSummary {
  recommendation: string | null;
  // ...other fields...
}
export interface ModelPortfolioHolding {
  recommendation: string | null;
  // ...other fields...
}
```

Mevcut helper fonksiyonlar (frontend/src/app/stocks/[symbol]/page.tsx ~line 142-206):
```typescript
function recColor(rec: string | null): string {
  if (!rec) return 'var(--text-muted)';
  if (rec.includes('GÜÇLÜ AL') || rec === 'AL') return 'var(--accent-green)';
  if (rec.includes('GÜÇLÜ SAT') || rec === 'SAT') return 'var(--accent-red)';
  return 'var(--accent)';
}

function decisionPrimaryLabel(decision: InvestmentDecision | null, recommendation: string | null) {
  if (!decision) {
    if (!recommendation) return 'İZLE';
    if (recommendation.includes('AL')) return 'AL';
    if (recommendation.includes('SAT')) return 'UZAK DUR';
    return 'İZLE';
  }
  if (decision.action === 'strong_buy' || decision.action === 'buy') return 'AL';
  if (decision.action === 'reduce' || decision.action === 'exit') return 'UZAK DUR';
  return 'İZLE';
}
```

Mevcut gösterim noktaları:
- frontend/src/app/page.tsx ~line 211: `{stock.recommendation ?? '-'}`
- frontend/src/app/stocks/page.tsx ~line 247: recommendation kolonu yok (sadece sembol/fiyat/hacim gösteriliyor — eklenecek)
- frontend/src/app/stocks/[symbol]/page.tsx ~line 567-574, 642-644, 715-716: primaryDecision kullanımı
- frontend/src/app/model-portfolio/page.tsx ~line 53: `{holding.recommendation ?? '-'}`
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: safeLabel helper + tooltip sabitlerini tanimla ve stocks list ile model-portfolio'ya uygula</name>
  <files>
    frontend/src/app/stocks/page.tsx
    frontend/src/app/model-portfolio/page.tsx
  </files>
  <read_first>
    - frontend/src/app/stocks/page.tsx (tam dosya — ozellikle satir 1-50 imports, 216-250 table rows)
    - frontend/src/app/model-portfolio/page.tsx (tam dosya — ozellikle satir 42-62 HoldingRow)
  </read_first>
  <action>
**stocks/page.tsx icinde:**

1. Dosyanin en ustune (// ── Helpers bölümü altinda, SortIcon öncesi) su sabitler ve fonksiyon eklenir:

```typescript
// ── Safe label mapping (KARAR-01) ─────────────────────────
const SAFE_LABEL_MAP: Record<string, string> = {
  'GÜÇLÜ AL': 'Yüksek Öncelikli İzleme',
  'AL': 'Pozitif Görünüm',
  'TUT': 'Nötr İzleme',
  'SAT': 'Zayıflayan Görünüm',
  'GÜÇLÜ SAT': 'Riskli Görünüm',
};

const SAFE_LABEL_TOOLTIP: Record<string, string> = {
  'GÜÇLÜ AL': 'Teknik ve temel göstergeler güçlü; yakından takip edilebilir.',
  'AL': 'Göstergeler genel olarak olumlu; dikkatli değerlendirilebilir.',
  'TUT': 'Karma sinyaller; net yön için bekleme önerilir.',
  'SAT': 'Göstergeler baskı altında; dikkatli olunmalı.',
  'GÜÇLÜ SAT': 'Yüksek risk sinyalleri mevcut; değerlendirme önerilmez.',
};

function safeLabel(rec: string | null): string {
  if (!rec) return '—';
  return SAFE_LABEL_MAP[rec] ?? rec;
}

function safeLabelTooltip(rec: string | null): string {
  if (!rec) return '';
  return SAFE_LABEL_TOOLTIP[rec] ?? '';
}
```

2. Tablo basligina "Görünüm" kolonu eklenir — mevcut `<th ... style={{ textAlign: 'center' }}>☆</th>` ONCESINE:
```tsx
<th className={styles.th} style={{ textAlign: 'center' }}>Görünüm</th>
```

3. Her row'da yıldız hücresi (`<td className={styles.tdStar}>☆</td>`) ÖNCESINE yeni hücre eklenir:
```tsx
<td
  className={styles.td}
  style={{ textAlign: 'center' }}
  title={safeLabelTooltip(stock.recommendation)}
>
  <span style={{ fontSize: '0.72rem', color: recSafeColor(stock.recommendation) }}>
    {safeLabel(stock.recommendation)}
  </span>
</td>
```

4. `recSafeColor` yardımcı fonksiyon eklenir (recColor eşdeğeri, ama stocks/page.tsx bu helper'a sahip değil):
```typescript
function recSafeColor(rec: string | null): string {
  if (!rec) return 'var(--text-muted)';
  if (rec === 'GÜÇLÜ AL' || rec === 'AL') return 'var(--accent-green)';
  if (rec === 'GÜÇLÜ SAT' || rec === 'SAT') return 'var(--accent-red)';
  return 'var(--accent)';
}
```

**model-portfolio/page.tsx icinde:**

5. Dosyanin `HoldingRow` fonksiyonunun ÜSTÜNE ayni SAFE_LABEL_MAP, SAFE_LABEL_TOOLTIP, `safeLabel`, `safeLabelTooltip` sabit ve fonksiyonlari kopyalanir.

6. `HoldingRow` icindeki satir 53: `<span>{holding.recommendation ?? '-'}</span>` su hale getirilir:
```tsx
<span
  title={safeLabelTooltip(holding.recommendation)}
  style={{ fontSize: '0.78rem', color: safeColor(holding.recommendation) }}
>
  {safeLabel(holding.recommendation)}
</span>
```

7. `safeColor` yardimci fonksiyon eklenir (model-portfolio icin):
```typescript
function safeColor(rec: string | null): string {
  if (!rec) return 'var(--text-muted)';
  if (rec === 'GÜÇLÜ AL' || rec === 'AL') return 'var(--accent-green)';
  if (rec === 'GÜÇLÜ SAT' || rec === 'SAT') return 'var(--accent-red)';
  return 'var(--accent)';
}
```

NOT: SAFE_LABEL_MAP ve SAFE_LABEL_TOOLTIP her iki dosyada kopyalanır (ortak lib dosyası yaratılmaz — mevcut proje paterni dosya bazlı helper kullanımını tercih eder).
  </action>
  <verify>
    <automated>grep -n "safeLabel\|SAFE_LABEL_MAP\|Yüksek Öncelikli İzleme" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.tsx && grep -n "safeLabel\|SAFE_LABEL_MAP\|Yüksek Öncelikli İzleme" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/model-portfolio/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "safeLabel" frontend/src/app/stocks/page.tsx` çıktısı >= 2
    - `grep -c "safeLabel" frontend/src/app/model-portfolio/page.tsx` çıktısı >= 2
    - `grep "Görünüm" frontend/src/app/stocks/page.tsx` table header satırını bulur
    - `grep "safeLabelTooltip\|title={" frontend/src/app/model-portfolio/page.tsx` tooltip uygulamasını bulur
    - `cd frontend && npm run build` hatasiz tamamlanir (TypeScript hata yok)
  </acceptance_criteria>
  <done>Hisse listesi tablosunda "Görünüm" kolonu gözükür ve güvenli etiketleri gösterir. Model portföy satırlarında recommendation güvenli etiket + tooltip ile gösterilir.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hisse detay hero ve dashboard ideaCard'larinda guvenli etiket uygula</name>
  <files>
    frontend/src/app/stocks/[symbol]/page.tsx
    frontend/src/app/page.tsx
  </files>
  <read_first>
    - frontend/src/app/stocks/[symbol]/page.tsx (satir 142-220 helpers, 560-660 hero JSX, 700-730 plan section)
    - frontend/src/app/page.tsx (satir 200-225 ideaGrid/ideaCard render)
  </read_first>
  <action>
**stocks/[symbol]/page.tsx icinde:**

1. `recColor` fonksiyonunun ALTINA (satir ~147'nin ardına) su sabitler ve fonksiyonlar eklenir:

```typescript
// ── Safe label mapping (KARAR-01) ─────────────────────────
const SAFE_LABEL_MAP: Record<string, string> = {
  'GÜÇLÜ AL': 'Yüksek Öncelikli İzleme',
  'AL': 'Pozitif Görünüm',
  'TUT': 'Nötr İzleme',
  'SAT': 'Zayıflayan Görünüm',
  'GÜÇLÜ SAT': 'Riskli Görünüm',
};

const SAFE_LABEL_TOOLTIP: Record<string, string> = {
  'GÜÇLÜ AL': 'Teknik ve temel göstergeler güçlü; yakından takip edilebilir.',
  'AL': 'Göstergeler genel olarak olumlu; dikkatli değerlendirilebilir.',
  'TUT': 'Karma sinyaller; net yön için bekleme önerilir.',
  'SAT': 'Göstergeler baskı altında; dikkatli olunmalı.',
  'GÜÇLÜ SAT': 'Yüksek risk sinyalleri mevcut; değerlendirme önerilmez.',
};

function safeLabel(rec: string | null): string {
  if (!rec) return '—';
  return SAFE_LABEL_MAP[rec] ?? rec;
}

function safeLabelTooltip(rec: string | null): string {
  if (!rec) return '';
  return SAFE_LABEL_TOOLTIP[rec] ?? '';
}
```

2. Hero desc paragrafinda (satir ~567-576) `primaryDecision` gösterimi DEGISTIRILMEZ (o zaten 'AL'/'UZAK DUR'/'İZLE' — kisa karar etiketleri, direktif dil sorununa girmez). Ancak "AI Karar Kartı" scoreCardSignal bölümünde (satir ~637-650) `{primaryDecision}` gösterimi:
```tsx
<div
  className={styles.signalLabel}
  style={{ color: recColor(recommendation) }}
>
  {primaryDecision}
</div>
```
su hale getirilir:
```tsx
<div
  className={styles.signalLabel}
  style={{ color: recColor(recommendation) }}
  title={safeLabelTooltip(recommendation)}
>
  {safeLabel(recommendation)}
</div>
```

3. Plan section'daki `actionPill` (satir ~715):
```tsx
<span className={`${styles.actionPill} ${decisionToneClass(primaryDecision)}`}>
  {primaryDecision}
</span>
```
DEGISTIRILMEZ — `actionPill` zaten kısa karar etiketi ('AL'/'UZAK DUR'/'İZLE') kullanıyor, bu OK.

NOT: Hero desc paragrafindaki `primaryDecision` span (`confidenceBadge`, satir ~570-572) de DEGISTIRILMEZ — bu "Güncel işlem kararı AL olarak okunuyor" metni, direktif olmayan bir bağlam cumlesidir.

**page.tsx icinde (dashboard):**

4. Dosyaya (// ── Helpers veya mevcut helper fonksiyonlarinin altina) safeLabel ve SAFE_LABEL_MAP eklenir.

5. ideaCard icinde (satir ~211): `<span>{stock.recommendation ?? '-'}</span>` su hale getirilir:
```tsx
<span title={safeLabelTooltip(stock.recommendation)}>
  {safeLabel(stock.recommendation)}
</span>
```

Dosyanin SAFE_LABEL_MAP, SAFE_LABEL_TOOLTIP, safeLabel, safeLabelTooltip tanimlarini içerdiginden emin olunur.
  </action>
  <verify>
    <automated>grep -n "safeLabel\|SAFE_LABEL_MAP\|Yüksek Öncelikli İzleme" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.tsx && grep -n "safeLabel\|SAFE_LABEL_MAP" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "safeLabel(recommendation)" frontend/src/app/stocks/\[symbol\]/page.tsx` signalLabel div'ini bulur
    - `grep "safeLabelTooltip" frontend/src/app/stocks/\[symbol\]/page.tsx` tooltip uygulamasini bulur
    - `grep "safeLabel(stock.recommendation)" frontend/src/app/page.tsx` ideaCard span'ini bulur
    - `grep "Riskli Görünüm" frontend/src/app/stocks/\[symbol\]/page.tsx` SAFE_LABEL_MAP'in tamamlandigini dogrular
    - `cd frontend && npm run build` hatasiz tamamlanir
  </acceptance_criteria>
  <done>Hisse detay AI Karar Kartinda ve dashboard ideaCard'larinda recommendation güvenli etiketle ve tooltip ile görünüyor.</done>
</task>

</tasks>

<verification>
Tüm 4 sayfa için güvenli etiket kontrolü:
```bash
grep -rn "safeLabel\|SAFE_LABEL_MAP" \
  frontend/src/app/stocks/page.tsx \
  frontend/src/app/stocks/\[symbol\]/page.tsx \
  frontend/src/app/model-portfolio/page.tsx \
  frontend/src/app/page.tsx
```

Frontend build hata yok:
```bash
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npm run build 2>&1 | tail -20
```

"GÜÇLÜ AL" gibi raw backend etiketler bu dosyalarda görünüm noktalarinda kullanilmiyor:
```bash
grep -n '"GÜÇLÜ AL"\|>GÜÇLÜ AL<\|>AL<\|>SAT<' \
  frontend/src/app/stocks/page.tsx \
  frontend/src/app/page.tsx \
  frontend/src/app/model-portfolio/page.tsx | grep -v "SAFE_LABEL_MAP\|safeLabel\|recColor\|recommendation\."
```
</verification>

<success_criteria>
- 4 sayfada recommendation gösteren tüm noktalar güvenli etiket kullanıyor
- Her güvenli etiketin title tooltip metni var
- Renk mantigi korunuyor (yeşil/sarı/kırmızı)
- TypeScript derleme hatası yok
- DB değişikliği yok — sadece display layer
</success_criteria>

<output>
Tamamlaninca `.planning/phases/43-karar-dili-guvenligi-skor-aciklanabilirligi/43-01-SUMMARY.md` olustur.
</output>
