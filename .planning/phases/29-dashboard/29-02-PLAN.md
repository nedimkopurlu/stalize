---
phase: 29-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/app/page.module.css
autonomous: false
requirements: [DASH-04]
must_haves:
  truths:
    - "User sees a portfolio summary widget on dashboard with explicit Türkçe empty-state copy"
    - "Empty state heading is 'Henüz portföy eklenmedi' and explanatory body matches UI-SPEC"
    - "Card uses dashed-border 'placeholder' visual treatment per UI-SPEC (1px dashed rgba(148,163,184,0.20))"
    - "Placeholder does NOT link out (Phase 32 not built); copy includes 'Portföy yönetimi yakında' as passive text"
    - "Existing BIST100/Döviz/Altın widgets from Plan 01 remain functional after Plan 02 changes"
  artifacts:
    - path: "frontend/src/app/page.tsx"
      provides: "Portfolio placeholder section refined per UI-SPEC Copywriting Contract"
      contains: "Henüz portföy eklenmedi"
    - path: "frontend/src/app/page.module.css"
      provides: ".portfolioPlaceholder dashed-border style + placeholder typography"
      contains: "1px dashed"
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/app/page.module.css"
      via: "styles.portfolioPlaceholder + styles.portfolioPlaceholderTitle + styles.portfolioPlaceholderBody"
      pattern: "styles\\.portfolioPlaceholder"
---

<objective>
Refine the portfolio summary widget on the dashboard so it satisfies DASH-04's "kullanıcı portföy özetini görür (boş durumda 'yakında' mesajı)" requirement with the exact UI-SPEC Copywriting Contract strings and dashed-border placeholder treatment. Plan 01 left a basic TerminalEmpty stub; this plan upgrades it to the proper UI-SPEC visual.

Purpose: DASH-04 expects "Kullanıcı portföyünün toplam değerini ve günlük değişimini özet olarak görür (portföy boşsa boş durum gösterir)". Phase 32 has not built portfolio data yet, so the dashboard ships the empty state. This plan finalizes that empty state to match Phase 29 UI-SPEC exactly (heading, body, CTA passive text, dashed border per D-11).

Output: Polished portfolio placeholder card on `/`, ending with a checkpoint:human-verify gate that confirms all four widgets render correctly in browser before phase verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-dashboard/29-CONTEXT.md
@.planning/phases/29-dashboard/29-UI-SPEC.md
@.planning/phases/29-dashboard/29-VALIDATION.md
@.planning/phases/29-dashboard/29-01-SUMMARY.md

@frontend/src/app/page.tsx
@frontend/src/app/page.module.css
@frontend/src/components/TerminalPrimitives.tsx

<interfaces>
From `frontend/src/components/TerminalPrimitives.tsx`:
```typescript
export function TerminalEmpty({ children }: { children: ReactNode }): JSX.Element;
```

From Plan 01 `frontend/src/app/page.module.css`:
- `.portfolioPlaceholder` already exists with: padding 24px, border 1px dashed rgba(148,163,184,0.20), border-radius 12px, background var(--bg-card)

UI-SPEC Copywriting Contract (must use these EXACT strings):
- Widget başlığı: "Portföyüm"
- Boş durum başlığı: "Henüz portföy eklenmedi"
- Boş durum açıklaması: "Alım-satım işlemlerinizi girdikten sonra toplam değer ve günlük değişiminiz burada görünür."
- Boş durum CTA (pasif): "Portföy yönetimi yakında"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add placeholder typography classes to page.module.css</name>
  <files>frontend/src/app/page.module.css</files>
  <read_first>
    - frontend/src/app/page.module.css (verify Plan 01 output: .portfolioPlaceholder already declared with dashed border)
    - .planning/phases/29-dashboard/29-UI-SPEC.md (Typography section + Copywriting Contract)
  </read_first>
  <action>
    Append (do NOT replace existing classes) two new classes to `frontend/src/app/page.module.css`:

    ```css
    .portfolioPlaceholderTitle {
      font-size: 16px;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: 8px;
    }

    .portfolioPlaceholderBody {
      font-size: 14px;
      font-weight: 400;
      line-height: 1.6;
      color: var(--text-secondary);
      margin-bottom: 12px;
    }

    .portfolioPlaceholderCta {
      font-size: 14px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--text-muted);
    }
    ```

    Use the Edit tool to insert these AFTER the existing `.portfolioPlaceholder { ... }` block. Do NOT delete or alter existing classes. Existing `.portfolioPlaceholder` (already 1px dashed border) stays as-is.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && grep -q "\.portfolioPlaceholder" src/app/page.module.css && grep -q "\.portfolioPlaceholderTitle" src/app/page.module.css && grep -q "\.portfolioPlaceholderBody" src/app/page.module.css && grep -q "\.portfolioPlaceholderCta" src/app/page.module.css && grep -q "1px dashed" src/app/page.module.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "\.portfolioPlaceholder " frontend/src/app/page.module.css || grep -q "\.portfolioPlaceholder{" frontend/src/app/page.module.css` exits 0 (Plan 01 base class still present)
    - `grep -q "\.portfolioPlaceholderTitle" frontend/src/app/page.module.css` exits 0
    - `grep -q "\.portfolioPlaceholderBody" frontend/src/app/page.module.css` exits 0
    - `grep -q "\.portfolioPlaceholderCta" frontend/src/app/page.module.css` exits 0
    - `grep -q "1px dashed rgba(148, 163, 184, 0.20)" frontend/src/app/page.module.css` exits 0 (dashed border per D-11/UI-SPEC preserved)
    - `grep -q "color: var(--text-secondary)" frontend/src/app/page.module.css` exits 0 (body text uses correct token)
    - `grep -q "color: var(--text-muted)" frontend/src/app/page.module.css` exits 0 (CTA uses muted token)
  </acceptance_criteria>
  <done>
    page.module.css contains 3 new placeholder typography classes alongside existing `.portfolioPlaceholder`. No existing styles altered.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace portfolio placeholder section in page.tsx with UI-SPEC structure</name>
  <files>frontend/src/app/page.tsx</files>
  <read_first>
    - frontend/src/app/page.tsx (locate the section currently labeled `aria-label="Portföyüm"` from Plan 01)
    - .planning/phases/29-dashboard/29-UI-SPEC.md (Copywriting Contract — exact strings)
  </read_first>
  <action>
    In `frontend/src/app/page.tsx`, locate the existing portfolio section produced by Plan 01:

    ```tsx
    <section className={styles.portfolioPlaceholder} aria-label="Portföyüm">
      <h2 className={styles.widgetTitle} style={{ marginBottom: 16 }}>Portföyüm</h2>
      <TerminalEmpty>
        Henüz portföy eklenmedi. Alım-satım işlemlerinizi girdikten sonra toplam değer ve günlük değişiminiz burada görünür. (Portföy yönetimi yakında)
      </TerminalEmpty>
    </section>
    ```

    Replace it EXACTLY with the following block (uses the new typography classes from Task 1 and the exact UI-SPEC Copywriting Contract strings):

    ```tsx
    <section className={styles.portfolioPlaceholder} aria-label="Portföyüm">
      <h2 className={styles.widgetTitle} style={{ marginBottom: 16 }}>Portföyüm</h2>
      <p className={styles.portfolioPlaceholderTitle}>Henüz portföy eklenmedi</p>
      <p className={styles.portfolioPlaceholderBody}>
        Alım-satım işlemlerinizi girdikten sonra toplam değer ve günlük değişiminiz burada görünür.
      </p>
      <p className={styles.portfolioPlaceholderCta}>Portföy yönetimi yakında</p>
    </section>
    ```

    DO NOT touch any other section of page.tsx (BIST100 banner, Döviz widget, Altın widget, useEffect, sub-components must remain untouched). DO NOT modify the import block — `TerminalEmpty` may remain imported even if no longer used in this section (other future phases may use it; leaving it is harmless and avoids ESLint churn).

    If after the change `TerminalEmpty` import becomes unused and ESLint flags it, remove ONLY that named import while leaving `TerminalShell`, `TerminalPageHeader`, `TerminalError` intact.

    Note: `ForexList` and `GoldList` sub-components still use `TerminalEmpty` for their own empty states, so the import is in fact still needed and must NOT be removed.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && grep -q "Henüz portföy eklenmedi" src/app/page.tsx && grep -q "Alım-satım işlemlerinizi girdikten sonra toplam değer ve günlük değişiminiz burada görünür" src/app/page.tsx && grep -q "Portföy yönetimi yakında" src/app/page.tsx && grep -q "portfolioPlaceholderTitle" src/app/page.tsx && grep -q "portfolioPlaceholderBody" src/app/page.tsx && grep -q "portfolioPlaceholderCta" src/app/page.tsx && grep -q "getMarketBist100" src/app/page.tsx && grep -q "getMarketForex" src/app/page.tsx && grep -q "getMarketGold" src/app/page.tsx && npx tsc --noEmit -p tsconfig.json && npx next build 2>&1 | grep -q "Compiled successfully\|✓ Compiled"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Henüz portföy eklenmedi" frontend/src/app/page.tsx` exits 0 (exact heading)
    - `grep -q "Alım-satım işlemlerinizi girdikten sonra toplam değer ve günlük değişiminiz burada görünür\." frontend/src/app/page.tsx` exits 0 (exact body)
    - `grep -q "Portföy yönetimi yakında" frontend/src/app/page.tsx` exits 0 (exact CTA)
    - `grep -q "styles.portfolioPlaceholderTitle" frontend/src/app/page.tsx` exits 0
    - `grep -q "styles.portfolioPlaceholderBody" frontend/src/app/page.tsx` exits 0
    - `grep -q "styles.portfolioPlaceholderCta" frontend/src/app/page.tsx` exits 0
    - Plan 01 widgets unchanged: `grep -q "api.getMarketBist100()" frontend/src/app/page.tsx`, `grep -q "api.getMarketForex()" frontend/src/app/page.tsx`, `grep -q "api.getMarketGold()" frontend/src/app/page.tsx` all exit 0
    - `grep -q "setInterval" frontend/src/app/page.tsx && grep -q "clearInterval" frontend/src/app/page.tsx` exits 0 (auto-refresh preserved)
    - `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0
    - `cd frontend && npx next build` exits 0
  </acceptance_criteria>
  <done>
    Portfolio placeholder section uses UI-SPEC exact copy (Henüz portföy eklenmedi / Alım-satım…/ Portföy yönetimi yakında) with correct semantic class hierarchy. Other dashboard widgets untouched. TypeScript + Next build pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser smoke test — verify all 4 widgets per UI-SPEC</name>
  <files>(none — verification only; reads frontend/src/app/page.tsx and frontend/src/app/page.module.css)</files>
  <read_first>
    - frontend/src/app/page.tsx
    - frontend/src/app/page.module.css
    - .planning/phases/29-dashboard/29-UI-SPEC.md
  </read_first>
  <action>
    Pause execution and present the verification instructions in the &lt;how-to-verify&gt; block to the user. Do not proceed to phase verification until the user replies "approved" or provides corrective feedback. If the user reports issues, surface them so a follow-up plan or task can address them before /gsd:verify-work runs.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — awaiting user approval (no automated command)."</automated>
  </verify>
  <acceptance_criteria>
    - User has visited http://localhost:3000/ and confirmed all 10 verification points in &lt;how-to-verify&gt;
    - User has typed "approved" (or equivalent affirmative) as resume signal
    - Any reported issues have been logged or addressed before phase verification
  </acceptance_criteria>
  <done>
    User has explicitly approved the dashboard. All 4 requirements (DASH-01..04) visually verified end-to-end in browser.
  </done>
  <what-built>
    Phase 29 Dashboard fully composed:
    - Plan 01: BIST100 banner, Döviz widget (6 pairs), Altın widget (5 forms), 30s auto-refresh
    - Plan 02: Portfolio placeholder card with UI-SPEC copy + dashed border styling
  </what-built>
  <how-to-verify>
    1. From repo root run: `./start.sh` (starts both backend + frontend)
    2. Open `http://localhost:3000/` in a browser
    3. **DASH-01 — BIST100 banner**: Verify full-width card at top showing:
       - "BIST100 ENDEKSİ" eyebrow (cyan, uppercase)
       - Endeks değeri (large monospace number)
       - "Günlük Değişim" with ▲ (green) or ▼ (red) and % value
       - "İşlem Hacmi" with formatted value (e.g., "1,2M") OR "—" if null
    4. **DASH-02 — Döviz card** (left in 2-col grid): 6 rows in order:
       - Dolar, Avro, Sterlin, Çin Yuanı, Japon Yeni, İsviçre Frangı
       - Each row: Türkçe label on left; "₺{price}" + ▲/▼ {change}% on right
    5. **DASH-03 — Altın card** (right in 2-col grid): 5 rows in order:
       - Gram Altın, Ons Altın, Çeyrek Altın, Yarım Altın, Tam Altın
       - Each row: label on left; "₺{price}" + "—" on right (gold change is unavailable per Phase 28 backend; this is intentional)
    6. **DASH-04 — Portföy card** (full-width, bottom):
       - Heading "Portföyüm"
       - Body heading "Henüz portföy eklenmedi"
       - Description "Alım-satım işlemlerinizi girdikten sonra toplam değer ve günlük değişiminiz burada görünür."
       - Passive CTA "PORTFÖY YÖNETİMİ YAKINDA" (uppercase due to letter-spacing styling)
       - Card has visible DASHED border (not solid)
    7. **Auto-refresh**: Watch "Otomatik yenileme: Xs" countdown in widget headers — should decrement every second from 30 → 0 → 30.
    8. **Network panel**: Open DevTools → Network → wait 30s → confirm three new requests fire to `/api/market/bist100`, `/api/market/forex`, `/api/market/gold`.
    9. **Responsive**: Resize browser to ≤768px → grid collapses Döviz + Altın into single column.
    10. **Independent failure**: Stop backend (`Ctrl+C` backend only) → reload page → each widget shows its own Türkçe error message; layout doesn't break.

    Expected: All 10 checks pass with no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any visual/functional issues to fix.</resume-signal>
</task>

</tasks>

<verification>
**Backend regression** (must remain green):
```bash
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -m pytest tests/test_market_endpoints.py -x -q
```

**Frontend build**:
```bash
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit -p tsconfig.json && npx next build
```

**Manual smoke**: covered by Task 3 checkpoint above.
</verification>

<success_criteria>
- DASH-04: Portfolio placeholder card on `/` shows exact UI-SPEC strings ("Henüz portföy eklenmedi", "Alım-satım işlemlerinizi…", "Portföy yönetimi yakında") with dashed border treatment.
- Plan 01 widgets (BIST100, Döviz, Altın) continue to render correctly post-Plan 02 changes.
- TypeScript + Next.js production build pass.
- Human checkpoint approved (Task 3).
</success_criteria>

<output>
After completion, create `.planning/phases/29-dashboard/29-02-SUMMARY.md` documenting:
- Files modified (page.tsx portfolio section + page.module.css typography classes)
- Confirmation that all 4 widgets coexist correctly
- Result of Task 3 human verification
- Phase 29 ready for `/gsd:verify-work`
</output>
