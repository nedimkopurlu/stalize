---
phase: 44-backtest-sinyal-performans-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - frontend/src/app/backtest/page.tsx
  - frontend/src/app/backtest/backtest.module.css
autonomous: true
requirements:
  - BACKTEST-01
  - BACKTEST-02
  - BACKTEST-03
  - BACKTEST-04

must_haves:
  truths:
    - "Kullanıcı /backtest sayfasına gidip sinyal performans tablosunu görebilir"
    - "KPI kartları (toplam sinyal, başarı oranı %, ortalama getiri) tablonun üstünde görünür"
    - "Dönem filtresi (1A/3A/6A) tabloyu limit parametresiyle filtreler"
    - "Güvenli Etiket filtresi action değerine göre satırları filtreler"
    - "Başarı Durumu filtresi (Tümü/Başarılı/Başarısız) outcome_1w alanına göre filtreler"
    - "Boş veri durumunda 'Henüz backtest verisi yok — sistem sinyal topluyor' mesajı gösterilir"
    - "Tablo 7 sütun içerir: Tarih, Hisse, Güvenli Etiket, 1H %, 1M %, BIST100 Relatif, Başarılı mı"
  artifacts:
    - path: "frontend/src/app/backtest/page.tsx"
      provides: "Backtest sayfası — KPI kartlar, filtreler, 7 sütunlu tablo, boş durum"
      min_lines: 200
    - path: "frontend/src/app/backtest/backtest.module.css"
      provides: "Backtest sayfasına özgü CSS modülü"
      contains: ".container"
  key_links:
    - from: "frontend/src/app/backtest/page.tsx"
      to: "api.getSignalOutcomes"
      via: "useEffect içinde loadData() çağrısı"
      pattern: "getSignalOutcomes"
    - from: "frontend/src/app/backtest/page.tsx"
      to: "api.getSignalCalibration"
      via: "useEffect içinde loadData() çağrısı"
      pattern: "getSignalCalibration"
    - from: "frontend/src/app/backtest/page.tsx"
      to: "SignalOutcomesResponse"
      via: "tip ithalatı api.ts'den"
      pattern: "SignalOutcomesResponse"
---

<objective>
/backtest rotası için yeni sayfa oluştur. Sinyal performans tablosu, KPI özet kartları, dönem/etiket/başarı filtreleri ve boş durum ekranı içerir.

Purpose: Kullanıcı kendi sinyal geçmişinin hit ratio'sunu ve getiri istatistiklerini somut tabloda görmeli; sistemin öğrenip öğrenmediğini anlayabilmeli (BACKTEST-01..04).
Output: frontend/src/app/backtest/page.tsx + backtest.module.css — tam çalışır sayfa.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-backtest-sinyal-performans-dashboard/44-01-SUMMARY.md

<interfaces>
<!-- api.ts'den mevcut tipler (dokunma, değiştirme): -->
```typescript
// SignalOutcomeItem (api.ts satır 442-469)
export interface SignalOutcomeItem {
  id: number;
  symbol: string;
  sector: string | null;
  decision_date: string;
  action: InvestmentAction;         // 'strong_buy' | 'buy' | 'watch' | 'hold' | 'reduce' | 'exit'
  action_label: string;
  confidence: number;
  risk_level: InvestmentRiskLevel;  // 'low' | 'medium' | 'high'
  current_price: number;
  stop_loss: number | null;
  target_price: number | null;
  risk_reward: number | null;
  suggested_shares: number | null;
  estimated_exposure_pct: number | null;
  overall_score: number | null;
  trend: string | null;
  actual_return_1w_pct: number | null;
  benchmark_return_1w_pct: number | null;
  excess_return_1w_pct: number | null;
  outcome_1w: 'success' | 'partial' | 'failure' | null;
  actual_return_1m_pct: number | null;
  benchmark_return_1m_pct: number | null;
  excess_return_1m_pct: number | null;
  outcome_1m: 'success' | 'partial' | 'failure' | null;
  generated_at: string | null;
  evaluated_at: string | null;
}

// SignalOutcomesResponse (api.ts satır 471-480)
export interface SignalOutcomesResponse {
  items: SignalOutcomeItem[];
  count: number;
  summary: {
    success: number;
    partial: number;
    failure: number;
    pending: number;
  };
}

// SignalCalibrationBucket (api.ts satır 482-491)
export interface SignalCalibrationBucket {
  key: string;
  count: number;
  success: number;
  partial: number;
  failure: number;
  success_rate: number;
  non_failure_rate: number;
  avg_return_pct: number | null;
  avg_excess_return_pct: number | null;
}

// SignalCalibrationResponse (api.ts satır 494-510)
export interface SignalCalibrationResponse {
  horizon: '1w' | '1m';
  sample_size: number;
  overall: SignalCalibrationBucket;
  by_action: SignalCalibrationBucket[];
  by_risk_level: SignalCalibrationBucket[];
  by_sector: SignalCalibrationBucket[];
  recommendations: string[];
  suggested_policy: { mode: string; measured_count: number; ... };
}

// api.ts metod imzaları (satır 1074-1078):
getSignalOutcomes: (limit: number = 20, horizon: '1w' | '1m' = '1w') =>
  Promise<SignalOutcomesResponse>

getSignalCalibration: (horizon: '1w' | '1m' = '1w', minCount: number = 1) =>
  Promise<SignalCalibrationResponse>
```

<!-- stocks/page.tsx'ten tablo pattern referansı: -->
```typescript
// Tablo şablonu (stocks/page.tsx'ten):
// - 'use client' direktifi
// - useEffect + useState + loadData() pattern
// - AppShell wrapper
// - styles from './page.module.css' (bu sayfada backtest.module.css kullanılacak)
// - try/catch/finally loading state
// - error state string | null
```

<!-- SAFE_LABEL_MAP (stocks/page.tsx'ten kopyalanacak): -->
```typescript
const SAFE_LABEL_MAP: Record<string, string> = {
  'GÜÇLÜ AL': 'Yüksek Öncelikli İzleme',
  'AL': 'Pozitif Görünüm',
  'TUT': 'Nötr İzleme',
  'SAT': 'Zayıflayan Görünüm',
  'GÜÇLÜ SAT': 'Riskli Görünüm',
};

// action -> raw recommendation eşlemesi:
const ACTION_TO_REC: Record<string, string> = {
  'strong_buy': 'GÜÇLÜ AL',
  'buy': 'AL',
  'watch': 'TUT',
  'hold': 'TUT',
  'reduce': 'SAT',
  'exit': 'GÜÇLÜ SAT',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backtest sayfası — page.tsx oluştur</name>
  <files>frontend/src/app/backtest/page.tsx</files>

  <read_first>
    - frontend/src/app/stocks/page.tsx — useEffect/loadData pattern, tablo şablonu (satır 1-120)
    - frontend/src/app/page.tsx — KPI kart pattern (satır 1-60)
    - frontend/src/lib/api.ts — getSignalOutcomes, getSignalCalibration imzaları (satır 1074-1078)
  </read_first>

  <action>
    Yeni dosya oluştur: `frontend/src/app/backtest/page.tsx`

    **Gereksinimler:**

    1. **Direktif ve importlar:**
       - `'use client'` direktifi
       - `useState`, `useEffect` React hook'ları
       - `AppShell` bileşeni (`@/components/AppShell`)
       - `api`, `SignalOutcomeItem`, `SignalOutcomesResponse`, `SignalCalibrationResponse` from `@/lib/api`
       - `styles` from `./backtest.module.css`

    2. **Sabit tanımlar:**
       ```typescript
       // Dönem filtresi: label -> limit eşlemesi (BACKTEST-02)
       const PERIOD_OPTIONS = [
         { label: '1A', limit: 30 },
         { label: '3A', limit: 90 },
         { label: '6A', limit: 180 },
       ] as const;

       // action -> DB recommendation string
       const ACTION_TO_REC: Record<string, string> = {
         strong_buy: 'GÜÇLÜ AL', buy: 'AL', watch: 'TUT',
         hold: 'TUT', reduce: 'SAT', exit: 'GÜÇLÜ SAT',
       };

       // DB recommendation -> kullanıcıya gösterilen güvenli etiket (KARAR-01)
       const SAFE_LABEL_MAP: Record<string, string> = {
         'GÜÇLÜ AL': 'Yüksek Öncelikli İzleme',
         'AL': 'Pozitif Görünüm',
         'TUT': 'Nötr İzleme',
         'SAT': 'Zayıflayan Görünüm',
         'GÜÇLÜ SAT': 'Riskli Görünüm',
       };

       // Filtre'de görünen etiket seçenekleri (BACKTEST-02)
       const ACTION_FILTER_OPTIONS = [
         { label: 'Tümü', value: '' },
         { label: 'Yüksek Öncelikli İzleme', value: 'strong_buy' },
         { label: 'Pozitif Görünüm', value: 'buy' },
         { label: 'Nötr İzleme', value: 'watch' },
         { label: 'Zayıflayan Görünüm', value: 'reduce' },
         { label: 'Riskli Görünüm', value: 'exit' },
       ];

       // Başarı durumu filtre seçenekleri (BACKTEST-02)
       const OUTCOME_FILTER_OPTIONS = [
         { label: 'Tümü', value: '' },
         { label: 'Başarılı', value: 'success' },
         { label: 'Başarısız', value: 'failure' },
         { label: 'Beklemede', value: 'pending' },
       ];
       ```

    3. **State yapısı:**
       ```typescript
       const [outcomes, setOutcomes] = useState<SignalOutcomeItem[]>([]);
       const [calibration, setCalibration] = useState<SignalCalibrationResponse | null>(null);
       const [summary, setSummary] = useState<SignalOutcomesResponse['summary'] | null>(null);
       const [loading, setLoading] = useState(true);
       const [error, setError] = useState<string | null>(null);
       const [period, setPeriod] = useState<'1A' | '3A' | '6A'>('3A');
       const [actionFilter, setActionFilter] = useState('');
       const [outcomeFilter, setOutcomeFilter] = useState('');
       ```

    4. **loadData() fonksiyonu:**
       ```typescript
       async function loadData() {
         setLoading(true);
         setError(null);
         try {
           const limitMap: Record<string, number> = { '1A': 30, '3A': 90, '6A': 180 };
           const limit = limitMap[period] ?? 90;
           const [outcomesRes, calibRes] = await Promise.all([
             api.getSignalOutcomes(limit, '1w'),
             api.getSignalCalibration('1w', 1),
           ]);
           setOutcomes(outcomesRes.items);
           setSummary(outcomesRes.summary);
           setCalibration(calibRes);
         } catch (e) {
           setError(e instanceof Error ? e.message : 'Veri yüklenemedi');
         } finally {
           setLoading(false);
         }
       }
       ```
       `useEffect(() => { loadData(); }, [period]);`

    5. **Filtrelenmiş veri (istemci tarafı):**
       ```typescript
       const filtered = outcomes.filter((item) => {
         if (actionFilter && item.action !== actionFilter) return false;
         if (outcomeFilter === 'success' && item.outcome_1w !== 'success') return false;
         if (outcomeFilter === 'failure' && item.outcome_1w !== 'failure') return false;
         if (outcomeFilter === 'pending' && item.outcome_1w !== null) return false;
         return true;
       });
       ```

    6. **KPI kartlar (BACKTEST-03) — tablonun üstünde:**
       - Toplam Sinyal: `outcomes.length`
       - 1H Başarı Oranı: `calibration?.overall.success_rate` varsa `%XX.X`, yoksa `—`
       - Ort. 1H Getiri: `calibration?.overall.avg_return_pct` varsa `+X.X%` (yeşil/kırmızı), yoksa `—`
       - Ort. 1M Getiri: 1M calibration için `api.getSignalCalibration('1m', 1)` çağrısı da yap ve state'e ekle; yoksa tek `—`

       **NOT:** 1M için ayrı state: `const [calibration1m, setCalibration1m] = useState<SignalCalibrationResponse | null>(null);` ve `Promise.all`'a üçüncü eleman olarak ekle.

    7. **Filtre bar:**
       ```tsx
       <div className={styles.filterBar}>
         {/* Dönem filtresi */}
         <div className={styles.filterGroup}>
           <span className={styles.filterLabel}>Dönem:</span>
           {['1A', '3A', '6A'].map((p) => (
             <button
               key={p}
               className={`${styles.filterBtn} ${period === p ? styles.filterBtnActive : ''}`}
               onClick={() => setPeriod(p as '1A' | '3A' | '6A')}
             >{p}</button>
           ))}
         </div>
         {/* Güvenli etiket filtresi */}
         <select
           className={styles.filterSelect}
           value={actionFilter}
           onChange={(e) => setActionFilter(e.target.value)}
         >
           {ACTION_FILTER_OPTIONS.map((o) => (
             <option key={o.value} value={o.value}>{o.label}</option>
           ))}
         </select>
         {/* Başarı durumu filtresi */}
         <select
           className={styles.filterSelect}
           value={outcomeFilter}
           onChange={(e) => setOutcomeFilter(e.target.value)}
         >
           {OUTCOME_FILTER_OPTIONS.map((o) => (
             <option key={o.value} value={o.value}>{o.label}</option>
           ))}
         </select>
       </div>
       ```

    8. **Boş durum (BACKTEST-04):**
       ```tsx
       {!loading && filtered.length === 0 && (
         <div className={styles.emptyState}>
           <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round" className={styles.emptyIcon}>
             <line x1="18" y1="20" x2="18" y2="10" />
             <line x1="12" y1="20" x2="12" y2="4" />
             <line x1="6" y1="20" x2="6" y2="14" />
           </svg>
           <p>Henüz backtest verisi yok — sistem sinyal topluyor</p>
         </div>
       )}
       ```

    9. **7 sütunlu tablo (BACKTEST-01):**

       Sütunlar: Tarih | Hisse | Güvenli Etiket | 1H % | 1M % | BIST100 Relatif | Başarılı mı

       ```tsx
       <table className={styles.table}>
         <thead>
           <tr>
             <th>Tarih</th>
             <th>Hisse</th>
             <th>Güvenli Etiket</th>
             <th>1H %</th>
             <th>1M %</th>
             <th>BIST100 Relatif</th>
             <th>Başarılı mı</th>
           </tr>
         </thead>
         <tbody>
           {filtered.map((item) => {
             const rec = ACTION_TO_REC[item.action] ?? item.action_label;
             const labelText = SAFE_LABEL_MAP[rec] ?? rec;
             const labelColor =
               item.action === 'strong_buy' || item.action === 'buy'
                 ? 'var(--accent-green)'
                 : item.action === 'exit' || item.action === 'reduce'
                 ? 'var(--accent-red)'
                 : 'var(--accent)';
             const outcome1wIcon =
               item.outcome_1w === 'success' ? '✓' :
               item.outcome_1w === 'failure' ? '✗' : '—';
             const outcome1wColor =
               item.outcome_1w === 'success' ? 'var(--accent-green)' :
               item.outcome_1w === 'failure' ? 'var(--accent-red)' :
               'var(--text-muted)';
             const fmt = (v: number | null) =>
               v === null ? '—' : `${v > 0 ? '+' : ''}${v.toFixed(1)}%`;
             const relative = item.excess_return_1w_pct;
             return (
               <tr key={item.id}>
                 <td>{item.decision_date ? new Date(item.decision_date).toLocaleDateString('tr-TR', { day: '2-digit', month: 'short', year: '2-digit' }) : '—'}</td>
                 <td className={styles.symbolCell}>{item.symbol}</td>
                 <td><span style={{ color: labelColor }}>{labelText}</span></td>
                 <td style={{ color: item.actual_return_1w_pct !== null && item.actual_return_1w_pct > 0 ? 'var(--accent-green)' : item.actual_return_1w_pct !== null ? 'var(--accent-red)' : 'var(--text-muted)' }}>{fmt(item.actual_return_1w_pct)}</td>
                 <td style={{ color: item.actual_return_1m_pct !== null && item.actual_return_1m_pct > 0 ? 'var(--accent-green)' : item.actual_return_1m_pct !== null ? 'var(--accent-red)' : 'var(--text-muted)' }}>{fmt(item.actual_return_1m_pct)}</td>
                 <td style={{ color: relative !== null && relative > 0 ? 'var(--accent-green)' : relative !== null ? 'var(--accent-red)' : 'var(--text-muted)' }}>{fmt(relative)}</td>
                 <td><span style={{ color: outcome1wColor, fontWeight: 600 }}>{outcome1wIcon}</span></td>
               </tr>
             );
           })}
         </tbody>
       </table>
       ```

    10. **JSX yapısı:**
        ```tsx
        export default function BacktestPage() {
          // ... state, loadData, filtered ...
          return (
            <AppShell>
              <div className={styles.container}>
                <div className={styles.header}>
                  <h1 className={styles.title}>Sinyal Performansı</h1>
                  <p className={styles.subtitle}>Geçmiş sinyal kararlarının gerçekleşen getiri ve başarı oranları</p>
                </div>
                {error && <div className={styles.error}>{error}</div>}
                {/* KPI Kartlar */}
                <div className={styles.kpiGrid}>
                  {/* 3-4 kart */}
                </div>
                {/* Filtre Bar */}
                <div className={styles.filterBar}>...</div>
                {/* Tablo veya boş durum */}
                {loading ? (
                  <div className={styles.loading}>Yükleniyor...</div>
                ) : filtered.length === 0 ? (
                  <div className={styles.emptyState}>...</div>
                ) : (
                  <div className={styles.tableWrapper}>
                    <table className={styles.table}>...</table>
                  </div>
                )}
              </div>
            </AppShell>
          );
        }
        ```

    TypeScript hatası yoksa dosyayı tamamla.
  </action>

  <verify>
    <automated>grep -c "getSignalOutcomes\|getSignalCalibration" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/backtest/page.tsx && grep -c "Henüz backtest verisi yok" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/backtest/page.tsx</automated>
  </verify>

  <acceptance_criteria>
    - grep "getSignalOutcomes" page.tsx — 1+ satır döner
    - grep "getSignalCalibration" page.tsx — 1+ satır döner
    - grep "Henüz backtest verisi yok" page.tsx — 1 satır döner (BACKTEST-04)
    - grep "Tarih\|Hisse\|Güvenli Etiket\|1H %\|1M %\|BIST100 Relatif\|Başarılı mı" page.tsx — 7 eşleşme döner (BACKTEST-01)
    - grep "setPeriod" page.tsx — 1+ satır döner (dönem filtresi, BACKTEST-02)
    - grep "'use client'" page.tsx — 1 satır döner
    - Dosya 200+ satır içerir: wc -l page.tsx beklenti >=200
  </acceptance_criteria>

  <done>
    /backtest sayfası tam çalışır: KPI kartlar, 3 filtre (dönem, güvenli etiket, başarı), 7 sütunlu tablo ve boş durum ekranı mevcut.
  </done>
</task>

<task type="auto">
  <name>Task 2: CSS modülü — backtest.module.css oluştur</name>
  <files>frontend/src/app/backtest/backtest.module.css</files>

  <read_first>
    - frontend/src/app/stocks/page.module.css — tablo stilleri referans (sadece tablo sınıf isimleri için, tam okuma gerekmez)
    - frontend/src/app/page.module.css — kpi kart stilleri referans
  </read_first>

  <action>
    Yeni dosya oluştur: `frontend/src/app/backtest/backtest.module.css`

    Aşağıdaki sınıfları tanımla; CSS değişkenleri mevcut globals.css'den gelir (var(--bg-card), var(--border), var(--text), var(--text-muted), var(--accent), var(--accent-green), var(--accent-red)):

    ```css
    /* Layout */
    .container { max-width: 1200px; margin: 0 auto; padding: 2rem 1.5rem; }
    .header { margin-bottom: 1.5rem; }
    .title { font-size: 1.5rem; font-weight: 700; color: var(--text); margin: 0 0 0.25rem; }
    .subtitle { font-size: 0.875rem; color: var(--text-muted); margin: 0; }

    /* Error / Loading */
    .error { background: rgba(239,68,68,0.1); border: 1px solid rgba(239,68,68,0.3); border-radius: 8px; padding: 0.75rem 1rem; color: var(--accent-red); margin-bottom: 1rem; font-size: 0.875rem; }
    .loading { text-align: center; padding: 3rem; color: var(--text-muted); font-size: 0.9rem; }

    /* KPI Grid */
    .kpiGrid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 1rem; margin-bottom: 1.5rem; }
    .kpiCard { background: var(--bg-card); border: 1px solid var(--border); border-radius: 12px; padding: 1.25rem; }
    .kpiLabel { font-size: 0.75rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; margin: 0 0 0.5rem; }
    .kpiValue { font-size: 1.5rem; font-weight: 700; color: var(--text); margin: 0; }
    .kpiSub { font-size: 0.75rem; color: var(--text-muted); margin: 0.25rem 0 0; }

    /* Filter Bar */
    .filterBar { display: flex; flex-wrap: wrap; gap: 0.75rem; align-items: center; margin-bottom: 1.25rem; }
    .filterGroup { display: flex; align-items: center; gap: 0.375rem; }
    .filterLabel { font-size: 0.8rem; color: var(--text-muted); white-space: nowrap; }
    .filterBtn { padding: 0.35rem 0.75rem; border-radius: 6px; border: 1px solid var(--border); background: transparent; color: var(--text-muted); font-size: 0.8rem; cursor: pointer; transition: all 0.15s; }
    .filterBtn:hover { border-color: var(--accent); color: var(--text); }
    .filterBtnActive { background: var(--accent); border-color: var(--accent); color: #fff; font-weight: 600; }
    .filterSelect { padding: 0.375rem 0.75rem; border-radius: 6px; border: 1px solid var(--border); background: var(--bg-card); color: var(--text); font-size: 0.8rem; cursor: pointer; }

    /* Table */
    .tableWrapper { overflow-x: auto; border-radius: 12px; border: 1px solid var(--border); }
    .table { width: 100%; border-collapse: collapse; font-size: 0.875rem; }
    .table thead tr { background: var(--bg-card); border-bottom: 1px solid var(--border); }
    .table th { padding: 0.75rem 1rem; text-align: left; font-size: 0.75rem; font-weight: 600; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.04em; white-space: nowrap; }
    .table tbody tr { border-bottom: 1px solid var(--border); transition: background 0.1s; }
    .table tbody tr:last-child { border-bottom: none; }
    .table tbody tr:hover { background: var(--bg-card); }
    .table td { padding: 0.75rem 1rem; color: var(--text); white-space: nowrap; }
    .symbolCell { font-weight: 600; font-family: monospace; }

    /* Empty State */
    .emptyState { text-align: center; padding: 4rem 2rem; color: var(--text-muted); }
    .emptyIcon { display: block; margin: 0 auto 1rem; opacity: 0.4; }
    .emptyState p { font-size: 0.9rem; margin: 0; }

    /* Responsive */
    @media (max-width: 768px) {
      .container { padding: 1rem; }
      .kpiGrid { grid-template-columns: repeat(2, 1fr); }
      .table th, .table td { padding: 0.5rem 0.75rem; }
    }
    ```
  </action>

  <verify>
    <automated>grep -c "\.container\|\.kpiGrid\|\.table\|\.emptyState\|\.filterBar" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/backtest/backtest.module.css</automated>
  </verify>

  <acceptance_criteria>
    - grep ".container" backtest.module.css — 1+ satır döner
    - grep ".kpiGrid" backtest.module.css — 1+ satır döner
    - grep ".emptyState" backtest.module.css — 1+ satır döner
    - grep ".filterBar" backtest.module.css — 1+ satır döner
    - grep ".table" backtest.module.css — 1+ satır döner
    - Dosya 30+ satır içerir
  </acceptance_criteria>

  <done>backtest.module.css tüm gerekli sınıfları içerir; page.tsx'in referans ettiği hiçbir sınıf eksik değildir.</done>
</task>

</tasks>

<verification>
```bash
# 1. Her iki dosya var mı?
ls /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/backtest/
# Beklenti: backtest.module.css  page.tsx

# 2. Backend endpoint çağrıları mevcut mu?
grep -c "getSignalOutcomes\|getSignalCalibration" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/backtest/page.tsx
# Beklenti: >=2

# 3. 7 tablo sütunu var mı?
grep -c "Tarih\|1H %\|1M %\|BIST100 Relatif\|Başarılı mı\|Güvenli Etiket\|Hisse" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/backtest/page.tsx
# Beklenti: 7

# 4. Boş durum mesajı
grep "Henüz backtest verisi yok" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/backtest/page.tsx
# Beklenti: 1 satır

# 5. TypeScript derleme hatası yok mu?
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -30
# Beklenti: hata yok (veya önceki hatalar — backtest'e özgü hata yok)
```
</verification>

<success_criteria>
- /backtest rotası çalışır; AppShell wrapper ile Sidebar görünür
- KPI kartlar API verisini yansıtır (BACKTEST-03); veri yokken `—` gösterir
- Dönem filtresi (1A/3A/6A) loadData'yı yeni limit ile tetikler (BACKTEST-02)
- Güvenli Etiket ve Başarı Durumu filtreleri istemci tarafında çalışır (BACKTEST-02)
- Tablo 7 sütun gösterir; satır rengi outcome_1w'e göre yeşil/kırmızı/gri (BACKTEST-01)
- outcomes.length === 0 iken boş durum mesajı görünür (BACKTEST-04)
- TypeScript strict mod hatası yok
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/44-backtest-sinyal-performans-dashboard/44-02-SUMMARY.md` oluştur.
</output>
