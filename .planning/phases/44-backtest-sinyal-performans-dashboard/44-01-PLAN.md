---
phase: 44-backtest-sinyal-performans-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Sidebar.tsx
autonomous: true
requirements:
  - BACKTEST-01
  - BACKTEST-02
  - BACKTEST-03
  - BACKTEST-04

must_haves:
  truths:
    - "Sidebar'da 'Backtest' nav item görünür, /backtest rotasına bağlıdır"
    - "Backtest nav item Haberler (intelligence) öğesinden sonra gelir"
    - "api.ts'deki getSignalOutcomes ve getSignalCalibration metodları mevcut (halihazırda var)"
  artifacts:
    - path: "frontend/src/components/Sidebar.tsx"
      provides: "NAV_ITEMS listesine IconChartBar + Backtest nav item eklendi"
      contains: "href: '/backtest'"
  key_links:
    - from: "frontend/src/components/Sidebar.tsx"
      to: "/backtest"
      via: "NAV_ITEMS array href"
      pattern: "href.*backtest"
---

<objective>
Sidebar'a Backtest navigasyon öğesi ekle. api.ts'deki backend metodları zaten mevcut (getSignalOutcomes, getSignalCalibration, SignalOutcomesResponse, SignalCalibrationResponse — satır 1074-1078 ve 442-510). Bu plan yalnızca Sidebar güncellemesini kapsar.

Purpose: Kullanıcı /backtest sayfasına Sidebar üzerinden ulaşabilmeli; Plan 02'deki sayfa inşa edilmeden önce navigasyon hazır olmalı.
Output: Sidebar.tsx güncellenmiş, yeni IconChartBar SVG ikonu ve /backtest nav item eklendi.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Mevcut NAV_ITEMS yapısı (frontend/src/components/Sidebar.tsx, satır 58-64): -->
```typescript
const NAV_ITEMS = [
  { href: '/',                 label: 'Genel Bakış',      Icon: IconGrid       },
  { href: '/portfolio',        label: 'Portföy',          Icon: IconBriefcase  },
  { href: '/stocks',           label: 'Tüm Hisseler',     Icon: IconLayers     },
  { href: '/model-portfolio',  label: 'Model Portföyler', Icon: IconSparkles   },
  { href: '/intelligence',     label: 'Haberler',         Icon: IconNewspaper  },
  // Buraya: { href: '/backtest', label: 'Backtest', Icon: IconChartBar }
];
```

<!-- Mevcut api.ts metodları — dokunma, zaten var: -->
```typescript
// satır 1074-1078
getSignalOutcomes: (limit: number = 20, horizon: '1w' | '1m' = '1w') =>
  apiFetch<SignalOutcomesResponse>(`/signals/outcomes?limit=${limit}&horizon=${horizon}`),

getSignalCalibration: (horizon: '1w' | '1m' = '1w', minCount: number = 1) =>
  apiFetch<SignalCalibrationResponse>(`/signals/calibration?horizon=${horizon}&min_count=${minCount}`),
```

<!-- Mevcut tip tanımları — dokunma: -->
```typescript
// SignalOutcomesResponse (satır 471-480), SignalCalibrationResponse (satır 494-510)
// SignalOutcomeItem (satır 442-469) — karar_tarihi, hisse, action, actual_return_1w_pct,
// actual_return_1m_pct, excess_return_1w_pct, excess_return_1m_pct, outcome_1w, outcome_1m
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar'a IconChartBar ve Backtest nav item ekle</name>
  <files>frontend/src/components/Sidebar.tsx</files>

  <read_first>
    - frontend/src/components/Sidebar.tsx — tam dosya (mevcut SVG icon pattern ve NAV_ITEMS yapısı)
  </read_first>

  <action>
    1. Mevcut SVG ikon fonksiyonları bölümüne (IconNewspaper'dan sonra) yeni `IconChartBar` fonksiyonu ekle:

    ```tsx
    function IconChartBar() {
      return (
        <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
          <line x1="18" y1="20" x2="18" y2="10" />
          <line x1="12" y1="20" x2="12" y2="4" />
          <line x1="6" y1="20" x2="6" y2="14" />
        </svg>
      );
    }
    ```

    2. NAV_ITEMS dizisine Haberler (intelligence) öğesinden hemen sonra yeni öğe ekle:

    ```typescript
    { href: '/backtest', label: 'Backtest', Icon: IconChartBar },
    ```

    Son NAV_ITEMS şu şekilde görünmeli:
    ```typescript
    const NAV_ITEMS = [
      { href: '/',                label: 'Genel Bakış',      Icon: IconGrid       },
      { href: '/portfolio',       label: 'Portföy',          Icon: IconBriefcase  },
      { href: '/stocks',          label: 'Tüm Hisseler',     Icon: IconLayers     },
      { href: '/model-portfolio', label: 'Model Portföyler', Icon: IconSparkles   },
      { href: '/intelligence',    label: 'Haberler',         Icon: IconNewspaper  },
      { href: '/backtest',        label: 'Backtest',         Icon: IconChartBar   },
    ];
    ```

    Dosyada başka hiçbir şeyi değiştirme.
  </action>

  <verify>
    <automated>grep -n "href.*backtest" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/components/Sidebar.tsx && grep -n "IconChartBar" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/components/Sidebar.tsx</automated>
  </verify>

  <acceptance_criteria>
    - grep "href.*backtest" Sidebar.tsx — 1 satır döner
    - grep "IconChartBar" Sidebar.tsx — en az 2 satır döner (tanım + kullanım)
    - grep "intelligence" Sidebar.tsx'ten önce geldiği doğrulanır: awk '/intelligence/{found=1} found && /backtest/{print NR; exit}' satır numarası döner
  </acceptance_criteria>

  <done>Sidebar'da /backtest nav item Haberler'den sonra görünür; ikon tanımlanmış ve dizi güncellenmiş.</done>
</task>

</tasks>

<verification>
```bash
# 1. Nav item var mı?
grep -c "href.*backtest" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/components/Sidebar.tsx
# Beklenti: 1

# 2. Ikon tanımlanmış mı?
grep -c "IconChartBar" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/components/Sidebar.tsx
# Beklenti: >=2

# 3. Sıra doğru mu? (intelligence satırı backtest satırından önce gelmeli)
grep -n "intelligence\|backtest" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/components/Sidebar.tsx
# intelligence satır numarası < backtest satır numarası olmalı
```
</verification>

<success_criteria>
- Sidebar.tsx'de /backtest href'i mevcuttur
- IconChartBar SVG ikonu tanımlanmış ve NAV_ITEMS'de kullanılmaktadır
- Backtest öğesi Haberler öğesinden sonra gelir
- Dosyada başka değişiklik yok (diğer nav item'lar, stil, logo vb. bozulmamış)
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/44-backtest-sinyal-performans-dashboard/44-01-SUMMARY.md` oluştur.
</output>
