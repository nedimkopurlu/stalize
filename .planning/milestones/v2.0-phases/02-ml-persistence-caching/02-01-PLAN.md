---
phase: 02-ml-persistence-caching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_ml_persistence.py
  - backend/tests/test_yf_cache.py
  - backend/tests/test_llm_cache.py
  - backend/models/.gitkeep
  - backend/.gitignore
  - backend/requirements.txt
autonomous: true
requirements:
  - MLCA-01
  - MLCA-02
  - MLCA-03

must_haves:
  truths:
    - "11 test stubs exist and all fail with pytest.fail() (RED phase confirmed)"
    - "backend/models/ directory is tracked in git via .gitkeep; *.ubj and *.joblib are gitignored"
    - "backend/cache/ directory gitignore entries exist for llm/ and yfinance/ subdirs"
    - "diskcache==5.6.3 is present in requirements.txt; joblib is NOT added (already transitive via scikit-learn)"
  artifacts:
    - path: "backend/tests/test_ml_persistence.py"
      provides: "4 failing stubs for MLCA-01"
      exports: [test_save_creates_files, test_load_restores_model, test_first_run_saves_model, test_weekly_retrain_job]
    - path: "backend/tests/test_yf_cache.py"
      provides: "4 failing stubs for MLCA-02"
      exports: [test_price_cache_hit, test_price_cache_ttl, test_info_cache_ttl, test_empty_not_cached]
    - path: "backend/tests/test_llm_cache.py"
      provides: "3 failing stubs for MLCA-03"
      exports: [test_cache_hit_skips_api, test_cache_key_format, test_cache_expiry]
    - path: "backend/models/.gitkeep"
      provides: "git-tracked empty directory for XGBoost model files"
  key_links:
    - from: "backend/tests/test_ml_persistence.py"
      to: "backend/app/services/ml.py"
      via: "imports MLAnalysisEngine — stubs reference the class even before it is modified"
    - from: "backend/tests/test_yf_cache.py"
      to: "backend/app/services/data_collector.py"
      via: "patches yf.Ticker at app.services.data_collector.yf.Ticker"
    - from: "backend/tests/test_llm_cache.py"
      to: "backend/app/services/llm_sentiment.py"
      via: "patches DeepSeekSentimentService.client"
---

<objective>
Create failing test stubs (RED phase) for all 11 Phase 2 test cases, add the diskcache dependency, and scaffold the directory structure that subsequent plans will populate.

Purpose: Establish the Nyquist-compliant test contract before any implementation begins. Every implementation plan in this phase will run these tests to confirm GREEN.
Output: 3 test files with 11 pytest.fail() stubs; backend/models/.gitkeep; diskcache in requirements.txt; .gitignore entries for generated artifacts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ml-persistence-caching/02-VALIDATION.md
@backend/tests/conftest.py
@backend/pytest.ini
</context>

<interfaces>
<!-- Key interfaces the executor needs. -->

From backend/app/services/ml.py:
```python
class MLAnalysisEngine:
    def __init__(self): ...  # no args; sets self.xgb_params dict
    async def analyze_stock(self, db: AsyncSession, stock: Stock) -> float: ...
    async def _train_and_predict(self, X, y, current_features) -> Optional[float]: ...
```

From backend/app/services/data_collector.py:
```python
import yfinance as yf   # patch target: "app.services.data_collector.yf.Ticker"

class DataCollector:
    async def _collect_stock_prices(self, db, symbol, period): ...
    async def collect_price_data(self, period="5d"): ...
```

From backend/app/services/llm_sentiment.py:
```python
class DeepSeekSentimentService:
    def __init__(self): ...
    self.client: AsyncOpenAI | None  # None when DEEPSEEK_API_KEY missing
    async def analyze(self, title, summary="", source="Unknown", symbol=None, event_type=None) -> Dict: ...
```

Existing conftest.py fixtures: mock_settings, mock_stock, mock_stock_with_macro
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add diskcache dependency and scaffold directories</name>
  <files>
    backend/requirements.txt
    backend/models/.gitkeep
    backend/.gitignore
  </files>
  <read_first>
    - backend/requirements.txt
    - backend/.gitignore (if exists — may not exist yet)
  </read_first>
  <action>
    1. In backend/requirements.txt, add under the "# Machine Learning" section (after xgboost==2.1.4):
       diskcache==5.6.3

       Do NOT add joblib — it is already installed transitively via scikit-learn==1.6.1.

    2. Create backend/models/.gitkeep — empty file so git tracks the directory.

    3. In backend/.gitignore (create if missing), add the following entries:
       # XGBoost per-stock model files (generated artifacts)
       models/*.ubj
       models/*.joblib

       # diskcache storage directories
       cache/llm/
       cache/yfinance/
       cache/

    4. Do NOT create the backend/cache/ directory — diskcache creates it at runtime when the Cache() is instantiated. The .gitignore entry is sufficient.
  </action>
  <verify>
    <automated>grep "diskcache==5.6.3" /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/requirements.txt && test -f /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/models/.gitkeep && grep "models/\*.ubj" /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/.gitignore</automated>
  </verify>
  <acceptance_criteria>
    - grep "diskcache==5.6.3" backend/requirements.txt exits 0
    - backend/models/.gitkeep exists (file, may be empty)
    - backend/.gitignore contains "models/*.ubj" and "models/*.joblib" and "cache/"
    - "joblib" does NOT appear as a new line in requirements.txt (it is already transitive)
  </acceptance_criteria>
  <done>diskcache pinned in requirements; models/ directory git-tracked; generated artifacts gitignored</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test stubs for MLCA-01, MLCA-02, MLCA-03 (RED phase)</name>
  <files>
    backend/tests/test_ml_persistence.py
    backend/tests/test_yf_cache.py
    backend/tests/test_llm_cache.py
  </files>
  <read_first>
    - backend/tests/conftest.py
    - backend/app/services/ml.py
    - backend/app/services/data_collector.py
    - backend/app/services/llm_sentiment.py
    - .planning/phases/02-ml-persistence-caching/02-VALIDATION.md
  </read_first>
  <behavior>
    MLCA-01 stubs (test_ml_persistence.py):
    - test_save_creates_files: pytest.fail("MLCA-01: save_model not implemented yet")
    - test_load_restores_model: pytest.fail("MLCA-01: load_model not implemented yet")
    - test_first_run_saves_model: pytest.fail("MLCA-01: first-run save path not implemented yet")
    - test_weekly_retrain_job: pytest.fail("MLCA-01: weekly retrain job not implemented yet")

    MLCA-02 stubs (test_yf_cache.py):
    - test_price_cache_hit: pytest.fail("MLCA-02: yfinance result cache not implemented yet")
    - test_price_cache_ttl: pytest.fail("MLCA-02: price TTL not implemented yet")
    - test_info_cache_ttl: pytest.fail("MLCA-02: info TTL not implemented yet")
    - test_empty_not_cached: pytest.fail("MLCA-02: empty DataFrame guard not implemented yet")

    MLCA-03 stubs (test_llm_cache.py):
    - test_cache_hit_skips_api: pytest.fail("MLCA-03: LLM cache not implemented yet")
    - test_cache_key_format: pytest.fail("MLCA-03: cache key format not verified yet")
    - test_cache_expiry: pytest.fail("MLCA-03: cache TTL not implemented yet")
  </behavior>
  <action>
    Write three test files with the exact function names from 02-VALIDATION.md. Each function body must contain exactly one statement: `pytest.fail("...")` with a descriptive message matching the requirement ID.

    IMPORTANT: Each file must import pytest. Do NOT import the production modules being tested yet — those imports will cause import errors because the new cache infrastructure does not exist. Use string-only stubs.

    File structure for each:
    ```python
    """
    Tests for [requirement] — Phase 2 ML Persistence + Caching
    These stubs are RED (failing). They turn GREEN after 02-0N-PLAN.md implementation.
    """
    import pytest


    def test_xxx():
        pytest.fail("[REQ-ID]: [description] not implemented yet")
    ```

    Use `xfail` marker ONLY if the test might accidentally pass before implementation.
    For these stubs, plain `pytest.fail()` is correct — it guarantees RED.

    Run the quick suite to confirm all 11 are RED before committing:
    `cd backend && pytest tests/test_ml_persistence.py tests/test_yf_cache.py tests/test_llm_cache.py -x -q`
    Expected: 11 FAILED (all pytest.fail). Zero passing.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -m pytest tests/test_ml_persistence.py tests/test_yf_cache.py tests/test_llm_cache.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cd backend && pytest tests/test_ml_persistence.py tests/test_yf_cache.py tests/test_llm_cache.py -q reports exactly 11 FAILED
    - grep "def test_save_creates_files" backend/tests/test_ml_persistence.py exits 0
    - grep "def test_load_restores_model" backend/tests/test_ml_persistence.py exits 0
    - grep "def test_first_run_saves_model" backend/tests/test_ml_persistence.py exits 0
    - grep "def test_weekly_retrain_job" backend/tests/test_ml_persistence.py exits 0
    - grep "def test_price_cache_hit" backend/tests/test_yf_cache.py exits 0
    - grep "def test_price_cache_ttl" backend/tests/test_yf_cache.py exits 0
    - grep "def test_info_cache_ttl" backend/tests/test_yf_cache.py exits 0
    - grep "def test_empty_not_cached" backend/tests/test_yf_cache.py exits 0
    - grep "def test_cache_hit_skips_api" backend/tests/test_llm_cache.py exits 0
    - grep "def test_cache_key_format" backend/tests/test_llm_cache.py exits 0
    - grep "def test_cache_expiry" backend/tests/test_llm_cache.py exits 0
    - No PASSED in test output — all must be FAILED
    - No ERROR (import error, syntax error) — only FAILED from pytest.fail()
  </acceptance_criteria>
  <done>11 stubs confirmed RED; full Phase 2 test contract established</done>
</task>

</tasks>

<verification>
Run full existing suite to confirm Wave 1 does not break Phase 1 tests:
`cd backend && pytest tests/ -x -q`

Expected: Phase 1 tests (test_scoring.py, test_models.py, test_routers.py, test_kap_parser.py) continue to pass. The 11 new stubs fail with pytest.fail (expected RED).
</verification>

<success_criteria>
- diskcache==5.6.3 in requirements.txt
- backend/models/.gitkeep exists
- backend/.gitignore has entries for *.ubj, *.joblib, cache/
- 3 test files exist with exactly 11 test functions
- All 11 tests fail with pytest.fail() (RED) — confirmed by pytest output
- Zero import errors in test collection
- Phase 1 tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/02-ml-persistence-caching/02-01-SUMMARY.md`
</output>
