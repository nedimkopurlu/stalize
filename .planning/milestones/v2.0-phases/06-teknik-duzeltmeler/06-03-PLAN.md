---
phase: 06-teknik-duzeltmeler
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/dynamic_correlation.py
  - backend/app/services/ml.py
  - backend/tests/test_pandas_ffill.py
  - backend/tests/test_ml_no_double_count.py
autonomous: true
requirements: [TECH-03, TECH-04]

must_haves:
  truths:
    - "dynamic_correlation.py calls df.ffill() positionally — no fillna(method=) remains"
    - "Running the correlation module against a sample frame emits zero pandas FutureWarning"
    - "ml.py _normalize_score() takes a single pred_return argument and ignores / no longer blends causal_score"
    - "overall_score computed via scoring.py weights counts causal exactly once (through WEIGHT_CAUSAL only)"
  artifacts:
    - path: "backend/app/services/dynamic_correlation.py"
      provides: "Correlation matrix with modern pandas API"
      contains: ".ffill()"
    - path: "backend/app/services/ml.py"
      provides: "_normalize_score() without causal blending"
      contains: "_normalize_score"
    - path: "backend/tests/test_pandas_ffill.py"
      provides: "Test asserting no FutureWarning from dynamic_correlation run"
      exports: ["test_ffill_no_deprecation_warning"]
    - path: "backend/tests/test_ml_no_double_count.py"
      provides: "Test asserting _normalize_score ignores causal_score and scoring weights cover causal exactly once"
      exports: ["test_normalize_score_ignores_causal", "test_causal_counted_once_in_overall"]
  key_links:
    - from: "backend/app/services/dynamic_correlation.py compute_correlation_matrix"
      to: "pandas DataFrame fill behavior"
      via: "df_pivot.ffill() (not fillna(method='ffill'))"
      pattern: "\\.ffill\\(\\)"
    - from: "backend/app/services/ml.py _normalize_score"
      to: "scoring.py _resolve_weights (causal_score weight)"
      via: "causal enters overall_score ONLY through WEIGHT_CAUSAL"
      pattern: "_normalize_score\\(self, pred_return"
---

<objective>
Close TECH-03 and TECH-04 together — both are small, isolated mutations in adjacent backend services with shared test infrastructure.

- TECH-03: Replace deprecated `df.fillna(method="ffill")` with the positional `df.ffill()` API in `dynamic_correlation.py`. Eliminates pandas FutureWarning spam.
- TECH-04: Remove the `causal_score * 0.2` blend from `ml.py _normalize_score()` so causal sentiment enters the overall score exactly once (via `WEIGHT_CAUSAL` in scoring.py), not twice.

Purpose: Both are silent-correctness defects flagged by the post-v1.0 audit. The pandas change is deprecation-prep; the ML change fixes a real numeric bug (causal was weighted ~0.2 inside ml_score *and* 0.15 via WEIGHT_CAUSAL in scoring.py — effective weight ~0.35 instead of 0.15).
Output: One-line edit in dynamic_correlation.py, signature/body edit in ml.py _normalize_score, two dedicated tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-teknik-duzeltmeler/06-CONTEXT.md
@backend/app/services/dynamic_correlation.py
@backend/app/services/ml.py
@backend/app/services/scoring.py

<interfaces>
From backend/app/services/dynamic_correlation.py (line 124):
```python
# CURRENT (deprecated):
df_pivot = df_pivot.fillna(method="ffill").dropna()
# TARGET:
df_pivot = df_pivot.ffill().dropna()
```

From backend/app/services/ml.py (lines 205-219):
```python
# CURRENT:
def _normalize_score(self, pred_return: float, causal_score: float = 50.0) -> float:
    base_score = 50.0 + (pred_return * 500)
    weighted_score = (base_score * 0.8) + (causal_score * 0.2)
    return max(0.0, min(100.0, weighted_score))

# TARGET:
def _normalize_score(self, pred_return: float) -> float:
    base_score = 50.0 + (pred_return * 500)
    return max(0.0, min(100.0, base_score))
```

Caller in ml.py analyze_stock (line ~250):
```python
# CURRENT:
c_score = stock.causal_score if stock.causal_score is not None else 50.0
score = self._normalize_score(pred_return or 0.0, c_score)

# TARGET:
score = self._normalize_score(pred_return or 0.0)
```

From backend/app/services/scoring.py _resolve_weights (line 45-52): causal_score already has its own weight key `causal_score: settings.WEIGHT_CAUSAL`. This is the ONLY legitimate place causal enters overall_score after TECH-04.

From 06-CONTEXT:
- "overall_score formula must still sum to 1.0 across all weights after ML fix"
- "causal enters only through scoring.py weights"
- "add a test asserting causal is not in ml.py _normalize_score internals"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests for TECH-03 (no ffill deprecation warning) and TECH-04 (causal counted once)</name>
  <files>backend/tests/test_pandas_ffill.py, backend/tests/test_ml_no_double_count.py</files>
  <read_first>
    - backend/app/services/dynamic_correlation.py (line 124 — the deprecated call)
    - backend/app/services/ml.py (lines 205-250 — _normalize_score and analyze_stock caller)
    - backend/app/services/scoring.py (_resolve_weights — how causal_score weight is applied)
    - backend/tests/test_scoring.py (existing pattern for asserting on stock score attributes)
  </read_first>
  <behavior>
    **test_pandas_ffill.py:**
    - Test 1 `test_ffill_no_deprecation_warning`: Use `warnings.catch_warnings(record=True)` with `simplefilter("always")`. Invoke a tiny end-to-end slice that exercises the pivot+ffill path: construct a `pd.DataFrame` with two symbols and a missing row, call `df_pivot.ffill().dropna()` directly through a helper that mimics dynamic_correlation's logic (OR import `compute_correlation_matrix` and run it against a monkeypatched DB — simpler: do a source-level assertion). Assert `len([w for w in caught if 'fillna' in str(w.message).lower() and 'method' in str(w.message).lower()]) == 0`.
    - Test 2 `test_source_has_no_fillna_method_kwarg`: Read the source of `backend/app/services/dynamic_correlation.py` and assert `'fillna(method=' not in source` AND `'.ffill()' in source`. This is the structural guard that complements the runtime warning check.

    **test_ml_no_double_count.py:**
    - Test 1 `test_normalize_score_signature_has_no_causal_param`: Using `inspect.signature(MLAnalysisEngine._normalize_score)`, assert `'causal_score' not in signature.parameters`.
    - Test 2 `test_normalize_score_ignores_causal_via_source`: Read source of `backend/app/services/ml.py`; locate the `_normalize_score` function block; assert `'causal_score' not in function_body` AND `'* 0.2' not in function_body` (scoped just to the function body, not the whole file — causal_score appears legitimately elsewhere).
    - Test 3 `test_normalize_score_returns_pure_base`: `MLAnalysisEngine()._normalize_score(0.0)` returns exactly 50.0 (pred_return=0 → base 50, no causal blending); `_normalize_score(0.1)` returns 100.0 (clamped); `_normalize_score(-0.1)` returns 0.0.
    - Test 4 `test_causal_counted_once_in_overall`: Construct a mock Stock with technical=50, fundamental=50, sentiment=50, causal=100, ml=50 (where ml was computed via the fixed _normalize_score — so ml no longer contains causal). Call `scoring_engine.calculate_overall_score(stock)`. Because only scoring.py's WEIGHT_CAUSAL weighs causal, assert the result differs from the same stock with causal=50 by exactly `(100-50) * settings.WEIGHT_CAUSAL / sum_of_weights_in_non_crisis_path`. In non-crisis mode (sentiment and causal within thresholds of 50 — but causal=100 means `abs(100-50)=50 >= 25` crisis trigger) — so use causal=70 instead (abs(70-50)=20 < 25, no crisis; also sentiment=50 within threshold). Document the arithmetic inline in a comment so the test is readable.

    All tests must fail RED on current code:
    - test_pandas_ffill tests fail because `fillna(method="ffill")` is still present.
    - test_ml_no_double_count tests fail because `causal_score` param and `* 0.2` blending still exist in _normalize_score.
  </behavior>
  <action>
    Create both test files. Use `pytest.mark.asyncio` only where needed (most of these are sync assertions on signatures/source). Run each file with `pytest -x -v` and confirm RED.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pandas_ffill.py tests/test_ml_no_double_count.py -x -v 2>&1 | grep -E "(FAILED|AssertionError)" | head -10</automated>
  </verify>
  <done>Both test files committed; all named tests fail — RED phase established for TECH-03 and TECH-04 simultaneously.</done>
  <acceptance_criteria>
    - File `backend/tests/test_pandas_ffill.py` exists
    - File `backend/tests/test_ml_no_double_count.py` exists
    - `grep -c "fillna(method=" backend/tests/test_pandas_ffill.py` >= 1 (string referenced in structural test)
    - `grep -c "ffill" backend/tests/test_pandas_ffill.py` >= 2
    - `grep -c "causal_score\\|_normalize_score" backend/tests/test_ml_no_double_count.py` >= 3
    - `grep -c "inspect.signature\\|signature.parameters" backend/tests/test_ml_no_double_count.py` >= 1
    - `pytest backend/tests/test_pandas_ffill.py backend/tests/test_ml_no_double_count.py` exits non-zero (RED)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TECH-03 — replace fillna(method="ffill") with ffill() in dynamic_correlation.py</name>
  <files>backend/app/services/dynamic_correlation.py</files>
  <read_first>
    - backend/app/services/dynamic_correlation.py (confirm line 124 is still the only site)
    - backend/tests/test_pandas_ffill.py (the RED contract)
  </read_first>
  <action>
    In `backend/app/services/dynamic_correlation.py`, locate the single occurrence on line ~124:
    ```python
    df_pivot = df_pivot.fillna(method="ffill").dropna()
    ```
    Replace with:
    ```python
    df_pivot = df_pivot.ffill().dropna()
    ```
    Do NOT change any other logic, comment, or variable name. The Turkish comment `# NaN değerleri doldur (forward fill)` above it remains accurate.

    Verify no other `fillna(method=` occurrences exist in the file.

    Per 06-CONTEXT decision: "replace fillna(method='ffill') with ffill() — pandas positional API".
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_pandas_ffill.py -x -v</automated>
  </verify>
  <done>dynamic_correlation.py uses `.ffill()`; test_pandas_ffill.py GREEN; running the module against a sample frame emits no fillna FutureWarning.</done>
  <acceptance_criteria>
    - `grep -c 'fillna(method=' backend/app/services/dynamic_correlation.py` equals 0
    - `grep -c '\.ffill()' backend/app/services/dynamic_correlation.py` >= 1
    - `pytest backend/tests/test_pandas_ffill.py` exits 0 (GREEN)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TECH-04 — remove causal_score blending from ml.py _normalize_score and its caller</name>
  <files>backend/app/services/ml.py</files>
  <read_first>
    - backend/app/services/ml.py (lines 205-219 for _normalize_score; line ~250 for the caller in analyze_stock)
    - backend/app/services/scoring.py (confirm causal_score weight is present in _resolve_weights — the one legitimate site)
    - backend/tests/test_ml_no_double_count.py (the RED contract)
  </read_first>
  <action>
    In `backend/app/services/ml.py`:

    1. Change the signature of `_normalize_score` (line 205) from:
       ```python
       def _normalize_score(self, pred_return: float, causal_score: float = 50.0) -> float:
       ```
       to:
       ```python
       def _normalize_score(self, pred_return: float) -> float:
       ```

    2. Replace the body (lines 211-219). Remove the comment block `# V1.3.1: Haber zekasını (Causal) modele optimal dengede dahil et` and the `weighted_score = (base_score * 0.8) + (causal_score * 0.2)` line. Update the docstring to drop the causal_score description. New body:
       ```python
       """
       Tahmin edilen 10 günlük getiriyi 0-100 ML skoru altında normalize eder.

       TECH-04: Causal/haber zekası artık _normalize_score'a girmiyor —
       causal skor yalnızca scoring.py'deki WEIGHT_CAUSAL ağırlığıyla
       overall_score'a katılır (çift sayım kaldırıldı).
       """
       base_score = 50.0 + (pred_return * 500)
       return max(0.0, min(100.0, base_score))
       ```

    3. Update the single caller inside `analyze_stock` (line ~250). Replace:
       ```python
       c_score = stock.causal_score if stock.causal_score is not None else 50.0
       score = self._normalize_score(pred_return or 0.0, c_score)
       ```
       with:
       ```python
       score = self._normalize_score(pred_return or 0.0)
       ```
       Remove the now-unused `c_score` local.

    4. The log line at ~line 261 currently says `f"AI/ML Analizi: {stock.symbol} (Haber Etkisi: {c_score:.1f}, Tahmin Skoru: {smoothed_score:.1f})"`. Since `c_score` is gone, change to `f"AI/ML Analizi: {stock.symbol} (Tahmin Skoru: {smoothed_score:.1f})"`.

    5. Do NOT touch `stock.causal_score` reads elsewhere in the file (e.g. the two fallback returns at lines 230 and 236 `return stock.causal_score or 50.0` — those return causal as the ml_score when ML has no data, which is a separate fallback concern outside TECH-04 scope per 06-CONTEXT wording "remove `causal_score * 0.2` from `ml.py`'s `_normalize_score()`"). Leave them as-is.

    6. Do NOT change scoring.py — its `causal_score: settings.WEIGHT_CAUSAL` line is now the single source of causal in overall_score, exactly as TECH-04 requires.

    Per 06-CONTEXT decision: "in ml.py _normalize_score(), remove the causal_score * 0.2 blend; causal enters only through scoring.py weights" and "overall_score formula must still sum to 1.0 across all weights after ML fix" — the scoring.py weights were not changed, so their sum is untouched.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_ml_no_double_count.py tests/test_scoring.py -x -v</automated>
  </verify>
  <done>_normalize_score takes one arg; caller updated; log line updated; test_ml_no_double_count.py GREEN; test_scoring.py still passes (scoring.py weight logic untouched).</done>
  <acceptance_criteria>
    - `grep -c 'def _normalize_score(self, pred_return: float, causal_score' backend/app/services/ml.py` equals 0
    - `grep -c 'def _normalize_score(self, pred_return: float) -> float' backend/app/services/ml.py` equals 1
    - `grep -c 'causal_score \* 0.2\|\* 0.2)' backend/app/services/ml.py` equals 0
    - `grep -c 'self._normalize_score(pred_return or 0.0, c_score)' backend/app/services/ml.py` equals 0
    - `grep -c 'Haber Etkisi' backend/app/services/ml.py` equals 0
    - `pytest backend/tests/test_ml_no_double_count.py` exits 0 (GREEN)
    - `pytest backend/tests/test_scoring.py` exits 0 (no regression)
    - `pytest backend/tests -x` exits 0 (full suite, no regressions anywhere)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- dynamic_correlation.py uses `.ffill()` positionally; no `fillna(method=` string remains
- ml.py `_normalize_score` signature has one param; body has no `causal_score` or `* 0.2`
- ml.py `analyze_stock` caller updated; log line updated
- scoring.py untouched — causal_score weight continues at WEIGHT_CAUSAL
- Both new test files GREEN; full backend suite no regressions
</verification>

<success_criteria>
TECH-03 closed: `dynamic_correlation.py` produces no pandas FutureWarning about `fillna(method=)` — all occurrences replaced with `ffill()`.

TECH-04 closed: `overall_score` for any stock counts causal exactly once — `causal_score * 0.2` is removed from `ml.py _normalize_score()`; causal enters only through `WEIGHT_CAUSAL` in `scoring.py`. Score stays within 0-100 and weight sum unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-teknik-duzeltmeler/06-03-SUMMARY.md` recording:
- dynamic_correlation.py ffill() single-line change
- ml.py _normalize_score signature + body + caller + log line updates
- test_pandas_ffill.py and test_ml_no_double_count.py GREEN
- test_scoring.py and full suite: no regressions
</output>
