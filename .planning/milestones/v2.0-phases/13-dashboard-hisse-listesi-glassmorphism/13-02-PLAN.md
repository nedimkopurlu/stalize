---
phase: 13-dashboard-hisse-listesi-glassmorphism
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - frontend/src/app/page.tsx
  - frontend/src/lib/api.ts
  - frontend/src/components/SparklineWidget.tsx
autonomous: true
requirements:
  - GLUI-02
  - CLEN-01
  - VIZZ-03

must_haves:
  truths:
    - "Dashboard açıldığında AI brifing card'ı yoktur — DashboardData.briefing veya BriefingData'ya hiçbir çağrı yapılmaz"
    - "Sağ sütunda son 10 KAP bildirimi (hisse adı + başlık + tarih + KAP linki) listelenir; API boş döndüğünde 'Henüz bildirim yok' gösterilir"
    - "BIST100 ve USD/TRY sparkline widget'ları dashboard'da görünür — yfinance'den gelen gerçek 30 günlük veri, hardcoded değil"
    - "Hisse skor tablosu ana alanda glassmorphism .glass-card içinde render edilir"
    - "Makro göstergeler (USD/TRY, BIST100, faiz, enflasyon) üst yatay bantta gösterilir"
  artifacts:
    - path: "frontend/src/app/page.tsx"
      provides: "Yeniden tasarlanmış dashboard — 3 bölge: makro bant, ana (skor tablosu + sparkline), sağ sütun (KAP)"
      min_lines: 80
    - path: "frontend/src/lib/api.ts"
      provides: "getKapFeed() ve getSparklineData() fonksiyonları"
      exports: ["getKapFeed", "getSparklineData"]
    - path: "frontend/src/components/SparklineWidget.tsx"
      provides: "lightweight-charts ile mini sparkline bileşeni"
      exports: ["SparklineWidget"]
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/lib/api.ts"
      via: "api.getKapFeed() ve api.getSparklineData() çağrıları"
      pattern: "getKapFeed|getSparklineData"
    - from: "frontend/src/components/SparklineWidget.tsx"
      to: "lightweight-charts"
      via: "createChart import"
      pattern: "lightweight-charts"
---

<objective>
Dashboard yeniden tasarlanır: AI brifing bölümü kaldırılır, makro göstergeler üst bant, KAP bildirimleri sağ sütun, hisse skor tablosu ana alan, BIST100 ve USD/TRY sparkline widget'ları eklenir.

Purpose: GLUI-02, CLEN-01 ve VIZZ-03 gereksinimlerini karşılar. AI/LLM kaldırıldıktan sonra (Phase 10) dashboard'un yeni bilgi mimarisi bu plandır.
Output: Yeniden yazılmış frontend/src/app/page.tsx, SparklineWidget.tsx bileşeni, api.ts genişletmesi
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dashboard-hisse-listesi-glassmorphism/13-01-SUMMARY.md

Key constraint from frontend/AGENTS.md: This is NOT the standard Next.js you know. Read node_modules/next/dist/docs/ for the version in use before writing code. Check for useEffect, 'use client', and chart library breaking changes.
</context>

<interfaces>
<!-- Mevcut api.ts'den executor'ın kullanacağı tipler -->

```typescript
// frontend/src/lib/api.ts — mevcut exports

export interface MacroIndicators {
  usdtry: number | null;
  gold_try: number | null;
  bist100: number | null;
  interest_rate: number | null;
  inflation_rate: number | null;
  as_of: string;
}

export interface StockSummary {
  symbol: string;
  name: string;
  sector: string | null;
  current_price: number | null;
  daily_change_pct: number | null;
  volume: number | null;
  volume_ratio: number | null;
  overall_score: number | null;
  recommendation: string | null;
  // ... diğer alanlar
}

// Mevcut api fonksiyonları:
api.getMacroIndicators()   // GET /api/macro/indicators → MacroIndicators
api.getStocks(params)      // GET /api/stocks → StockListResponse
```

<!-- Eklenecek yeni tipler ve fonksiyonlar -->

```typescript
// api.ts'e eklenecek:

export interface KapNotification {
  id: number;
  symbol: string;
  title: string;
  published_at: string;   // ISO datetime
  kap_url: string | null;
}

export interface SparklinePoint {
  date: string;   // "YYYY-MM-DD"
  close: number;
}

export interface SparklineResponse {
  symbol: string;   // "XU100" veya "USDTRY=X"
  points: SparklinePoint[];
}

// Yeni api fonksiyonları:
api.getKapFeed(limit?: number)           // GET /api/stocks/kap-feed?limit=10
api.getSparklineData(symbol: string)     // GET /api/stocks/sparkline?symbol=XU100&days=30
```

<!-- lightweight-charts v5 minimal kullanım (SparklineWidget için) -->
```typescript
// lightweight-charts v5'te breaking change: createChart artık named export
import { createChart, LineStyle } from 'lightweight-charts';
// v5'te chart.addLineSeries() değil chart.addSeries(LineSeries) — versiyonu doğrula
```

<!-- Mevcut dashboard page.tsx: frontend/src/app/page.tsx (dashboard klasörü yok, root page) -->
<!-- Not: dashboard route MEVCUT DEĞİL — frontend/src/app/page.tsx ana sayfa -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — KAP feed ve sparkline endpoint fonksiyonları ekle</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
frontend/src/lib/api.ts dosyasına iki yeni tip ve iki yeni api fonksiyonu ekle (per CLEN-01 ve VIZZ-03):

1. Tip tanımları (mevcut interface bloğunun sonuna, `// ── API Functions` satırından ÖNCE):
```typescript
export interface KapNotification {
  id: number;
  symbol: string;
  title: string;
  published_at: string;
  kap_url: string | null;
}

export interface SparklinePoint {
  date: string;
  close: number;
}

export interface SparklineResponse {
  symbol: string;
  points: SparklinePoint[];
}
```

2. api objesine fonksiyonlar ekle (mevcut `getMacroIndicators` satırının ALTINA):
```typescript
getKapFeed: (limit: number = 10) =>
  apiFetch<KapNotification[]>(`/stocks/kap-feed?limit=${limit}`),

getSparklineData: (symbol: string, days: number = 30) =>
  apiFetch<SparklineResponse>(`/stocks/sparkline?symbol=${encodeURIComponent(symbol)}&days=${days}`),
```

MEVCUT fonksiyonları silme veya değiştirme — sadece ekle.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS|Error" | head -20</automated>
  </verify>
  <done>
    - KapNotification, SparklinePoint, SparklineResponse tipleri api.ts'te export ediliyor
    - api.getKapFeed() ve api.getSparklineData() fonksiyonları mevcut
    - TypeScript derleme hatası yok
  </done>
</task>

<task type="auto">
  <name>Task 2: SparklineWidget.tsx bileşeni oluştur</name>
  <files>frontend/src/components/SparklineWidget.tsx</files>
  <action>
Yeni bir React bileşeni oluştur: frontend/src/components/SparklineWidget.tsx

ÖNCE: node_modules/lightweight-charts/package.json'a bakarak major versiyonu doğrula. v4 ile v5 arasında createChart API'si değişti.

Bileşen gereksinimleri (per VIZZ-03):
- 'use client' direktifi olmalı (chart DOM manipulation yapıyor)
- Props: `{ points: SparklinePoint[], color?: string, height?: number, label: string }`
- lightweight-charts ile minimal area chart render et — sadece close değerleri, eksen gösterge yok, grid yok
- Pozitif/negatif trend için renk: son kapanış > ilk kapanışsa --accent-green (#10b981), değilse --accent-red (#ef4444)
- Bileşen boyutu: genişlik %100 (parent), yükseklik prop'tan (default 60px)
- Container: .glass-card className ile, padding 12px, label üstte küçük text
- Chart referansı cleanup: useEffect return ile chart.remove() çağır

Örnek yapı:
```typescript
'use client';
import { useEffect, useRef } from 'react';
import { SparklinePoint } from '@/lib/api';

interface Props {
  points: SparklinePoint[];
  label: string;
  color?: string;
  height?: number;
}

export default function SparklineWidget({ points, label, color, height = 60 }: Props) {
  const containerRef = useRef<HTMLDivElement>(null);
  // lightweight-charts ile chart oluştur
  // version-aware API kullan
}
```

NOT: Eğer lightweight-charts yüklü değilse veya SSR uyumsuzluğu varsa, dynamic import kullan:
`const { createChart } = await import('lightweight-charts');`
Bu bileşen 'use client' olduğu için SSR sorunu olmaz ama import hatası olabilir.

Eğer lightweight-charts paketi node_modules'de yoksa, package.json'a ekle:
`cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npm install lightweight-charts`
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | grep "SparklineWidget" | head -10</automated>
  </verify>
  <done>
    - SparklineWidget.tsx mevcut ve 'use client' direktifi var
    - lightweight-charts import ediliyor
    - Props tipi doğru (points, label, color, height)
    - TypeScript hatası yok
  </done>
</task>

<task type="auto">
  <name>Task 3: page.tsx — dashboard yeniden tasarımı (AI brifing kaldır, 3 bölge layout)</name>
  <files>frontend/src/app/page.tsx</files>
  <action>
ÖNCE mevcut frontend/src/app/page.tsx'i oku. Sayfa mevcut değilse oluştur; mevcutsa tamamen yeniden yaz (per GLUI-02, CLEN-01, VIZZ-03).

Yeni layout yapısı (3 bölge):

**1. Makro Bant (üst, yatay):**
- api.getMacroIndicators() ile USD/TRY, BIST100, faiz oranı, enflasyon verisi çek
- Her gösterge için glassmorphism pill/card: değer + etiket + değişim yönü oku
- Responsive: mobilde 2 satır, masaüstünde tek satır

**2. Ana İçerik (orta-sol, ~%70 genişlik):**
- Sol üst: SparklineWidget bileşeni × 2 (BIST100: symbol="XU100", USD/TRY: symbol="USDTRY=X")
  - api.getSparklineData() ile 30 günlük veri çek
  - Her widget .glass-card içinde, yan yana (grid 2 kolon)
- Alt: api.getStocks({ sort_by: 'overall_score', limit: 20 }) ile hisse skor tablosu
  - Tablo: sıra / sembol / skor / öneri / günlük değişim
  - Tablo satırları .glass-card veya glassmorphism table row stili
  - "Tümünü Gör" linki /stocks'a

**3. Sağ Sütun (~%30 genişlik, per CLEN-01):**
- Başlık: "KAP Bildirimleri"
- api.getKapFeed(10) ile son 10 bildirimi çek
- Her bildirim: hisse sembolü (bold) + başlık (truncate) + tarih + KAP linkine tıklanabilir dış link
- Boş gelirse: "Henüz bildirim yok" empty state (merkezi, muted text)
- Container: .glass-card, overflow-y scroll, max-height 500px

**AI brifing bölümünü tamamen kaldır:**
- api.getBriefing() çağrısı YOK
- BriefingData tipi import YOK
- IntelligenceOverview veya CausalFeedItem çağrısı YOK

**Loading state:** Her bölüm için ayrı loading skeleton — tek bir global spinner değil.
**Error state:** API hata verirse sessizce boş göster, sayfa çökmemeli.

Layout CSS: CSS grid kullan — `grid-template-columns: 1fr 320px` masaüstünde, mobilde tek kolon.
Tüm kartlar .glass-card className kullanmalı (13-01'de tanımlandı).

'use client' direktifi gerekli (useState, useEffect kullanılıyor).
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <done>
    - page.tsx 'use client' direktifi ile yazıldı
    - getBriefing(), getIntelligenceOverview() çağrısı yok
    - getKapFeed(), getSparklineData(), getMacroIndicators(), getStocks() çağrıları mevcut
    - SparklineWidget bileşeni sayfada import ediliyor ve kullanılıyor
    - KAP listesi boş geldiğinde "Henüz bildirim yok" görünüyor
    - TypeScript hatası yok
    - `next build --no-lint` tamamlanıyor
  </done>
</task>

</tasks>

<verification>
```bash
# TypeScript type check
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"
# Çıktı: 0

# getBriefing çağrısı kaldırıldı mı?
grep -n "getBriefing\|getIntelligenceOverview\|BriefingData" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/page.tsx
# Çıktı: boş (hiç satır olmamalı)

# KAP feed çağrısı var mı?
grep -n "getKapFeed\|getSparklineData" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/page.tsx
# Çıktı: en az 2 satır

# Build
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx next build --no-lint 2>&1 | tail -5
```
</verification>

<success_criteria>
- ai.ts'te KapNotification, SparklinePoint, SparklineResponse tipleri ve getKapFeed/getSparklineData fonksiyonları mevcut
- SparklineWidget.tsx lightweight-charts ile çalışıyor, 'use client' direktifi var
- Dashboard page.tsx'te getBriefing/getIntelligenceOverview çağrısı yok
- KAP bildirimleri sağ sütunda, boş state "Henüz bildirim yok"
- BIST100 ve USD/TRY sparkline widget'ları ana alanda
- Makro göstergeler üst bantta
- `next build --no-lint` sıfır hata
</success_criteria>

<output>
After completion, create `/Users/nedimkopurlu/Downloads/PROJELER/stalize/.planning/phases/13-dashboard-hisse-listesi-glassmorphism/13-02-SUMMARY.md`
</output>
