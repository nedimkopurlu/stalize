---
phase: 13-dashboard-hisse-listesi-glassmorphism
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - backend/app/api/stocks.py
  - frontend/src/app/stocks/page.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - GLUI-03
  - CLEN-03

must_haves:
  truths:
    - "GET /api/stocks/sectors endpoint'i mevcut stocks tablosundaki distinct sector değerlerini döner — hardcoded liste değil"
    - "Hisse listesi sayfasında sektör dropdown'u sektörleri /api/stocks/sectors'dan dinamik çeker"
    - "Sektör seçildiğinde liste anlık filtrelenir — sayfa yenilemesi olmaz, URL değişmez"
    - "'Tümü' seçildiğinde tüm liste geri gelir"
    - "Tablo satırları glassmorphism stilindedir — .glass-card veya dark glass row background"
    - "Mevcut tüm filtreler (arama, öneri filtresi, skor aralığı, BIST30) korunur — hiçbiri kaldırılmaz"
    - "causal_score sütunu kaldırılır (v1.2 AIRF-02 çerçevesinde causal engine silindiğinden); Nedensellik sütunu silindi"
  artifacts:
    - path: "backend/app/api/stocks.py"
      provides: "GET /api/stocks/sectors endpoint — distinct sektör listesi"
      contains: "sectors"
    - path: "frontend/src/app/stocks/page.tsx"
      provides: "Sektör dropdown + glassmorphism tablo + tüm filtreler"
      min_lines: 150
    - path: "frontend/src/lib/api.ts"
      provides: "api.getStockSectors() fonksiyonu"
      exports: ["getStockSectors"]
  key_links:
    - from: "frontend/src/app/stocks/page.tsx"
      to: "frontend/src/lib/api.ts"
      via: "api.getStockSectors() çağrısı"
      pattern: "getStockSectors"
    - from: "frontend/src/lib/api.ts"
      to: "/api/stocks/sectors"
      via: "apiFetch('/stocks/sectors')"
      pattern: "stocks/sectors"
    - from: "backend/app/api/stocks.py"
      to: "app.models.stock.Stock"
      via: "select(func.distinct(Stock.sector))"
      pattern: "distinct.*sector"
---

<objective>
Hisse listesi sayfası glassmorphism ile yeniden tasarlanır. Backend'e GET /api/stocks/sectors endpoint'i eklenir. Frontend'de sektör dropdown dinamik veri çeker; mevcut filtreler korunur; tablo satırları glassmorphism stili alır.

Purpose: GLUI-03 ve CLEN-03 gereksinimlerini karşılar. Bu plan 13-01'e (CSS temellerine) bağımlıdır ama 13-02'den bağımsızdır — aynı wave'de paralel çalışabilir.
Output: Güncellenmiş stocks.py (yeni endpoint), güncellenmiş stocks/page.tsx, api.ts genişletmesi
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-dashboard-hisse-listesi-glassmorphism/13-01-SUMMARY.md

Key constraint from frontend/AGENTS.md: This is NOT the standard Next.js you know. Read node_modules/next/dist/docs/ for the version in use before writing code. In particular verify useEffectEvent availability in this Next.js version.
</context>

<interfaces>
<!-- Mevcut backend stocks router'dan executor'ın bilmesi gerekenler -->

```python
# backend/app/api/stocks.py — mevcut imports (bunları koruyun)
from sqlalchemy import select, func, and_
from app.core.database import AsyncSessionLocal
from app.models.stock import Stock

# router = APIRouter() zaten tanımlı, @router.get ile yeni endpoint ekle
```

<!-- Mevcut frontend stocks/page.tsx'ten korunacak state ve fonksiyonlar -->

```typescript
// frontend/src/app/stocks/page.tsx — KORUNACAK state (silme):
const [stocks, setStocks] = useState<StockSummary[]>([]);
const [search, setSearch] = useState('');
const [sortBy, setSortBy] = useState('overall_score');
const [filterBist30, setFilterBist30] = useState(false);
const [filterRec, setFilterRec] = useState('');
const [minScore, setMinScore] = useState<number | ''>('');
const [maxScore, setMaxScore] = useState<number | ''>('');

// EKLENECEK yeni state:
const [filterSector, setFilterSector] = useState('');
const [sectors, setSectors] = useState<string[]>([]);

// api.getStocks() çağrısı zaten sector param destekliyor:
// api.getStocks({ sector: filterSector || undefined, ... })
```

<!-- Mevcut tablo sütunları — değişecekler -->
```
Mevcut: # | Hisse | Fiyat | Değişim | Hacim | P.Değeri | Sektör | Teknik | Temel | Algı | Nedensellik | Genel | Sinyal
Yeni:   # | Hisse | Fiyat | Değişim | Hacim (ratio) | Stop-Loss | Hedef | Teknik | Temel | Algı | Genel | Sinyal

DEĞİŞİKLİKLER:
- "Nedensellik" sütunu KALDIR (causal_score kaldırılıyor — AIRF-02)
- "P.Değeri" sütunu KALDIR (yer kazanmak için)
- "Sektör" sütunu KALDIR (dropdown filtre var artık)
- Stop-Loss sütunu EKLE: stock.stop_loss (TechnicalResult'tan değil, StockSummary'den geliyorsa — alan yoksa "—")
- Hedef Fiyat sütunu EKLE: stock.target_price (alan yoksa "—")
- Hacim sütunu: volume_ratio zaten var (SGNL-02'den), formatı "2.4x" olarak koru
```

<!-- Backend stocks.py'ye eklenecek endpoint örneği -->
```python
@router.get("/stocks/sectors")
async def get_stock_sectors():
    """Aktif hisselerin distinct sektör listesi."""
    async with AsyncSessionLocal() as db:
        result = await db.execute(
            select(func.distinct(Stock.sector))
            .where(Stock.is_active)
            .where(Stock.sector.isnot(None))
            .order_by(Stock.sector)
        )
        sectors = [row[0] for row in result.fetchall()]
        return {"sectors": sectors}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: backend — GET /api/stocks/sectors endpoint ekle</name>
  <files>backend/app/api/stocks.py</files>
  <action>
backend/app/api/stocks.py dosyasını oku. Mevcut router'a yeni bir endpoint ekle (per CLEN-03):

Dosyanın en altına (son @router.get dekoratöründen sonra) ekle:

```python
@router.get("/stocks/sectors")
async def get_stock_sectors():
    """Aktif hisselerin distinct sektör listesi — frontend dropdown için."""
    async with AsyncSessionLocal() as db:
        result = await db.execute(
            select(func.distinct(Stock.sector))
            .where(Stock.is_active)
            .where(Stock.sector.isnot(None))
            .order_by(Stock.sector)
        )
        sectors = [row[0] for row in result.fetchall()]
        return {"sectors": sectors}
```

ÖNEMLI: Bu endpoint'i `/stocks` route'undan ÖNCE tanımla yoksa FastAPI `/stocks/sectors` yerine `/stocks/{symbol}` pattern'ini yakalar. Dosyadaki sıralamayı kontrol et — `/stocks/{symbol}` gibi bir dynamic segment varsa, `/stocks/sectors` ondan ÖNCE gelmeli. Mevcut endpoint sıralamasını bozmadan doğru konuma yerleştir.

Mevcut imports zaten `select, func, Stock, AsyncSessionLocal` içeriyor — tekrar import etme.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "from app.api.stocks import router; routes = [r.path for r in router.routes]; print(routes)"</automated>
  </verify>
  <done>
    - /stocks/sectors route router'da kayıtlı
    - Endpoint /stocks/{symbol} dynamic route'undan ÖNCE geliyor (sıralama doğru)
    - Python syntax hatası yok (import check geçiyor)
  </done>
</task>

<task type="auto">
  <name>Task 2: api.ts — getStockSectors fonksiyonu ekle</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
frontend/src/lib/api.ts dosyasına yeni tip ve fonksiyon ekle (per CLEN-03):

1. Mevcut interface bloğuna ekle (KapNotification varsa onun yanına, yoksa MacroIndicators altına):
```typescript
export interface StockSectorsResponse {
  sectors: string[];
}
```

2. api objesine fonksiyon ekle (getSectors() zaten mevcut — o SectorSummary döndürüyor, bu farklı):
```typescript
getStockSectors: () =>
  apiFetch<StockSectorsResponse>('/stocks/sectors'),
```

NOT: Mevcut `getSectors()` fonksiyonu `/sectors` endpoint'ini çağırıyor ve SectorSummary[] döndürüyor. Yeni `getStockSectors()` farklı bir endpoint (`/stocks/sectors`) ve sadece string[] döndürüyor. İkisi birbirini karıştırma — yenisinin adı `getStockSectors`.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -10</automated>
  </verify>
  <done>
    - StockSectorsResponse tipi export ediliyor
    - api.getStockSectors() fonksiyonu mevcut, /stocks/sectors endpoint'ini çağırıyor
    - TypeScript hatası yok
  </done>
</task>

<task type="auto">
  <name>Task 3: stocks/page.tsx — sektör dropdown, glassmorphism tablo, sütun güncelleme</name>
  <files>frontend/src/app/stocks/page.tsx</files>
  <action>
Mevcut frontend/src/app/stocks/page.tsx'i oku ve şu değişiklikleri yap (per GLUI-03, CLEN-03):

**1. Sektör filtresi state ve veri çekme ekle:**
```typescript
const [filterSector, setFilterSector] = useState('');
const [sectors, setSectors] = useState<string[]>([]);

// useEffect ile sayfa yüklendiğinde sektörleri çek:
useEffect(() => {
  api.getStockSectors().then(res => setSectors(res.sectors)).catch(() => {});
}, []);
```

**2. loadStocks fonksiyonunu güncelle:**
- api.getStocks() çağrısına `sector: filterSector || undefined` ekle
- filterSector değiştiğinde loadStocks tetiklensin (useEffect dependency'e ekle)

**3. Filtre bar'a sektör dropdown'u ekle (mevcut filtrelerden ÖNCE, "Hisse ara" input'undan sonra):**
```tsx
<select
  className="search-input"
  style={{ flex: '0 0 200px', paddingLeft: 12 }}
  value={filterSector}
  onChange={(e) => setFilterSector(e.target.value)}
>
  <option value="">Tüm Sektörler</option>
  {sectors.map(s => (
    <option key={s} value={s}>{s}</option>
  ))}
</select>
```

**4. Tablo sütunlarını güncelle (per GLUI-03):**

Kaldırılacak sütunlar (thead ve tbody'den):
- "Nedensellik" başlığı ve `<ScoreRing score={stock.causal_score} />` hücresi
- "P. Değeri" başlığı ve `formatMarketCap(stock.market_cap)` hücresi
- "Sektör" başlığı ve sektör hücresi (artık dropdown filtrede var)

Eklenecek sütunlar (Sinyal'den ÖNCE):
- "Stop-Loss" başlığı: `{stock.stop_loss != null ? formatPrice(stock.stop_loss) : '—'}` (StockSummary'de stop_loss yoksa '—' göster)
- "Hedef" başlığı: `{stock.target_price != null ? formatPrice(stock.target_price) : '—'}` (yoksa '—')

NOT: StockSummary tipinde stop_loss ve target_price alanları yoksa, TypeScript hatası almamak için `(stock as any).stop_loss` kullanabilirsin veya StockSummary'ye optional field ekle:
```typescript
// api.ts StockSummary interface'ine ekle (opsiyonel):
stop_loss?: number | null;
target_price?: number | null;
```

**5. Tablo ve satır glassmorphism stili:**

Mevcut `<div className="card" style={{ overflow: 'hidden', padding: 0 }}>` elementini güncelle:
- `className="card"` yerine `className="glass-card"` kullan
- Stil: `style={{ overflow: 'hidden', padding: 0, margin: '0' }}`

Tablo satırları (`<tr>`) için hover stili:
- Mevcut `className="animate-fade-in"` koru
- style'a ekle: `background: 'rgba(255,255,255,0.02)'` ve hover'da `rgba(255,255,255,0.05)` (CSS ile veya inline onMouseEnter/Leave)
- Alternatif: globals.css'te `data-table tbody tr:hover` stili zaten varsa koru

**6. Sortlama seçeneğinden "Nedensellik Skoru" seçeneğini kaldır:**
```tsx
// Bu option'ı sil:
<option value="causal_score">Nedensellik Skoru</option>
```

MEVCUT filtreleri KORUYACAKLAR (dokunma):
- Arama input'u (search state)
- Öneri filtresi (filterRec state)
- Skor aralığı (minScore, maxScore state)
- BIST30 toggle (filterBist30 state)
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <done>
    - filterSector state ve sectors state mevcut
    - api.getStockSectors() çağrısı useEffect'te var
    - Sektör dropdown sayfada render ediliyor, "Tüm Sektörler" default seçenek
    - "Nedensellik" sütunu ve causal_score ScoreRing yok
    - Stop-Loss ve Hedef sütunları tabloda mevcut
    - Tablo container .glass-card sınıfını kullanıyor
    - Mevcut filtreler (arama, öneri, skor aralığı, BIST30) hepsi korunuyor
    - `causal_score` option'ı sort select'ten kaldırıldı
    - TypeScript hatası yok
    - `next build --no-lint` tamamlanıyor
  </done>
</task>

</tasks>

<verification>
```bash
# Backend endpoint mevcut mu?
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "
from app.api.stocks import router
paths = [r.path for r in router.routes]
print('/stocks/sectors' in paths, paths)
"

# Frontend TypeScript temiz mi?
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"
# Çıktı: 0

# causal_score referansı kaldırıldı mı?
grep -n "causal_score" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.tsx
# Çıktı: boş

# Sektör filtresi var mı?
grep -n "getStockSectors\|filterSector\|Tüm Sektörler" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/page.tsx
# En az 3 satır çıkmalı

# Build geçiyor mu?
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx next build --no-lint 2>&1 | tail -5
```
</verification>

<success_criteria>
- GET /api/stocks/sectors backend endpoint çalışıyor, distinct sektör listesi döndürüyor
- api.getStockSectors() fonksiyonu frontend'de mevcut
- Hisse listesi sayfasında sektör dropdown'u sektörleri dinamik çekiyor
- Sektör seçiminde sayfa yenilenmeden liste filtreleniyor
- "Tüm Sektörler" seçilince tüm liste geri geliyor
- "Nedensellik" sütunu ve causal_score referansları kaldırıldı
- Stop-Loss ve Hedef fiyat sütunları tabloda mevcut
- Tablo glassmorphism .glass-card container içinde
- Tüm mevcut filtreler (arama, öneri, skor aralığı, BIST30) çalışıyor
- `next build --no-lint` sıfır hata
</success_criteria>

<output>
After completion, create `/Users/nedimkopurlu/Downloads/PROJELER/stalize/.planning/phases/13-dashboard-hisse-listesi-glassmorphism/13-03-SUMMARY.md`
</output>
