---
phase: 13-dashboard-hisse-listesi-glassmorphism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
autonomous: true
requirements:
  - GLUI-01

must_haves:
  truths:
    - "Tüm sayfalarda body arka planı #0a0f1e koyu tonundadır — eski #0a0e1a değeri artık kullanılmaz"
    - "globals.css'te --glass-bg, --glass-border, --glass-blur CSS değişkenleri tanımlıdır ve mevcut değerlerin üzerine yazılır"
    - ".glass-card utility sınıfı codebase'de tanımlanmıştır — backdrop-filter, rgba border ve 16px border-radius içerir"
    - "layout.tsx'te Inter fontu next/font/google üzerinden yüklenir ve html elementine uygulanır"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Glassmorphism CSS değişkenleri ve .glass-card utility"
      contains: "--glass-bg"
    - path: "frontend/src/app/layout.tsx"
      provides: "Inter font yükleme ve body className ataması"
      contains: "Inter"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/app/globals.css"
      via: "import ./globals.css"
      pattern: "import.*globals\\.css"
    - from: "globals.css :root"
      to: ".glass-card"
      via: "var(--glass-bg) referansı"
      pattern: "var\\(--glass-bg\\)"
---

<objective>
Glassmorphism CSS temel sistemi kurulur. globals.css'teki mevcut CSS değişkenleri GLUI-01 spec'ine göre güncellenir, .glass-card utility sınıfı eklenir ve layout.tsx Inter fontunu next/font/google üzerinden yükler.

Purpose: Plan 13-02 ve 13-03'teki tüm UI bileşenleri bu CSS temelini kullanır. Bu plan olmadan diğer planlar .glass-card ve CSS değişkenlerine bağımlı olamaz.
Output: Güncellenmiş globals.css, güncellenmiş layout.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Key constraint from frontend/AGENTS.md: This is NOT the standard Next.js you know. Read node_modules/next/dist/docs/ for the version in use before writing any code. Check for breaking changes in font loading API.
</context>

<interfaces>
<!-- Mevcut globals.css :root değişkenleri (güncelleme öncesi) -->
<!-- Executor bu değerleri YENİ spec değerleriyle REPLACE edecek — silmeyecek -->

Mevcut --bg-primary: #0a0e1a  →  Yeni: #0a0f1e
Mevcut --bg-secondary: #111827  →  Yeni: #131929
Mevcut --bg-glass: rgba(17,24,39,0.5)  →  Yeni değişken adları:
  - --glass-bg: rgba(255, 255, 255, 0.05)   (YENİ — ekle)
  - --glass-border: rgba(255, 255, 255, 0.10)   (YENİ — ekle)
  - --glass-blur: blur(16px)   (YENİ — ekle)
  - --text-primary: rgba(255, 255, 255, 0.95)   (güncelle)
  - --text-secondary: rgba(255, 255, 255, 0.60)   (güncelle)
  - --accent-green: #10b981   (YENİ — ekle)
  - --accent-red: #ef4444   (zaten var --red-500 olarak, --accent-red alias ekle)
  - --accent-blue: #3b82f6   (zaten var, alias ekle)

Mevcut layout.tsx: body'de className yok, font import yok.
Yeni layout.tsx: Inter font'u next/font/google ile yükle, body'e className ata.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: globals.css — glassmorphism değişkenleri ve .glass-card utility</name>
  <files>frontend/src/app/globals.css</files>
  <action>
Mevcut globals.css dosyasını oku ve :root bloğuna aşağıdaki değişiklikleri yap (per GLUI-01):

1. --bg-primary değerini #0a0e1a'dan #0a0f1e'ye güncelle.
2. --bg-secondary değerini #111827'den #131929'a güncelle.
3. :root içinde, mevcut --bg-glass satırının ALTINA şu üç yeni değişkeni ekle:
   ```
   --glass-bg: rgba(255, 255, 255, 0.05);
   --glass-border: rgba(255, 255, 255, 0.10);
   --glass-blur: blur(16px);
   ```
4. --text-primary değerini #f1f5f9'dan rgba(255, 255, 255, 0.95)'e güncelle.
5. --text-secondary değerini #94a3b8'den rgba(255, 255, 255, 0.60)'a güncelle.
6. :root'un Accent Colors bölümüne şu üç alias ekle:
   ```
   --accent-green: #10b981;
   --accent-red: #ef4444;
   --accent-blue: #3b82f6;
   ```
7. Dosyanın en altına (mevcut utility sınıflarının sonuna) .glass-card sınıfını ekle:
   ```css
   /* ── GLASSMORPHISM UTILITIES ─────────────────────────────── */
   .glass-card {
     background: var(--glass-bg);
     backdrop-filter: var(--glass-blur);
     -webkit-backdrop-filter: var(--glass-blur);
     border: 1px solid var(--glass-border);
     border-radius: 16px;
   }

   .glass-card-hover {
     transition: border-color var(--transition-base), background var(--transition-base);
   }

   .glass-card-hover:hover {
     background: rgba(255, 255, 255, 0.08);
     border-color: rgba(255, 255, 255, 0.18);
   }
   ```

MEVCUT sınıfları (.card, .data-table vb.) SİLME — sadece yukarıdaki değişiklikleri yap. Mevcut border, shadow ve gradient değişkenlerine dokunma.
  </action>
  <verify>
    <automated>grep -n "glass-bg\|glass-border\|glass-blur\|glass-card\|#0a0f1e\|#131929" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/globals.css</automated>
  </verify>
  <done>
    - --glass-bg, --glass-border, --glass-blur değişkenleri :root'ta mevcut
    - --bg-primary #0a0f1e, --bg-secondary #131929 güncellendi
    - .glass-card sınıfı backdrop-filter ve rgba border ile tanımlı
    - --accent-green, --accent-red, --accent-blue eklendi
  </done>
</task>

<task type="auto">
  <name>Task 2: layout.tsx — Inter font yükleme ve body className</name>
  <files>frontend/src/app/layout.tsx</files>
  <action>
ÖNCE: frontend/AGENTS.md uyarısını dikkate al — bu Next.js sürümünün font API'sini node_modules/next/dist/docs/ veya package.json'dan doğrula. Ardından:

1. layout.tsx dosyasını oku.
2. next/font/google'dan Inter'i import et:
   ```typescript
   import { Inter } from 'next/font/google';

   const inter = Inter({
     subsets: ['latin'],
     variable: '--font-inter',
     display: 'swap',
   });
   ```
3. html elementine `className={inter.variable}` veya `lang="tr"` yanına ekle.
4. body elementine `className={inter.className}` ekle — mevcut body sınıfları varsa birleştir, yoksa sadece className ata.
5. Metadata'da description güncelle: "BIST100 hisselerini teknik ve temel analiz ile değerlendirin" (AI/LLM referansları kaldır per v1.2 yönü).

NOT: next/font/google API sürüme göre farklılık gösterebilir. Yükleme sırasında hata alınırsa doğrudan @import url() ile Google Fonts import'unu globals.css'te bırak (zaten mevcut) ve layout.tsx'e sadece metadata değişikliğini uygula. Inter zaten globals.css'teki @import ile yükleniyor.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx next build --no-lint 2>&1 | tail -20</automated>
  </verify>
  <done>
    - layout.tsx Inter fontu tanımlı (next/font veya mevcut @import ile)
    - body elementinde font className ataması var
    - `next build` sıfır hata ile tamamlanır
  </done>
</task>

</tasks>

<verification>
```bash
# CSS değişkenleri mevcut mu?
grep -c "glass-bg\|glass-border\|glass-blur" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/globals.css
# Çıktı: en az 3 satır

# .glass-card tanımlı mı?
grep -A 5 "\.glass-card {" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/globals.css

# Build geçiyor mu?
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx next build --no-lint 2>&1 | grep -E "error|Error|compiled"
```
</verification>

<success_criteria>
- globals.css'te 7 yeni CSS değişkeni eklendi (--glass-bg, --glass-border, --glass-blur, --accent-green, --accent-red, --accent-blue ve 2 güncelleme)
- .glass-card utility sınıfı backdop-filter: blur(16px) ve rgba border ile tanımlı
- --bg-primary #0a0f1e, --bg-secondary #131929 değerlerine güncellendi
- layout.tsx Inter font yükleme içeriyor (next/font veya @import)
- `next build --no-lint` sıfır TypeScript/build hatası
</success_criteria>

<output>
After completion, create `/Users/nedimkopurlu/Downloads/PROJELER/stalize/.planning/phases/13-dashboard-hisse-listesi-glassmorphism/13-01-SUMMARY.md`
</output>
