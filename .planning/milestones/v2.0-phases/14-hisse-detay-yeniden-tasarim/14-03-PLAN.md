---
phase: 14-hisse-detay-yeniden-tasarim
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - backend/app/api/stocks.py
  - frontend/src/lib/api.ts
  - frontend/src/components/FundamentalMetricCard.tsx
  - frontend/src/components/KAPNewsCard.tsx
  - frontend/src/app/stocks/[symbol]/page.tsx
autonomous: true
requirements:
  - CLEN-02
  - MIDT-03

must_haves:
  truths:
    - "Hisse detay sayfasında F/K, PD/DD, ROE, net marj, borç/özsermaye değerleri glassmorphism kart içinde görünür"
    - "Temel verisi tam olan hisse için hiçbir alan 'N/A' göstermez — DB'deki gerçek değerler okunur"
    - "Son 5 KAP bildirimi (başlık + tarih + kategori) hisse detay sayfasında listelenir"
    - "Her KAP bildirimi KAP.com.tr'ye dış link içerir"
    - "GET /api/stocks/{symbol}/news?limit=5 endpoint'i 200 döner ve news_items tablosundan veri çeker"
  artifacts:
    - path: "backend/app/api/stocks.py"
      provides: "GET /api/stocks/{symbol}/news?limit=5 endpoint"
      contains: "NewsItem, published_at, category, title, url"
    - path: "frontend/src/components/FundamentalMetricCard.tsx"
      provides: "F/K, PD/DD, ROE, net marj, borç/özsermaye glassmorphism kartı"
      exports: ["FundamentalMetricCard"]
    - path: "frontend/src/components/KAPNewsCard.tsx"
      provides: "Son 5 KAP bildirimi listesi — başlık + tarih + kategori + dış link"
      exports: ["KAPNewsCard"]
    - path: "frontend/src/lib/api.ts"
      provides: "KapNewsItem interface ve api.getStockNews() fonksiyonu"
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "FundamentalMetricCard ve KAPNewsCard bileşenleri metricsRow + bottomGrid placeholder'larını doldurur"
  key_links:
    - from: "frontend/src/components/FundamentalMetricCard.tsx"
      to: "backend/app/models/fundamental.py"
      via: "GET /api/stocks/{symbol} — stock.fundamentals ilişkisi üzerinden veya ayrı fundamentals endpoint"
    - from: "frontend/src/components/KAPNewsCard.tsx"
      to: "backend/app/api/stocks.py"
      via: "GET /api/stocks/{symbol}/news?limit=5 → news_items tablosu"
    - from: "frontend/src/app/stocks/[symbol]/page.tsx"
      to: "frontend/src/components/FundamentalMetricCard.tsx"
      via: "detail.stock prop geçilir — pe_ratio, pb_ratio, roe, net_margin, debt_equity alanları"
---

<objective>
İki paralel iş: (1) Temel Metrik glassmorphism kartı — F/K, PD/DD, ROE, net marj, borç/özsermaye verilerini DB'den okur; (2) KAP haber endpoint'i + frontend bileşeni — son 5 KAP bildirimi news_items tablosundan çekilir, başlık + tarih + kategori + dış link gösterir.

Purpose: CLEN-02 ve MIDT-03 gereksinimleri — hisse detay sayfasında tam temel veri + KAP bildirim listesi.

Output:
- backend/app/api/stocks.py: GET /api/stocks/{symbol}/news endpoint eklendi
- frontend/src/lib/api.ts: KapNewsItem türü ve api.getStockNews() eklendi
- frontend/src/components/FundamentalMetricCard.tsx: Yeni glassmorphism bileşeni
- frontend/src/components/KAPNewsCard.tsx: Yeni KAP haber listesi bileşeni
- frontend/src/app/stocks/[symbol]/page.tsx: Placeholder'lar gerçek bileşenlerle değiştirildi
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

@backend/app/api/stocks.py
@backend/app/models/news.py
@backend/app/models/fundamental.py
@backend/app/models/stock.py
@frontend/src/lib/api.ts
@frontend/src/app/stocks/[symbol]/page.tsx
@frontend/src/app/globals.css

<interfaces>
<!-- Mevcut DB modelleri — executor doğrudan kullanır -->

From backend/app/models/fundamental.py:
```python
class Fundamental(Base):
    __tablename__ = "fundamentals"
    id: int (PK)
    stock_id: int (FK → stocks.id)
    period: str           # "2024-Q4", "2024-FY"
    period_type: str      # "quarterly" | "annual"
    pe_ratio: float       # F/K
    pb_ratio: float       # PD/DD
    roe: float            # Özsermaye Karlılığı
    net_margin: float     # Net Kar Marjı
    debt_to_equity: float # Borç/Özsermaye
    # ... diğer alanlar
```

From backend/app/models/news.py:
```python
class NewsItem(Base):
    __tablename__ = "news_items"
    id: int
    stock_id: int | None  # nullable (genel piyasa haberleri için)
    title: str
    summary: str | None
    url: str | None       # KAP'a dış link buradan
    source: str | None    # "KAP", "Bloomberg HT" vb.
    category: str | None  # "geopolitics", "macro", "sector", "company"
    published_at: datetime | None
    sentiment_score: float | None
    # ... diğer alanlar
```

From backend/app/api/stocks.py (mevcut import'lar):
```python
from sqlalchemy import select, func, and_
from app.core.database import AsyncSessionLocal
from app.models.stock import Stock
from app.models.price import PriceHistory
# Eklenecek:
from app.models.news import NewsItem
from app.models.fundamental import Fundamental
```

From backend/app/models/stock.py (ORM ilişkileri):
```python
class Stock(Base):
    fundamentals = relationship("Fundamental", back_populates="stock", cascade="all, delete-orphan")
    news_items = relationship("NewsItem", back_populates="stock", cascade="all, delete-orphan")
```

<!-- StockDetail şu an fundamentals alanı içermiyor — backend'de GET /api/stocks/{symbol}
     endpoint'i fundamental verileri dönmüyor. Bu plan fundamentals için ayrı bir sorgu yapar. -->

From frontend/src/lib/api.ts (mevcut StockDetail):
```typescript
export interface StockDetail {
  stock: StockSummary & { currency: string; last_data_update: string | null };
  recent_prices: PricePoint[];
  // fundamentals yok — ayrı endpoint veya get_stock_detail güncellenmesi gerekecek
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — GET /api/stocks/{symbol}/news endpoint + GET /api/stocks/{symbol}/fundamentals</name>
  <files>backend/app/api/stocks.py</files>
  <action>
`backend/app/api/stocks.py` dosyasına iki yeni endpoint ekle.

**Import güncellemesi (dosyanın üstüne ekle):**
```python
from app.models.news import NewsItem
from app.models.fundamental import Fundamental
```

**Endpoint 1: GET /api/stocks/{symbol}/news**

Mevcut `@router.get("/stocks/{symbol}/technical")` endpoint'inin altına ekle:

```python
@router.get("/stocks/{symbol}/news")
async def get_stock_news(
    symbol: str,
    limit: int = Query(5, ge=1, le=20),
):
    """Hisse için son KAP haberleri — news_items tablosundan."""
    async with AsyncSessionLocal() as db:
        # Sembol ile hisseyi bul
        stock_result = await db.execute(
            select(Stock).where(Stock.symbol == symbol.upper())
        )
        stock = stock_result.scalar_one_or_none()
        if not stock:
            raise HTTPException(status_code=404, detail=f"Hisse bulunamadı: {symbol}")

        # Son N haber — stock_id ile eşleşen, published_at desc
        news_result = await db.execute(
            select(NewsItem)
            .where(NewsItem.stock_id == stock.id)
            .order_by(NewsItem.published_at.desc().nullslast())
            .limit(limit)
        )
        news_items = news_result.scalars().all()

    return {
        "symbol": stock.symbol,
        "news": [
            {
                "id": item.id,
                "title": item.title,
                "summary": item.summary,
                "url": item.url,
                "source": item.source,
                "category": item.category,
                "published_at": item.published_at.isoformat() if item.published_at else None,
                "sentiment_score": item.sentiment_score,
                "sentiment_label": item.sentiment_label if hasattr(item, 'sentiment_label') else None,
            }
            for item in news_items
        ],
        "count": len(news_items),
    }
```

**Endpoint 2: GET /api/stocks/{symbol}/fundamentals**

Temel metrik verisi için — en son fundamentals kaydını döner:

```python
@router.get("/stocks/{symbol}/fundamentals")
async def get_stock_fundamentals(symbol: str):
    """Hisse için en güncel temel analiz verileri."""
    async with AsyncSessionLocal() as db:
        stock_result = await db.execute(
            select(Stock).where(Stock.symbol == symbol.upper())
        )
        stock = stock_result.scalar_one_or_none()
        if not stock:
            raise HTTPException(status_code=404, detail=f"Hisse bulunamadı: {symbol}")

        # En son period kaydını al (updated_at desc)
        fund_result = await db.execute(
            select(Fundamental)
            .where(Fundamental.stock_id == stock.id)
            .order_by(Fundamental.updated_at.desc())
            .limit(1)
        )
        fund = fund_result.scalar_one_or_none()

        if not fund:
            return {
                "symbol": stock.symbol,
                "period": None,
                "pe_ratio": None,
                "pb_ratio": None,
                "roe": None,
                "net_margin": None,
                "debt_to_equity": None,
            }

    return {
        "symbol": stock.symbol,
        "period": fund.period,
        "pe_ratio": fund.pe_ratio,
        "pb_ratio": fund.pb_ratio,
        "roe": fund.roe,
        "net_margin": fund.net_margin,
        "debt_to_equity": fund.debt_to_equity,
        "fundamental_score": fund.fundamental_score,
    }
```

**Not:** stocks.py dosyasının mevcut `@router.get("/stocks/{symbol}/prices")` ve `@router.get("/stocks/{symbol}/technical")` endpoint'leri korunur — değiştirilmez.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && python -c "
import ast, sys
with open('backend/app/api/stocks.py') as f:
    src = f.read()
assert '/news' in src, 'news endpoint eksik'
assert '/fundamentals' in src, 'fundamentals endpoint eksik'
assert 'NewsItem' in src, 'NewsItem import eksik'
ast.parse(src)
print('OK: Python parse basarili, endpoint tanımlari mevcut')
" 2>&1</automated>
  </verify>
  <done>
    - GET /api/stocks/{symbol}/news endpoint stocks.py'de mevcut
    - GET /api/stocks/{symbol}/fundamentals endpoint stocks.py'de mevcut
    - NewsItem ve Fundamental import edilmiş
    - Python syntax hatası yok
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — FundamentalMetricCard + KAPNewsCard + api.ts + page.tsx entegrasyonu</name>
  <files>
    frontend/src/lib/api.ts,
    frontend/src/components/FundamentalMetricCard.tsx,
    frontend/src/components/KAPNewsCard.tsx,
    frontend/src/app/stocks/[symbol]/page.tsx
  </files>
  <action>
**Adım A — api.ts güncellemesi**

`api.ts` dosyasına yeni tür ve fonksiyon ekle:

```typescript
// KAP haber türü — ekle (PortfolioResponse'un altına):
export interface KapNewsItem {
  id: number;
  title: string;
  summary: string | null;
  url: string | null;
  source: string | null;
  category: string | null;
  published_at: string | null;
  sentiment_score: number | null;
  sentiment_label: string | null;
}

export interface StockNewsResponse {
  symbol: string;
  news: KapNewsItem[];
  count: number;
}

export interface StockFundamentals {
  symbol: string;
  period: string | null;
  pe_ratio: number | null;
  pb_ratio: number | null;
  roe: number | null;
  net_margin: number | null;
  debt_to_equity: number | null;
  fundamental_score: number | null;
}
```

`api` nesnesine iki fonksiyon ekle (`getStockTechnical` satırının altına):
```typescript
getStockNews: (symbol: string, limit: number = 5) =>
  apiFetch<StockNewsResponse>(`/stocks/${symbol}/news?limit=${limit}`),

getStockFundamentals: (symbol: string) =>
  apiFetch<StockFundamentals>(`/stocks/${symbol}/fundamentals`),
```

**Adım B — FundamentalMetricCard.tsx oluştur**

```typescript
'use client';

import type { StockFundamentals } from '@/lib/api';

interface Props {
  fundamentals: StockFundamentals | null;
  loading?: boolean;
}

function MetricRow({ label, value, unit = '', decimals = 2 }: {
  label: string;
  value: number | null | undefined;
  unit?: string;
  decimals?: number;
}) {
  const formatted = value != null
    ? `${value.toFixed(decimals)}${unit}`
    : '—';
  const isNull = value == null;
  return (
    <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: '8px 0', borderBottom: '1px solid rgba(148,163,184,0.06)' }}>
      <span style={{ color: 'var(--text-muted)', fontSize: '0.82rem' }}>{label}</span>
      <span style={{ fontFamily: 'monospace', fontWeight: 600, fontSize: '0.9rem', color: isNull ? 'var(--text-muted)' : 'var(--text-primary)' }}>
        {formatted}
      </span>
    </div>
  );
}

export default function FundamentalMetricCard({ fundamentals, loading }: Props) {
  if (loading) {
    return (
      <div>
        <div style={{ fontSize: '0.75rem', fontWeight: 600, letterSpacing: '0.08em', textTransform: 'uppercase', color: 'var(--text-muted)', marginBottom: 12 }}>
          Temel Metrikler
        </div>
        <p style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Yükleniyor...</p>
      </div>
    );
  }

  if (!fundamentals) {
    return (
      <div>
        <div style={{ fontSize: '0.75rem', fontWeight: 600, letterSpacing: '0.08em', textTransform: 'uppercase', color: 'var(--text-muted)', marginBottom: 12 }}>
          Temel Metrikler
        </div>
        <p style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Temel veri bulunamadı</p>
      </div>
    );
  }

  return (
    <div>
      <div style={{ fontSize: '0.75rem', fontWeight: 600, letterSpacing: '0.08em', textTransform: 'uppercase', color: 'var(--text-muted)', marginBottom: 12 }}>
        Temel Metrikler
        {fundamentals.period && (
          <span style={{ marginLeft: 8, fontWeight: 400, fontSize: '0.7rem', opacity: 0.7 }}>({fundamentals.period})</span>
        )}
      </div>
      <MetricRow label="F/K (P/E)" value={fundamentals.pe_ratio} />
      <MetricRow label="PD/DD (P/B)" value={fundamentals.pb_ratio} />
      <MetricRow label="ROE" value={fundamentals.roe != null ? fundamentals.roe * 100 : null} unit="%" decimals={1} />
      <MetricRow label="Net Marj" value={fundamentals.net_margin != null ? fundamentals.net_margin * 100 : null} unit="%" decimals={1} />
      <MetricRow label="Borç/Özsermaye" value={fundamentals.debt_to_equity} />
    </div>
  );
}
```

**Adım C — KAPNewsCard.tsx oluştur**

```typescript
'use client';

import type { KapNewsItem } from '@/lib/api';

interface Props {
  news: KapNewsItem[];
  loading?: boolean;
}

function categoryLabel(cat: string | null): string {
  const map: Record<string, string> = {
    geopolitics: 'Jeopolitik',
    macro: 'Makro',
    sector: 'Sektör',
    company: 'Şirket',
  };
  return cat ? (map[cat] ?? cat) : 'Genel';
}

function formatDate(iso: string | null): string {
  if (!iso) return '—';
  try {
    return new Date(iso).toLocaleDateString('tr-TR', { day: '2-digit', month: 'short', year: 'numeric' });
  } catch {
    return iso;
  }
}

export default function KAPNewsCard({ news, loading }: Props) {
  const header = (
    <div style={{ fontSize: '0.75rem', fontWeight: 600, letterSpacing: '0.08em', textTransform: 'uppercase', color: 'var(--text-muted)', marginBottom: 12 }}>
      Son KAP Bildirimleri
    </div>
  );

  if (loading) {
    return <div>{header}<p style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Yükleniyor...</p></div>;
  }

  if (news.length === 0) {
    return <div>{header}<p style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Henüz bildirim yok</p></div>;
  }

  return (
    <div>
      {header}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
        {news.map((item) => (
          <div key={item.id} style={{ padding: '10px 0', borderBottom: '1px solid rgba(148,163,184,0.06)' }}>
            {/* Başlık — tıklanabilir dış link */}
            {item.url ? (
              <a
                href={item.url}
                target="_blank"
                rel="noopener noreferrer"
                style={{ color: 'var(--text-primary)', fontSize: '0.85rem', fontWeight: 500, lineHeight: 1.4, textDecoration: 'none', display: 'block', marginBottom: 4 }}
                onMouseEnter={e => (e.currentTarget.style.color = 'var(--accent-cyan)')}
                onMouseLeave={e => (e.currentTarget.style.color = 'var(--text-primary)')}
              >
                {item.title}
              </a>
            ) : (
              <span style={{ color: 'var(--text-primary)', fontSize: '0.85rem', fontWeight: 500, lineHeight: 1.4, display: 'block', marginBottom: 4 }}>
                {item.title}
              </span>
            )}
            {/* Meta: tarih + kategori */}
            <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
              <span style={{ fontSize: '0.72rem', color: 'var(--text-muted)' }}>{formatDate(item.published_at)}</span>
              {item.category && (
                <span style={{
                  fontSize: '0.65rem', fontWeight: 600, padding: '1px 6px', borderRadius: 4,
                  background: 'rgba(148,163,184,0.08)', color: 'var(--text-muted)',
                  textTransform: 'uppercase', letterSpacing: '0.05em',
                }}>
                  {categoryLabel(item.category)}
                </span>
              )}
              {item.source && (
                <span style={{ fontSize: '0.65rem', color: 'var(--text-muted)', opacity: 0.7 }}>{item.source}</span>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

**Adım D — page.tsx güncelleme**

`page.tsx` dosyasında şu değişiklikleri yap:

1. Import'lara ekle:
   ```typescript
   import FundamentalMetricCard from '@/components/FundamentalMetricCard';
   import KAPNewsCard from '@/components/KAPNewsCard';
   import type { StockFundamentals, KapNewsItem } from '@/lib/api';
   ```

2. State'lere ekle (mevcut state'lerin yanına):
   ```typescript
   const [fundamentals, setFundamentals] = useState<StockFundamentals | null>(null);
   const [news, setNews] = useState<KapNewsItem[]>([]);
   const [fundLoading, setFundLoading] = useState(true);
   const [newsLoading, setNewsLoading] = useState(true);
   ```

3. `loadStock` (veya eşdeğer) içinde paralel fetch'e ekle:
   ```typescript
   // Promise.all içine ya da ayrı useEffect içinde:
   api.getStockFundamentals(symbol)
     .then(f => setFundamentals(f))
     .catch(() => setFundamentals(null))
     .finally(() => setFundLoading(false));

   api.getStockNews(symbol, 5)
     .then(r => setNews(r.news))
     .catch(() => setNews([]))
     .finally(() => setNewsLoading(false));
   ```

4. metricsRow içindeki "Temel Metrikler" placeholder'ını değiştir:
   ```jsx
   <div className={styles.glassCard} style={{ flex: 1 }}>
     <FundamentalMetricCard fundamentals={fundamentals} loading={fundLoading} />
   </div>
   ```

5. bottomGrid içindeki "Son KAP Bildirimleri" placeholder'ını değiştir:
   ```jsx
   <div className={styles.glassCard}>
     <KAPNewsCard news={news} loading={newsLoading} />
   </div>
   ```
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - TypeScript derleme sıfır hata
    - FundamentalMetricCard.tsx ve KAPNewsCard.tsx dosyaları mevcut ve export default var
    - api.ts'te KapNewsItem, StockNewsResponse, StockFundamentals türleri ve getStockNews(), getStockFundamentals() fonksiyonları mevcut
    - page.tsx her iki bileşeni import edip kullanıyor
    - F/K, PD/DD, ROE, net marj, borç/özsermaye MetricRow'ları FundamentalMetricCard'da mevcut
    - KAPNewsCard: her item için url varsa dış link (target="_blank"), yoksa düz metin
  </done>
</task>

</tasks>

<verification>
```bash
# Backend endpoint kontrolü
grep -n "stocks.*news\|stocks.*fundamentals" /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/app/api/stocks.py

# Python syntax kontrolü
python -c "
import ast
with open('/Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/app/api/stocks.py') as f:
    ast.parse(f.read())
print('OK: Python syntax clean')
"

# Frontend TypeScript
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit

# Bileşen varlık kontrolü
ls /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/components/FundamentalMetricCard.tsx
ls /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/components/KAPNewsCard.tsx

# api.ts yeni türler kontrolü
grep -c "KapNewsItem\|StockFundamentals\|getStockNews\|getStockFundamentals" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/lib/api.ts
```
</verification>

<success_criteria>
- GET /api/stocks/{symbol}/news?limit=5 → 200 döner, news[] dizisi mevcut (boş bile olsa)
- GET /api/stocks/{symbol}/fundamentals → 200 döner, pe_ratio/pb_ratio/roe/net_margin/debt_to_equity alanları mevcut (null olabilir)
- FundamentalMetricCard: 5 MetricRow (F/K, PD/DD, ROE, net marj, borç/özsermaye) glassmorphism kart içinde render edilir
- KAPNewsCard: news boşsa "Henüz bildirim yok"; doluysa başlık + tarih + kategori listesi; url varsa KAP dış linki
- TypeScript derleme temiz
- page.tsx metricsRow[0] ve bottomGrid[1] placeholder'ları gerçek bileşenlerle doldu
</success_criteria>

<output>
Tamamlanınca `.planning/phases/14-hisse-detay-yeniden-tasarim/14-03-SUMMARY.md` oluştur.
</output>
