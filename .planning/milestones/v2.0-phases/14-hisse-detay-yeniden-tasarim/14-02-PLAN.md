---
phase: 14-hisse-detay-yeniden-tasarim
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - backend/app/api/stocks.py
  - backend/app/services/technical.py
  - frontend/src/lib/api.ts
  - frontend/src/components/CandlestickEMAPanel.tsx
  - frontend/src/app/stocks/[symbol]/page.tsx
autonomous: true
requirements:
  - VIZZ-01

must_haves:
  truths:
    - "GET /api/stocks/{symbol}/technical endpoint'i yanıtında ema_50 ve ema_200 dizileri mevcut — her eleman {date: string, value: number} formatında"
    - "Hisse detay sayfasında candlestick grafiği üzerinde turuncu EMA 50 ve mavi EMA 200 overlay çizgileri görünür"
    - "Grafik altında hacim bar grafiği paneli render edilir; ortalamanın üzerindeki hacimler vurgulanır"
    - "Grafik altında RSI paneli render edilir; 70 ve 30 seviyelerinde yatay referans çizgileri görünür"
  artifacts:
    - path: "backend/app/services/technical.py"
      provides: "analyze_stock dönüşüne ema_50[] ve ema_200[] eklendi"
      contains: "_build_ema_series"
    - path: "backend/app/api/stocks.py"
      provides: "GET /api/stocks/{symbol}/technical — değişiklik yok (technical_engine.analyze_stock sonucunu pass-through eder)"
    - path: "frontend/src/components/CandlestickEMAPanel.tsx"
      provides: "lightweight-charts v5: CandlestickSeries + 2 LineSeries (EMA) + HistogramSeries (hacim) + LineSeries (RSI) ayrı paneller"
    - path: "frontend/src/lib/api.ts"
      provides: "TechnicalResult interface'e ema_50 ve ema_200 alanları eklendi"
  key_links:
    - from: "frontend/src/components/CandlestickEMAPanel.tsx"
      to: "technical.ema_50 / technical.ema_200"
      via: "props olarak alınan EMA dizileri LineSeries.setData() ile beslenir"
    - from: "frontend/src/components/CandlestickEMAPanel.tsx"
      to: "prices (PricePoint[])"
      via: "hacim HistogramSeries ve RSI LineSeries prices dizisinden hesaplanır"
    - from: "backend/app/services/technical.py"
      to: "backend/app/api/stocks.py"
      via: "analyze_stock() dönüş dict'ine ema_50 ve ema_200 key'leri eklenir; endpoint pass-through yapar"
---

<objective>
Backend technical analiz endpoint'ine EMA 50 ve EMA 200 dizi verisini ekler; frontend'de lightweight-charts v5 kullanarak EMA overlay'li candlestick grafiği, hacim paneli ve RSI panelini tek bileşen olarak oluşturur.

Purpose: VIZZ-01 — Hisse detay sayfası candlestick grafiği, EMA 50/200 overlay çizgileri, hacim histogramı ve RSI paneli ile kullanıcıya teknik görünüm sağlar.

Output:
- backend/app/services/technical.py: analyze_stock() dönüşüne ema_50[], ema_200[] eklendi
- frontend/src/lib/api.ts: TechnicalResult güncellemesi
- frontend/src/components/CandlestickEMAPanel.tsx: Yeni grafik bileşeni
- frontend/src/app/stocks/[symbol]/page.tsx: Grafik bölümü CandlestickEMAPanel ile güncellendi
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

@backend/app/services/technical.py
@backend/app/api/stocks.py
@frontend/src/components/CandlestickPanel.tsx
@frontend/src/lib/api.ts
@frontend/src/app/stocks/[symbol]/page.tsx

<interfaces>
<!-- lightweight-charts v5 API — mevcut CandlestickPanel.tsx'ten alındı -->

```typescript
// v5 import şekli (mevcut CandlestickPanel.tsx'te bu pattern kullanılıyor):
import { createChart, CandlestickSeries, ColorType } from 'lightweight-charts';
// LineSeries ve HistogramSeries de aynı paket'ten import edilir:
import { createChart, CandlestickSeries, LineSeries, HistogramSeries, ColorType } from 'lightweight-charts';

// v5'te seri ekleme — addSeries(SeriesType, options) şeklinde:
const candleSeries = chart.addSeries(CandlestickSeries, { upColor: '#22c55e', downColor: '#ef4444', ... });
const emaSeries   = chart.addSeries(LineSeries, { color: '#f97316', lineWidth: 2, priceLineVisible: false });
const volSeries   = chart.addSeries(HistogramSeries, { color: '#3b82f6', priceScaleId: 'volume' });
const rsiSeries   = chart.addSeries(LineSeries, { color: '#a855f7', priceScaleId: 'rsi', lineWidth: 1.5 });

// Ayrı fiyat eksenleri (alt paneller):
chart.applyOptions({
  rightPriceScale: { scaleMargins: { top: 0.3, bottom: 0.35 } },  // ana fiyat
});
// Hacim paneli:
candleSeries.priceScale().applyOptions({ scaleMargins: { top: 0.0, bottom: 0.35 } });
volSeries.priceScale().applyOptions({ scaleMargins: { top: 0.75, bottom: 0.0 } });
// RSI paneli (eğer ayrı eksen kullanılırsa):
rsiSeries.priceScale().applyOptions({ scaleMargins: { top: 0.5, bottom: 0.0 } });

// Referans çizgileri (RSI 70/30):
rsiSeries.createPriceLine({ price: 70, color: 'rgba(239,68,68,0.4)', lineWidth: 1, lineStyle: 2 });
rsiSeries.createPriceLine({ price: 30, color: 'rgba(34,197,94,0.4)', lineWidth: 1, lineStyle: 2 });

// Veri formatı:
// Candlestick: { time: 'YYYY-MM-DD', open, high, low, close }
// LineSeries:  { time: 'YYYY-MM-DD', value: number }
// Histogram:   { time: 'YYYY-MM-DD', value: number, color?: string }
```

<!-- Backend: technical.py'deki mevcut EMA hesabı -->
```python
# technical.py — calculate_indicators() içinde zaten hesaplanıyor:
df["ema_12"] = ta.trend.ema_indicator(close, window=12)
df["ema_26"] = ta.trend.ema_indicator(close, window=26)
df["ema_50"] = ta.trend.ema_indicator(close, window=50)
# ema_200 eksik — eklenecek:
df["ema_200"] = ta.trend.ema_indicator(close, window=200)

# analyze_stock() dönüş dict'i (şu anki hali):
return {
    "symbol": symbol,
    "score": score,
    "recommendation": recommendation,
    "signals": signals["signals"],
    "support": ..., "resistance": ...,
    "stop_loss": stop_loss,
    "target_price": target_price,
    "indicators": { "last_close": ..., "rsi_14": ..., ... },
    # YENİ — ema_50 ve ema_200 ekleneceği yer:
    # "ema_50": [{"date": "2024-01-15", "value": 123.45}, ...],
    # "ema_200": [{"date": "2024-01-15", "value": 120.10}, ...],
}
```

<!-- Frontend TechnicalResult interface (api.ts'teki mevcut hali) -->
```typescript
export interface TechnicalResult {
  symbol: string;
  score: number;
  recommendation: string;
  signals: Array<{ type: string; name: string; direction: string; strength: number }>;
  support: number | null;
  resistance: number | null;
  stop_loss: number | null;
  target_price: number | null;
  indicators: Record<string, number | null>;
  // Eklenecek:
  // ema_50?: Array<{ date: string; value: number }>;
  // ema_200?: Array<{ date: string; value: number }>;
}
```

<!-- PricePoint (hacim + RSI için prices dizisinden kullanılacak) -->
```typescript
export interface PricePoint {
  date: string; open: number; high: number; low: number; close: number; volume: number;
  rsi_14?: number | null;
  // ... diğer alanlar
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — EMA 50/200 dizilerini technical endpoint yanıtına ekle</name>
  <files>backend/app/services/technical.py</files>
  <action>
İki değişiklik:

**1. calculate_indicators() içine ema_200 ekle:**
`df["ema_50"]` satırının hemen altına:
```python
df["ema_200"] = ta.trend.ema_indicator(close, window=200)
```

**2. analyze_stock() içine _build_ema_series helper ve dönüş key'leri ekle:**

`analyze_stock()` metoduna `return` ifadesinden önce şu yardımcı fonksiyon çağrısını ekle:

```python
def _build_ema_series(df: pd.DataFrame, column: str) -> list:
    """Belirtilen EMA sütununu [{date, value}] listesine dönüştürür. NaN atlanır."""
    result = []
    for _, row in df.iterrows():
        val = row.get(column)
        if val is not None and not pd.isna(val):
            result.append({
                "date": row["date"].isoformat() if hasattr(row["date"], "isoformat") else str(row["date"]),
                "value": round(float(val), 4),
            })
    return result
```

Bu bir instance metodu değil — `TechnicalAnalysisEngine` sınıfının dışına veya `analyze_stock` içine bir iç fonksiyon olarak ekle.

`analyze_stock()` dönüş dict'ine şu iki key'i ekle:
```python
return {
    # ... mevcut key'ler aynen korunur ...
    "ema_50": _build_ema_series(df, "ema_50"),
    "ema_200": _build_ema_series(df, "ema_200"),
}
```

**Dikkat:** EMA 200 için en az 200 bar veri gerekir. Daha az verisi olan hisseler için boş liste döner (`[]`) — bu beklenen davranış, frontend bunu handle eder.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize && python -c "
from backend.app.services.technical import TechnicalAnalysisEngine
e = TechnicalAnalysisEngine()
import inspect
src = inspect.getsource(e.analyze_stock)
assert 'ema_50' in src, 'ema_50 missing from analyze_stock return'
assert 'ema_200' in src, 'ema_200 missing from analyze_stock return'
print('OK: ema_50 and ema_200 in analyze_stock')
" 2>&1</automated>
  </verify>
  <done>
    - calculate_indicators() df["ema_200"] hesaplar
    - analyze_stock() dönüş dict'inde "ema_50" ve "ema_200" key'leri mevcut
    - Her key [{date, value}] listesi döner; eksik veri için [] döner
    - Python import hatası yok
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — CandlestickEMAPanel bileşeni + api.ts güncelleme + page.tsx entegrasyonu</name>
  <files>
    frontend/src/lib/api.ts,
    frontend/src/components/CandlestickEMAPanel.tsx,
    frontend/src/app/stocks/[symbol]/page.tsx
  </files>
  <action>
**Adım A — api.ts: TechnicalResult güncellemesi**

`TechnicalResult` interface'ine iki opsiyonel alan ekle:
```typescript
export interface TechnicalResult {
  // ... mevcut alanlar aynen korunur ...
  ema_50?: Array<{ date: string; value: number }>;
  ema_200?: Array<{ date: string; value: number }>;
}
```

**Adım B — CandlestickEMAPanel.tsx: Yeni bileşen yaz**

`frontend/src/components/CandlestickEMAPanel.tsx` oluştur:

```typescript
'use client';

import { useEffect, useRef } from 'react';
import { createChart, CandlestickSeries, LineSeries, HistogramSeries, ColorType } from 'lightweight-charts';
import type { PricePoint } from '@/lib/api';

interface EMAPoint {
  date: string;
  value: number;
}

interface CandlestickEMAPanelProps {
  prices: PricePoint[];
  ema50?: EMAPoint[];
  ema200?: EMAPoint[];
}

export default function CandlestickEMAPanel({ prices, ema50 = [], ema200 = [] }: CandlestickEMAPanelProps) {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const container = containerRef.current;
    if (!container || prices.length === 0) return;

    // Hacim ortalaması — üstündekileri vurgula
    const volumes = prices.map(p => p.volume ?? 0);
    const avgVol = volumes.reduce((a, b) => a + b, 0) / (volumes.length || 1);

    const chart = createChart(container, {
      layout: {
        background: { type: ColorType.Solid, color: 'transparent' },
        textColor: '#94a3b8',
      },
      grid: {
        vertLines: { color: 'rgba(148,163,184,0.06)' },
        horzLines: { color: 'rgba(148,163,184,0.06)' },
      },
      crosshair: {
        vertLine: { color: 'rgba(148,163,184,0.3)' },
        horzLine: { color: 'rgba(148,163,184,0.3)' },
      },
      width: container.clientWidth,
      height: 480,
      timeScale: {
        borderColor: 'rgba(148,163,184,0.1)',
        timeVisible: false,
      },
    });

    // ── Candlestick serisi ──
    const candleSeries = chart.addSeries(CandlestickSeries, {
      upColor: '#22c55e',
      downColor: '#ef4444',
      borderVisible: false,
      wickUpColor: '#22c55e',
      wickDownColor: '#ef4444',
    });
    candleSeries.priceScale().applyOptions({
      scaleMargins: { top: 0.05, bottom: 0.40 },
    });

    const candleData = prices
      .filter(p => p.open != null && p.high != null && p.low != null && p.close != null)
      .map(p => ({ time: p.date as string, open: p.open, high: p.high, low: p.low, close: p.close }));
    candleSeries.setData(candleData);

    // ── EMA 50 — turuncu ──
    if (ema50.length > 0) {
      const ema50Series = chart.addSeries(LineSeries, {
        color: '#f97316',
        lineWidth: 2,
        priceLineVisible: false,
        lastValueVisible: false,
        crosshairMarkerVisible: false,
      });
      ema50Series.priceScale().applyOptions({
        scaleMargins: { top: 0.05, bottom: 0.40 },
      });
      ema50Series.setData(ema50.map(e => ({ time: e.date as string, value: e.value })));
    }

    // ── EMA 200 — mavi ──
    if (ema200.length > 0) {
      const ema200Series = chart.addSeries(LineSeries, {
        color: '#3b82f6',
        lineWidth: 2,
        priceLineVisible: false,
        lastValueVisible: false,
        crosshairMarkerVisible: false,
      });
      ema200Series.priceScale().applyOptions({
        scaleMargins: { top: 0.05, bottom: 0.40 },
      });
      ema200Series.setData(ema200.map(e => ({ time: e.date as string, value: e.value })));
    }

    // ── Hacim Histogram ──
    const volSeries = chart.addSeries(HistogramSeries, {
      priceScaleId: 'volume',
      priceLineVisible: false,
      lastValueVisible: false,
    });
    volSeries.priceScale().applyOptions({
      scaleMargins: { top: 0.72, bottom: 0.14 },
    });
    const volData = prices
      .filter(p => p.volume != null)
      .map(p => ({
        time: p.date as string,
        value: p.volume ?? 0,
        color: (p.volume ?? 0) > avgVol ? 'rgba(59,130,246,0.8)' : 'rgba(59,130,246,0.35)',
      }));
    volSeries.setData(volData);

    // ── RSI Çizgisi ──
    const rsiPoints = prices.filter(p => p.rsi_14 != null);
    if (rsiPoints.length > 0) {
      const rsiSeries = chart.addSeries(LineSeries, {
        color: '#a855f7',
        lineWidth: 1.5,
        priceScaleId: 'rsi',
        priceLineVisible: false,
        lastValueVisible: false,
      });
      rsiSeries.priceScale().applyOptions({
        scaleMargins: { top: 0.84, bottom: 0.0 },
      });
      rsiSeries.setData(rsiPoints.map(p => ({ time: p.date as string, value: p.rsi_14! })));
      // RSI referans çizgileri
      rsiSeries.createPriceLine({ price: 70, color: 'rgba(239,68,68,0.45)', lineWidth: 1, lineStyle: 2, axisLabelVisible: false, title: '' });
      rsiSeries.createPriceLine({ price: 30, color: 'rgba(34,197,94,0.45)', lineWidth: 1, lineStyle: 2, axisLabelVisible: false, title: '' });
    }

    chart.timeScale().fitContent();

    const observer = new ResizeObserver(() => {
      if (container) chart.applyOptions({ width: container.clientWidth });
    });
    observer.observe(container);

    return () => {
      observer.disconnect();
      chart.remove();
    };
  }, [prices, ema50, ema200]);

  if (prices.length === 0) {
    return (
      <div style={{ minHeight: 480, display: 'flex', alignItems: 'center', justifyContent: 'center', color: 'var(--text-muted)' }}>
        Fiyat verisi yükleniyor...
      </div>
    );
  }

  return <div ref={containerRef} style={{ width: '100%', minHeight: 480 }} />;
}
```

**Önemli:** lightweight-charts v5 bileşen API'sini `node_modules/next/dist/docs/` altındaki Next.js belgelerinden doğrula. Mevcut CandlestickPanel.tsx'teki `chart.addSeries(CandlestickSeries, ...)` pattern'i v5'te çalışıyor — aynı pattern'i kullan.

**Adım C — page.tsx: CandlestickPanel'i CandlestickEMAPanel ile değiştir**

`frontend/src/app/stocks/[symbol]/page.tsx` içinde:

1. Import satırını değiştir:
   ```typescript
   // Eski: import CandlestickPanel from '@/components/CandlestickPanel';
   // Yeni:
   import CandlestickEMAPanel from '@/components/CandlestickEMAPanel';
   ```

2. Grafik bölümündeki bileşen çağrısını güncelle:
   ```jsx
   // Eski: <CandlestickPanel prices={prices?.prices ?? []} />
   // Yeni:
   <CandlestickEMAPanel
     prices={prices?.prices ?? []}
     ema50={technical?.ema_50 ?? []}
     ema200={technical?.ema_200 ?? []}
   />
   ```
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>
    - TypeScript derleme sıfır hata
    - CandlestickEMAPanel.tsx dosyası mevcut ve export default var
    - api.ts TechnicalResult interface'inde ema_50? ve ema_200? alanları var
    - page.tsx CandlestickEMAPanel import ediyor, ema50 ve ema200 props geçiyor
    - CandlestickPanel.tsx silinmedi (başka yerler hâlâ kullanıyor olabilir)
  </done>
</task>

</tasks>

<verification>
```bash
# Backend EMA kontrolü
grep -n "ema_200\|ema_50\|_build_ema" /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend/app/services/technical.py

# Frontend TypeScript kontrolü
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit

# Yeni bileşen varlık kontrolü
ls /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/components/CandlestickEMAPanel.tsx

# page.tsx import kontrolü
grep "CandlestickEMAPanel\|ema50\|ema200" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/page.tsx
```
</verification>

<success_criteria>
- GET /api/stocks/{symbol}/technical yanıtında ema_50 ve ema_200 key'leri mevcut (her biri [{date, value}] listesi)
- CandlestickEMAPanel.tsx: candlestick + EMA50 (turuncu) + EMA200 (mavi) + hacim histogram + RSI (mor, 70/30 çizgileri) render eder
- page.tsx grafik bölümünde CandlestickEMAPanel kullanılıyor
- TypeScript derleme temiz
</success_criteria>

<output>
Tamamlanınca `.planning/phases/14-hisse-detay-yeniden-tasarim/14-02-SUMMARY.md` oluştur.
</output>
