---
phase: 14-hisse-detay-yeniden-tasarim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/stocks/[symbol]/page.tsx
  - frontend/src/app/stocks/[symbol]/stock.module.css
autonomous: true
requirements:
  - GLUI-04

must_haves:
  truths:
    - "Hisse detay sayfasında 'Nedensellik' tab'ı (button ve içeriği) codebase'de hiç bulunmaz"
    - "Sayfa yüklendiğinde tam genişlikte (width: 100%) bir grafik alanı en üstte görünür"
    - "Ortada glassmorphism metrik kartları bölümü yer alır"
    - "Altta Teknik Sinyaller ve KAP Bildirimleri için placeholder bölümler glassmorphism card içinde render edilir"
    - "Sayfa glassmorphism stilinde —  backdrop-blur, rgba border, koyu arka plan — görünür"
  artifacts:
    - path: "frontend/src/app/stocks/[symbol]/page.tsx"
      provides: "Yeni layout: tam ekran grafik üst, metrik kartlar orta, sinyaller+KAP alt"
      contains: "activeTab state yok, causal referansı yok, glass-card class"
    - path: "frontend/src/app/stocks/[symbol]/stock.module.css"
      provides: "Glassmorphism layout stilleri"
      contains: "glassCard, chartSection, metricsRow, bottomGrid"
  key_links:
    - from: "frontend/src/app/stocks/[symbol]/page.tsx"
      to: "frontend/src/components/CandlestickPanel.tsx"
      via: "import ve render — grafik bölümü doğrudan page içinde, tab olmadan"
    - from: "frontend/src/app/stocks/[symbol]/page.tsx"
      to: "frontend/src/lib/api.ts"
      via: "api.getStockPrices, api.getStockDetail, api.getStockTechnical"
---

<objective>
Hisse detay sayfasını (stocks/[symbol]/page.tsx) sıfırdan yeniden yazar: tab sistemi kaldırılır, Causal tab tamamen silinir, yeni dikey layout uygulanır — tam genişlikte grafik üstte, metrik kartları ortada, teknik sinyaller ve KAP bildirimleri altta. Tüm bölümler glassmorphism card stilinde.

Purpose: GLUI-04 gereksinimi — Nedensellik tab'ı kaldırılır, glassmorphism ile yeniden yapılandırılmış tek-sayfa layout oluşturulur. Plan 02 ve 03 bu layout içindeki placeholder bölümleri gerçek bileşenlerle dolduracak.

Output:
- Yeniden yazılmış frontend/src/app/stocks/[symbol]/page.tsx (tab yok, causal yok, yeni layout)
- Glassmorphism stilleri için frontend/src/app/stocks/[symbol]/stock.module.css (güncellenmiş)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@frontend/src/app/stocks/[symbol]/page.tsx
@frontend/src/app/globals.css
@frontend/src/lib/api.ts
@frontend/src/components/CandlestickPanel.tsx

<interfaces>
<!-- Mevcut API türleri — doğrudan kullan, codebase araştırma gerek yok -->

From frontend/src/lib/api.ts:
```typescript
// Mevcut API çağrıları:
api.getStockDetail(symbol)     // → StockDetail
api.getStockPrices(symbol, period) // → StockPricesResponse
api.getStockTechnical(symbol)  // → TechnicalResult

export interface StockDetail {
  stock: StockSummary & { currency: string; last_data_update: string | null };
  recent_prices: PricePoint[];
}

export interface TechnicalResult {
  symbol: string;
  score: number;
  recommendation: string;
  signals: Array<{ type: string; name: string; direction: string; strength: number }>;
  support: number | null;
  resistance: number | null;
  stop_loss: number | null;
  target_price: number | null;
  indicators: Record<string, number | null>;
}

export interface StockPricesResponse {
  symbol: string; name: string; period: string;
  prices: PricePoint[];
}
```

From frontend/src/app/globals.css (glassmorphism token'lar):
```css
--bg-primary: #0a0e1a;
--bg-secondary: #111827;
--bg-card: rgba(17, 24, 39, 0.8);
--bg-glass: rgba(17, 24, 39, 0.5);
--border-primary: rgba(148, 163, 184, 0.1);
--border-glass: rgba(148, 163, 184, 0.08);
--text-primary: #f1f5f9;
--text-secondary: #94a3b8;
--text-muted: #64748b;
--shadow-lg: 0 10px 25px rgba(0,0,0,0.4), 0 6px 10px rgba(0,0,0,0.2);
```

Mevcut CandlestickPanel props:
```typescript
interface CandlestickPanelProps {
  prices: PricePoint[];
}
// Kullanım: <CandlestickPanel prices={prices?.prices ?? []} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Causal tab'ı sil, yeni glassmorphism layout yaz</name>
  <files>frontend/src/app/stocks/[symbol]/page.tsx</files>
  <action>
Dosyayı tamamen yeniden yaz. Mevcut tab sistemi (`activeTab` state, tab-bar, causal tab button ve içeriği) tamamen kaldırılır. Yeni dikey layout:

**State:** `detail`, `prices`, `technical`, `period`, `loading` — aynı. `activeTab` ve her türlü causal referansı (import dahil `api.getStockCausal`, `CausalImpact` türleri) kaldır.

**Layout yapısı (yukarıdan aşağı):**

1. **Header bölümü** — hisse adı, sektör, fiyat, değişim%. Mevcut breadcrumb + stockHeader korunur, sadece glassmorphism class'larına geçilir.

2. **3-Layer Score Panel** — `<ScoreLayerPanel stock={s} />` korunur.

3. **Grafik bölümü** (tam genişlik glassmorphism card):
   ```jsx
   <div className={styles.glassCard} style={{ marginBottom: 20 }}>
     <div className={styles.chartHeader}>
       <h3 className={styles.sectionTitle}>Fiyat Grafiği</h3>
       <div className={styles.periodBar}>
         {['1m','3m','6m','1y','5y'].map(...)}
       </div>
     </div>
     <CandlestickPanel prices={prices?.prices ?? []} />
   </div>
   ```
   Plan 02'de bu bölüm EMA overlay'li yeni bileşenle değiştirilecek. Şimdilik mevcut `CandlestickPanel` ile çalışır.

4. **Metrik Kartları bölümü** (glassmorphism card, orta):
   ```jsx
   <div className={styles.metricsRow} style={{ marginBottom: 20 }}>
     {/* CLEN-02 — Plan 03'te FundamentalMetricCard buraya gelecek */}
     <div className={styles.glassCard} style={{ flex: 1 }}>
       <h3 className={styles.sectionTitle}>Temel Metrikler</h3>
       <p style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Yükleniyor...</p>
     </div>
     <div className={styles.glassCard} style={{ flex: 1 }}>
       <h3 className={styles.sectionTitle}>Risk Seviyeleri</h3>
       {technical && (
         <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
           {technical.stop_loss != null && (
             <div style={{ display: 'flex', justifyContent: 'space-between' }}>
               <span style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Stop-Loss (2×ATR)</span>
               <span style={{ color: 'var(--red-500)', fontFamily: 'monospace', fontWeight: 600 }}>{technical.stop_loss.toFixed(2)} ₺</span>
             </div>
           )}
           {technical.target_price != null && (
             <div style={{ display: 'flex', justifyContent: 'space-between' }}>
               <span style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Hedef Fiyat</span>
               <span style={{ color: 'var(--green-500)', fontFamily: 'monospace', fontWeight: 600 }}>{technical.target_price.toFixed(2)} ₺</span>
             </div>
           )}
         </div>
       )}
     </div>
   </div>
   ```

5. **Alt grid bölümü** (2 sütun, glassmorphism card'lar):
   ```jsx
   <div className={styles.bottomGrid}>
     {/* Sol: Teknik Sinyaller */}
     <div className={styles.glassCard}>
       <h3 className={styles.sectionTitle} style={{ marginBottom: 12 }}>Teknik Sinyaller</h3>
       {technical && technical.signals.length > 0 ? (
         <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
           {technical.signals.slice(0, 6).map((signal, i) => (
             <div key={i} style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
               <span style={{
                 padding: '2px 8px', borderRadius: 4, fontSize: '0.72rem', fontWeight: 600,
                 background: signal.direction === 'bullish' ? 'rgba(34,197,94,0.1)' : signal.direction === 'bearish' ? 'rgba(239,68,68,0.1)' : 'rgba(148,163,184,0.1)',
                 color: signal.direction === 'bullish' ? 'var(--green-500)' : signal.direction === 'bearish' ? 'var(--red-500)' : 'var(--text-muted)',
               }}>
                 {signal.direction === 'bullish' ? '▲' : signal.direction === 'bearish' ? '▼' : '●'}
               </span>
               <span style={{ fontSize: '0.82rem', color: 'var(--text-secondary)', flex: 1 }}>{signal.name}</span>
               <span style={{ fontSize: '0.72rem', color: 'var(--text-muted)', fontFamily: 'monospace' }}>{(signal.strength * 100).toFixed(0)}%</span>
             </div>
           ))}
         </div>
       ) : (
         <p style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Aktif sinyal bulunamadı</p>
       )}
     </div>
     {/* Sag: KAP Bildirimleri — Plan 03'te KAPNewsCard bileşeniyle doldurulacak */}
     <div className={styles.glassCard}>
       <h3 className={styles.sectionTitle} style={{ marginBottom: 12 }}>Son KAP Bildirimleri</h3>
       <p style={{ color: 'var(--text-muted)', fontSize: '0.85rem' }}>Yükleniyor...</p>
     </div>
   </div>
   ```

**Import temizliği:** `CausalImpact`, `CausalScenarioResult`, `CausalChainLink` türlerini ve `api.getStockCausal` çağrısını import'lardan kaldır. `useEffectEvent` React 19 API'si — `frontend/node_modules/next/dist/docs/` klasöründe Next.js kılavuzunu oku ve mevcut Next.js sürümünde desteklenip desteklenmediğini doğrula; desteklenmiyorsa `useCallback` + `useEffect` kombinasyonuna dönüştür.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - TypeScript derleme hatası yok
    - "causal" veya "Nedensellik" kelimesi page.tsx'te sıfır occurrence (grep ile doğrula)
    - Sayfa CandlestickPanel'i tab olmadan render ediyor
    - metricsRow ve bottomGrid CSS class'ları kullanılıyor
  </done>
</task>

<task type="auto">
  <name>Task 2: Glassmorphism layout stilleri — stock.module.css güncelle</name>
  <files>frontend/src/app/stocks/[symbol]/stock.module.css</files>
  <action>
Mevcut CSS modülüne yeni glassmorphism class'ları ekle. Mevcut class'ları silme — gerekli olanları koru.

Eklenecek yeni class'lar:

```css
/* ── Glassmorphism Card ─── */
.glassCard {
  background: rgba(17, 24, 39, 0.6);
  border: 1px solid rgba(148, 163, 184, 0.10);
  border-radius: 16px;
  padding: 24px;
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  box-shadow: 0 10px 25px rgba(0,0,0,0.4);
  margin-bottom: 0;
}

/* ── Metrik Satırı (yan yana 2 kart) ─── */
.metricsRow {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
}

@media (max-width: 768px) {
  .metricsRow {
    grid-template-columns: 1fr;
  }
}

/* ── Alt Grid (Sinyaller + KAP) ─── */
.bottomGrid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-top: 0;
}

@media (max-width: 768px) {
  .bottomGrid {
    grid-template-columns: 1fr;
  }
}

/* ── Chart Header ─── */
.chartHeader {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 16px;
  flex-wrap: wrap;
  gap: 8px;
}

/* ── Section Title ─── */
.sectionTitle {
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--text-muted, #64748b);
  margin-bottom: 0;
}

/* ── Period Bar ─── */
.periodBar {
  display: flex;
  gap: 4px;
}
```

Mevcut `.spinner`, `.breadcrumb`, `.stockHeader`, `.symbolBig`, `.nameBig`, `.sectorBig`, `.stockHeaderLeft`, `.stockHeaderRight`, `.priceBig` class'larını koru — bunlar header bölümünde hâlâ kullanılıyor.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - glassCard, metricsRow, bottomGrid, chartHeader, sectionTitle, periodBar class'ları CSS modülünde mevcut
    - TypeScript hatası yok
    - Sayfa tarayıcıda açıldığında glassmorphism card'lar görünür (checkpoint:human-verify bu planın dışında — Plan 02 sonrası yapılacak)
  </done>
</task>

</tasks>

<verification>
```bash
# Causal temizlik kontrolü
grep -r "causal\|Nedensellik\|activeTab.*causal" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/ | grep -v ".module.css" | wc -l
# Beklenen: 0

# TypeScript derleme
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend && npx tsc --noEmit

# CSS class varlık kontrolü
grep -c "glassCard\|metricsRow\|bottomGrid" /Users/nedimkopurlu/Downloads/PROJELER/stalize/frontend/src/app/stocks/\[symbol\]/stock.module.css
# Beklenen: >= 3
```
</verification>

<success_criteria>
- stocks/[symbol]/page.tsx: Nedensellik tab'ı, causal state, causal API çağrısı tamamen yok
- Yeni layout: grafik (üst) → metrik kartlar (orta, 2 sütun) → alt grid (sinyaller + KAP, 2 sütun)
- stock.module.css: glassCard, metricsRow, bottomGrid class'ları eklenmiş
- TypeScript derleme temiz (npx tsc --noEmit sıfır hata)
- Plan 02 için CandlestickPanel'in grafik bölümünde kullanımı korunmuş (sonraki plan değiştirecek)
- Plan 03 için metricsRow[0] ve bottomGrid[1] placeholder içeriyor (sonraki plan dolduracak)
</success_criteria>

<output>
Tamamlanınca `.planning/phases/14-hisse-detay-yeniden-tasarim/14-01-SUMMARY.md` oluştur.
</output>
