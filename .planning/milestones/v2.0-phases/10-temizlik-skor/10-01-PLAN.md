---
plan: 10-01
phase: 10
title: "LLM ve Causal Engine Kaldırma"
goal: "llm_sentiment.py, briefing_generator.py, DailyBriefing model, /api/briefing router, knowledge_graph.py, causal.py, event_fusion.py ve /api/causal router silinir; main.py'deki tüm referanslar temizlenir"
requirements: [AIRF-01, AIRF-02]
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/services/llm_sentiment.py
  - backend/app/services/briefing_generator.py
  - backend/app/services/knowledge_graph.py
  - backend/app/services/causal.py
  - backend/app/services/event_fusion.py
  - backend/app/api/briefing.py
  - backend/app/api/causal.py
  - backend/app/models/model_daily_briefing.py
  - backend/tests/test_daily_briefing.py
  - backend/tests/test_llm_cache.py
  - backend/tests/test_llm_infrastructure.py
autonomous: true

must_haves:
  truths:
    - "DEEPSEEK_API_KEY ortam degiskeni kodun hicbir yerinde okunmuyor veya kullanilmiyor"
    - "GET /api/briefing/* endpoint'leri 404 donuyor — router kaydi main.py'de yok"
    - "GET /api/causal/* endpoint'leri 404 donuyor — router kaydi main.py'de yok"
    - "Backend python -c 'from app.main import app' komutuyla hatasiz basliyor"
    - "Silinen dosyalar filesystem'den yoktur — import denemeleri ModuleNotFoundError firlatir"
  artifacts:
    - path: "backend/app/main.py"
      provides: "Temizlenmis main — LLM/causal scheduler job'lari, router kayitlari ve importlar yok"
      contains: "from app.api import stocks, macro, admin"
    - path: "backend/app/services/llm_sentiment.py"
      provides: "DELETED — dosya mevcut olmamali"
    - path: "backend/app/api/briefing.py"
      provides: "DELETED — dosya mevcut olmamali"
    - path: "backend/app/api/causal.py"
      provides: "DELETED — dosya mevcut olmamali"
    - path: "backend/app/models/model_daily_briefing.py"
      provides: "DELETED — dosya mevcut olmamali"
  key_links:
    - from: "backend/app/main.py"
      to: "app.api.briefing / app.api.causal"
      via: "include_router"
      pattern: "include_router.*briefing|include_router.*causal"
    - from: "backend/app/main.py"
      to: "app.services.briefing_generator"
      via: "import + scheduler.add_job"
      pattern: "generate_daily_briefing|briefing_generator"
---

<objective>
AIRF-01 ve AIRF-02: DeepSeek LLM entegrasyonunu ve causal knowledge graph'i tamamen kaldir.

Purpose: v1.2 pure veri analizi platformuna gecis icin LLM/AI altyapisini sifirla. Backend bu dosyalara hic bagimli olmamali.
Output: 9 dosya silinmis, 3 test dosyasi silinmis, main.py temizlenmis, backend hatasiz basliyor.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Mevcut main.py'nin kritik yapisi (bak: Task 1):
- import satiri: `from app.api import stocks, macro, portfolio, intelligence, causal, admin, briefing`
- import satiri: `from app.services.briefing_generator import generate_daily_briefing`
- scheduler.add_job(generate_daily_briefing, ...) — LLM briefing cron
- scheduler.add_job(background_macro_scan, ...) — causal_engine.run_realtime_scenarios() cagirir
- scheduler.add_job(background_event_fusion, ...) — event_fusion.run_event_fusion() cagirir
- scheduler.add_job(background_audit_and_learn, ...) — performance_monitor cagirir
- scheduler.add_job(background_model_portfolio_generation, ...) — portfolio_optimizer cagirir
- app.include_router(causal.router, prefix="/api")
- app.include_router(briefing.router, prefix="/api")

Lifespan icinde: `from app.services.ml import ml_engine` + `ml_engine.preload_all_models()` — Plan 10-02'de silinecek.
</context>

<tasks>

<task type="auto">
  <name>Task 1: LLM dosyalarini sil ve main.py'den LLM referanslarini kaldir</name>
  <files>
    backend/app/services/llm_sentiment.py,
    backend/app/services/briefing_generator.py,
    backend/app/api/briefing.py,
    backend/app/models/model_daily_briefing.py,
    backend/tests/test_daily_briefing.py,
    backend/tests/test_llm_cache.py,
    backend/tests/test_llm_infrastructure.py,
    backend/app/main.py
  </files>
  <action>
1. Asagidaki dosyalari sil (rm):
   - backend/app/services/llm_sentiment.py
   - backend/app/services/briefing_generator.py
   - backend/app/api/briefing.py
   - backend/app/models/model_daily_briefing.py
   - backend/tests/test_daily_briefing.py
   - backend/tests/test_llm_cache.py
   - backend/tests/test_llm_infrastructure.py

2. backend/app/main.py icinde su degisiklikleri yap:
   a. import satirini degistir:
      ONCE: `from app.api import stocks, macro, portfolio, intelligence, causal, admin, briefing`
      SONRA: `from app.api import stocks, macro, portfolio, intelligence, causal, admin`

   b. Asagidaki import satirini tamamen sil:
      `from app.services.briefing_generator import generate_daily_briefing`

   c. Tam generate_daily_briefing scheduler.add_job blogunu sil (main.py dosyasinin ustundeki cron job, lifespan oncesinde):
      ```
      scheduler.add_job(
          generate_daily_briefing,
          "cron",
          day_of_week="mon-fri",
          hour=6,
          minute=30,
          timezone="Europe/Istanbul",
          max_instances=1,
          misfire_grace_time=300,
      )
      ```

   d. app.include_router(briefing.router, prefix="/api") satirini sil.

   e. Dosyanin tepesindeki module-level scheduler.add_job(generate_daily_briefing, ...) cagrisinin tamamen gittigini dogrula — lifespan ici job eklemeleri ayri.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "import ast, sys; ast.parse(open('app/main.py').read()); print('main.py syntax OK')" && grep -L "briefing" app/main.py || echo "briefing gone" && grep -L "llm_sentiment" app/main.py || echo "llm_sentiment gone"</automated>
  </verify>
  <done>
    - 7 LLM dosyasi filesystem'den silindi
    - main.py briefing import ve router kaydini icermiyor
    - main.py syntax hatasi yok
  </done>
</task>

<task type="auto">
  <name>Task 2: Causal engine dosyalarini sil ve main.py'den causal referanslarini kaldir</name>
  <files>
    backend/app/services/knowledge_graph.py,
    backend/app/services/causal.py,
    backend/app/services/event_fusion.py,
    backend/app/api/causal.py,
    backend/app/main.py
  </files>
  <action>
1. Asagidaki dosyalari sil (rm):
   - backend/app/services/knowledge_graph.py
   - backend/app/services/causal.py
   - backend/app/services/event_fusion.py
   - backend/app/api/causal.py

2. backend/app/main.py icinde su degisiklikleri yap:
   a. import satirini degistir:
      ONCE: `from app.api import stocks, macro, portfolio, intelligence, causal, admin`
      SONRA: `from app.api import stocks, macro, portfolio, intelligence, admin`

   b. background_macro_scan async fonksiyonunu tamamen sil:
      ```python
      async def background_macro_scan():
          from app.services.causal import causal_engine
          ...
      ```

   c. background_event_fusion async fonksiyonunu tamamen sil:
      ```python
      async def background_event_fusion():
          from app.services.event_fusion import run_event_fusion
          ...
      ```

   d. Lifespan icindeki su scheduler.add_job cagrilarini sil:
      - scheduler.add_job(background_macro_scan, "interval", hours=1, ...)
      - scheduler.add_job(background_event_fusion, "interval", hours=1, ...)

   e. app.include_router(causal.router, prefix="/api") satirini sil.

   f. FastAPI app tanimindaki description'i guncelle:
      ONCE: "Teknik + Temel + Duygu + Nedensellik + Yapay Zeka ile BIST100 hisse analizi"
      SONRA: "Teknik + Temel + Haber Analizi ile BIST100 hisse analizi"

3. Dogrulama: main.py'de hic "causal" veya "event_fusion" string'i kalmadigini kontrol et.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "import ast; ast.parse(open('app/main.py').read()); print('OK')" && ! grep -q "causal" app/main.py && echo "causal gone from main.py" && ! grep -q "event_fusion" app/main.py && echo "event_fusion gone from main.py"</automated>
  </verify>
  <done>
    - 4 causal dosyasi filesystem'den silindi
    - main.py causal import, router kaydi, scheduler job ve fonksiyonlarini icermiyor
    - Backend import edilebiliyor: python -c "from app.main import app" hata vermiyor
    - GET /api/causal/... ve GET /api/briefing/... 404 donuyor (router yok)
  </done>
</task>

</tasks>

<verification>
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend

# 1. Silinen dosyalar yok
for f in app/services/llm_sentiment.py app/services/briefing_generator.py app/api/briefing.py app/models/model_daily_briefing.py app/services/knowledge_graph.py app/services/causal.py app/services/event_fusion.py app/api/causal.py; do
  [ ! -f "$f" ] && echo "DELETED: $f" || echo "STILL EXISTS (FAIL): $f"
done

# 2. main.py import edilebiliyor
python -c "from app.main import app; print('app imported OK')"

# 3. main.py'de yasakli referanslar yok
! grep -qE "briefing|causal|event_fusion|llm_sentiment|generate_daily_briefing" app/main.py && echo "main.py clean"

# 4. Mevcut testler gecmeli (LLM/causal testleri zaten silindi)
python -m pytest tests/test_scoring.py tests/test_signal_quality.py -x -q
</verification>

<success_criteria>
- llm_sentiment.py, briefing_generator.py, knowledge_graph.py, causal.py, event_fusion.py, app/api/briefing.py, app/api/causal.py, model_daily_briefing.py dosyalari mevcut degil
- test_daily_briefing.py, test_llm_cache.py, test_llm_infrastructure.py mevcut degil
- main.py syntax hatasi yok, "briefing" ve "causal" string'leri icermiyor
- Backend basliyor: python -c "from app.main import app" hata yok
- test_scoring.py ve test_signal_quality.py geciyor
</success_criteria>

<output>
Tamamlaninca .planning/phases/10-temizlik-skor/10-01-SUMMARY.md olustur:
- Silinen dosyalar listesi
- main.py'de yapilan degisiklikler
- Kalan testlerin durumu
</output>
