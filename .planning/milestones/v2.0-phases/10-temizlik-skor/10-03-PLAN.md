---
plan: 10-03
phase: 10
title: "Scoring Yeniden Yapilandirma ve requirements.txt Temizligi"
goal: "config.py agirliklar WEIGHT_FUNDAMENTAL=0.45, WEIGHT_TECHNICAL=0.40, WEIGHT_NEWS=0.15 olarak guncellenir; scoring.py ML/causal referanslari kaldirip uc agirlikla yeniden yazilir; xgboost, instructor, shap, diskcache requirements.txt'ten cikarilir"
requirements: [MIDT-02, CLEN-04]
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - backend/app/core/config.py
  - backend/app/services/scoring.py
  - backend/requirements.txt
autonomous: true

must_haves:
  truths:
    - "Herhangi bir hisse icin calculate_overall_score() Temel %45 / Teknik %40 / Haber %15 agirliklarini kullaniyor"
    - "Scoring motoru ml_score veya causal_score'a hic referans vermiyor"
    - "config.py'de WEIGHT_ML, WEIGHT_CAUSAL, WEIGHT_MACRO sabitleri artik tanimlanmamis"
    - "pip install -r requirements.txt sonrasi xgboost, instructor, shap paketleri yuklu degil"
    - "overall_score 0-100 araliginda kaliyor; uc sinyal icin de dogru normalize ediliyor"
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "Guncellenmis agirliklar: WEIGHT_FUNDAMENTAL=0.45, WEIGHT_TECHNICAL=0.40, WEIGHT_NEWS=0.15"
      contains: "WEIGHT_FUNDAMENTAL"
    - path: "backend/app/services/scoring.py"
      provides: "Yeniden yazilmis scoring motoru — sadece fundamental, technical, news/sentiment agirliklarini kullaniyor"
      contains: "WEIGHT_NEWS"
    - path: "backend/requirements.txt"
      provides: "xgboost, instructor, shap satirlari yok; diskcache kontrol edilmis"
  key_links:
    - from: "backend/app/services/scoring.py"
      to: "backend/app/core/config.py"
      via: "settings.WEIGHT_FUNDAMENTAL / settings.WEIGHT_TECHNICAL / settings.WEIGHT_NEWS"
      pattern: "settings\\.WEIGHT_"
    - from: "backend/app/services/scoring.py"
      to: "backend/app/models/stock.py"
      via: "stock.technical_score, stock.fundamental_score, stock.sentiment_score"
      pattern: "stock\\.technical_score|stock\\.fundamental_score|stock\\.sentiment_score"
---

<objective>
MIDT-02 ve CLEN-04: Scoring agirliklarini orta vadeli yatirimci odagina gore yeniden yapilandir ve kullanilmayan ML/LLM bagimliliklerini requirements.txt'ten kaldir.

Purpose: v1.2 pure veri analizi sisteminin cekirdegi — overall_score artik uc gercek sinyal katmanina dayanir: Temel analiz (hakkaniyetli deger), Teknik analiz (trend/momentum), Haber etkisi (KAP/makro duygu).
Output: config.py yeni agirliklarla guncellendi, scoring.py yeniden yazildi, requirements.txt temizlendi.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-temizlik-skor/10-02-SUMMARY.md

Mevcut config.py agirlik sabitleri (degistirilecek):
```python
WEIGHT_TECHNICAL: float = 0.20
WEIGHT_FUNDAMENTAL: float = 0.25
WEIGHT_ML: float = 0.20
WEIGHT_SENTIMENT: float = 0.10
WEIGHT_CAUSAL: float = 0.15
WEIGHT_MACRO: float = 0.10
```

Hedef agirliklar (MIDT-02):
- WEIGHT_FUNDAMENTAL: float = 0.45   (Temel Analiz — F/K, PD/DD, ROE, marj)
- WEIGHT_TECHNICAL: float = 0.40     (Teknik Analiz — EMA, ATR, RSI, divergence, hacim)
- WEIGHT_NEWS: float = 0.15          (Haber Etkisi — KAP, TCMB, TUIK duygu skoru)

Kaldirilan agirliklar: WEIGHT_ML, WEIGHT_CAUSAL, WEIGHT_MACRO (hepsi siliniyor)

Mevcut scoring.py kritik yapi:
- _resolve_weights(): 6 agirlik donduruyor (technical, fundamental, sentiment, causal, ml, macro)
- Crisis mode mantigini destekliyor (causal_score sapmasina gore agirlik kayma)
- calculate_overall_score(): 6 skor girdisini normalizasyon ile birlestiriyor

Yeni scoring.py:
- _resolve_weights(): 3 agirlik dondurmeli (fundamental, technical, news)
- Crisis mode kaldiriliyor — kausal veri olmadan anlamsiz
- calculate_overall_score(): stock.fundamental_score, stock.technical_score, stock.sentiment_score kullan
  NOT: sentiment_score sutunu DB'de korunuyor (haber/sentiment skoru burada saklanir)

Stock model sutunlari korunan: technical_score, fundamental_score, sentiment_score, overall_score, recommendation
Stock model sutunlari kullanilmayan (silinmiyor, sadece scoring'de yoksayiliyor): ml_score, causal_score
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.py agirlik sabitlerini guncelle ve scoring.py'yi yeniden yaz</name>
  <files>
    backend/app/core/config.py,
    backend/app/services/scoring.py,
    backend/tests/test_scoring.py
  </files>
  <behavior>
    - Test 1: calculate_overall_score() fundamental=80, technical=60, sentiment=40 ile cagrildiginda: (80*0.45 + 60*0.40 + 40*0.15) = 36+24+6 = 66.0 donmeli
    - Test 2: Sadece fundamental_score mevcut (technical=None, sentiment=None): normalize ederek fundamental'i 100 agirlik ile dondurmeli (temel skor degeri = overall_score)
    - Test 3: Tum skorlar None: DEFAULT_SCORE=50.0 ve "TUT" donmeli
    - Test 4: overall_score >= 65 oldugunda recommendation "AL", >= 80 "GUCLU AL", < 45 "SAT", < 25 "GUCLU SAT"
    - Test 5: ml_score veya causal_score girmek sonucu degistirmemeli (yoksayiliyor)
    - Test 6: config.py'de WEIGHT_FUNDAMENTAL + WEIGHT_TECHNICAL + WEIGHT_NEWS = 1.0
  </behavior>
  <action>
1. backend/app/tests/test_scoring.py icinde yeni test beklentileri dogrulayan testler yaz (once RED).
   Mevcut test_scoring.py dosyasini oku, varsa causal/ml_score testlerini kaldir, yeni agirlik testlerini ekle.

2. backend/app/core/config.py Ensemble Weights bolumunu guncelle:
   SIL: WEIGHT_ML, WEIGHT_SENTIMENT, WEIGHT_CAUSAL, WEIGHT_MACRO
   GUNCELLE:
   ```python
   # Ensemble Weights — Orta vadeli yatirimci odagi (MIDT-02)
   WEIGHT_FUNDAMENTAL: float = 0.45  # Temel analiz: F/K, PD/DD, ROE, marj
   WEIGHT_TECHNICAL: float = 0.40    # Teknik analiz: EMA, ATR, RSI, hacim, divergence
   WEIGHT_NEWS: float = 0.15         # Haber etkisi: KAP, TCMB, TUIK duygu
   ```
   NOT: config.py'deki diger ML sabitlerini de sil (ML_MODEL_DIR, LSTM_WINDOW_SIZE, LSTM_EPOCHS, XGBOOST_N_ESTIMATORS, KNOWLEDGE_DIR, LLM_ENABLED, LLM_MODEL, OPENAI_API_KEY, DEEPSEEK_API_KEY).

3. backend/app/services/scoring.py'yi tamamen yeniden yaz:
   ```python
   """
   Scoring Service — Genel Skor Hesaplama Motoru (v1.2)

   Uc katman: Temel (%45) + Teknik (%40) + Haber (%15)
   """
   import logging
   from typing import Dict, List, Optional

   from sqlalchemy import select

   from app.core.config import settings
   from app.core.database import AsyncSessionLocal
   from app.models.stock import Stock

   logger = logging.getLogger(__name__)


   class ScoringEngine:
       """Temel, teknik ve haber skorlarini agirliklandirarak birlestirir."""

       DEFAULT_SCORE = 50.0

       def _resolve_weights(self) -> Dict[str, float]:
           """
           Agirliklar config.py uzerinden okunur.
           Toplam her zaman 1.0 olmali: 0.45 + 0.40 + 0.15 = 1.0
           """
           return {
               "fundamental_score": settings.WEIGHT_FUNDAMENTAL,
               "technical_score": settings.WEIGHT_TECHNICAL,
               "sentiment_score": settings.WEIGHT_NEWS,
           }

       def calculate_overall_score(self, stock: Stock) -> tuple:
           """
           Agirlikli ortalama ile genel skor hesapla.
           Eksik skorlar icin agirlik kalan skorlara normalize edilir.

           Returns:
               (overall_score, recommendation)
           """
           scores = {
               "fundamental_score": stock.fundamental_score,
               "technical_score": stock.technical_score,
               "sentiment_score": stock.sentiment_score,
           }
           weights = self._resolve_weights()

           total_weight = 0.0
           weighted_sum = 0.0

           for score_name, weight in weights.items():
               value = scores.get(score_name)
               if value is not None:
                   weighted_sum += value * weight
                   total_weight += weight

           if total_weight == 0:
               return self.DEFAULT_SCORE, "TUT"

           # Normalize (eksik skorlar icin agirlik yeniden dagitilir)
           overall = weighted_sum / total_weight
           overall = round(max(0.0, min(100.0, overall)), 2)

           if overall >= 80:
               recommendation = "GUCLU AL"
           elif overall >= 65:
               recommendation = "AL"
           elif overall >= 45:
               recommendation = "TUT"
           elif overall >= 25:
               recommendation = "SAT"
           else:
               recommendation = "GUCLU SAT"

           return overall, recommendation

       async def update_all_scores(self):
           """Tum hisseler icin genel skoru guncelle."""
           logger.info("Genel skor hesaplamasi basliyor...")

           async with AsyncSessionLocal() as db:
               result = await db.execute(select(Stock).where(Stock.is_active))
               stocks = result.scalars().all()

               updated = 0
               for stock in stocks:
                   overall, recommendation = self.calculate_overall_score(stock)
                   stock.overall_score = overall
                   stock.recommendation = recommendation
                   updated += 1

               await db.commit()

           logger.info(f"{updated} hisse skoru guncellendi")
           return updated

       async def get_rankings(
           self,
           sort_by: str = "overall_score",
           limit: int = 30,
           sector: Optional[str] = None,
           bist30_only: bool = False,
       ) -> List[Dict]:
           """Hisseleri skorlarina gore sirala."""
           async with AsyncSessionLocal() as db:
               query = select(Stock).where(Stock.is_active)

               if bist30_only:
                   query = query.where(Stock.is_bist30)
               if sector:
                   query = query.where(Stock.sector == sector)

               sort_column = getattr(Stock, sort_by, Stock.overall_score)
               query = query.order_by(sort_column.desc().nullslast()).limit(limit)

               result = await db.execute(query)
               stocks = result.scalars().all()

               return [
                   {
                       "symbol": s.symbol,
                       "name": s.name,
                       "sector": s.sector,
                       "current_price": s.current_price,
                       "daily_change_pct": s.daily_change_pct,
                       "volume": s.volume,
                       "market_cap": s.market_cap,
                       "is_bist30": s.is_bist30,
                       "technical_score": s.technical_score,
                       "fundamental_score": s.fundamental_score,
                       "sentiment_score": s.sentiment_score,
                       "overall_score": s.overall_score,
                       "recommendation": s.recommendation,
                   }
                   for s in stocks
               ]


   # Singleton
   scoring_engine = ScoringEngine()
   ```

4. Testlerin gectigini dogrula (GREEN).
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -m pytest tests/test_scoring.py -x -q && python -c "from app.core.config import settings; assert abs(settings.WEIGHT_FUNDAMENTAL + settings.WEIGHT_TECHNICAL + settings.WEIGHT_NEWS - 1.0) < 0.001, 'Agirlik toplami 1.0 olmali'; print(f'Agirliklar: F={settings.WEIGHT_FUNDAMENTAL} T={settings.WEIGHT_TECHNICAL} N={settings.WEIGHT_NEWS} Toplam={settings.WEIGHT_FUNDAMENTAL+settings.WEIGHT_TECHNICAL+settings.WEIGHT_NEWS}')"</automated>
  </verify>
  <done>
    - config.py: WEIGHT_FUNDAMENTAL=0.45, WEIGHT_TECHNICAL=0.40, WEIGHT_NEWS=0.15; agirlik toplami 1.0
    - scoring.py: sadece uc agirlik kullaniliyor, ml_score/causal_score yoksayiliyor
    - test_scoring.py gecmeli — yeni agirlik beklentileri karsiliyor
  </done>
</task>

<task type="auto">
  <name>Task 2: requirements.txt'ten ML/LLM bagimliliklerini kaldir</name>
  <files>
    backend/requirements.txt
  </files>
  <action>
1. backend/requirements.txt'ten asagidaki satirlari tamamen sil:
   - `xgboost==2.1.4`
   - `shap==0.46.0`
   - `instructor==1.15.1`
   - `diskcache==5.6.3`  (LLM ve yfinance cache icin kullaniliyordu — artik gerek yok; onceden dogrula: grep -r "diskcache" backend/app/ ile hala kullaniliyor mu bak; kullanilmiyorsa sil)

   NOT: scikit-learn==1.6.1 KORU — fundamental/technical scoring'de hala kullaniliyor olabilir.

2. LLM API satiri varsa sil:
   - deepseek veya openai sdk satirlari (orn. `openai`, `deepseek-openai`) — mevcut requirements.txt'te yoksa islem gerekmiyor.

3. requirements.txt'teki yorum satirlarini kontrol et: "# Machine Learning" bolumunun altinda kalan satirlar varsa baslikla birlikte sil.
   "# NLP & Sentiment" bolumunu tamamen sil (instructor gittikten sonra bos kalacak).

4. Temizlenmis requirements.txt'i kaydet.

5. Dogrulama: pip install --dry-run -r requirements.txt calistir (buyuk indirme gerekmez, sadece bagimliliklari listeler).
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && ! grep -qE "^xgboost|^shap|^instructor" requirements.txt && echo "ML/LLM packages removed" && python -m pip install --dry-run -r requirements.txt 2>&1 | tail -5</automated>
  </verify>
  <done>
    - requirements.txt'te xgboost, shap, instructor satirlari yok
    - pip install --dry-run hata vermiyor
    - Kalan bagimliliklar: fastapi, uvicorn, sqlalchemy, asyncpg, pandas, numpy, yfinance, ta, requests, beautifulsoup4, feedparser, lxml, scikit-learn, apscheduler, python-dotenv, pydantic, pydantic-settings, httpx, aiohttp, starlette
  </done>
</task>

</tasks>

<verification>
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend

# 1. Agirlik toplami 1.0
python -c "
from app.core.config import settings
total = settings.WEIGHT_FUNDAMENTAL + settings.WEIGHT_TECHNICAL + settings.WEIGHT_NEWS
assert abs(total - 1.0) < 0.001, f'FAIL: toplam={total}'
assert settings.WEIGHT_FUNDAMENTAL == 0.45
assert settings.WEIGHT_TECHNICAL == 0.40
assert settings.WEIGHT_NEWS == 0.15
print('Agirliklar OK: F=0.45, T=0.40, N=0.15')
"

# 2. Scoring motoru dogru hesapliyor
python -c "
from unittest.mock import MagicMock
from app.services.scoring import ScoringEngine
engine = ScoringEngine()
stock = MagicMock()
stock.fundamental_score = 80.0
stock.technical_score = 60.0
stock.sentiment_score = 40.0
overall, rec = engine.calculate_overall_score(stock)
expected = 80*0.45 + 60*0.40 + 40*0.15  # = 36+24+6 = 66
assert abs(overall - expected) < 0.01, f'FAIL: {overall} != {expected}'
print(f'Skor OK: {overall} ({rec})')
"

# 3. scoring.py'de ml_score veya causal_score referansi yok
! grep -q "ml_score\|causal_score\|WEIGHT_ML\|WEIGHT_CAUSAL" app/services/scoring.py && echo "scoring.py temiz"

# 4. requirements.txt temiz
! grep -qE "^xgboost|^shap|^instructor" requirements.txt && echo "requirements.txt temiz"

# 5. Tum testler
python -m pytest tests/test_scoring.py tests/test_signal_quality.py tests/test_routers.py -x -q
</verification>

<success_criteria>
- config.py: WEIGHT_FUNDAMENTAL=0.45, WEIGHT_TECHNICAL=0.40, WEIGHT_NEWS=0.15; toplam=1.0
- scoring.py: sadece uc skor referansi var, ml_score/causal_score yoksayiliyor
- calculate_overall_score(fundamental=80, technical=60, sentiment=40) = 66.0 ("AL")
- requirements.txt: xgboost, shap, instructor satirlari yok
- test_scoring.py gecmeli
</success_criteria>

<output>
Tamamlaninca .planning/phases/10-temizlik-skor/10-03-SUMMARY.md olustur:
- Yeni agirlik degerleri ve mantigi
- scoring.py degisikliklerinin ozeti
- requirements.txt'ten kaldirilan paketler
- Phase 10 genel tamamlanma notu
</output>
