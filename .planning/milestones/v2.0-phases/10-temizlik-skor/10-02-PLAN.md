---
plan: 10-02
phase: 10
title: "XGBoost ML ve Mock Portfolio Backend Kaldirma"
goal: "ml.py, test_ml_*.py ve disk model dosyalari silinir; portfolio.py (api+service), performance_monitor.py, portfolio_optimizer.py silinir; main.py'deki tum ML/portfolio referanslari temizlenir"
requirements: [AIRF-03, AIRF-04]
wave: 2
depends_on: [10-01]
files_modified:
  - backend/app/main.py
  - backend/app/services/ml.py
  - backend/app/services/performance_monitor.py
  - backend/app/services/portfolio_optimizer.py
  - backend/app/api/portfolio.py
  - backend/app/services/portfolio.py
  - backend/app/models/portfolio.py
  - backend/tests/test_ml_persistence.py
  - backend/tests/test_ml_no_double_count.py
autonomous: true

must_haves:
  truths:
    - "GET /api/portfolio/* endpoint'leri 404 donuyor — router kaydi main.py'de yok"
    - "XGBoost retrain scheduler job main.py'den kaldirildi"
    - "ml.py, performance_monitor.py, portfolio_optimizer.py, api/portfolio.py, services/portfolio.py dosyalari mevcut degil"
    - "Backend python -c 'from app.main import app' hatasiz basliyor"
    - "Silinen ML test dosyalari mevcut degil"
  artifacts:
    - path: "backend/app/main.py"
      provides: "ML preload, retrain job, portfolio scheduler ve fonksiyonlari kaldirilmis temiz main"
      contains: "from app.api import stocks, macro, intelligence, admin"
    - path: "backend/app/services/ml.py"
      provides: "DELETED"
    - path: "backend/app/api/portfolio.py"
      provides: "DELETED"
    - path: "backend/app/services/portfolio.py"
      provides: "DELETED — mock portfolio servisi"
    - path: "backend/app/services/performance_monitor.py"
      provides: "DELETED"
  key_links:
    - from: "backend/app/main.py"
      to: "app.api.portfolio"
      via: "include_router"
      pattern: "include_router.*portfolio"
    - from: "backend/app/main.py"
      to: "app.services.ml"
      via: "ml_engine.preload_all_models() + background_xgb_retrain"
      pattern: "ml_engine|xgb_retrain|background_xgb"
---

<objective>
AIRF-03 ve AIRF-04: XGBoost ML motorunu ve mock portfolio backend'ini tamamen kaldir.

Purpose: ML skoru artik overall_score'a girmiyor (Plan 10-03'te yeniden yazilacak); mock portfolio Phase 11'deki gercek portfolio backend'ine yer aciyor.
Output: 9 dosya silindi, main.py'de ML preload + retrain + portfolio scheduler + fonksiyonlari yok.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-temizlik-skor/10-01-SUMMARY.md

main.py'de Plan 10-01 sonrasi kalan silinecek bloklar:

Lifespan basinda:
```python
from app.services.ml import ml_engine
loaded_count = ml_engine.preload_all_models()
logging.info(f"XGBoost modelleri yuklendi: {loaded_count} hisse")
```

background fonksiyonlari (tum fonksiyon tanimi silinmeli):
- background_audit_and_learn() — performance_monitor.audit_and_learn() cagirir
- background_model_portfolio_generation() — portfolio_optimizer.generate_daily_model() cagirir
- background_xgb_retrain() — ml_engine.retrain_and_save() cagirir

Lifespan ici scheduler.add_job cagrilari silinmeli:
- scheduler.add_job(background_audit_and_learn, "cron", day_of_week="mon-fri", hour=18, minute=5, ...)
- scheduler.add_job(background_model_portfolio_generation, "cron", day_of_week="mon-fri", hour=18, minute=15, ...)
- scheduler.add_job(background_xgb_retrain, "cron", day_of_week="sun", hour=2, minute=0, ...)

Router kaydi silinmeli:
- app.include_router(portfolio.router, prefix="/api")

Import silinmeli:
- `portfolio` kismini import satirindan cikar
</context>

<tasks>

<task type="auto">
  <name>Task 1: XGBoost ML dosyalarini sil ve main.py'den ML referanslarini kaldir</name>
  <files>
    backend/app/services/ml.py,
    backend/tests/test_ml_persistence.py,
    backend/tests/test_ml_no_double_count.py,
    backend/app/main.py
  </files>
  <action>
1. Asagidaki dosyalari sil (rm):
   - backend/app/services/ml.py
   - backend/tests/test_ml_persistence.py
   - backend/tests/test_ml_no_double_count.py

2. backend/app/ml_models/ dizini varsa icindeki tum .ubj dosyalarini sil. Dizin bossa birakilabilir.

3. backend/app/main.py icinde su degisiklikleri yap:
   a. background_xgb_retrain async fonksiyonunu tamamen sil:
      ```python
      async def background_xgb_retrain():
          from app.services.ml import ml_engine
          ...
      ```

   b. Lifespan icindeki XGBoost preload blogunu sil:
      ```python
      from app.services.ml import ml_engine
      loaded_count = ml_engine.preload_all_models()
      logging.info(f"XGBoost modelleri yuklendi: {loaded_count} hisse")
      ```

   c. Lifespan icindeki XGBoost retrain scheduler.add_job'u sil:
      ```python
      scheduler.add_job(background_xgb_retrain, "cron", day_of_week="sun", hour=2, minute=0, max_instances=1, misfire_grace_time=300)
      ```

4. Dogrulama: main.py'de "ml_engine", "xgb_retrain", "background_xgb" string'leri kalmadigi kontrol et.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && ! test -f app/services/ml.py && echo "ml.py deleted" && python -c "import ast; ast.parse(open('app/main.py').read()); print('syntax OK')" && ! grep -q "ml_engine\|xgb_retrain\|background_xgb" app/main.py && echo "ML refs gone"</automated>
  </verify>
  <done>
    - ml.py, test_ml_persistence.py, test_ml_no_double_count.py silinmis
    - main.py'de ml_engine preload blogu ve XGBoost scheduler job'u yok
    - main.py syntax hatasi yok
  </done>
</task>

<task type="auto">
  <name>Task 2: Mock portfolio backend'ini sil ve main.py'den portfolio referanslarini kaldir</name>
  <files>
    backend/app/api/portfolio.py,
    backend/app/services/portfolio.py,
    backend/app/services/performance_monitor.py,
    backend/app/services/portfolio_optimizer.py,
    backend/app/models/portfolio.py,
    backend/app/main.py
  </files>
  <action>
1. Asagidaki dosyalari sil (rm):
   - backend/app/api/portfolio.py
   - backend/app/services/portfolio.py
   - backend/app/services/performance_monitor.py
   - backend/app/services/portfolio_optimizer.py

   NOT: backend/app/models/portfolio.py dosyasini kontrol et. Eger iceriginde AIRF-04 kapsaminin disinda (orn. Phase 11 icin kullanilacak tablolar) varsa SILME — sadece mock portfolio servisiyle ilgili ise sil.
   Kontrol: dosyada ModelPortfolio, PortfolioPosition, MockPortfolio gibi siniflara bak. Mock veya simulation iceren siniflar varsa dosyayi sil. Phase 11'de yeniden yazilacak.

2. backend/app/main.py icinde su degisiklikleri yap:
   a. import satirini guncelle:
      ONCE: `from app.api import stocks, macro, portfolio, intelligence, admin`
      SONRA: `from app.api import stocks, macro, intelligence, admin`

   b. background_audit_and_learn async fonksiyonunu tamamen sil:
      ```python
      async def background_audit_and_learn():
          from app.services.performance_monitor import performance_monitor
          ...
      ```

   c. background_model_portfolio_generation async fonksiyonunu tamamen sil:
      ```python
      async def background_model_portfolio_generation():
          from app.services.portfolio_optimizer import portfolio_optimizer
          ...
      ```

   d. Lifespan icindeki bu iki scheduler.add_job cagrilarini sil:
      - scheduler.add_job(background_audit_and_learn, "cron", day_of_week="mon-fri", hour=18, minute=5, ...)
      - scheduler.add_job(background_model_portfolio_generation, "cron", day_of_week="mon-fri", hour=18, minute=15, ...)

   e. app.include_router(portfolio.router, prefix="/api") satirini sil.

   f. Logging mesajini guncelle (lifespan sonundaki scheduler.start() sonrasi):
      ONCE: "KAP: {settings.KAP_SCAN_INTERVAL_MIN}dk, TCMB: 2h, TUIK: 9:00, Fusion: 1h, Audit: 18:05, Portfolio: 18:15"
      SONRA: "KAP: {settings.KAP_SCAN_INTERVAL_MIN}dk, TCMB: 2h, TUIK: 9:00"

3. Dogrulama: main.py'de "portfolio", "performance_monitor", "portfolio_optimizer", "audit_and_learn" string'leri kalmamis olmali.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && ! test -f app/api/portfolio.py && echo "api/portfolio.py deleted" && ! test -f app/services/portfolio.py && echo "services/portfolio.py deleted" && ! test -f app/services/performance_monitor.py && echo "performance_monitor.py deleted" && python -c "from app.main import app; print('app imported OK')" && ! grep -q "portfolio\.router\|performance_monitor\|portfolio_optimizer" app/main.py && echo "portfolio refs gone"</automated>
  </verify>
  <done>
    - api/portfolio.py, services/portfolio.py, performance_monitor.py, portfolio_optimizer.py silinmis
    - main.py portfolio router kaydi, audit ve portfolio scheduler job'lari yok
    - Backend basliyor: from app.main import app hata yok
    - GET /api/portfolio/* 404 donuyor
  </done>
</task>

</tasks>

<verification>
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend

# 1. Silinen dosyalar yok
for f in app/services/ml.py app/api/portfolio.py app/services/portfolio.py app/services/performance_monitor.py app/services/portfolio_optimizer.py tests/test_ml_persistence.py tests/test_ml_no_double_count.py; do
  [ ! -f "$f" ] && echo "DELETED OK: $f" || echo "FAIL — STILL EXISTS: $f"
done

# 2. Backend import edilebiliyor
python -c "from app.main import app; print('app import OK')"

# 3. main.py temiz
! grep -qE "portfolio\.router|ml_engine|xgb_retrain|performance_monitor|portfolio_optimizer|audit_and_learn" app/main.py && echo "main.py clean"

# 4. Kalan testler geciyor
python -m pytest tests/test_scoring.py tests/test_signal_quality.py tests/test_routers.py -x -q
</verification>

<success_criteria>
- ml.py, api/portfolio.py, services/portfolio.py, performance_monitor.py, portfolio_optimizer.py mevcut degil
- test_ml_persistence.py ve test_ml_no_double_count.py mevcut degil
- main.py syntax hatasi yok, ML ve portfolio referanslari icermiyor
- python -c "from app.main import app" hata yok
- test_scoring.py, test_signal_quality.py gecmeli
</success_criteria>

<output>
Tamamlaninca .planning/phases/10-temizlik-skor/10-02-SUMMARY.md olustur:
- Silinen dosyalar listesi
- main.py'de yapilan degisiklikler ozeti
- Kalan scheduler job listesi (sadece tutulacaklar)
</output>
