---
phase: 11-model-portfolyo-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/portfolio_v2.py
  - backend/app/core/database.py
  - backend/alembic/env.py
  - backend/alembic/versions/001_portfolio_v2_tables.py
autonomous: true
requirements:
  - MPRT-01

must_haves:
  truths:
    - "PostgreSQL'de portfolio_positions tablosu mevcuttur ve doğru sütunlara sahiptir"
    - "PostgreSQL'de portfolio_daily_snapshots tablosu mevcuttur ve positions_json JSONB sütunu içerir"
    - "PostgreSQL'de portfolio_change_log tablosu mevcuttur"
    - "Tablolar Alembic migration ile oluşturulur — Base.metadata.create_all'a dayanmaz"
  artifacts:
    - path: "backend/app/models/portfolio_v2.py"
      provides: "PortfolioPosition, PortfolioDailySnapshot, PortfolioChangeLog ORM modelleri"
      exports:
        - PortfolioPosition
        - PortfolioDailySnapshot
        - PortfolioChangeLog
    - path: "backend/alembic/versions/001_portfolio_v2_tables.py"
      provides: "Alembic migration — 3 tablo oluşturur"
      contains: "portfolio_positions, portfolio_daily_snapshots, portfolio_change_log"
  key_links:
    - from: "backend/app/models/portfolio_v2.py"
      to: "backend/app/core/database.py"
      via: "from app.core.database import Base"
      pattern: "class.*Base"
    - from: "backend/alembic/env.py"
      to: "backend/app/models/portfolio_v2.py"
      via: "import for metadata"
      pattern: "portfolio_v2"
---

<objective>
Model portföy için üç PostgreSQL tablosunu ORM modeli olarak tanımla ve Alembic migration ile oluştur.

Purpose: Phase 11'in geri kalanı (snapshot job ve API endpoint'leri) bu tablolara yazar. Tablolar olmadan hiçbir şey çalışmaz.
Output: portfolio_positions, portfolio_daily_snapshots, portfolio_change_log tabloları DB'de mevcuttur; ORM modelleri Plan 02 ve Plan 03 tarafından import edilebilir.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Mevcut pattern — tüm modeller bu Base'den türetilir -->
<!-- backend/app/core/database.py -->
```python
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker
from sqlalchemy.orm import DeclarativeBase

class Base(DeclarativeBase):
    """Base class for all SQLAlchemy models."""
    pass

AsyncSessionLocal = async_sessionmaker(
    bind=async_engine,
    class_=AsyncSession,
    expire_on_commit=False,
)
```

<!-- Mevcut model pattern — backend/app/models/stock.py -->
```python
from sqlalchemy import Column, Integer, String, Float, Boolean, DateTime
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from app.core.database import Base

class Stock(Base):
    __tablename__ = "stocks"
    id = Column(Integer, primary_key=True, index=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now())
    updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ORM modelleri oluştur (portfolio_v2.py)</name>
  <files>backend/app/models/portfolio_v2.py</files>
  <action>
backend/app/models/portfolio_v2.py dosyasını oluştur. Üç SQLAlchemy model sınıfı tanımla:

**PortfolioPosition** (`portfolio_positions` tablosu):
```
id            Integer, primary_key, index
symbol        String(20), nullable=False, index=True  # örn. "THYAO"
entry_price   Float, nullable=False                   # TRY cinsinden giriş fiyatı
quantity      Float, nullable=False                   # lot miktarı
entry_date    Date, nullable=False                    # pozisyon açılış tarihi
stop_loss     Float, nullable=True                    # ATR stop-loss seviyesi
target_price  Float, nullable=True                    # hedef fiyat
rationale     Text, nullable=True                     # pozisyon gerekçesi (Türkçe)
is_active     Boolean, default=True, nullable=False   # False = kapatılmış pozisyon
created_at    DateTime(timezone=True), server_default=func.now()
updated_at    DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
```

**PortfolioDailySnapshot** (`portfolio_daily_snapshots` tablosu):
```
id             Integer, primary_key, index
date           Date, nullable=False, unique=True, index=True  # bir günde tek snapshot
total_value    Float, nullable=False                          # TRY cinsinden toplam portföy değeri
daily_pnl_pct  Float, nullable=True                          # günlük kazanç/kayıp yüzde (%)
positions_json JSONB, nullable=True                          # o günkü pozisyon detayları
created_at     DateTime(timezone=True), server_default=func.now()
```

**PortfolioChangeLog** (`portfolio_change_log` tablosu):
```
id         Integer, primary_key, index
date       Date, nullable=False, index=True
action     String(10), nullable=False   # "ADD" veya "REMOVE"
symbol     String(20), nullable=False, index=True
reason     Text, nullable=True
created_at DateTime(timezone=True), server_default=func.now()
```

Import'lar:
- `from sqlalchemy import Column, Integer, String, Float, Boolean, DateTime, Date, Text`
- `from sqlalchemy.dialects.postgresql import JSONB`
- `from sqlalchemy.sql import func`
- `from app.core.database import Base`

Dosya başına docstring ekle: `"""Model portföy ORM modelleri — MPRT-01"""`
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "from app.models.portfolio_v2 import PortfolioPosition, PortfolioDailySnapshot, PortfolioChangeLog; print('OK:', PortfolioPosition.__tablename__, PortfolioDailySnapshot.__tablename__, PortfolioChangeLog.__tablename__)"</automated>
  </verify>
  <done>Üç model sınıfı import edilebilir; __tablename__ değerleri sırasıyla portfolio_positions, portfolio_daily_snapshots, portfolio_change_log'dur; Python import hatası yoktur.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic kurulumu ve migration oluştur</name>
  <files>
    backend/alembic.ini
    backend/alembic/env.py
    backend/alembic/script.py.mako
    backend/alembic/versions/001_portfolio_v2_tables.py
  </files>
  <action>
Alembic dizini boş — tam kurulum yapılması gerekiyor.

**Adım 1 — Alembic başlat:**
```bash
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend
.venv/bin/alembic init alembic
```

**Adım 2 — alembic.ini güncelle:**
`sqlalchemy.url` satırını değiştir:
```
sqlalchemy.url = postgresql://%(DB_USER)s:%(DB_PASS)s@%(DB_HOST)s/%(DB_NAME)s
```
NOT: Gerçek URL'i settings'den alacağız — env.py'de override edeceğiz.

**Adım 3 — alembic/env.py güncelle:**
env.py'de `target_metadata = None` satırını bul ve şununla değiştir:
```python
import sys
import os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

from app.core.config import settings
from app.core.database import Base
# Tüm modelleri import et — metadata'ya kayıt için
import app.models.stock  # noqa
import app.models.portfolio_v2  # noqa

target_metadata = Base.metadata

# sync URL override (Alembic sync bağlantı gerektirir)
config.set_main_option("sqlalchemy.url", settings.DATABASE_SYNC_URL)
```

**Adım 4 — Manuel migration dosyası oluştur:**
`backend/alembic/versions/001_portfolio_v2_tables.py` dosyasını oluştur:

```python
"""Portfolio v2 tabloları — MPRT-01

Revision ID: 001
Revises:
Create Date: 2026-04-20
"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import JSONB

revision = '001'
down_revision = None
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.create_table(
        'portfolio_positions',
        sa.Column('id', sa.Integer(), primary_key=True, index=True),
        sa.Column('symbol', sa.String(20), nullable=False, index=True),
        sa.Column('entry_price', sa.Float(), nullable=False),
        sa.Column('quantity', sa.Float(), nullable=False),
        sa.Column('entry_date', sa.Date(), nullable=False),
        sa.Column('stop_loss', sa.Float(), nullable=True),
        sa.Column('target_price', sa.Float(), nullable=True),
        sa.Column('rationale', sa.Text(), nullable=True),
        sa.Column('is_active', sa.Boolean(), nullable=False, server_default='true'),
        sa.Column('created_at', sa.DateTime(timezone=True), server_default=sa.func.now()),
        sa.Column('updated_at', sa.DateTime(timezone=True), server_default=sa.func.now()),
    )
    op.create_table(
        'portfolio_daily_snapshots',
        sa.Column('id', sa.Integer(), primary_key=True, index=True),
        sa.Column('date', sa.Date(), nullable=False, unique=True, index=True),
        sa.Column('total_value', sa.Float(), nullable=False),
        sa.Column('daily_pnl_pct', sa.Float(), nullable=True),
        sa.Column('positions_json', JSONB(), nullable=True),
        sa.Column('created_at', sa.DateTime(timezone=True), server_default=sa.func.now()),
    )
    op.create_table(
        'portfolio_change_log',
        sa.Column('id', sa.Integer(), primary_key=True, index=True),
        sa.Column('date', sa.Date(), nullable=False, index=True),
        sa.Column('action', sa.String(10), nullable=False),
        sa.Column('symbol', sa.String(20), nullable=False, index=True),
        sa.Column('reason', sa.Text(), nullable=True),
        sa.Column('created_at', sa.DateTime(timezone=True), server_default=sa.func.now()),
    )


def downgrade() -> None:
    op.drop_table('portfolio_change_log')
    op.drop_table('portfolio_daily_snapshots')
    op.drop_table('portfolio_positions')
```

**Adım 5 — Migration uygula:**
```bash
cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend
.venv/bin/alembic upgrade head
```

Eğer alembic.ini'deki URL sorun çıkarırsa, doğrudan env.py'deki DATABASE_SYNC_URL override'ı çalışır — config.set_main_option('sqlalchemy.url', ...) satırı bunu sağlar.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && .venv/bin/alembic current 2>&1 | grep -E "(001|head|current)" && .venv/bin/python -c "import psycopg2; from app.core.config import settings; conn = psycopg2.connect(settings.DATABASE_SYNC_URL); cur = conn.cursor(); cur.execute(\"SELECT tablename FROM pg_tables WHERE tablename IN ('portfolio_positions','portfolio_daily_snapshots','portfolio_change_log')\"); rows = cur.fetchall(); print('Tables:', [r[0] for r in rows]); conn.close()"</automated>
  </verify>
  <done>alembic current 001 (head) gösterir; psycopg2 sorgusu üç tablo adını döner; DB'de üç tablo mevcuttur.</done>
</task>

</tasks>

<verification>
- `python -c "from app.models.portfolio_v2 import PortfolioPosition, PortfolioDailySnapshot, PortfolioChangeLog"` hatasız çalışır
- `alembic current` 001 (head) gösterir
- `SELECT tablename FROM pg_tables WHERE tablename LIKE 'portfolio_%'` üç satır döner
- portfolio_daily_snapshots tablosunda positions_json sütunu JSONB tipindedir
- portfolio_change_log.action sütunu String(10) — sadece "ADD" veya "REMOVE" değerleri
</verification>

<success_criteria>
PostgreSQL'de üç tablo mevcuttur. Alembic migration 001 uygulanmıştır (head). ORM modelleri import edilebilir ve Plan 02/03 tarafından kullanılabilir. Mock veri veya simülasyon yoktur — sadece şema.
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/11-model-portfolyo-backend/11-01-SUMMARY.md` oluştur.
</output>
