---
phase: 11-model-portfolyo-backend
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - backend/app/services/portfolio_snapshot.py
  - backend/app/main.py
autonomous: true
requirements:
  - MPRT-02

must_haves:
  truths:
    - "APScheduler job hafta içi 18:30 İstanbul saatinde otomatik çalışır"
    - "Job yfinance'den gerçek kapanış fiyatı çeker — mock/hardcoded fiyat yoktur"
    - "Günlük kazanç/kayıp yüzdesi hesaplanır ve portfolio_daily_snapshots'a kaydedilir"
    - "Aynı gün iki kez çalışırsa upsert yapar — mükerrer kayıt oluşmaz"
    - "Aktif pozisyon yoksa job sessizce çıkar, hata atmaz"
  artifacts:
    - path: "backend/app/services/portfolio_snapshot.py"
      provides: "take_daily_snapshot() async fonksiyonu — tam snapshot mantığı"
      exports:
        - take_daily_snapshot
    - path: "backend/app/main.py"
      provides: "APScheduler job kaydı — 18:30 İstanbul, hafta içi"
      contains: "take_daily_snapshot"
  key_links:
    - from: "backend/app/services/portfolio_snapshot.py"
      to: "yfinance"
      via: "loop.run_in_executor(None, lambda: yf.download(...))"
      pattern: "run_in_executor"
    - from: "backend/app/main.py"
      to: "backend/app/services/portfolio_snapshot.py"
      via: "scheduler.add_job import"
      pattern: "take_daily_snapshot"
---

<objective>
Türk piyasası kapanışında (18:30 İstanbul) otomatik çalışan APScheduler job kur. Job, aktif pozisyonların gerçek kapanış fiyatlarını yfinance'den çeker, günlük P&L hesaplar ve portfolio_daily_snapshots tablosuna upsert yapar.

Purpose: Günlük snapshot olmadan tarih geçmişi birikmez. Phase 15'teki kümülatif getiri grafiği bu veriye bağlıdır.
Output: Her hafta içi 18:30'da otomatik çalışan, gerçek yfinance verisinden snapshot alan servis.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-model-portfolyo-backend/11-01-SUMMARY.md

<interfaces>
<!-- Plan 01'den gelen ORM modelleri -->
<!-- backend/app/models/portfolio_v2.py -->
```python
class PortfolioPosition(Base):
    __tablename__ = "portfolio_positions"
    id: int (PK)
    symbol: str          # "THYAO" (uzantısız)
    entry_price: float   # TRY giriş fiyatı
    quantity: float      # lot
    is_active: bool      # sadece True olanlar snapshot'a dahil

class PortfolioDailySnapshot(Base):
    __tablename__ = "portfolio_daily_snapshots"
    id: int (PK)
    date: date           # unique — bir günde tek kayıt
    total_value: float   # TRY toplam portföy değeri
    daily_pnl_pct: float # günlük kazanç/kayıp %
    positions_json: dict  # JSONB — pozisyon detayları
```

<!-- Async DB session pattern — backend/app/core/database.py -->
```python
AsyncSessionLocal = async_sessionmaker(bind=async_engine, class_=AsyncSession, expire_on_commit=False)

async with AsyncSessionLocal() as db:
    result = await db.execute(select(Model).where(...))
    rows = result.scalars().all()
    await db.commit()
```

<!-- yfinance async pattern — Phase 6'da yerleşik (TECH-02) -->
```python
import asyncio
import yfinance as yf

async def fetch_close_price(yahoo_symbol: str) -> float | None:
    loop = asyncio.get_event_loop()
    def _fetch():
        data = yf.download(yahoo_symbol, period="2d", interval="1d", progress=False)
        if data.empty:
            return None
        return float(data["Close"].iloc[-1])
    return await loop.run_in_executor(None, _fetch)
```

<!-- APScheduler cron job pattern — backend/app/main.py -->
```python
scheduler.add_job(
    some_async_func,
    "cron",
    day_of_week="mon-fri",
    hour=18,
    minute=30,
    timezone="Europe/Istanbul",
    max_instances=1,
    misfire_grace_time=300,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: portfolio_snapshot.py servisini oluştur</name>
  <files>backend/app/services/portfolio_snapshot.py</files>
  <action>
`backend/app/services/portfolio_snapshot.py` dosyasını oluştur. Tek public fonksiyon: `take_daily_snapshot()`.

**Dosya içeriği:**

```python
"""
Günlük portföy snapshot servisi — MPRT-02

Her hafta içi 18:30 İstanbul'da APScheduler tarafından tetiklenir.
Gerçek yfinance kapanış fiyatlarını çeker, günlük P&L hesaplar,
portfolio_daily_snapshots tablosuna upsert yapar.
Mock fiyat veya simülasyon yoktur.
"""
import asyncio
import logging
from datetime import date, datetime
from typing import Optional

import yfinance as yf
from sqlalchemy import select
from sqlalchemy.dialects.postgresql import insert as pg_insert

from app.core.database import AsyncSessionLocal
from app.models.portfolio_v2 import PortfolioPosition, PortfolioDailySnapshot

logger = logging.getLogger(__name__)


async def _fetch_close_price(yahoo_symbol: str) -> Optional[float]:
    """yfinance'den son kapanış fiyatını çek — async (run_in_executor)."""
    loop = asyncio.get_event_loop()

    def _sync_fetch():
        try:
            data = yf.download(yahoo_symbol, period="5d", interval="1d", progress=False)
            if data is None or data.empty:
                return None
            # Son satır bugünkü veya en son işlem günü kapanışı
            return float(data["Close"].iloc[-1])
        except Exception as exc:
            logger.warning(f"yfinance fiyat çekme hatası [{yahoo_symbol}]: {exc}")
            return None

    return await loop.run_in_executor(None, _sync_fetch)


async def take_daily_snapshot() -> None:
    """
    Aktif pozisyonların gerçek kapanış fiyatlarını çek,
    günlük P&L hesapla ve portfolio_daily_snapshots'a upsert yap.

    APScheduler tarafından çağrılır — hafta içi 18:30 İstanbul.
    """
    today = date.today()
    logger.info(f"[portfolio_snapshot] {today} snapshot başlıyor...")

    async with AsyncSessionLocal() as db:
        # Aktif pozisyonları çek
        result = await db.execute(
            select(PortfolioPosition).where(PortfolioPosition.is_active == True)
        )
        positions = result.scalars().all()

        if not positions:
            logger.info("[portfolio_snapshot] Aktif pozisyon yok — snapshot atlandı.")
            return

        # Her pozisyon için gerçek fiyat çek
        total_value = 0.0
        total_cost = 0.0
        positions_detail = []

        for pos in positions:
            yahoo_symbol = f"{pos.symbol}.IS"
            current_price = await _fetch_close_price(yahoo_symbol)

            if current_price is None:
                logger.warning(f"[portfolio_snapshot] {pos.symbol} fiyatı alınamadı — atlanıyor.")
                continue

            position_value = current_price * pos.quantity
            position_cost = pos.entry_price * pos.quantity
            pnl_pct = ((current_price - pos.entry_price) / pos.entry_price) * 100

            total_value += position_value
            total_cost += position_cost

            positions_detail.append({
                "symbol": pos.symbol,
                "quantity": pos.quantity,
                "entry_price": pos.entry_price,
                "current_price": current_price,
                "position_value": round(position_value, 2),
                "pnl_pct": round(pnl_pct, 4),
                "stop_loss": pos.stop_loss,
                "target_price": pos.target_price,
            })

        if total_cost > 0:
            daily_pnl_pct = ((total_value - total_cost) / total_cost) * 100
        else:
            daily_pnl_pct = 0.0

        # Upsert — aynı gün için mükerrer kayıt oluşturmaz
        # ON CONFLICT (date) DO UPDATE
        stmt = pg_insert(PortfolioDailySnapshot).values(
            date=today,
            total_value=round(total_value, 2),
            daily_pnl_pct=round(daily_pnl_pct, 4),
            positions_json=positions_detail,
            created_at=datetime.utcnow(),
        ).on_conflict_do_update(
            index_elements=["date"],
            set_={
                "total_value": round(total_value, 2),
                "daily_pnl_pct": round(daily_pnl_pct, 4),
                "positions_json": positions_detail,
            }
        )
        await db.execute(stmt)
        await db.commit()

        logger.info(
            f"[portfolio_snapshot] {today} snapshot kaydedildi — "
            f"toplam değer: {total_value:.2f} TRY, günlük P&L: {daily_pnl_pct:.2f}%, "
            f"{len(positions_detail)} pozisyon."
        )
```

Kritik kurallar:
- Asla mock/hardcoded fiyat kullanma
- yfinance çağrısı mutlaka run_in_executor içinde — event loop bloke etme (TECH-02 pattern)
- Aktif pozisyon yoksa sessizce çık
- Upsert kullan — aynı günde çift çalışma sorun olmaz
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "from app.services.portfolio_snapshot import take_daily_snapshot; import asyncio; print('Import OK')"</automated>
  </verify>
  <done>take_daily_snapshot import edilebilir; dosya söz dizimi hatasızdır; run_in_executor kullanılmaktadır; mock fiyat yoktur.</done>
</task>

<task type="auto">
  <name>Task 2: APScheduler job'ını main.py'e kaydet</name>
  <files>backend/app/main.py</files>
  <action>
`backend/app/main.py` dosyasını düzenle. İki değişiklik:

**Değişiklik 1 — Import bloğuna ekle (dosyanın en üstündeki import'ların altına):**
Mevcut import satırlarının bulunduğu bloğa ekle — `from app.api import ...` satırının altına:
```python
from app.services.portfolio_snapshot import take_daily_snapshot
```

NOT: Bu import lifespan fonksiyonu dışında, modül seviyesinde olmalı.

**Değişiklik 2 — lifespan fonksiyonu içine, scheduler.start() satırından ÖNCE ekle:**
Mevcut `scheduler.add_job(background_model_portfolio_generation, ...)` satırını BUL ve onun HEMEN ALTINA şunu ekle:

```python
    # Model Portföy Günlük Snapshot (18:30 Istanbul) — MPRT-02
    scheduler.add_job(
        take_daily_snapshot,
        "cron",
        day_of_week="mon-fri",
        hour=18,
        minute=30,
        timezone="Europe/Istanbul",
        max_instances=1,
        misfire_grace_time=300,
    )
```

UYARI: Mevcut `background_model_portfolio_generation` job'ını (18:15) kaldırma — Phase 10 bunu zaten kaldıracak. Sadece yeni job'ı ekle.

UYARI: `from app.api import ... portfolio ...` satırında `portfolio` hâlâ varsa dokunma — Phase 10 onu kaldıracak.
  </action>
  <verify>
    <automated>cd /Users/nedimkopurlu/Downloads/PROJELER/stalize/backend && python -c "import app.main; print('main.py import OK')" 2>&1 | head -5 && grep -n "take_daily_snapshot" app/main.py</automated>
  </verify>
  <done>main.py import edilebilir (söz dizimi hatası yoktur); grep take_daily_snapshot iki satır döner (import satırı + scheduler.add_job satırı); timezone="Europe/Istanbul" ve day_of_week="mon-fri" mevcuttur.</done>
</task>

</tasks>

<verification>
- `python -c "from app.services.portfolio_snapshot import take_daily_snapshot"` hatasız çalışır
- `grep -n "run_in_executor" backend/app/services/portfolio_snapshot.py` en az 1 satır döner
- `grep -n "take_daily_snapshot" backend/app/main.py` 2 satır döner (import + add_job)
- `grep "mock\|hardcode\|fake\|simul" backend/app/services/portfolio_snapshot.py` boş döner — mock fiyat yoktur
- `grep "Europe/Istanbul" backend/app/main.py` en az 2 satır döner (briefing job + snapshot job)
</verification>

<success_criteria>
Hafta içi 18:30 İstanbul'da otomatik tetiklenen APScheduler job mevcuttur. Job gerçek yfinance kapanış fiyatlarını çeker (run_in_executor ile), günlük P&L hesaplar ve portfolio_daily_snapshots tablosuna upsert yapar. Mock veri ve simülasyon yoktur.
</success_criteria>

<output>
Tamamlandıktan sonra `.planning/phases/11-model-portfolyo-backend/11-02-SUMMARY.md` oluştur.
</output>
